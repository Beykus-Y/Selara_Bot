# Модернизация серверного Web UI Selara — рабочий TODO

> [!IMPORTANT]
> **Обязательная инструкция для всех ИИ, агентов и разработчиков.**
>
> Перед любыми изменениями Jinja-шаблонов, серверного Web UI, его CSS, JavaScript,
> документации или UI-тестов необходимо полностью прочитать этот документ.
> Этот файл является единым источником правды по модернизации интерфейса и должен
> обновляться в том же коммите, что и выполненная работа.
> Порядок выполнения, визуального сравнения и сохранения результатов каждого среза
> определён в обязательной инструкции `docs/WEB_UI_SLICE_WORKFLOW.md`.
>
> Каждый исполнитель обязан:
>
> 1. Перед началом работы перечитать разделы «Зафиксированные решения»,
>    «Порядок реализации» и относящийся к задаче checklist.
> 2. Проверить актуальный `git status` и не перезаписывать чужие незавершённые
>    изменения.
> 3. Пометить взятую задачу как `[~]`, указать исполнителя и дату в журнале работ.
> 4. Не ставить `[x]`, пока реализация, адаптивность и обязательные проверки задачи
>    не завершены.
> 5. После завершения заменить `[~]` на `[x]` и записать дату, commit SHA и перечень
>    пройденных проверок. Если коммита ещё нет, временно указать `commit: pending` и
>    заменить его после публикации.
> 6. Новые обнаруженные проблемы, зависимости и решения добавлять в этот документ,
>    а не хранить только в переписке или памяти агента.
> 7. При блокировке поставить `[!]`, описать точную причину и требуемое решение.
> 8. Не менять подтверждённые продуктовые решения молча. Любое отступление сначала
>    фиксируется в разделе «Журнал решений» с объяснением.
> 9. После каждого рабочего блока обновлять сводный прогресс и журнал работ, даже
>    если блок завершён частично.
>
> Обозначения: `[ ]` — не начато, `[~]` — в работе, `[x]` — завершено и проверено,
> `[!]` — заблокировано, `[?]` — требуется решение пользователя.

## 1. Цель

Обновить весь серверный Web UI Selara, сделать его современнее, удобнее и
последовательнее, сохранив узнаваемый тёмный неоновый стиль проекта.

Главные результаты:

- функциональная и визуально цельная серверная админка;
- история сообщений, читаемая как лента группового диалога, а не таблица БД;
- единая пользовательская и администраторская документация с поиском;
- полноценная адаптация для современных смартфонов и планшетов;
- проверяемая доступность, семантика, адаптивность и производительность;
- единая система компонентов вместо несвязанных шаблонов, inline-стилей и
  монолитного CSS;
- отсутствие дублирующихся активных реализаций одного интерфейса.

## 2. Зафиксированные решения

Эти решения подтверждены пользователем 2026-08-15.

### Архитектура

- [x] Серверные страницы `/app`, админка и документация остаются на Jinja.
- [x] React остаётся интерфейсом Telegram Mini App (`/miniapp`).
- [x] Неиспользуемые React-копии серверной админки и документации подлежат удалению
  только после инвентаризации зависимостей и подтверждения функционального паритета.
- [x] Новый переключатель между старым и новым дизайном не нужен.
- [x] Существующие URL, авторизация и серверные сценарии должны сохраняться при
  последовательной замене страниц.

### Приоритеты

1. Серверная админка.
2. Внутри админки: системные рассылки и история сообщений.
3. Пользовательская и администраторская документация.
4. Остальные серверные страницы.
5. Игровые страницы — после стабилизации общей системы компонентов.

### Визуальное направление

- [x] Сохранить тёмную неоновую идентичность Selara.
- [x] Освежить визуальный язык: меньше декоративного шума, яснее иерархия,
  современная типографика, более спокойные поверхности и выразительные состояния.
- [x] Проектировать mobile-first, но не превращать desktop-интерфейс в растянутую
  мобильную колонку.
- [x] Поддерживать современные смартфоны и планшеты.
- [x] Обязательные браузерные семейства: Chrome/Chromium и Safari/WebKit.

### История сообщений

- [x] Основное представление — список чатов и читаемая лента сообщений.
- [x] Нужны группировка по датам, авторы, время, reply-контекст, редакции, поиск и
  фильтры.
- [x] Нужны превью поддерживаемых медиа и понятный fallback, если файл недоступен.
- [x] Raw JSON остаётся доступным как отдельный технический режим, но не является
  основным представлением.
- [x] Сохранение всех исходящих сообщений бота отложено на будущий этап.
- [x] На текущем этапе интерфейс использует уже сохраняемые входящие сообщения и
  снимки редакций. Он не должен вводить администратора в заблуждение и называться
  полной копией истории Telegram.

### Документация

- [x] Нужны отдельные пользовательская и администраторская области.
- [x] Пользовательская документация должна быть доступна без административного
  входа, если материал не раскрывает защищённые данные.
- [x] Нужен полнотекстовый поиск.
- [x] Нужны оглавление, стабильные anchors/deep links и мобильная навигация.
- [x] Команды, настройки и описания должны получать данные из единого источника,
  чтобы Jinja, Mini App и бот не расходились вручную.

### Качество

- [x] Stylelint для CSS.
- [x] djLint с Jinja-профилем для исходных шаблонов.
- [x] HTMLHint для отрендерированного HTML, где он даёт полезный сигнал без ложных
  срабатываний на Jinja.
- [x] ESLint для вынесенного серверного JavaScript.
- [x] Playwright для функциональных, responsive и визуальных сценариев.
- [x] axe-core для автоматически проверяемой части WCAG A/AA.
- [x] Lighthouse CI: сначала отчёты и baseline, затем согласованные бюджеты.
- [x] Автоматические проверки не заменяют ручную проверку клавиатуры, читаемости,
  порядка фокуса и сложных responsive-состояний.

### Выкладка

- [?] Стратегия первой production-выкладки нового интерфейса пока не определена.
- [x] Feature flag или пользовательский переключатель старого/нового дизайна не
  требуется.
- [ ] До начала production-выкладки отдельно согласовать размер первого релиза,
  порядок миграции страниц и способ отката.

## 3. Текущая техническая база

Инвентаризация на 2026-08-15:

- 21 Jinja HTML-файл в `src/selara/web/templates`;
- около 6 206 строк Jinja/HTML;
- `src/selara/web/static/panel.css` — около 6 145 строк;
- около 1 942 строк inline-JavaScript в пяти шаблонах;
- 38 inline-атрибутов `style`;
- таблицы используются в `chat.html`, `admin_table.html` и
  `admin_messages_compact.html`;
- текущий Python Playwright используется точечно, полноценной матрицы серверного UI
  пока нет;
- React-исходники содержат старые/параллельные страницы админки и документации, но
  активный router Mini App их не публикует;
- Jinja autoescape включён, но тестовый режим с `StrictUndefined` не настроен;
- адаптивные правила существуют, но покрывают лишь отдельные области большого
  монолитного stylesheet.

Самые рискованные по объёму шаблоны:

- `_games_dashboard.html` — около 1 244 строк;
- `chat.html` — около 1 321 строки и большой inline-JavaScript;
- `games.html` — около 964 строк JavaScript поверх крупного partial;
- `admin.html` — около 385 строк после extraction и добавления preview/confirm;
- `economy.html` — около 353 строк.

## 4. Общие правила реализации

- [ ] Не выполнять массовый rewrite всех страниц одним коммитом.
- [ ] Следовать полному workflow из `docs/WEB_UI_SLICE_WORKFLOW.md`, включая ручной
  просмотр каждого финального screenshot и сохранение в `~/selara_screen`.
- [ ] Работать вертикальными срезами: тесты и состояние до изменения → компоненты →
  страница → responsive → accessibility → проверки → обновление TODO.
- [ ] Сохранять существующие серверные маршруты и backend-контракты, если изменение
  контракта не согласовано отдельно.
- [ ] Не смешивать редизайн с несвязанными изменениями бизнес-логики.
- [ ] Не скрывать технические возможности generic DB explorer; отделить их от
  продуктовых представлений.
- [ ] Не использовать цвет как единственный способ передачи статуса.
- [ ] Все интерактивные элементы должны работать с клавиатуры и иметь видимый focus.
- [ ] Touch targets основных действий — не менее 44×44 CSS px либо эквивалентная
  удобная зона нажатия.
- [ ] Горизонтальная прокрутка всей страницы запрещена. Она допустима только внутри
  явно обозначенного технического контейнера таблицы.
- [ ] Длинные Telegram-имена, непрерывные строки, emoji, RTL/CJK, пустые данные и
  большие наборы записей входят в обязательные тестовые состояния.
- [ ] Все destructive-действия имеют понятное подтверждение, описание объекта и
  безопасное восстановление фокуса после закрытия диалога.
- [ ] Визуальные эффекты учитывают `prefers-reduced-motion`.
- [ ] Секреты, сессионные данные и приватное содержимое не попадают в публичные
  Lighthouse/Playwright-артефакты.

## 5. Порядок реализации

### Этап 0. Baseline, аудит и инфраструктура проверок `[~]`

#### Инвентаризация

- [x] Составить таблицу всех серверных маршрутов, шаблонов, API и требуемой
  авторизации.
- [x] Для каждой страницы перечислить состояния: normal, empty, loading/submit,
  validation error, permission denied, long content и large dataset.
- [x] Найти и задокументировать используемые CSS-классы, design tokens, inline-стили,
  inline-JavaScript и дубли компонентов.
- [x] Проверить, какие React admin/docs modules действительно не достижимы из
  production router и не импортируются активным Mini App.
- [ ] Зафиксировать текущие desktop/mobile screenshots ключевых страниц до
  визуальных изменений.
- [x] Составить карту текущих форм и destructive-действий, включая требования к
  авторизации и redirect/JSON-режимам.

#### Тестовая инфраструктура

- [ ] Определить единый Playwright runner для широкого UI-набора. Предпочтительный
  вариант: Playwright Test в Node-пакете с запуском реального FastAPI и тестовой БД;
  существующие Python browser-тесты мигрировать только после появления паритета.
- [ ] Добавить воспроизводимые test fixtures: администратор, обычный пользователь,
  пустой чат, активный чат, длинные имена, рассылка, ответы и архив сообщений.
- [ ] Настроить проекты:
  - [ ] Desktop Chromium, 1440×900.
  - [ ] Mobile Chromium, современный Android viewport.
  - [ ] Mobile WebKit, современный iPhone viewport.
  - [ ] Tablet WebKit, современный iPad viewport.
- [ ] Добавить smoke-проверки всех серверных маршрутов без console/page errors.
- [~] Добавить проверку отсутствия горизонтального page overflow на mobile/tablet.
- [ ] Настроить trace, screenshot и video только при ошибке CI.
- [ ] Определить стабильную среду визуальных snapshots: одинаковые Linux image,
  Chromium/WebKit versions, fonts и timezone.
- [ ] Добавить axe-core scan ключевых страниц и интерактивных состояний.
- [ ] Настроить Lighthouse CI в report-only режиме без публичной отправки приватных
  отчётов.

#### Линтинг и шаблоны

- [ ] Подключить djLint в check-only режиме с `profile=jinja`.
- [ ] Сначала зафиксировать baseline существующих нарушений, затем запрещать новые.
- [x] Добавить тест компиляции всех Jinja-шаблонов.
- [~] Добавить representative render-тесты с `StrictUndefined`, не включая его в
  production до проверки всех контекстов.
- [~] Подключить HTMLHint к отрендерированным representative pages.
- [~] Подключить Stylelint и official standard config в report-only режиме.
- [x] Подключить ESLint к серверным JavaScript-модулям после их появления.
- [ ] Постепенно перевести линтеры из report-only в blocking CI после очистки
  baseline.

**Критерий завершения этапа 0:** текущий интерфейс имеет воспроизводимый baseline,
основные сценарии защищены тестами, а новые проверки не создают неконтролируемый
шум в CI.

### Этап 1. Дизайн-система и общий серверный layout

#### Design tokens

- [ ] Инвентаризировать текущие CSS custom properties и удалить смысловые дубли.
- [ ] Определить токены цветов: background, surface, elevated surface, border,
  primary, accent, success, warning, danger, muted и focus.
- [ ] Определить шкалы spacing, radius, shadow, typography, line-height и z-index.
- [ ] Проверить контраст основных сочетаний на WCAG AA.
- [ ] Зафиксировать правила плотности desktop и mobile.

#### CSS-архитектура

- [ ] Определить структуру серверных стилей: tokens, reset/base, layout, components,
  utilities и pages.
- [ ] Разделить `panel.css` без изменения поведения на первом механическом этапе.
- [ ] Ввести понятное соглашение имён классов.
- [ ] Удалять мёртвые селекторы только после автоматической и ручной проверки.
- [ ] Запретить новые inline-стили, кроме действительно динамических значений,
  безопасно переданных через CSS custom properties.

#### Jinja-компоненты

- [ ] Создать macros/partials для:
  - [ ] page header и breadcrumbs;
  - [ ] buttons и icon buttons;
  - [ ] form field, label, hint и validation error;
  - [ ] card/panel/section;
  - [ ] badge/status;
  - [ ] tabs/segmented control;
  - [ ] pagination;
  - [ ] empty/error states;
  - [ ] confirm dialog;
  - [ ] responsive data card;
  - [ ] toast/banner.
- [ ] Добавить gallery/dev fixture основных компонентов и их состояний либо
  эквивалентные template tests.

#### `base.html` и навигация

- [~] Переработать `base.html` на семантический layout с skip link.
- [~] Сделать responsive topbar/navigation для smartphone, tablet и desktop.
- [ ] Упорядочить page title, description и основные landmarks.
- [~] Унифицировать flash/error rendering и доступное уведомление screen reader.
- [~] Вынести общий JavaScript из шаблонов в статические модули.
- [x] Добавить безопасный механизм page-specific CSS/JS без inline script blocks.
- [~] Проверить logout, redirects, session expiry и keyboard navigation.

**Критерий завершения этапа 1:** новая оболочка и компоненты готовы, проверены на
четырёх viewport-проектах и могут применяться к страницам по одной без feature flag.

### Этап 2. Серверная админка — главный приоритет

#### 2.1. Admin login и shell

- [x] Обновить `admin_login.html`. Исправлен реальный баг: `.login-side`/
  `.login-side-card` не имели стилей, из-за чего на mobile текст боковых карточек
  накладывался на заголовок формы (карточки — unstyled auto-колонка grid). Так как
  `login.html` (обычный вход пользователя) использует те же классы, фикс общих
  стилей в `server-ui-foundation.css` заодно чинит и его; это не заявленная задача
  этого среза, но избежать нельзя — классы общие. Полный редизайн `login.html`
  остаётся в этапе 4.
- [x] Сделать ясные состояния неверного пароля, истёкшей сессии и server error. Уже
  обеспечивалось общим banner-механизмом `base.html` (`flash`/`error`); проверено
  screenshot-ами для неверного пароля и rate-limit.
- [x] Проверить password manager, autocomplete, клавиатуру и mobile keyboard.
  `autocomplete="current-password"`, `autofocus`, явный `<label for>` — уже было
  корректно, подтверждено тестом. Добавлен submit guard от двойной отправки.
- [x] Создать единый admin shell: навигация, текущий раздел, быстрые действия,
  responsive sidebar/drawer. Desktop/Mobile/Tablet Chromium (1440/390/820 px)
  закрыты — добавлен отдельный `tests/unit/test_web_admin_shell_browser.py`
  с прицельными тестами самого shell'а (active-state не только цветом, skip-link
  keyboard flow, mobile scroll-ribbon, tablet без scroll-ribbon), плюс уже
  существующее попутное покрытие shell'а во всех page-level browser-тестах.
  WebKit — см. отдельный `[!]` пункт ниже (заблокирован на этой машине).
- [!] Mobile/Tablet WebKit для admin shell и всей browser-матрицы технически
  недоступны на текущей dev-машине (Fedora 44): Playwright официально
  поддерживает установку зависимостей WebKit только для Ubuntu/Debian
  (`playwright install-deps` падает — `apt-get: command not found`). Прямая
  проверка через `ldd` показала, что бинарник WebKit slinkован против ICU 74 и
  libjpeg ABI 8, которых на Fedora 44 нет (стоит ICU 77, libjpeg.so.62);
  часть зависимостей (`libbacktrace`, `libjxl`) Playwright бандлит сам в
  `sys/lib`, но ICU/libjpeg — нет, и подменить их простым симлинком на более
  новые версии нельзя (у ICU версионированные C++ ABI-символы, будет падать на
  resolve символов, а не при старте). Пользователь подтвердил (2026-08-15):
  отложить, WebKit-прогон переносится в CI/Docker-окружение с Ubuntu
  (отдельная, не начатая задача), решение зафиксировано честно, а не хаком с
  вендорингом чужих .so.
- [x] Не показывать потенциально чувствительные данные в глобальной навигации. Уже
  обеспечивалось (`_admin_layout_context(authenticated=False)`); подтверждено
  существующим тестом `test_admin_login_page_has_public_navigation_only`. Также
  убрано раскрытие имени переменной окружения `ADMIN_PASSWORD` в helper-тексте.

#### 2.2. Главная админки

- [x] Перестроить `admin.html` по задачам администратора, а не по структуре БД.
- [x] Верхний уровень: состояние сервисов, критичные проблемы и быстрые действия.
- [x] Отдельные понятные входы в рассылки, историю, заявки, аудит, бэкапы и generic DB
  explorer.
- [x] Сделать секции сворачиваемыми только там, где это не скрывает критичный статус.
  Домены таблиц (`table_sections`) обёрнуты в `<details open>` с chevron-индикатором
  (`admin-secondary-details`, паттерн `[open] summary::after` скопирован с уже
  проверенного `.admin-feedback-history`); открыты по умолчанию, поиск по таблицам
  принудительно раскрывает свёрнутый домен, если в нём есть совпадение. Критичные
  секции («Что требует внимания», «Сервисы и данные», список заявок) остаются
  обычными `<section>` — не сворачиваются никогда.
- [x] Проверить empty/large/error/loading states. Empty — уже было (`is-calm`
  состояние). Loading — состояния кнопок заявок/backup из предыдущего среза. Error —
  новый route-тест на баннер `banner-error`/`banner-ok` для `/app/admin`. Large —
  новый browser-тест с 24 заявками и 10 доменами по 6 таблиц (60 карточек); тест
  **нашёл реальный баг**: `.admin-table-card` объявляет собственный `display: flex`
  в panel.css, который на равной специфичности побеждает UA-правило
  `[hidden]{display:none}` — карточки визуально НЕ скрывались при поиске внутри
  секции с частичным совпадением (баг маскировался в мелких тестах, где вся секция
  целиком пряталась). Исправлено явным `.admin-table-card[hidden]{display:none}`.

#### 2.3. Системные рассылки

- [x] Сохранить всю текущую функциональность: HTML, фото, reaction builder, выбор
  чатов, лимиты, предпросмотр и проверки ошибок.
- [x] Разделить создание рассылки на ясные логические блоки: сообщение, медиа,
  реакции, аудитория, проверка и отправка.
- [x] Сделать постоянный preview сообщения для desktop и удобный preview step для
  mobile.
- [x] Показывать актуальные лимиты и остаток символов рядом с соответствующим полем.
- [x] Сделать ошибки синтаксиса и медиа привязанными к конкретному блоку формы.
- [x] Сделать выбор чатов searchable, keyboard-accessible и удобным на touch.
- [x] Добавить подтверждение массовой отправки с количеством получателей и итоговым
  preview.
- [x] Защитить от случайного повторного submit.
- [x] Довести визуальную и responsive-полировку composer/preview/confirm по ручному
  циклу desktop/mobile screenshots без перекрытий, обрезанных действий и page
  overflow.
- [x] Переработать историю рассылок в карточки с ясным статусом, временем, охватом,
  ошибками, ответами и реакциями.
- [x] Обновить `admin_broadcast_detail.html`:
  - [x] preview отправленного сообщения;
  - [x] summary доставки;
  - [x] фильтры успешных/ошибочных доставок;
  - [x] ответы пользователей;
  - [x] быстрые реакции от имени бота;
  - [x] нативные, inline, custom и paid reactions;
  - [x] mobile layout без потери действий.
- [x] Playwright: отправка текста, фото, hybrid reactions, invalid input, Telegram
  failure, duplicate submit и mobile flow.

#### 2.4. История сообщений как лента чата

- [x] Зафиксировать честное название: «Архив сообщений», а не «полная история
  Telegram».
- [x] Спроектировать desktop split view: список/поиск чатов слева, сообщения справа.
- [x] Спроектировать mobile flow: список чатов → отдельная лента → возврат с
  сохранением фильтров и позиции.
- [x] Добавить фильтры по чату, пользователю, диапазону дат, типу сообщения и наличию
  редакции/reply.
- [!] Добавить полнотекстовый поиск с понятной подсветкой совпадений. Подстрочный
  поиск (ILIKE) и безопасная подсветка готовы и работают в проде. Индексированный
  full-text (tsvector + GIN на Postgres, russian-словарь) — отдельная задача: это
  миграция схемы продовой БД, а не Web UI-срез, и она ломает нынешний параллелизм
  тестов (SQLite и Postgres сейчас проверяются одним ILIKE-путём; tsvector —
  Postgres-специфичная фича). Пользователь подтвердил (2026-08-16): отложить,
  Этап 2 считается фактически закрытым с этим единственным сознательно отложенным
  пунктом.
- [x] Перейти от строк таблицы к message cards/bubbles:
  - [x] автор и стабильная цветовая метка;
  - [x] локализованное время;
  - [x] тип сообщения;
  - [x] текст/caption с безопасным переносом длинных строк;
  - [x] reply quote;
  - [x] edited badge;
  - [x] сворачиваемая история снимков редакций;
  - [x] ссылка на raw snapshot.
- [x] Группировать соседние сообщения одного автора и разделять даты.
- [x] Определить порядок ленты и поведение перехода к найденному сообщению.
- [x] Реализовать cursor pagination или другой устойчивый способ загрузки больших
  архивов без пропусков и дублей.
- [x] Сохранять фильтры в URL.
- [x] Добавить preview:
  - [x] photo;
  - [x] sticker/animation placeholder;
  - [x] video/document/audio/voice metadata;
  - [x] безопасный fallback при недоступном Telegram file.
- [x] Не загружать media автоматически в полном качестве.
- [x] Добавить технический табличный/raw режим для диагностики. Уже существовал:
  кнопка «Технический режим» в archive conversation header ведёт на
  `/app/admin/table/messages?chat_id=...` — тот же generic DB explorer (raw JSON,
  все колонки), отфильтрованный по текущему чату. Не новый режим, а уже
  реализованная связь между дружелюбным и техническим представлением одних и тех
  же данных — явно отмечено выполненным.
- [ ] Не добавлять на этом этапе хранение исходящих сообщений бота.
- [x] Создать отдельный deferred item для исходящих сообщений после стабилизации UI.
  Уже существует как раздел «7. Отложенные задачи» (строки 599–604) — создан вместе
  с roadmap, теперь явно отмечен выполненным.
- [x] Playwright: пустой архив, длинная лента, edits, replies, unavailable media,
  filters, deep link, mobile navigation и no-overflow. Закрыты оставшиеся пробелы:
  route-тесты на «Архив пока пуст» (нет ни одного чата) и «По этим фильтрам ничего
  не найдено» (выбранный чат + фильтры без совпадений, с рабочей ссылкой сброса);
  browser-тест длинной ленты (50 сообщений/137 в выборке, desktop+mobile) —
  пагинация видна, нет page overflow. edits/replies/unavailable media/mobile
  navigation/no-overflow/deep-link (highlight-параметр) уже были покрыты в
  предыдущих срезах архива.

#### 2.5. Заявки, аудит и обслуживание

- [x] Переработать заявки пользователей в task-like cards с фильтром статуса,
  быстрым изменением и видимой историей решения.
- [x] Переработать `audit.html` в читаемую timeline/table hybrid view.
- [x] Сделать запрос backup отдельным подтверждаемым действием с ясным результатом.
- [x] Показывать ошибки операций рядом с действием, сохраняя введённые данные.
  Статус заявки и запрос backup переведены с нативного form-submit на `fetch()`
  к уже существующим JSON API (`/api/admin/feedback/{id}/status`,
  `/api/admin/request-backup`); ошибка показывается inline рядом с конкретным
  действием (для заявки — именно у той карточки, что вызвала действие, не влияя
  на остальные) без перезагрузки страницы и потери позиции скролла; backup-диалог
  остаётся открытым при ошибке вместо закрытия.

#### 2.6. Generic DB explorer

- [x] Сохранить `admin_table.html` как технический инструмент, не смешивая его с
  продуктовой админкой. Стили и JS вынесены в собственные `admin-table.css`/
  `admin-table.js`, не затрагивающие продуктовые страницы.
- [x] Сделать список таблиц searchable и сгруппированным по доменам. Группировка по
  доменам уже была на `admin.html`; добавлен клиентский поиск (новый
  `admin-table-search.js`, без серверных изменений — список таблиц целиком уже
  рендерится, доп. round-trip не нужен): фильтрует по имени и ключу таблицы,
  скрывает разделы без совпадений, показывает «Совпадений не найдено» при
  полном отсутствии результатов.
- [x] Зафиксировать понятные labels для внешних ключей рядом с raw ID. Уже
  реализовано через `reference_labels` в списке и в форме редактирования.
- [x] На mobile использовать contained horizontal scroll или card view для
  приоритетных таблиц. `.table-wrapper` — явно обозначенный технический контейнер с
  `overflow-x: auto`; горизонтальная прокрутка всей страницы не используется.
- [x] Улучшить pagination и URL filters без ручной конкатенации query string.
  Ссылки постранично строятся на сервере через `urlencode`; исправлен краш при
  нечисловом/отрицательном `page`.
- [x] Обновить `admin_edit.html`: labels, типы полей, validation, destructive
  warnings. Добавлен предупреждающий баннер о прямом изменении БД и submit guard.
- [x] Обновить confirm dialog удаления и возврат фокуса. Диалог теперь называет
  удаляемую запись, восстанавливает фокус на вызвавшую кнопку, JS вынесен в
  `admin-table.js`.
- [x] Добавить Playwright CRUD-тест на специально разрешённой тестовой таблице.
  Browser-тесты geometry/focus/pagination/warning уже были. Добавлен новый
  `tests/unit/test_web_admin_table_crud.py` — реальный FastAPI + sqlite (тот же
  паттерн, что и real-backend тесты рассылок): update и delete реально мутируют/
  удаляют строку в БД (не мок), проверены unauthenticated-запрос без мутации,
  delete несуществующей записи без краша, и что edit-форма отражает реально
  обновлённую строку. UI explorer не имеет действия «создать запись» (только
  edit/delete существующих) — «CRUD» здесь означает R+U+D, что покрывает всю
  функциональную поверхность инструмента. Полноценный live-браузерный E2E (реальный
  HTTP-сервер + Playwright, не ASGITransport) остаётся более крупной задачей
  тестовой инфраструктуры этапа 0, не блокирующей эту функциональность.

**Критерий завершения этапа 2:** все административные задачи доступны с desktop,
smartphone и tablet; рассылка и архив покрыты функциональными тестами; generic DB
режим сохранён, но больше не определяет UX всей админки.

**Статус на 2026-08-16: критерий выполнен, этап 2 фактически закрыт.** Единственный
формально открытый пункт — индексированный full-text поиск (`[!]`, требует миграции
продовой БД) — сознательно отложен по решению пользователя, не блокирует переход к
Этапу 3. WebKit-проверка admin shell также отложена (`[!]`, недоступна на текущей
dev-машине) и не блокирует остальной roadmap.

### Этап 3. Единая документация

#### Модель контента

- [x] Инвентаризировать `admin_docs.html`, `user_docs.html`, `docs/*.md`, metadata
  команд и настроек. Полный отчёт: `admin_docs.html`/`user_docs.html` — тонкие
  шаблоны, данные из `admin_docs.py` (в основном хардкод, кроме настроек) и
  `user_docs.py` (671 строка, полностью хардкод). `docs/*.md` разделены на
  dev/agent-доки (не в скоупе) и user/admin-кандидаты (`USER_GUIDE.md`,
  `ADMIN_GUIDE.md`, `ACTIONS.md`) плюс отдельно `docs/bot_docs/*.md` — это
  system-prompt контент для LLM-инструментов, другая аудитория, не Web UI.
  React docs-страницы существуют, но недостижимы из router — подтверждено.
  Метаданные команд разбросаны по `presentation/commands/catalog.py`,
  `access.py`, `core/roles.py`, `presentation/handlers/help.py`.
- [x] Найти противоречия и дубли между Jinja, Markdown, React и текстами бота.
  Конкретные находки: (1) список RP-действий независимо дублируется в 4 местах,
  версия в `help.py` уже неполная; (2) списки slash-команд по разделам вручную
  дублируются в `help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/`user_docs.py`;
  (3) **исправлено в этом срезе**: `docs/bot_docs/roles.md` не содержал роль
  `co_owner`, а секция ролей в `admin_docs.py` вообще не подключала
  `core/roles.py` — просто текст-заглушка; (4) **исправлено в этом срезе**:
  labels прав существовали в 3 независимых копиях (`web/presenters.py`,
  `presentation/handlers/moderation.py`, и de facto в `admin_docs.py` их не
  было вовсе) — бот и веб-панель показывали разный текст для одного и того же
  права (`manage_command_access`: «доступ команд» в боте vs «доступ к
  командам» в вебе).
- [~] Определить единый структурированный источник для команд, настроек, ролей,
  permissions, форматов и примеров. Настройки уже были единым источником
  (`SETTINGS_GROUPS`/`CHAT_SETTINGS_KEYS`) до этого среза. В этом срезе роли и
  permissions переведены на единый источник: `PERMISSION_LABELS_RU` +
  `permission_label_ru()`/`permissions_text_ru()` перенесены в `core/roles.py`
  (было 3 копии — в `presenters.py`, `moderation.py` и вообще без реализации в
  `admin_docs.py`); `admin_docs.py` получил `build_roles_docs()`, строящий
  таблицу ролей/рангов/прав напрямую из `SYSTEM_ROLE_TEMPLATES`; бот-хендлер
  `moderation.py` теперь тоже берёт текст прав из `core/roles.py`.
  RP-действия (reply-команды типа «обнять»/«трахнуть») тоже переведены на
  единый источник: `catalog.py` получил `SOCIAL_ACTION_18_PLUS` (перенесён из
  `text_commands.py`, где раньше был приватным дублем) и
  `build_social_action_docs()`, строящий список
  `{command_key, canonical trigger, is_18_plus}` из уже существовавших
  `COMMAND_KEY_DEFAULT_SOURCE_TRIGGER` + `SOCIAL_COMMAND_KEY_TO_ACTION` +
  `SOCIAL_ACTION_18_PLUS`; `user_docs.py` теперь строит оба списка
  («Reply-действия без 18+» и «18+ reply-действия») из этой единой функции
  вместо ручных литералов. **Найденный этим переводом баг** (ручные списки
  были рассинхронизированы с реальным набором команд): 20 реальных RP-действий
  отсутствовали в публичной документации вообще (`потрепать`, `покатать на
  спине`, `лупануть`, `поиграть`, `треснуть`, `подарить цветок`, `закидать
  снегом`, `согреть`, `воскресить`, `пригласить на свидание`, `спеть
  серенаду`, `покормить`, `пустить на шашлык`, `напоить`, `пригласить на
  чай`, `засветить`, `принести кофе`, `пригласить на танец`, `щёлкнуть по
  носу`, `уебать`, `укусить`), и 4 действия были показаны под неканоническим
  альтернативным триггером вместо основного (`разнести`→`разъебать`,
  `протащить`→`оттаскать`, `дать подзатыльник`→`настучать по голове`,
  `минет`→`отсосать`, с пропуском реального `поводить членом`). Регрессия
  закрыта тестами: `tests/unit/test_commands_catalog.py` (покрытие всех
  118 social-ключей, синхронизация `is_18_plus` с runtime-гейтом
  `text_commands._send_social_action`, уникальность триггеров) и
  `test_web_user_docs.py::test_user_docs_rp_action_lists_match_canonical_catalog_source`.
  Slash-команды по разделам (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/
  `user_docs.py`) остаются отдельной, более крупной задачей унификации — не
  закрыто в этом срезе; краткий список в `help.py` намеренно оставлен
  сокращённой выжимкой (Telegram-сообщение не вместит все 118 триггеров), а
  не полным дублем — не тот же тип бага, что был у RP-списков.
  **[?] Требуется решение пользователя по объёму** (2026-08-16, зафиксировано
  и пропущено, чтобы не блокировать остальной Этап 3, согласно инструкции
  «если нужно моё решение — помечай в TODO и пропускай шаг»): `help.py`
  (347 строк) + `docs/USER_GUIDE.md` (324) + `docs/ADMIN_GUIDE.md` (233) +
  `user_docs.py`/`admin_docs.py` вместе описывают, по грубой оценке, 80–150+
  различных slash-команд бота — на порядок больше по объёму, чем сегодняшние
  срезы (роли/RP-действия были уже готовыми небольшими каталогами, которые
  нужно было просто переиспользовать; здесь готового каталога slash-команд
  нет вообще). Нужно решить: (a) полный единый каталог команд с генерацией
  всех 4 поверхностей — самая точная, но большая многосрезовая задача; (b)
  только аудит на расхождения без переписывания на единый источник (как уже
  сделано для «не хранить admin procedures в публичном разделе» и «task-
  oriented разделы» — закрыто верификацией без кода); (c) отложить целиком
  до отдельного будущего этапа, как WebKit/индексированный FTS. См. также
  строку про перенос адаптации ниже.
- [x] Не хранить секретные admin procedures в публичном пользовательском разделе.
  Проверено построчным аудитом всех 671 строки `user_docs.py` против
  `docs/ADMIN_GUIDE.md`: админских команд управления (роли, настройки,
  модерация, алиасы, триггеры) в публичном разделе нет вообще — контент уже
  корректно разделён автором, есть явная переадресация "для модерации/
  настроек — `/app/docs/admin`" (строка 641). Единственное упоминание
  модерации — `/modstat` (просмотр СВОЕГО статуса, не инструмент модерации).
  Вне скоупа этого пункта, но зафиксировано отдельно: секция 18+ reply-действий
  лежит открытым текстом на публичной, не защищённой auth странице без
  возрастного ограничения — не является "admin procedure", но может требовать
  отдельного решения о контентных ограничениях, если это важно для проекта.
- [x] Добавить автоматическую проверку уникальности anchors и внутренних ссылок.
  Новый `tests/unit/test_web_docs_anchors.py`: рендерит `admin_docs.html` и
  `user_docs.html` через их реальные context-билдеры, парсит HTML (без regex —
  `html.parser.HTMLParser`), проверяет отсутствие дублей `id` и что каждая
  `href="#..."` ссылка ведёт на существующий anchor. Работоспособность самой
  проверки подтверждена: на синтетическом HTML с дублем/битой ссылкой тест
  корректно падает. Оба реальных документа сейчас проходят без нарушений.

#### Пользовательская документация

- [x] Публичный доступ без admin session. Подтверждено чтением кода и реальным
  route-тестом: `_load_user_from_request` (app.py:1203) возвращает `None` без
  единого обращения к БД, если в запросе нет session cookie; `/app/docs/user`
  и `/api/app/docs/user` никогда не редиректят по результату — `redirect_path`
  у пользовательских docs всегда `None` (в отличие от admin-роутов). Новый
  `tests/unit/test_web_user_docs_public_access.py` бьёт по реальному ASGI-приложению
  без cookie и с `session_factory`, которая упала бы при попытке обращения к БД —
  доказывает отсутствие auth-проверки, а не просто читает код.
- [ ] Responsive оглавление/drawer. Подтверждено конкретным замером (2026-08-16,
  во время работы над admin_docs.html): на 390px `.docs-layout`/`.docs-card-grid`
  не имеют mobile-брейкпоинта вообще — двухколоночная раскладка (nav + content)
  не схлопывается, карточки вылезают за viewport на ~45px независимо от секции
  (проверено на уже существующей `docs-trigger-types` и на новой roles-таблице
  — одинаковый overflow, то есть дефект не точечный, а всей раскладки). Эта
  задача действительно нужна и не мельче, чем выглядела; чинить сразу в срезе
  про роли не стал — не относится к его объёму, а фикс всей `.docs-layout`
  responsive-раскладки — отдельный срез (затронет и user_docs.html, т.к.
  общие классы).
- [x] Полнотекстовый поиск. Клиентский фильтр по тому же паттерну, что уже
  принят в проекте для `admin-table-search.js` (список таблиц админки):
  `docs-search.js`/`.css` (22/29 строк), новый `<input type="search">` в
  `.docs-nav` с `data-docs-search-input`. Каждая `.docs-card` получила
  `data-docs-search-text` — единая lowercased строка из title/text/badges/
  commands/triggers/examples/steps/notes, посчитанная один раз в
  `_docs_item()` (`user_docs.py`), не в шаблоне. При вводе скрываются
  несовпадающие карточки и секции с нулём совпадений; пустой результат
  показывает «Совпадений не найдено». Playwright-тесты подтвердили: фильтрация
  по уникальному слову находит ровно одну карточку, полностью
  несуществующий запрос скрывает всё и показывает empty-state, очистка
  запроса возвращает все 28 карточек. Не путать с пунктом «Search index
  строится детерминированно…» в «Качестве документации» ниже — это про
  отдельный индексированный поиск по всей документации (`bot_docs/`), не про
  этот in-page фильтр.
- [x] Разделы по пользовательским задачам, а не только по списку команд.
  Проверено: все 8 секций `_USER_DOC_SECTIONS` уже сгруппированы по
  пользовательским задачам/темам («С чего начать», «Пары и брак», «Семья,
  титулы и имя», «Социальные действия и мемы» и т.д.), а не по алфавиту или
  внутреннему списку команд; то же для заголовков карточек внутри секций
  («Карма», «Reply-действия без 18+», а не голые `/command`-имена). Изменений
  кода не требовалось — структура уже соответствовала требованию.
- [x] Копируемые примеры и deep links. Каждая карточка (`.docs-card`) в
  `user_docs.html` теперь имеет стабильный id (`{section.anchor}-item-{n}`) и
  видимую ссылку-якорь `#` в шапке (`.docs-item-anchor`, opacity 0.55 в покое —
  видна и на touch-устройствах без hover, не только по наведению). Каждый
  пример в блоке «Примеры» получил кнопку «Копировать»
  (`.docs-clip-button`, `Clipboard API` с fallback на `execCommand('copy')`
  для небезопасного контекста) с временной обратной связью «Скопировано».
  Новые файлы `docs-item-actions.css`/`.js` (56/50 строк, под baseline-лимитом
  60/60), подключены только на `user_docs.html` через `extra_styles`/
  `extra_scripts`. **Найден и исправлен реальный баг по пути**: переход по
  новому deep-link якорю прокручивал карточку ПОД sticky-топбар (`.topbar`,
  `position: sticky; z-index: 20`), закрывая заголовок карточки — `.docs-card`
  не входил в существующее правило `scroll-margin-top: 148px`
  (`panel.css`, до этого применялось только к `.docs-section`/
  `.docs-setting-card`/`#docs-trigger-types`); добавлен в тот же список.
  Новый `scripts/render_user_docs_fixture.py` (первый HTMLHint-fixture для
  `user_docs.html` вообще — раньше эта страница не проверялась в CI) вызывает
  `build_user_docs_context()` напрямую (в отличие от `render_admin_docs_fixture.py`)
  — проверено, что у неё нет транзитивной зависимости на pydantic, против
  изолированного Jinja2-only venv, зеркалящего CI. Playwright-проверка
  desktop/tablet: копирование, hover-feedback и прокрутка к якорю работают
  корректно, карточка не обрезается. Mobile (390px): карточка выходит за
  экран — тот же уже задокументированный page-wide баг `.docs-card-grid` без
  mobile-брейкпоинта (не регрессия от этого среза, будет исправлен в
  отдельном срезе адаптации после Этапа 3). Скриншоты 97–101.
- [x] Понятные отметки доступности функции и требуемых прав. `_docs_item()`
  получил `requires_settings: tuple[str, ...]`, генерирующий бейдж
  `"<short_ru> по настройке"` для каждого ключа из уже существующего единого
  источника `SETTING_META` (`settings_common.py`) — тот же паттерн, что и
  сегодняшняя унификация ролей/RP-действий: не выдумывать текст заново, а
  выводить его из данных. Проверено по коду хендлеров (не угадано), какие
  реальные настройки гейтят какие команды: `economy_enabled` — вся
  «Экономика и предметы» (панель/ферма/магазин+инвентарь+крафт/
  рынок+переводы+аукцион/рост); `craft_enabled` — доп. гейт на `/craft`;
  `auctions_enabled` — доп. гейт на `/auction`/`/bid`; `titles_enabled` +
  `economy_enabled` — титулы; `family_tree_enabled` — усыновление/питомцы/
  `/family`; `actions_18_enabled` — 18+ reply-действия и явное действие
  роста. Заменены 2 расплывчатых плейсхолдер-бейджа «по настройкам» (не
  говорили, какая именно настройка) на точные, и убраны 4 дублирующих
  расплывчатых prose-заметки того же смысла («Крафт может быть отключён
  настройками чата», «Титулы должны быть включены настройками чата» и т.д.).
  Сознательно НЕ добавлен бейдж на «Как играть в «Кто я»» — там `actions_18_enabled`
  открывает только одну тему внутри игры, а не всю функцию, бейдж на всю
  карточку был бы неточным (существующая prose-заметка точнее). **Найден и
  исправлен по пути CI-баг того же класса**, что уже был у admin_docs:
  `render_user_docs_fixture.py` вызывал `build_user_docs_context()` напрямую,
  но новый импорт `SETTING_META` транзитивно тянет `selara.core.config`
  (pydantic) через `chat_settings.py` — сломало бы CI frontend job (только
  `Jinja2`). Переписан на ручную сборку контекста (как у всех остальных
  `render_*_fixture.py`), проверено против изолированного Jinja2-only venv.
  Новый `test_web_user_docs.py::test_user_docs_availability_badges_are_derived_from_setting_meta`
  фиксирует все 8 бейджей и отсутствие старого плейсхолдера. Playwright
  spot-check (desktop): бейджи не переполняют карточку, хорошо читаются.

#### Администраторская документация

- [x] Доступ только в подходящем защищённом контексте. **Найден реальный
  баг** при подготовке этого пункта: `/app/docs/admin` использовал ту же
  форму context-builder'а, что и намеренно публичный `/app/docs/user`
  (`redirect_path` всегда `None`) — страница была так же полностью доступна
  без сессии, хотя по этому же пункту roadmap'а должна быть за защищённым
  доступом. Не найдено ни одного места, которое реально его гейтило. Исправлено:
  `_build_admin_docs_page_context` теперь требует обычную пользовательскую
  веб-сессию (тот же уровень, что и `/app` — `_load_user_from_request`,
  редирект на `/login` при отсутствии), а НЕ отдельную
  ADMIN_PASSWORD-сессию `/app/admin` (это другая концепция — общие
  bot-wide инструменты оператора, а не документация по администрированию
  конкретного чата). Новый `tests/unit/test_web_admin_docs_access.py`:
  редирект на `/login` без cookie, `200` с валидной сессией (реальный
  ASGI-запрос, не просто чтение кода).
- [x] Настройки чата, роли, permissions, aliases, triggers, рассылки и обслуживание.
  Все темы, кроме двух последних, уже были покрыты секциями `admin_docs.py`
  (настройки — через `SETTINGS_GROUPS`; роли/permissions — через
  `build_roles_docs()`, добавленный в срезе про RP-действия; aliases и
  triggers — отдельные существующие секции). **Рассылки** и
  **обслуживание** не имели вообще ни одной секции — добавлены две новые:
  «Рассылки» (текст/медиа, реакции, поиск аудитории с select all/none,
  история с прогрессом и статусами доставки, submit guard от повторной
  отправки) и «Обслуживание» (ручной запрос backup, клиентский поиск по
  списку таблиц, edit/delete с destructive-warning и восстановлением
  фокуса, «Технический режим» на странице чата как переход в общий
  обозреватель БД, а не отдельная функция). Контент сверен с реальным кодом
  композера (`admin.html` — `data-broadcast-*` атрибуты форм) и роутов
  (`/app/admin/request-backup`, `/app/admin/table/{table_name}`), не
  придуман. Регрессия закрыта
  `test_web_admin_docs.py::test_admin_docs_covers_broadcasts_and_maintenance`.
- [x] Ссылки из полей админки ведут сразу к релевантному anchor. Уже было
  реализовано: каждая карточка настройки в `chat.html` — `href="{{
  admin_docs_url }}#{{ item.doc_anchor }}"`, `doc_anchor` строится через
  `setting_anchor(key)` (`presenters.py:620`), а `admin_docs.py` использует
  ТУ ЖЕ функцию `setting_anchor()` для anchor'ов секции настроек — единый
  источник, не может рассинхронизироваться. Просто не было ни строчки теста
  и не был отмечен в TODO. Закрыто верификацией (без изменений кода):
  новый `test_web_docs_anchors.py::test_setting_field_doc_links_resolve_on_the_admin_docs_page`
  строит реальные `doc_anchor` из `build_settings_sections()` и сверяет их
  с реальными `id` на отрендеренной `admin_docs.html` — не просто «обе
  стороны вызывают одну функцию», а честная сквозная проверка результата.
- [x] Примеры включают ошибочные варианты и способы исправления. `_FEATURE_DOC_SECTIONS`
  items получили опциональные `examples`/`notes` (шаблон `admin_docs.html`
  расширен блоками «Частые ошибки»/«Важно», переиспользуют существующие
  CSS-классы `.docs-card-block`/`.docs-code-list` из user_docs.html — новый
  CSS не понадобился). «Подсказки по формату» (работа с настройками)
  получила 2 пары ❌/✅ с текстом ошибок, процитированным дословно из
  `core.chat_settings.parse_chat_setting_value` («Значение должно быть целым
  числом», «Значение должно быть > 0»), не придуманным.
- [x] Отображать ограничения Telegram рядом с соответствующими возможностями.
  2 факта, проверенных по реальному коду, а не угаданных: «Текст и медиа»
  (рассылки) — лимит фото 10 МБ/JPEG-PNG (клиентская проверка в
  `admin-broadcast.js:340`); «Ручной запрос backup» (обслуживание) —
  архив режется на части по 45 МБ, потому что боты не могут слать файлы
  крупнее ~50 МБ (`infrastructure/backup.py: BACKUP_CHUNK_SIZE_BYTES`).
  Оба пункта закрывают весь раздел «Администраторская документация»
  полностью (5 из 5). Регрессия зафиксирована двумя новыми тестами в
  `test_web_admin_docs.py`.

#### Качество документации

- [x] Search index строится детерминированно и не содержит защищённые данные.
  `admin_docs.html` получил тот же клиентский поиск, что и `user_docs.html`
  (переиспользованы `docs-search.css`/`.js` без изменений — общие
  `data-docs-search-*` атрибуты), `search_text` считается один раз в
  `build_docs_sections()` из статичных строк секций, без обращения к БД.
  Область поиска — 11 секций `_FEATURE_DOC_SECTIONS` (вход, панель, группа,
  настройки, роли, алиасы, триггеры, аудит, игры, рассылки, обслуживание);
  таблица ролей и группы настроек сознательно не включены — у них уже есть
  своя компактная структура (полноценный поиск по ним — не в объёме этого
  пункта). Тест `test_admin_docs_search_text_is_deterministic_and_holds_no_protected_data`
  доказывает: два независимых вызова `build_admin_docs_context()` дают
  идентичный результат, и что реальные данные чата (`chat_id`, `chat_title`)
  не попадают в `search_text`, даже когда передан `chat`.
  **Найден и исправлен реальный баг по пути** (тот же класс, что уже был у
  `.admin-table-card[hidden]` в предыдущем срезе этой сессии): `.docs-card`
  имеет собственный `display: grid`, который побеждает браузерный
  `[hidden]{display:none}` при равной специфичности — карточки, скрытые
  поиском внутри секции, у которой ЕСТЬ другие совпадения, оставались
  визуально видимыми. Обнаружено не чтением кода, а тем, что тест на
  видимость (`:visible`, а не просто атрибут `[hidden]`) не совпал со
  скриншотом. Существовавшие Playwright-тесты поиска (для `user_docs.html`
  из прошлого среза) проверяли только наличие атрибута `[hidden]`, а не
  реальную видимость — тот же баг был и там, просто не пойман тестами;
  оба файла переведены на CSS-селектор `:visible`, который отражает
  фактическое состояние рендера. Фикс — `[data-docs-search-card][hidden] {
  display: none; }` в `docs-search.css`.
- [x] Проверить keyboard navigation, heading hierarchy, links и mobile layout.
  Новый `test_web_docs_quality.py`: (1) heading hierarchy — обе страницы
  парсятся на `<h1>`–`<h6>`, проверено отсутствие пропуска уровня (h2 → h4 и
  т.п.) в реальной последовательности заголовков, не выборочно; обе страницы
  чистые. (2) keyboard navigation — реальный `.focus()` в Chromium
  подтверждает, что поиск, кнопка «Копировать» и deep-link иконка — настоящие
  фокусируемые `<input>`/`<button>`/`<a>` (не `<div>` с обработчиком клика),
  плюс сквозной клик подтверждает, что активация действительно работает. (3)
  links — уже покрыто `test_web_docs_anchors.py` (уникальность id,
  разрешаемость `#`-ссылок), включая новый сквозной тест deep-link из полей
  настроек. (4) mobile layout — не переисследовано заново: уже
  задокументированный `.docs-layout` баг без mobile-брейкпоинта остаётся
  сознательно отложенным до отдельного среза адаптации (решение от
  2026-08-16), само наличие бага уже подтверждено замером в предыдущих
  срезах.
- [~] Playwright: public user docs, protected admin docs, search, deep link и mobile
  TOC. Четыре из пяти пунктов закрыты реальными Playwright/ASGI-тестами:
  public user docs (`test_web_user_docs_public_access.py`), protected admin
  docs (`test_web_admin_docs_access.py`), search
  (`test_web_user_docs_browser.py`, `test_web_admin_docs_browser.py`), deep
  link (`test_item_deep_link_anchor_scrolls_clear_of_sticky_topbar` +
  `test_every_docs_card_has_a_unique_deep_link_id`). Mobile TOC — не может
  быть протестирован раньше, чем реализован: responsive TOC/drawer сам по
  себе намеренно отложен до отдельного среза адаптации после Этапа 3.

**Критерий завершения этапа 3:** документация ищется, открывается по стабильным
ссылкам и не расходится с источником команд/настроек.

### Этап 4. Остальные серверные страницы

Каждая страница проходит общий pipeline: baseline tests → новый layout/components →
responsive → accessibility → visual review → TODO update.

- [ ] `landing.html`: ясное позиционирование, CTA, performance и mobile layout.
- [ ] `login.html`: Telegram login flow, ошибки и mobile keyboard.
- [ ] `home.html`: группы, состояния аккаунта, быстрые переходы и empty state.
- [ ] `achievements.html`: responsive cards, progress semantics и длинные названия.
- [ ] `feedback.html`: форма, validation, success/error и защита повторной отправки.
- [ ] `chat.html`:
  - [ ] разделить большой шаблон на partials/components;
  - [ ] рейтинг и статистика;
  - [ ] настройки и permissions;
  - [ ] aliases/triggers;
  - [ ] live updates;
  - [ ] mobile navigation между секциями;
  - [ ] вынести inline-JavaScript в тестируемые modules.
- [ ] `family.html`: responsive graph controls, empty/error и JS extraction.
- [ ] `economy.html`: responsive data presentation, forms и JS extraction.
- [ ] `audit.html`: если не завершён в admin-этапе, применить общий timeline pattern.
- [ ] `error.html`: понятные 4xx/5xx состояния, recovery actions и request ID при
  наличии.
- [ ] Проверить все cross-page links и возврат назад после действий.

### Этап 5. Игры — отдельный высокорисковый блок

- [ ] Зафиксировать все игровые сценарии и состояния до изменения DOM.
- [ ] Разделить `_games_dashboard.html` на небольшие Jinja partials.
- [ ] Разделить inline-JavaScript `games.html` на modules по игровым режимам.
- [ ] Сохранить SSE/live update contracts и reconnect behavior.
- [ ] Унифицировать lobby, create game, active game, results и error components.
- [ ] Обеспечить touch-first управление без hover-only действий.
- [ ] Проверить длинные имена, максимальное количество игроков и reconnect.
- [ ] Playwright: создание/подключение/ход/завершение для каждого режима, desktop и
  mobile.
- [ ] Visual snapshots только стабильных ключевых сцен, с маскированием динамических
  timer/id значений.

### Этап 6. Очистка, performance и финальные quality gates

- [ ] Удалить подтверждённо недостижимые React admin/docs modules и их CSS/API types.
- [ ] Удалить мёртвые Jinja partials, CSS selectors и JavaScript.
- [ ] Проверить отсутствие дублирующих активных UI-реализаций.
- [ ] Перевести djLint, Stylelint, ESLint, rendered HTMLHint и axe smoke в blocking
  CI.
- [ ] Зафиксировать Lighthouse baseline для публичной landing, login, user docs и
  репрезентативной авторизованной страницы.
- [ ] После нескольких стабильных прогонов согласовать performance/accessibility
  budgets и сделать их блокирующими.
- [ ] Проверить размер CSS/JS, количество шрифтов и third-party ресурсов.
- [ ] Проверить cache headers и fingerprinting статических ресурсов.
- [ ] Провести ручной keyboard-only проход критических сценариев.
- [ ] Провести ручную проверку smartphone и tablet в реальном Safari/Chrome.
- [ ] Обновить `README`, admin/user guides и developer instructions.
- [ ] Согласовать production rollout и rollback plan.
- [ ] Добавить итоговую запись в `CHANGELOG.md`.

## 6. Матрица обязательной проверки каждой страницы

Исполнитель не помечает страницу `[x]`, пока не проверены применимые пункты:

### Функциональность

- [ ] Основной happy path.
- [ ] Пустые данные.
- [ ] Ошибка backend/Telegram/сети.
- [ ] Ошибка validation с сохранением ввода.
- [ ] Истёкшая сессия и отсутствие прав.
- [ ] Повторный submit/double click.
- [ ] Большой объём данных и pagination.

### Responsive

- [ ] Desktop Chromium 1440×900.
- [ ] Mobile Chromium.
- [ ] Mobile WebKit.
- [ ] Tablet WebKit.
- [ ] Нет горизонтального page overflow.
- [ ] Touch actions доступны и не перекрываются.
- [ ] Virtual/mobile keyboard не скрывает активное поле или submit.

### Accessibility

- [ ] Семантические landmarks и корректная heading hierarchy.
- [ ] Labels и accessible names всех controls.
- [ ] Полный keyboard flow.
- [ ] Видимый focus.
- [ ] Корректный порядок focus после dialog/navigation.
- [ ] Status/error не передаётся только цветом.
- [ ] axe WCAG A/AA scan без необъяснённых нарушений.
- [ ] Контраст подтверждён для основных состояний.
- [ ] Reduced motion учтён.

### Контент и устойчивость

- [ ] Длинные русские и английские строки.
- [ ] Непрерывная строка/ID/URL.
- [ ] Emoji и composed emoji.
- [ ] CJK и RTL sample.
- [ ] Отсутствующие optional fields.
- [ ] Безопасное escaping пользовательского содержимого.

### Инженерные проверки

- [ ] Backend unit/integration tests.
- [ ] Jinja compile/render tests.
- [ ] djLint.
- [ ] Stylelint.
- [ ] ESLint, если есть JavaScript.
- [ ] Playwright functional/responsive tests.
- [ ] Visual snapshot только для согласованных стабильных состояний.
- [ ] `git diff --check`.

## 7. Отложенные задачи

- [ ] Начать сохранять исходящие сообщения бота для будущей полной истории диалога.
- [ ] Определить модель хранения reply-связей исходящих сообщений.
- [ ] Определить retention/privacy policy полного архива.
- [ ] Решить, нужно ли сохранять удаления сообщений и service events.
- [ ] Решить, какие Telegram media кешировать локально и на какой срок.
- [ ] После появления данных расширить историю до полной двусторонней ленты.

## 8. Сводный прогресс

**Общий прогресс roadmap: 27%** (было 26%). Значение рассчитано как среднее завершение семи
этапов; прогресс приоритетного этапа админки отслеживается отдельно.

| Этап | Статус | Выполнено | Примечание |
|---|---|---:|---|
| 0. Baseline и проверки | `[~]` | 40% | Debt guards; ESLint/Stylelint; rendered admin HTMLHint fixture |
| 1. Дизайн-система | `[~]` | 15% | Семантический base и page-specific CSS/JS foundation |
| 2. Админка | `[~]` | 98% | 2.2, 2.3, 2.5, 2.6 закрыты полностью; admin shell Desktop/Mobile/Tablet Chromium закрыт (WebKit заблокирован, `[!]`); 2.4 закрыт кроме одного пункта — единственное, что осталось во всём Этапе 2: индексированный full-text поиск (нужна миграция на продовый Postgres — решение вынесено пользователю, см. журнал решений) |
| 3. Документация | `[~]` | 38% | Модель контента: закрыта, кроме slash-команд по разделам (`[?]`, зафиксировано в журнале решений, требует выбора объёма от пользователя). «Пользовательская документация» и «Администраторская документация» закрыты полностью, кроме намеренно отложенного responsive TOC/drawer. «Качество документации» закрыта: heading hierarchy, keyboard navigation, links, поиск на обеих страницах (найден и исправлен реальный `[hidden]` CSS-баг того же класса, что был у admin-table-card), Playwright-покрытие public/protected/search/deep-link (mobile TOC — блокирован на adaptation-срезе). Остаётся: решение по slash-командам, затем адаптация под mobile/tablet |
| 4. Остальные страницы | `[ ]` | 0% | После админки и документации |
| 5. Игры | `[ ]` | 0% | Высокий риск, выполнять последними |
| 6. Очистка и quality gates | `[ ]` | 0% | После миграции страниц |

Процент обновляется по фактически завершённым дочерним задачам, а не субъективной
оценке исполнителя.

## 9. Журнал решений

| Дата | Решение | Причина | Кто подтвердил |
|---|---|---|---|
| 2026-08-15 | Jinja остаётся для server Web UI, React — для Mini App | Один активный источник истины для каждого интерфейса | Пользователь |
| 2026-08-15 | Первый приоритет — админка, особенно рассылки и история | Самая высокая операционная ценность | Пользователь |
| 2026-08-15 | После админки выполняется единая документация | Все варианты документации подтверждены | Пользователь |
| 2026-08-15 | Стиль Selara сохраняется, но модернизируется | Нужна узнаваемость без устаревшего визуального шума | Пользователь |
| 2026-08-15 | Поддерживаются современные smartphone/tablet, Chrome и Safari | Подтверждённая целевая матрица | Пользователь |
| 2026-08-15 | Полный outbound archive отложен | Для первого этапа достаточно улучшить представление существующих данных | Пользователь |
| 2026-08-15 | Feature toggle не нужен, rollout пока не определён | Решение о выкладке будет принято позднее | Пользователь |
| 2026-08-15 | WebKit-прогон browser-тестов отложен, перенесён в будущее CI/Docker-окружение | На dev-машине (Fedora 44) недоступны нужные системные библиотеки (ICU 74, libjpeg ABI 8); Playwright официально поддерживает автоустановку зависимостей только для Ubuntu/Debian | Пользователь |
| 2026-08-16 | Индексированный full-text поиск в архиве отложен, Этап 2 считается фактически закрытым | Это миграция схемы продовой Postgres-БД (tsvector + GIN + russian-словарь), а не Web UI-срез; ломает нынешний тестовый параллелизм SQLite/Postgres. Подстрочный ILIKE-поиск с подсветкой уже работает и покрывает текущую потребность | Пользователь |
| 2026-08-16 | Адаптация документации (`admin_docs.html`/`user_docs.html`) под смартфоны и планшеты выполняется отдельным срезом ПОСЛЕ завершения остальной «Модели контента» и остальных пунктов Этапа 3, а не сразу после обнаружения | Найденный page-wide баг (`.docs-layout`/`.docs-card-grid` без mobile-брейкпоинта, подтверждён замером) — реальная и нужная задача, но пользователь предпочёл сначала закрыть контентную часть Этапа 3 (единый источник команд/примеров, anchors, секретность admin-процедур), а адаптацию под маленькие экраны сделать отдельным целевым срезом после | Пользователь |
| 2026-08-16 | Единый источник для slash-команд по разделам (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/`user_docs.py`/`admin_docs.py`) отмечен `[?]` и пропущен, Этап 3 продолжается без него — переход к адаптации, затем Этап 4 | Объём (80–150+ команд по грубой оценке, 904 строки в 3 файлах документации + сами web-доки, без готового каталога-источника, в отличие от ролей/RP-действий) требует решения пользователя об объёме подхода (полный генерируемый каталог vs. аудит на расхождения vs. отложить), а пользователь явно разрешил помечать такие пункты в TODO и идти дальше вместо блокировки | Claude — ожидает подтверждения пользователя |

## 10. Журнал работ

Каждая строка добавляется в начале таблицы под заголовком.

| Дата | Исполнитель | Задача | Статус | Commit/проверки | Примечание |
|---|---|---|---|---|---|
| 2026-08-16 | Claude | Этап 3: «Качество документации» закрыта, поиск добавлен в admin_docs, slash-команды помечены `[?]` | `[~]` | commit: pending; 1034 unit (новые: `test_web_docs_quality.py` 3/3, `test_web_admin_docs_browser.py` 1/1, +тесты search/determinism в `test_web_admin_docs.py`); ESLint/Stylelint (`docs-search.css` расширен); HTMLHint ×9; `git diff --check`; Playwright spot-check | Поиск добавлен на `admin_docs.html` (переиспользует `docs-search.css`/`.js` без изменений). **Найден и исправлен реальный баг** того же класса, что был у `.admin-table-card[hidden]` в прошлом срезе: `.docs-card{display:grid}` побеждал браузерный `[hidden]` — карточки, отфильтрованные поиском внутри частично совпадающей секции, оставались видимыми. Существующие Playwright-тесты поиска (написанные в прошлом срезе для user_docs) не ловили это, потому что проверяли только атрибут `[hidden]`, а не факт рендера — оба файла переведены на CSS-селектор `:visible`. Закрыты: heading hierarchy (без пропуска уровней на обеих страницах), keyboard navigation (реальный `.focus()` на поиске/копировании/deep-link), search index детерминированность и отсутствие защищённых данных (два вызова context-builder'а дают идентичный результат; реальный `chat_id`/`chat_title` не попадают в индекс). Slash-команды по разделам зафиксированы как `[?]` в журнале решений — объём (80–150+ команд, 904 строки в 3 файлах без готового каталога-источника) требует решения пользователя, пропущено без блокировки по его инструкции. Этап 3: ~31% → ~38%. Дальше: решение по slash-командам или сразу адаптация под mobile/tablet. |
| 2026-08-16 | Claude | Этап 3: «Администраторская документация» закрыта полностью (5 из 5) | `[x]` | commit: 6147a52; 1027 unit (2 новых теста в `test_web_admin_docs.py`); HTMLHint ×9 (обновлён `render_admin_docs_fixture.py` с примером examples/notes); Playwright spot-check 3 карточек; `git diff --check` | Добавлены секции «Рассылки» (текст/медиа, реакции, аудитория с поиском, история доставки, submit guard) и «Обслуживание» (backup, обзор таблиц, edit/delete, «Технический режим») — контент сверен с реальным кодом композера и роутов, не придуман. Верифицирован (не изменён) уже рабочий deep-link `?` из карточек настроек в `chat.html` на anchor в `admin_docs.html` — новый сквозной тест `test_setting_field_doc_links_resolve_on_the_admin_docs_page`. Добавлены `examples`/`notes` в шаблон (переиспользуют существующие CSS-классы, новый CSS не понадобился): пары ❌/✅ с текстом ошибок настроек, процитированным дословно из `parse_chat_setting_value`; 2 факта об ограничениях Telegram, проверенных по коду (`admin-broadcast.js` 10 МБ фото, `backup.py` chunking на 45 МБ из-за лимита ~50 МБ). Этап 3: ~26% → ~31%. |
| 2026-08-16 | Claude | Этап 3: отметки доступности функции/настроек — закрывает «Пользовательскую документацию» кроме responsive TOC | `[x]` | commit: 210bcb0; 1023 unit (новый тест в `test_web_user_docs.py`); ESLint/Stylelint/HTMLHint ×9 без изменений (переписан `render_user_docs_fixture.py`); Playwright spot-check; `git diff --check` | `_docs_item()` получил `requires_settings`, генерирующий бейджи `"<label> по настройке"` из единого источника `SETTING_META` вместо расплывчатых hand-typed плейсхолдеров («по настройкам» без указания какой). 8 бейджей на 6 карточек, все проверены по реальному коду хендлеров (`economy_enabled`/`craft_enabled`/`auctions_enabled`/`titles_enabled`/`family_tree_enabled`/`actions_18_enabled`), не угаданы. Сознательно пропущена «Кто я» — там 18+ гейтит только одну тему внутри игры, не всю карточку. Найден и исправлен CI-баг того же класса, что уже был у admin_docs: новый импорт `SETTING_META` в `user_docs.py` транзитивно тянет pydantic через `chat_settings.py`, `render_user_docs_fixture.py` пришлось переписать на ручную сборку контекста (проверено против Jinja2-only venv). Это закрывает весь раздел «Пользовательская документация» кроме намеренно отложенного responsive TOC/drawer (решение от 2026-08-16). Этап 3: ~22% → ~26%. |
| 2026-08-16 | Claude | Этап 3: закрыт auth-баг на `/app/docs/admin` (был полностью публичным) | `[x]` | commit: 69d3ee0; 1022 unit (новый `test_web_admin_docs_access.py` 2/2); `git diff --check` | Найдено при работе над user-docs срезом: `/app/docs/admin` использовал форму context-builder'а, скопированную с намеренно публичного `/app/docs/user` — `redirect_path` был всегда `None`, страница отдавалась без единой проверки сессии. По этому же пункту roadmap'а («Администраторская документация» → «Доступ только в подходящем защищённом контексте») это прямое нарушение — не найдено. Исправлено минимально: `_load_user_from_request` + редирект на `/login`, тот же уровень, что у `/app` (`_build_home_page_context`), НЕ отдельная ADMIN_PASSWORD-сессия `/app/admin` (разные концепции: bot-wide операторские инструменты vs. документация по администрированию чата). Раздел «Администраторская документация»: 1 из 5 пунктов закрыт. |
| 2026-08-16 | Claude | Этап 3: публичный доступ (route-тест), task-oriented разделы, копируемые примеры + deep links, полнотекстовый поиск | `[~]` | commit: f6e0a9f; 1020 unit (новые: `test_web_user_docs_public_access.py` 2/2, `test_web_user_docs_browser.py` 7/7, `test_server_ui_baseline.py`+2, `test_web_user_docs.py`+4); ESLint/Stylelint (новые `docs-item-actions.css`/`.js`, `docs-search.css`/`.js`); HTMLHint ×9 (новый `render_user_docs_fixture.py`, первый CI-фикстур для `user_docs.html`); `git diff --check`; 5 итоговых screenshot (97–101, desktop/tablet/mobile) | Четыре закрытых пункта «Пользовательской документации». (1) Публичный доступ: подтверждено чтением кода + реальным ASGI route-тестом без cookie (`_load_user_from_request` не трогает БД без токена). (2) Task-oriented разделы: уже соответствовали требованию, чек-лист закрыт без изменений кода. (3) Копируемые примеры + deep links: каждая карточка получила стабильный id (`{section}-item-{n}`) и видимую deep-link иконку `#` (opacity 0.55 в покое — доступна и без hover), каждый пример — кнопку «Копировать» (Clipboard API + `execCommand` fallback). Найден и исправлен реальный баг: переход по новому deep-link якорю прятал заголовок карточки под sticky-топбар — `.docs-card` не входил в существующее `scroll-margin-top: 148px` в `panel.css` (уже применялось к `.docs-section` и т.п.), просто забыли добавить туда новый селектор. (4) Полнотекстовый поиск: клиентский фильтр по паттерну `admin-table-search.js`, `search_text` считается один раз на сервере в `_docs_item()`. Playwright подтвердил: desktop/tablet работают чисто, поиск/копирование/deep-link — всё корректно; mobile показывает уже задокументированный `.docs-card-grid` overflow (не новый, будет исправлен отдельным срезом адаптации). Slash-команды по разделам (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`) и «Понятные отметки доступности функции» остаются открытыми пунктами. Этап 3: ~13% → ~20%. |
| 2026-08-16 | Claude | Этап 3: единый источник RP-действий (Reply-триггеры без 18+/18+) | `[~]` | commit: 2116a20; 1008 unit (3 новых файла/дополнения: `test_commands_catalog.py`, `test_web_user_docs.py`, `test_text_commands_18_gate.py`); Playwright spot-check desktop (карточка не переполняется, все 101 li видимы); `git diff --check` | Последний открытый под-пункт «Модели контента»: команды/примеры для RP-действий. `catalog.py` получил публичный `SOCIAL_ACTION_18_PLUS` (перенесён из приватного дубля в `text_commands.py`) и `build_social_action_docs()`, строящий `{command_key, canonical trigger, is_18_plus}` из уже существовавших `COMMAND_KEY_DEFAULT_SOURCE_TRIGGER`+`SOCIAL_COMMAND_KEY_TO_ACTION`+`SOCIAL_ACTION_18_PLUS`; `user_docs.py` больше не хардкодит два списка триггеров, а строит их из этой функции. Перевод на единый источник вскрыл реальный баг рассинхронизации: 20 настоящих RP-действий отсутствовали в публичной документации целиком, ещё 4 показывались под неканоническим альтернативным триггером вместо основного (подробности — в чек-листе выше и в `test_web_user_docs.py::test_user_docs_rp_action_lists_match_canonical_catalog_source`). Playwright-проверка (desktop 1440px) подтвердила: карточка со 101 items (98 триггеров + 3 примера) не переполняется по горизонтали, все элементы видимы. На mobile (390px) карточка выходит за пределы viewport — это уже задокументированный, неисправленный в этом срезе page-wide баг `.docs-layout`/`.docs-card-grid` без mobile-брейкпоинта (см. решение от 2026-08-16 про отдельный срез адаптации после Этапа 3), не регрессия от этого среза. `help.py` намеренно НЕ переведён на этот источник — его краткая строка для `/help` физически не вместит 118 триггеров, это осознанная выжимка, а не дублирующий баг. Slash-команды по разделам (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/`user_docs.py`) остаются отдельной открытой задачей. Этап 3: ~10% → ~13%. |
| 2026-08-16 | Claude | CI-фикс fixture-рендерера + anchors/internal-links тест + аудит секретности | `[x]` | commit: 09b8da6; anchors/internal-links: новый `test_web_docs_anchors.py` (2/2, self-verified на синтетическом broken HTML); проверено против изолированного Jinja2-only venv, зеркалящего CI; реальный GitHub Actions run подтверждён зелёным после пуша | `render_admin_docs_fixture.py` (добавлен в предыдущем срезе) вызывал `build_admin_docs_context()` напрямую, что транзитивно тянет `selara.core.config` → pydantic-settings; CI frontend job ставит только `Jinja2`. Root-caused через `gh run view --log-failed`, переписан на ручную сборку контекста (как у всех остальных `render_*_fixture.py`). Заодно закрыты ещё два пункта «Модели контента» Этапа 3: автоматическая проверка уникальности anchors и внутренних `#`-ссылок (`html.parser.HTMLParser`, без regex, реальный рендер `admin_docs.html`/`user_docs.html`); построчный аудит всех 671 строки `user_docs.py` против `ADMIN_GUIDE.md`, подтвердивший отсутствие admin-процедур в публичном разделе (найден отдельный, вне-скоупный momент: 18+ секция лежит открытым текстом без возрастного гейта на публичной странице — зафиксировано, решение не принято). Этап 3: ~8% → ~10%. |
| 2026-08-16 | Claude | Этап 3: инвентаризация документации + единый источник ролей/прав | `[~]` | commit: 9eb7167; 941 unit; ESLint/Stylelint не затронуты (нет JS/CSS правок); HTMLHint ×8 (новый `render_admin_docs_fixture.py`); `git diff --check`; 1 итоговый screenshot (96) | Первый срез Этапа 3. Инвентаризация (агентом) нашла конкретные расхождения: `docs/bot_docs/roles.md` не содержал `co_owner`; секция ролей в `admin_docs.py` не подключала `core/roles.py` вовсе; labels прав существовали в 3 независимых копиях с реальным расхождением текста между ботом и вебом (`moderation.py`: «доступ команд» vs web: «доступ к командам»). Всё исправлено: `PERMISSION_LABELS_RU`/`permission_label_ru`/`permissions_text_ru` перенесены в `core/roles.py` как единственный источник, `admin_docs.py` получил `build_roles_docs()` (реальная таблица ролей/рангов/прав из `SYSTEM_ROLE_TEMPLATES`), `moderation.py` и `presenters.py` теперь читают эти labels оттуда же вместо своих копий. По пути была попытка «улучшить» сортировку прав в `permissions_text_ru` — сломала 2 существующих теста, сразу же отменена (не входило в объём среза, сохранён оригинальный порядок). Также была попытка унифицировать `RoleTemplate.title_ru` для junior_admin («Мл. админ» → «Младший админ») — отменена после того, как выяснилось, что абревиатура «Мл. админ» устоявшаяся и используется как пример в `stats.py`, `private_panel.py`, `ADMIN_GUIDE.md` и тестах; попытка внести исправление привела бы к новому расхождению вместо старого. Найден и задокументирован (не исправлен — вне объёма) pre-existing page-wide баг: `.docs-layout`/`.docs-card-grid` не имеют mobile-брейкпоинта на всей странице (не только в новой roles-секции — подтверждено сравнением с уже существующей `docs-trigger-types`). Команды/примеры/anchors-проверка остаются открытыми пунктами «Модели контента». Этап 3: 0% → ~8%. |
| 2026-08-16 | Claude | Архив: закрытие Playwright-матрицы + raw-режим (уже существовал) | `[x]` | commit: 3ee68a4; 932 unit; route 2 новых; browser 2 новых (long feed, 2 viewport); `git diff --check`; 2 итоговых screenshot (94–95) | «Технический табличный/raw режим» отмечен выполненным задним числом — кнопка «Технический режим» уже вела на generic DB explorer той же таблицы `messages`, отфильтрованной по чату; это не новая функция, а уже существующая связь. Закрыты последние пробелы Playwright-матрицы архива: route-тесты на полностью пустой архив («Архив пока пуст») и отфильтрованный-в-ноль архив («По этим фильтрам ничего не найдено» + рабочий сброс); browser-тест длинной ленты (50/137 сообщений, desktop+mobile) без page overflow с видимой пагинацией. Остальные пункты матрицы (edits/replies/media/mobile/no-overflow/deep-link) уже были закрыты в предыдущих срезах архива. Единственный оставшийся пункт всего Этапа 2 — индексированный full-text поиск (требует миграции на продовый Postgres, отдельное архитектурное решение, вынесено пользователю). |
| 2026-08-15 | Claude | Главная админки: сворачиваемые секции + empty/large/error/loading (2.2 закрыт полностью) | `[x]` | commit: 3ee68a4; 928 unit; focused overview browser 11/11 (4 новых); focused route 1/1 новый; ESLint; Stylelint; HTMLHint; `git diff --check`; 4 итоговых screenshot (90–93) | Домены `table_sections` теперь `<details open>` со стрелкой-индикатором (новый CSS-файл `admin-table-search.css`, вынесен из admin-overview.css — тот был впритык у лимита 520/520); поиск по таблицам принудительно раскрывает свёрнутый домен при совпадении. Критичные секции (внимание/сервисы/заявки) остаются обычными `<section>`, никогда не сворачиваются. Large-state тест (24 заявки + 60 карточек таблиц) поймал реальный баг: `.admin-table-card` свой `display:flex` побеждал `[hidden]` на равной специфичности — карточки не скрывались при частичном совпадении внутри секции; малые тесты его не ловили, потому что там пряталась вся секция целиком. Исправлено явным `[hidden]{display:none}` для карточки. Error/flash баннер на `/app/admin` теперь route-тестирован явно. По пути тоже словил и разобрался с ложным CSS-багом (артефакт незавершённого transition при чтении computed style без задержки — не настоящая проблема, тот же класс issue, что и раньше в сессии). Раздел 2.2 закрыт полностью. Этап 2: 93% → 95%. |
| 2026-08-15 | Claude | DB explorer: real-backend CRUD-тест (2.6 закрыт полностью) | `[x]` | commit: 3ee68a4; 923 unit; новый `test_web_admin_table_crud.py` 5/5; `git diff --check` | Новый файл с реальным FastAPI + sqlite (без моков) для update/delete generic DB explorer — доказывает реальную мутацию/удаление строки, не только вызов мока. Explorer не имеет UI-действия «создать запись» (только edit/delete существующих), поэтому «CRUD» здесь = R+U+D, покрывающий весь функционал инструмента; полный live-браузерный E2E (реальный HTTP + Playwright) — отдельная, более крупная задача тестовой инфраструктуры этапа 0. Раздел 2.6 (Generic DB explorer) закрыт полностью. Визуальных изменений нет — чисто тестовый срез, screenshot не требовался. Этап 2: 91% → 93%. |
| 2026-08-15 | Claude | DB explorer: клиентский поиск по списку таблиц | `[x]` | commit: 3ee68a4; 918 unit; focused overview browser 7/7 (1 новый); route-тест обновлён; ESLint; Stylelint; HTMLHint; `git diff --check`; 4 итоговых screenshot (86–89) | Новый маленький файл `admin-table-search.js` (24 строки, добавлен baseline-guard в `test_server_ui_baseline.py`) — чистая клиентская фильтрация без нового серверного round-trip (список таблиц уже целиком в DOM). Фильтрует по имени+ключу (`data-table-search-text`), скрывает секции с 0 совпадений, показывает «Совпадений не найдено» при полном промахе. `admin-overview.css` уперался в лимит 520/520 — стили нового блока уложены без превышения (переиспользованы глобальные `.admin-search-input`/`.sr-only`, добавлено только 12 строк layout/empty-state). Label поиска — sr-only (не видимый визуально «kicker», как в archive/broadcast), решил не добавлять новый CSS-класс ради этого при нулевом бюджете CSS-лимита; placeholder поля даёт достаточный визуальный контекст, что подтверждено на скриншотах. Этап 2: 90% → 91%. |
| 2026-08-15 | Claude | Заявки и backup: inline-ошибки рядом с действием | `[x]` | commit: 3ee68a4; 917 unit; focused feedback browser 6/6 (2 новых); focused overview browser 6/6 (1 новый); ESLint; Stylelint; HTMLHint; `git diff --check`; 4 итоговых screenshot (82–85) | Оба действия (статус заявки, запрос backup) переведены с native form-submit на `fetch()` к уже существующим JSON API — серверный код не менялся, чисто фронтенд. Ошибка показывается в новом `[data-feedback-item-error]`/`[data-admin-backup-error]` anchor рядом с конкретным действием, без reload; при ошибке заявки остальные карточки списка не затрагиваются; backup-диалог остаётся открытым вместо скрытия. Обновлены/переписаны 2 существующих browser-теста, которые раньше полагались на native-submit workaround (`event.preventDefault()` инъекция) — тот же класс проблемы с `about:blank`+relative fetch, что и в срезе рассылок; решено тем же паттерном (routed fake origin + `page.route`). Deferred-item чек-лист пункт (2.4) отмечен выполненным — он уже существовал как раздел 7 «Отложенные задачи». Этап 2: 88% → 90%. |
| 2026-08-15 | Claude | Admin shell: выделенные Desktop/Mobile/Tablet Chromium тесты | `[x]` | commit: 3ee68a4; 915 unit; новый `test_web_admin_shell_browser.py` 6/6; `git diff --check`; 4 итоговых screenshot (78–81) | Новый файл с тестами именно shell'а (раньше проверялся только попутно внутри тестов каждой страницы): active-state pill отличается не только цветом (border+background+box-shadow), skip-link — hidden/focus-visible переход и keyboard flow до `#main-content`, mobile nav — `.top-actions` реально `overflow-x: auto` (scroll-ribbon), tablet (820px) — подтверждено, что все 5 пунктов помещаются в один ряд без scroll-ribbon (континуальный responsive без отдельного tablet-брейкпоинта — осознанное решение, теперь закреплено регресс-тестом, а не только визуальной проверкой). По пути найден и исправлен flaky-баг в самих тестах (не в проде): `page.set_content()` + последовательные `add_style_tag()` оставляют CSS-transition skip-link'а «в полёте» на момент чтения `getComputedStyle` — computed transform читался как identity вместо реального translateY; исправлено принудительным отключением transition в тестовом окружении. Пункт «Создать единый admin shell» закрыт (кроме WebKit, отдельный `[!]`). Этап 2: 87% → 88%. |
| 2026-08-15 | Claude | Admin shell: диагностика WebKit-блокера на Fedora | `[!]` | commit: none (только диагностика, код не менялся) | `playwright install webkit` скачал бинарник успешно, но `install-deps` не работает на Fedora (нет apt-get). Через `ldd` подтверждено: не хватает ICU 74 и libjpeg ABI 8, которых на Fedora 44 нет (ICU 77, libjpeg.so.62) и подмена версий через симлинк технически некорректна (versioned C++ symbols у ICU). Решение с пользователем: честно пометить `[!]` в TODO, WebKit-прогон — отдельная будущая задача (CI/Docker с Ubuntu), не блокирует остальной roadmap. |
| 2026-08-15 | Claude | Рассылки: ошибки формы по блокам + Playwright-матрица отправки | `[x]` | commit: 3ee68a4; 909 unit; focused route 17/17 (8 новых, включая Telegram-failure); focused browser 11/11 (2 новых, 4 переписаны под fetch); ESLint; Stylelint; HTMLHint (rendered fixture); frontend build; `git diff --check`; 5 итоговых screenshot (73–77, desktop/mobile/tablet) | Composer переведён с нативного `<form action>` POST на `fetch()` к уже существующему `/api/admin/broadcasts/send` — иначе привязка ошибок к блоку была невозможна (при redirect-based submit состояние формы всегда терялось). `_execute_admin_broadcast` и `_json_result` теперь возвращают/принимают `field` (`message`/`media`/`audience`/`None`); три новых anchor'а в секциях 1–3 (`data-broadcast-{message,media,audience}-error`), общий `data-broadcast-form-error` остаётся для непривязанных ошибок (сеть, сессия истекла). Обнаружено и исправлено при ручном прогоне тестов: (1) старые browser-тесты полагались на `page.set_content()`, где страница живёт на `about:blank` — relative `fetch()` там падает с TypeError ещё до сетевого запроса, `page.route()` перехват никогда не срабатывал; решение — маленький helper `_goto_admin_page()`, который отдаёт ту же разметку через `page.route` на фейковом `http://selara.test/admin` origin, чтобы `fetch("/api/...")` резолвился и перехватывался нормально; (2) `page.route(..., lambda route: route.fulfill(...))` без `await` внутри лямбды не await-ится Playwright и просто зависает — заменено на `async def`-хендлеры везде; (3) тест на "медиа"-ошибку падал из-за нативной HTML5 `required`-валидации файлового поля, блокирующей submit ещё до JS — обойдено через `form.noValidate = true`. admin-broadcast.js держится на 459/460 строк лимита (введён `resetSubmitButton()` вместо дублирования сброса состояния в трёх местах). Полностью закрыт весь список Playwright-сценариев: текст/фото/hybrid reactions (уже существовавшие route-тесты с реальной sqlite БД), invalid input (новые field-scoped тесты), Telegram failure (новый `PartiallyFailingBot`, проверка `sent`/`failed` статусов доставок), duplicate submit (один fetch на confirm), mobile flow (существующий + новый mobile screenshot). Раздел 2.3 закрыт полностью. Прогресс этапа 2: 85% → 87%. |
| 2026-08-15 | Claude | Архив сообщений: переход к найденному сообщению («Показать в контексте») | `[x]` | commit: 3ee68a4; 899 unit; focused route 21/21 (6 новых); focused browser 8/8 (3 новых); ESLint; Stylelint; HTMLHint (rendered fixture, включая новую markup-ветку); frontend build; `git diff --check`; 5 итоговых screenshot (68–72, desktop/mobile/tablet) | Под любым активным фильтром карточка сообщения получает кнопку «Показать в контексте» (`.button.ghost.small`, 40px — тот же паттерн, что и у других вторичных действий страницы) → новый endpoint `/app/admin/archive/jump/{id}` (admin-only) вычисляет page по rank сообщения в неотфильтрованной (только chat_id) ленте одним COUNT-запросом и редиректит на `messages_compact?chat_id&page&highlight=<id>`; landing page скроллит к целевому сообщению, подсвечивает его (`.is-jump-target`, outline+background, не только цветом) на ~2.6s и переводит на него клавиатурный фокус (`tabindex="-1"` + `.focus()`), с уважением `prefers-reduced-motion`. Итерация по визуальному циклу: первая версия ссылки была обычным текстовым линком (~16.5px высоты — ниже 44px и вслед за уже существующим паттерном «Открыть raw-снимок»), при ручном просмотре screenshot решено сделать её полноценной small-кнопкой, чтобы не заводить новый sub-44px тач-таргет в этом срезе; «Открыть raw-снимок» оставлен как есть (не в объёме среза). Также при первом рендере скриншотов скролл на mobile обрывал карточку по нижнему краю viewport — оказалось, что screenshot снимался до завершения smooth-scroll анимации; исправлено увеличением задержки перед снимком в render-скрипте (не в проде — там анимация не блокирует функциональность). Действие нацелено только на основную (последнюю) версию сообщения группы, не на отдельные snapshot-версии в истории редакций — осознанное ограничение объёма. djLint пока не подключён в репозитории (отдельный незакрытый пункт этапа 0) — Jinja-шаблон проверен через рендер + HTMLHint + существующие render-тесты. Этап 2: 84% → 85%. |
| 2026-08-15 | Claude | Admin login: mobile-баг наложения карточек, submit guard, info hygiene | `[x]` | commit: pending; 888 unit; focused browser 6/6; ESLint; Stylelint; HTMLHint ×7; frontend build; 6 desktop/mobile/tablet/error/rate-limit/submit-guard screenshots | Найден реальный баг: `.login-side`/`.login-side-card` не имели CSS, из-за чего на mobile боковые карточки накладывались на текст формы (unstyled auto-колонка grid). Общий фикс в `server-ui-foundation.css` заодно чинит `login.html` (тот же shared markup) — вне заявленного объёма среза, но неизбежно из-за общих классов. Добавлен submit guard, убрано раскрытие `ADMIN_PASSWORD` в helper-тексте, добавлен `for`/`id` для HTMLHint. Прогресс этапа 2: 78% → 84%; общий прогресс roadmap: 19% → 20% |
| 2026-08-15 | Claude | Generic DB explorer: пагинация, edit/delete safety, touch targets | `[x]` | commit: pending; 882 unit; focused route 11/11; focused browser 6/6; ESLint; Stylelint; HTMLHint ×6; frontend build; 7 desktop/mobile/tablet/dialog/empty screenshots | Исправлен краш и отрицательный offset при нечисловом `page`; urlencode-пагинация вместо ручной конкатенации; delete-диалог называет удаляемую запись и восстанавливает фокус на вызвавшую кнопку; destructive-warning + submit guard на edit; JS вынесен в `admin-table.js`; badge/touch-target/warning CSS — в новый `admin-table.css`, чтобы не расширять `panel.css` сверх baseline-лимита; добавлены sr-only labels для фильтров и `type="button"`/`for`/`id` по замечаниям HTMLHint. Поиск по списку таблиц на `admin.html` оставлен отдельным незакрытым пунктом ниже. |
| 2026-08-15 | Codex | Журнал аудита чата: timeline/table hybrid | `[x]` | commit: pending; 874 unit; focused 50/50; audit Chromium 4/4; ESLint; Stylelint; HTMLHint ×4; frontend build; 8 desktop/mobile/tablet screenshots | Русские labels и категории; группировка по датам; URL-поиск/фильтры; раскрываемые event/action/actor/target ID без raw meta; безопасный DB error, empty/filtered/long/responsive states |
| 2026-08-15 | Codex | Заявки пользователей: task-like workflow | `[x]` | commit: pending; 866 unit; focused 76/76; feedback Chromium 5/5; ESLint; Stylelint; HTMLHint ×3; frontend build; 8 desktop/mobile/tablet screenshots | URL-фильтр статуса, lifecycle/history, быстрый status update с сохранением фильтра/позиции, submit guard, empty/error/long/responsive states |
| 2026-08-15 | Codex | Обязательный workflow UI-срезов | `[x]` | commit: pending; baseline contract test; document review | Tests-first, временный рендер в `/tmp`, ручная проверка каждого PNG, итерационное сравнение и финальное сохранение в `~/selara_screen` обязательны для всех исполнителей |
| 2026-08-15 | Codex | Главная админки: operational overview и безопасные быстрые действия | `[x]` | commit: pending; 857 unit; focused 67/67; overview Chromium 5/5; ESLint; Stylelint; HTMLHint ×3; frontend build; 7 desktop/mobile/tablet screenshots | Честный status/attention summary без ложного Telegram health; шесть task-oriented переходов; backup dialog с focus restore и submit guard; empty/responsive states; устранены дублирующий logout, нативный button cascade и видимый край скрытой skip-link |
| 2026-08-15 | Codex | Архив сообщений: защищённый photo preview | `[x]` | commit: pending; 845 unit; focused 51/51; archive Chromium 5/5; ESLint; Stylelint; HTMLHint ×3; frontend build; 6 desktop/mobile/tablet screenshots | Admin-only media endpoint без раскрытия file_id; явная загрузка и сворачивание без повторного запроса; session/type/10 МБ guards; responsive preview и восстановимая ошибка |
| 2026-08-15 | Codex | Системные рассылки: ×4-срез истории и detail view | `[x]` | commit: pending; 839 unit; focused 56/56; broadcast Chromium 5/5; ESLint; Stylelint; HTMLHint ×3; frontend build; 9 desktop/mobile/tablet screenshots | Статусные history cards с прогрессом и реакциями; безопасный message preview и полная summary; URL-фильтры доставок; responsive native/inline/custom/paid reactions, reply-ответы и действия бота без дублирования данных |
| 2026-08-15 | Codex | Архив сообщений: тройной срез навигации и читаемости | `[x]` | commit: pending; 832 unit; focused 44/44; archive Chromium 3/3; ESLint; Stylelint; HTMLHint ×2; frontend build; desktop/mobile screenshots | Стабильная cursor pagination без offset-дублей; группировка соседних сообщений одного автора; безопасные media metadata без раскрытия file_id и автоматической загрузки Telegram files |
| 2026-08-15 | Codex | Архив сообщений: сворачиваемая история редакций | `[x]` | commit: pending; 824 unit; focused 36/36; Chromium archive 4/4; ESLint; Stylelint; HTMLHint ×2; desktop/mobile screenshots | Самый свежий snapshot остаётся основной карточкой, предыдущие версии доступны через keyboard/touch details и отдельные raw-ссылки; устранены шаблонные пробелы в pre-wrap тексте |
| 2026-08-15 | Codex | Архив сообщений: фильтры, URL-состояние и подсветка поиска | `[x]` | commit: pending; 824 unit; focused 36/36; archive Chromium 4/4; ESLint; Stylelint; HTMLHint ×2; frontend build; desktop/mobile/tablet screenshots | Диапазон дат, все Telegram content types, edited/reply, защита от malformed URL, безопасная подсветка; индексированный full-text остаётся следующим улучшением поиска |
| 2026-08-15 | Codex | Архив сообщений: mobile overflow на длинном имени | `[x]` | commit: pending; archive Chromium 4/4; geometry check; повторный screenshot 09 | Grid track ограничен доступной шириной; кнопка «Фильтры» и message cards снова имеют правый внутренний отступ и полные скругления |
| 2026-08-15 | Codex | Архив сообщений: первый dialogue-view срез | `[x]` | commit: pending; 822 unit; archive Chromium 4/4; ESLint; Stylelint; HTMLHint ×2; frontend build; 4 screenshot slices | Desktop split view; mobile/tablet list→dialog; даты, авторы, reply, edited, raw и честный media fallback; WebKit, media preview и edit grouping остаются следующими задачами |
| 2026-08-15 | Codex | Устранение дублирующего photo preview в composer | `[x]` | commit: pending; focused tests 15/15; Chromium 7/7; ESLint; Stylelint; HTMLHint; 6 screenshot slices | В форме остались имя файла и компактная галочка, изображение показывается только в live preview |
| 2026-08-15 | Codex | Визуальная responsive-полировка системной рассылки | `[x]` | commit: pending; 817 unit; Chromium 7/7; ESLint; Stylelint; HTMLHint; frontend build; npm audit 0; 6 screenshot slices | Исправлены textarea cascade, modal focus/scroll, file picker, mobile width, tablet/sticky layout; WebKit matrix остаётся отдельной задачей этапа 0 |
| 2026-08-15 | Codex | Рассылка: постоянный preview и подтверждение отправки | `[x]` | commit: pending; 815 unit; Chromium 5/5; ESLint; Stylelint; HTMLHint; frontend build | Whitelist sanitizer, photo/reactions preview, mobile dialog и focus restore |
| 2026-08-15 | Codex | Рассылка: JS extraction, audience search, inline validation, submit guard | `[x]` | commit: pending; 813 unit; ESLint; Stylelint; HTMLHint; Chromium 3/3; frontend build; npm audit 0 | Сохранены фото, hybrid reactions и backend-контракт |
| 2026-08-15 | Codex | Общий admin shell: разделы, active state, mobile overflow, keyboard skip-link | `[~]` | commit: pending; unit suite 810 passed | Chromium 390 px пройден; WebKit/tablet ещё впереди |
| 2026-08-15 | Codex | Регрессии admin auth/logout | `[x]` | commit: pending; unit suite 810 passed | Закрыт unauthenticated GET edit; admin logout направлен в отдельную admin-сессию |
| 2026-08-15 | Codex | Этап 0: инвентаризация и baseline-проверки server UI | `[~]` | commit: pending | Активный первый рабочий блок |
| 2026-08-15 | Codex | Создание единого roadmap/TODO | `[x]` | commit: pending; document review | Зафиксированы решения пользователя, baseline и полный фронт работ |
