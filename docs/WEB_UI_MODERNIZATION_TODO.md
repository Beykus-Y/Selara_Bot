# Модернизация серверного Web UI Selara — рабочий TODO

> [!IMPORTANT]
> **Обязательная инструкция для всех ИИ, агентов и разработчиков.**
>
> Перед любыми изменениями Jinja-шаблонов, серверного Web UI, его CSS, JavaScript,
> документации или UI-тестов необходимо полностью прочитать этот документ.
> Этот файл является единым источником правды по модернизации интерфейса и должен
> обновляться в том же коммите, что и выполненная работа.
> Порядок выполнения, визуального сравнения и сохранения результатов каждого среза
> определён в обязательной инструкции `docs/WEB_UI_SLICE_WORKFLOW.md`.
>
> Каждый исполнитель обязан:
>
> 1. Перед началом работы перечитать разделы «Зафиксированные решения»,
>    «Порядок реализации» и относящийся к задаче checklist.
> 2. Проверить актуальный `git status` и не перезаписывать чужие незавершённые
>    изменения.
> 3. Пометить взятую задачу как `[~]`, указать исполнителя и дату в журнале работ.
> 4. Не ставить `[x]`, пока реализация, адаптивность и обязательные проверки задачи
>    не завершены.
> 5. После завершения заменить `[~]` на `[x]` и записать дату, commit SHA и перечень
>    пройденных проверок. Если коммита ещё нет, временно указать `commit: pending` и
>    заменить его после публикации.
> 6. Новые обнаруженные проблемы, зависимости и решения добавлять в этот документ,
>    а не хранить только в переписке или памяти агента.
> 7. При блокировке поставить `[!]`, описать точную причину и требуемое решение.
> 8. Не менять подтверждённые продуктовые решения молча. Любое отступление сначала
>    фиксируется в разделе «Журнал решений» с объяснением.
> 9. После каждого рабочего блока обновлять сводный прогресс и журнал работ, даже
>    если блок завершён частично.
>
> Обозначения: `[ ]` — не начато, `[~]` — в работе, `[x]` — завершено и проверено,
> `[!]` — заблокировано, `[?]` — требуется решение пользователя.

## 1. Цель

Обновить весь серверный Web UI Selara, сделать его современнее, удобнее и
последовательнее, сохранив узнаваемый тёмный неоновый стиль проекта.

Главные результаты:

- функциональная и визуально цельная серверная админка;
- история сообщений, читаемая как лента группового диалога, а не таблица БД;
- единая пользовательская и администраторская документация с поиском;
- полноценная адаптация для современных смартфонов и планшетов;
- проверяемая доступность, семантика, адаптивность и производительность;
- единая система компонентов вместо несвязанных шаблонов, inline-стилей и
  монолитного CSS;
- отсутствие дублирующихся активных реализаций одного интерфейса.

## 2. Зафиксированные решения

Эти решения подтверждены пользователем 2026-08-15.

### Архитектура

- [x] Серверные страницы `/app`, админка и документация остаются на Jinja.
- [x] React остаётся интерфейсом Telegram Mini App (`/miniapp`).
- [x] Неиспользуемые React-копии серверной админки и документации подлежат удалению
  только после инвентаризации зависимостей и подтверждения функционального паритета.
- [x] Новый переключатель между старым и новым дизайном не нужен.
- [x] Существующие URL, авторизация и серверные сценарии должны сохраняться при
  последовательной замене страниц.

### Приоритеты

1. Серверная админка.
2. Внутри админки: системные рассылки и история сообщений.
3. Пользовательская и администраторская документация.
4. Остальные серверные страницы.
5. Игровые страницы — после стабилизации общей системы компонентов.

### Визуальное направление

- [x] Сохранить тёмную неоновую идентичность Selara.
- [x] Освежить визуальный язык: меньше декоративного шума, яснее иерархия,
  современная типографика, более спокойные поверхности и выразительные состояния.
- [x] Проектировать mobile-first, но не превращать desktop-интерфейс в растянутую
  мобильную колонку.
- [x] Поддерживать современные смартфоны и планшеты.
- [x] Обязательные браузерные семейства: Chrome/Chromium и Safari/WebKit.

### История сообщений

- [x] Основное представление — список чатов и читаемая лента сообщений.
- [x] Нужны группировка по датам, авторы, время, reply-контекст, редакции, поиск и
  фильтры.
- [x] Нужны превью поддерживаемых медиа и понятный fallback, если файл недоступен.
- [x] Raw JSON остаётся доступным как отдельный технический режим, но не является
  основным представлением.
- [x] Сохранение всех исходящих сообщений бота отложено на будущий этап.
- [x] На текущем этапе интерфейс использует уже сохраняемые входящие сообщения и
  снимки редакций. Он не должен вводить администратора в заблуждение и называться
  полной копией истории Telegram.

### Документация

- [x] Нужны отдельные пользовательская и администраторская области.
- [x] Пользовательская документация должна быть доступна без административного
  входа, если материал не раскрывает защищённые данные.
- [x] Нужен полнотекстовый поиск.
- [x] Нужны оглавление, стабильные anchors/deep links и мобильная навигация.
- [x] Команды, настройки и описания должны получать данные из единого источника,
  чтобы Jinja, Mini App и бот не расходились вручную.

### Качество

- [x] Stylelint для CSS.
- [x] djLint с Jinja-профилем для исходных шаблонов.
- [x] HTMLHint для отрендерированного HTML, где он даёт полезный сигнал без ложных
  срабатываний на Jinja.
- [x] ESLint для вынесенного серверного JavaScript.
- [x] Playwright для функциональных, responsive и визуальных сценариев.
- [x] axe-core для автоматически проверяемой части WCAG A/AA.
- [x] Lighthouse CI: сначала отчёты и baseline, затем согласованные бюджеты.
- [x] Автоматические проверки не заменяют ручную проверку клавиатуры, читаемости,
  порядка фокуса и сложных responsive-состояний.

### Выкладка

- [?] Стратегия первой production-выкладки нового интерфейса пока не определена.
- [x] Feature flag или пользовательский переключатель старого/нового дизайна не
  требуется.
- [ ] До начала production-выкладки отдельно согласовать размер первого релиза,
  порядок миграции страниц и способ отката.

## 3. Текущая техническая база

Инвентаризация на 2026-08-15:

- 21 Jinja HTML-файл в `src/selara/web/templates`;
- около 6 206 строк Jinja/HTML;
- `src/selara/web/static/panel.css` — около 6 145 строк;
- около 1 942 строк inline-JavaScript в пяти шаблонах;
- 38 inline-атрибутов `style`;
- таблицы используются в `chat.html`, `admin_table.html` и
  `admin_messages_compact.html`;
- текущий Python Playwright используется точечно, полноценной матрицы серверного UI
  пока нет;
- React-исходники содержат старые/параллельные страницы админки и документации, но
  активный router Mini App их не публикует;
- Jinja autoescape включён, но тестовый режим с `StrictUndefined` не настроен;
- адаптивные правила существуют, но покрывают лишь отдельные области большого
  монолитного stylesheet.

Самые рискованные по объёму шаблоны:

- `_games_dashboard.html` — около 1 244 строк;
- `chat.html` — около 1 321 строки и большой inline-JavaScript;
- `games.html` — около 964 строк JavaScript поверх крупного partial;
- `admin.html` — около 385 строк после extraction и добавления preview/confirm;
- `economy.html` — около 353 строк.

## 4. Общие правила реализации

- [ ] Не выполнять массовый rewrite всех страниц одним коммитом.
- [ ] Следовать полному workflow из `docs/WEB_UI_SLICE_WORKFLOW.md`, включая ручной
  просмотр каждого финального screenshot и сохранение в `~/selara_screen`.
- [ ] Работать вертикальными срезами: тесты и состояние до изменения → компоненты →
  страница → responsive → accessibility → проверки → обновление TODO.
- [ ] Сохранять существующие серверные маршруты и backend-контракты, если изменение
  контракта не согласовано отдельно.
- [ ] Не смешивать редизайн с несвязанными изменениями бизнес-логики.
- [ ] Не скрывать технические возможности generic DB explorer; отделить их от
  продуктовых представлений.
- [ ] Не использовать цвет как единственный способ передачи статуса.
- [ ] Все интерактивные элементы должны работать с клавиатуры и иметь видимый focus.
- [ ] Touch targets основных действий — не менее 44×44 CSS px либо эквивалентная
  удобная зона нажатия.
- [ ] Горизонтальная прокрутка всей страницы запрещена. Она допустима только внутри
  явно обозначенного технического контейнера таблицы.
- [ ] Длинные Telegram-имена, непрерывные строки, emoji, RTL/CJK, пустые данные и
  большие наборы записей входят в обязательные тестовые состояния.
- [ ] Все destructive-действия имеют понятное подтверждение, описание объекта и
  безопасное восстановление фокуса после закрытия диалога.
- [ ] Визуальные эффекты учитывают `prefers-reduced-motion`.
- [ ] Секреты, сессионные данные и приватное содержимое не попадают в публичные
  Lighthouse/Playwright-артефакты.

## 5. Порядок реализации

### Этап 0. Baseline, аудит и инфраструктура проверок `[~]`

#### Инвентаризация

- [x] Составить таблицу всех серверных маршрутов, шаблонов, API и требуемой
  авторизации.
- [x] Для каждой страницы перечислить состояния: normal, empty, loading/submit,
  validation error, permission denied, long content и large dataset.
- [x] Найти и задокументировать используемые CSS-классы, design tokens, inline-стили,
  inline-JavaScript и дубли компонентов.
- [x] Проверить, какие React admin/docs modules действительно не достижимы из
  production router и не импортируются активным Mini App.
- [ ] Зафиксировать текущие desktop/mobile screenshots ключевых страниц до
  визуальных изменений.
- [x] Составить карту текущих форм и destructive-действий, включая требования к
  авторизации и redirect/JSON-режимам.

#### Тестовая инфраструктура

- [ ] Определить единый Playwright runner для широкого UI-набора. Предпочтительный
  вариант: Playwright Test в Node-пакете с запуском реального FastAPI и тестовой БД;
  существующие Python browser-тесты мигрировать только после появления паритета.
- [ ] Добавить воспроизводимые test fixtures: администратор, обычный пользователь,
  пустой чат, активный чат, длинные имена, рассылка, ответы и архив сообщений.
- [ ] Настроить проекты:
  - [ ] Desktop Chromium, 1440×900.
  - [ ] Mobile Chromium, современный Android viewport.
  - [ ] Mobile WebKit, современный iPhone viewport.
  - [ ] Tablet WebKit, современный iPad viewport.
- [ ] Добавить smoke-проверки всех серверных маршрутов без console/page errors.
- [~] Добавить проверку отсутствия горизонтального page overflow на mobile/tablet.
- [!] **Нестабильность browser-тестов в полном прогоне.** За сессию 2026-08-16
  трижды наблюдалась одна и та же картина: тест на Playwright падает только
  в полном `pytest` прогоне и проходит и в изоляции, и на чистом дереве
  (`test_activity_event_runtime_backfill_enables_event_reads`,
  `test_server_admin_broadcast_composer_handles_photo_and_reactions`,
  `test_admin_overview_prioritizes_tasks_without_overflow[390px]`). Каждый раз
  проверялось `git stash` + повторным запуском, регрессии не подтверждались.
  Это не блокирует работу сейчас (CI пока зелёный), но при росте browser-набора
  станет источником ложных красных прогонов.
  - **2026-08-17: найдена и исправлена одна из трёх причин.**
    `test_admin_overview_prioritizes_tasks_without_overflow[390px]` читал
    `bounding_box()` skip-link'а через фиксированные `wait_for_timeout(180)`
    после `Tab`, а `.skip-link{transition:transform 140ms ease}` — запас всего
    40мс. Под нагрузкой полного прогона (тысячи предыдущих ассертов,
    GC/scheduler jitter) этого запаса не хватает, и геометрия читается
    «в полёте» — тот же класс бага, что уже был найден и исправлен в
    `test_web_admin_shell_browser.py` (2026-08-15, commit 3ee68a4) тем же
    способом: `page.add_style_tag(content="*, *::before, *::after
    { transition: none !important; }")` в `_mount()`
    (`tests/unit/test_web_admin_overview_browser.py`). Проверено в изоляции
    (15 прогонов подряд зелёные — но тест был стабилен в изоляции и раньше,
    флак проявлялся только в полном прогоне) и полным прогоном `tests/unit`
    после фикса.
  - **2026-08-17 (позже): найдена и исправлена вторая из трёх причин.**
    `test_activity_event_runtime_backfill_enables_event_reads` — не
    browser-тест, а датозависимый флейк: `now = datetime.now(timezone.utc)`
    в тесте, события создаются на `now - 1ч/-3ч/-6ч`, затем
    `get_chat_activity_daily_series(days=1)` бакетирует по **календарному
    дню UTC** (`repositories.py:1896`: `today = datetime.now(timezone.utc).date()`,
    фильтр `activity_date >= today`) и тест утверждает, что в бакете «сегодня»
    ровно 3 события. Если тест запускался в окне 00:00-06:00 UTC, событие
    «6 часов назад» попадало на ВЧЕРА и выпадало из бакета — assertion видел
    2, а не 3, и падал. Причина понята математически точно (не байки), фикс
    без новых зависимостей (`freezegun` и т.п. в проекте нет): `now` заякорен
    на полдень UTC того же дня (`replace(hour=12, ...)`) — `now - 6ч` = 06:00,
    гарантированно тот же календарный день при любом реальном времени запуска
    теста. Заодно объясняет более раннюю находку в журнале (2026-08-16, Opus):
    "датозависимым флейком (переход полночи в рамках сессии)" — тот же баг,
    тогда не был исправлен, только констатирован. Третья причина
    (`test_server_admin_broadcast_composer_handles_photo_and_reactions` —
    беглый просмотр `admin-broadcast.js` не выявил похожей проблемы: клик по
    подтверждению вызывает `form.requestSubmit()` синхронно, disable кнопки
    происходит до `fetch`, гонки не видно) **остаётся неподтверждённой** — не
    гадать и не чинить вслепую, ждать следующего наблюдаемого падения.
  - **2026-08-18: четвёртая, отдельная причина найдена и исправлена в CI
    (не в полном локальном прогоне).** `test_archive_jump_target_scrolls_
    highlights_and_focuses[False]` упал в GitHub Actions (`admin-archive.js`
    вызывает `target.scrollIntoView({ behavior: "smooth" })`, тест ждал
    ровно фиксированные `wait_for_timeout(400)`, затем один раз проверял
    геометрию) — воспроизведено и локально (1 из 3 прогонов в изоляции).
    Длительность `smooth`-скролла в headless Chromium не гарантирована в
    400мс, особенно под нагрузкой CPU (тот же класс проблемы, что уже
    дважды чинился в этом разделе — жёсткий `wait_for_timeout` вместо
    ожидания реального условия). Исправлено: вместо одной проверки после
    сна — цикл опроса `getBoundingClientRect()` каждые 100мс до 2с, тест
    падает только если элемент реально не попал в viewport за 2 секунды
    (гарантия теста не ослаблена, устранена только гонка со временем).
    Проверено 5 прогонов подряд в изоляции — 0 падений (был 1/3 до фикса);
    полный `tests/unit` 1143 passed.
- [ ] Настроить trace, screenshot и video только при ошибке CI.
- [x] Определить стабильную среду визуальных snapshots: одинаковые Linux image,
  Chromium/WebKit versions, fonts и timezone.
  - Закрыто (2026-08-17). Проверено чтением `.github/workflows/ci.yml`, не
    предположено: Linux image уже зафиксирован (`ubuntu-latest`), Chromium
    ставится детерминированно версией из `playwright install --with-deps
    chromium` (привязана к запакованной версии `playwright` в lockfile), шрифты
    уже пофиксены отдельным шагом («Install snapshot fonts» —
    `fonts-noto-core`/`fonts-noto-cjk`/`fonts-noto-color-emoji`, добавлено в
    более раннем срезе этой же инициативы). Единственный реально
    отсутствовавший компонент — timezone: раннее в этой же сессии найден и
    исправлен датозависимый флейк-тест именно из-за незафиксированной
    полуночной UTC-границы (`test_activity_event_runtime_backfill_enables_
    event_reads`, см. журнал выше) — добавлен явный `TZ: UTC` в `env:` job'а
    `backend` (было implicit по умолчанию раннера, не задокументировано и не
    гарантировано). WebKit — уже отдельно зафиксирован `[!]` (недоступен на
    dev-машине, решение пользователя от 2026-08-15), не блокирует закрытие
    этого пункта.
- [x] Добавить axe-core scan ключевых страниц и интерактивных состояний.
  - Закрыто (2026-08-17), по решению пользователя «ABC», шаг C. Установлен
    `axe-core` (npm, `frontend/package.json` devDependency) — реальная
    библиотека Deque Systems, не самописная эвристика. Новый
    `scripts/axe_scan_report.py`, report-only (как djLint), переиспользует
    все 22 состояния из `css_coverage_report.py` (тот же список
    `_fixture_pages()`/`_app_rendered_pages()`/`_direct_render_pages()`/
    `_family_page()` — импортированы напрямую, не продублированы) —
    инжектит `axe.min.js` через `page.add_script_tag()`, гоняет
    `window.axe.run()`, агрегирует находки по странице и по правилу.
    **Первый прогон нашёл 5 категорий реальных нарушений**, не пропущено
    ни одной: `select-name` (critical, 6 элементов `<select>` без
    доступного имени — `_chat_settings_panel.html`, `_chat_triggers_panel.html`
    ×2, `_chat_audit_and_aliases.html` ×2, `_games_dashboard.html`,
    `economy.html` ×2 — исправлено добавлением `aria-label`, для полей с
    существующим заголовком (`item.title`) переиспользован он же, не
    выдуман новый текст); `aria-required-children` (critical,
    `.mode-switch[role="tablist"]` в `_chat_overview_tab.html` — 3 кнопки
    без `role="tab"`/`aria-selected`, ARIA-паттерн был начат, но не
    закончен — добавлены оба атрибута, `aria-selected` теперь также
    синхронизируется в `chat-overview.js`'s `syncModeButtons()`, не только
    в разметке); `page-has-heading-one` (moderate, 4 admin-страницы без
    `<h1>` — `admin_messages_compact.html`, `admin_table.html` (оба
    состояния: список и пустой список), `admin_edit.html` — добавлен
    визуально скрытый `<h1 class="sr-only">` с тем же текстом, что видимый
    `<h2>`, вместо замены самого `<h2>` на `<h1>` — `.panel h2` стилизован
    отдельным CSS-правилом, замена тега сломала бы визуальный размер
    заголовка; `.sr-only` уже существовал в `server-ui-foundation.css`, не
    придуман новый); `heading-order` (moderate, `landing.html` таймлайн
    прыгал h2→h4 — `.step-title` стилизован по классу, не по тегу,
    поэтому замена `<h4>` на `<h3>` безопасна визуально). Повторный прогон
    после всех фиксов — **0 находок на всех 22 состояниях**. Полный
    `tests/unit` 1143 passed; djLint — 0 новых находок (1 существующая,
    подтверждено `git diff` — не в изменённых строках); HTMLHint на 3
    рендер-фикстуры — 0; `npm run lint:server-ui:js` — чисто; скриншоты
    подтверждают отсутствие визуальных изменений. Не проверено (честно):
    `admin.html` (сложный дашборд, ещё не в списке 22 состояний coverage-
    скрипта), состояния с реальными данными (алиасы/триггеры/семья с
    непустым графом) — та же неполнота покрытия, что уже задокументирована
    для CSS coverage, отдельный возможный следующий шаг.
- [ ] Настроить Lighthouse CI в report-only режиме без публичной отправки приватных
  отчётов.

#### Линтинг и шаблоны

- [x] Подключить djLint в check-only режиме с `profile=jinja`.
- [x] Сначала зафиксировать baseline существующих нарушений, затем запрещать новые.
  - 2026-08-17: `djlint>=1.36.4,<2` добавлен в `[project.optional-dependencies].dev`
    (pyproject.toml), конфиг `[tool.djlint]` (`profile="jinja"`,
    `ignore="J018"`). `J018` («используйте `{{ url_for() }}`») отключён явно и
    с обоснованием в комментарии — это соглашение Flask, а этот проект
    намеренно использует raw string paths везде (проверено многократно за всю
    эту инициативу) плюс свой `static_url()` для статики; без отключения
    J018 давал 63 из 71 находки — чистый шум от несовпадения конвенций, не
    реальные проблемы. Новый npm-скрипт `lint:server-ui:jinja` — **намеренно
    не включён** в цепочку `lint`/`lint:server-ui`, которую вызывает CI
    (`npm run lint`, `.github/workflows/ci.yml:112`), проверено запуском
    `npm run lint` до и после — поведение CI не изменилось. Это и есть
    «check-only режим» из формулировки пункта: линтер подключён и
    запускаем вручную, но не блокирует. Baseline после игнора J018 — 8
    находок, все не реальные баги: 5 — известный false positive парсера
    djLint на корректном экранировании литерных скобок `{{ '{{' }} и {{ '}}' }}`
    (используется в `_chat_triggers_panel.html`/`admin_docs.html` для
    документирования синтаксиса переменных пользователю), 2 — стилистические
    рекомендации (spaceless tags, meta description), сознательно не
    подавлены дальше — оставлены видимыми, чтобы не ослеплять линтер на
    будущие реальные находки того же класса.
- [x] Добавить тест компиляции всех Jinja-шаблонов.
- [~] Добавить representative render-тесты с `StrictUndefined`, не включая его в
  production до проверки всех контекстов.
- [x] Подключить HTMLHint к отрендерированным representative pages.
- [x] Подключить Stylelint и official standard config в report-only режиме.
- [x] Подключить ESLint к серверным JavaScript-модулям после их появления.
- [x] Постепенно перевести линтеры из report-only в blocking CI после очистки
  baseline.
  - Закрыто (2026-08-17) как исправление устаревшей отметки, не как новая
    работа: `HTMLHint`/`Stylelint` были помечены `[~]` «report-only», но по
    факту оба уже давно **блокируют CI** — `.github/workflows/ci.yml:113`
    (`npm run lint`) запускает `eslint . && npm run lint:server-ui`, а
    `lint:server-ui` — чистую `&&`-цепочку `lint:server-ui:js &&
    lint:server-ui:css && lint:server-ui:html` без единого `|| true` или
    `continue-on-error`. Проверено запуском обоих напрямую: `npm run
    lint:server-ui:css` — exit 0, `npm run lint:server-ui:html` — exit 0,
    оба реально упали бы CI при находке. ESLint (и frontend, и server-ui)
    блокирует так же. Единственные два линтера НЕ в blocking-цепочке —
    оба намеренно, не по недосмотру: `djLint` (решение зафиксировано ранее
    в этой же сессии — 8 baseline-находок, 5 из них подтверждённый false
    positive парсера на корректном `{{ '{{' }}`-экранировании, блокировать
    CI на неисправимых ложных срабатываниях не имеет смысла) и `axe-core`
    (`scripts/axe_scan_report.py`, добавлен позже в этой же сессии —
    намеренно report-only: покрытие состояний ещё не полное — `admin.html`
    и данные-насыщенные состояния не охвачены, включение в blocking CI до
    расширения покрытия дало бы либо ложное чувство полноты, либо
    периодические красные билды на нововизитированных страницах).

**Критерий завершения этапа 0:** текущий интерфейс имеет воспроизводимый baseline,
основные сценарии защищены тестами, а новые проверки не создают неконтролируемый
шум в CI.

### Этап 1. Дизайн-система и общий серверный layout

#### Design tokens

- [~] Инвентаризировать текущие CSS custom properties и удалить смысловые дубли.
  - Инвентаризация закрыта (2026-08-17), удаление дублей — отдельный
    большой трек, не форсирован здесь. `panel.css` определяет 28 базовых
    токенов в `:root` (шрифты, `--bg-0/1/2`, `--surface*`, `--line*`,
    `--text`/`--muted*`, акцентные цвета, `--shadow*`, `--radius-*`, `--gap`,
    `--content-width`) плюс легитимные scoped-переопределения: 4×
    `--card-accent` (per-card-type акцент), `body.page-games` (светлая тема
    games-страницы поверх тёмного приложения — `--text`/`--muted`/`--line`/
    `--shadow` переопределены на светлые значения) и landing-специфичный
    блок (`--font-ui`/`--font-display`/`--font-mono` + отдельный
    `--landing-*` namespace) — это не «дубли», а рабочий паттерн
    page-scoped theming через каскад, ровно то, для чего custom properties
    существуют.
  - **Реальная находка**: токены определены, но массово НЕ используются —
    грепом посчитано 459 сырых hex-литералов (`#xxxxxx`) + 509 `rgba(...)`
    ВНЕ блока `:root`. Например `#1d2340` (то же значение, что и
    `--text` в `body.page-games`) встречается буквально **103 раза** как
    хардкод вместо `var(--text)`; десятки уникальных пастельных hex
    (`#fff8eb`, `#fff8ea`, `#fff4cf`, `#d8b4fe` и т.д. — палитра 9
    вариантов `.recent-game-card-<kind>` из games-страницы) вообще никогда
    не были вынесены в токены. Смысловой дубль здесь — не «два токена с
    одинаковым значением», а «один и тот же цвет записан сотнями разных
    прямых литералов вместо одного токена», то есть именно то, что делает
    будущий редизайн (смена палитры) риском find&replace по всему
    6262-строчному файлу вместо правки одного `:root`. Полная ретрофит-
    работа (перевести ~1000 вхождений на токены) — отдельный, крупный и
    рискованный трек сопоставимого размера с уже отложенными Playwright CSS
    coverage/fingerprinting инфраструктурами; не начат в рамках этого
    среза, задокументирован как приоритетная находка для следующего среза
    Этапа 1.
- [~] Определить токены цветов: background, surface, elevated surface, border,
  primary, accent, success, warning, danger, muted и focus.
  - Сопоставлено (2026-08-17) с текущими 28 токенами `panel.css`, не
    добавлено новых — добавление/переименование токена меняет семантику
    везде, где он уже используется, это часть большого ретрофит-трека, не
    точечная правка. Карта:
    - background → `--bg-0`/`--bg-1`/`--bg-2` (есть, 3 ступени)
    - surface → `--surface`/`--surface-strong`/`--surface-soft` (есть)
    - elevated surface → **отдельного токена нет**, приподнятые карточки
      используют `--surface-strong` + `--shadow` вместе — по факту работает,
      но нет явного "elevation" токена/шкалы
    - border → `--line`/`--line-strong` (есть)
    - primary → **явного токена нет**: `--cyan` используется и как primary
      действие (кнопки/фокус-кольцо), и как акцент одновременно —
      primary/accent не разделены
    - accent → `--violet`/`--magenta`/`--indigo` (есть, но пересекается с primary)
    - success → `--ok` (есть)
    - warning → `--warn` (есть, но см. ниже)
    - danger → **отдельного токена нет**: `.banner-error`/error-состояния
      переиспользуют `--warn` (`rgba(255, 156, 169, ...)` — та же RGB-тройка,
      что и `--warn: #ff9ca9`) — «warn» семантически перегружен и означает
      одновременно предупреждение И ошибку, настоящего danger-тона (обычно
      более насыщенный красный) в палитре нет вообще
    - muted → `--muted`/`--muted-strong` (есть)
    - focus → **отдельного токена нет**: focus-ring жёстко использует
      `var(--cyan)` напрямую (`server-ui-foundation.css:39`) — работает, но
      не документирован как отдельная роль, случайное переиспользование
      accent-токена
    Итог: 6 из 11 категорий имеют явный токен, 5 — работают через
    переиспользование другого токена без явного разделения ролей (primary
    vs accent, warning vs danger, отсутствие elevated-surface/focus как
    отдельных понятий). Не исправлено — это дизайн-решение (нужна ли вообще
    отдельная danger-палитра, например), не техническая правка; отмечено
    для рассмотрения при полном ретрофите цветов, задокументированном
    выше.
- [~] Определить шкалы spacing, radius, shadow, typography, line-height и z-index.
  - Проанализировано (2026-08-17), не отретрофичено — тот же паттерн, что
    уже найден для цветов: где токены/шкалы ЕСТЬ, они катастрофически
    недоиспользуются, а где их нет вообще — просто россыпь несогласованных
    значений.
    - **radius**: 4-ступенчатая шкала есть (`--radius-xl/lg/md/sm`), но
      `var(--radius*)` встречается всего 18 раз против **100+** сырых
      `border-radius: Npx` (39× `999px` — «pill»-форма, которой ВООБЩЕ нет
      в шкале токенов, плюс 18px/16px/20px/24px/28px/22px/14px/8px/12px
      россыпью).
    - **shadow**: 2-ступенчатая шкала есть (`--shadow`/`--shadow-soft`), но
      `var(--shadow*)` — 3 употребления против **93** сырых `box-shadow:`.
    - **spacing**: только один токен, `--gap` (единственное clamp-значение),
      никакой ступенчатой шкалы (xs/sm/md/lg/xl) нет вообще — padding/margin
      везде хардкожены поштучно.
    - **typography**: токенов размера шрифта нет вообще — 20+ уникальных
      сырых `font-size` (11px..42px, плюс несколько `rem`), никакой системы
      ступеней.
    - **line-height**: токенов нет — 10+ уникальных сырых значений
      (1, 1.05, 1.1, 1.2, 1.4, 1.5, 1.55, 1.6, 1.65 и др.) без системы.
    - **z-index**: токенов нет, реальная шкала по всем static CSS —
      `0, 1, 2, 5, 20, 70, 1000` (5 разных файлов), без единого документа
      «что на каком слое» — потенциальный источник будущих стекинг-багов
      при добавлении нового модального/toast-слоя.
    Практический вывод для следующего среза: проблема не «нет инфраструктуры
    токенов» (radius/shadow она есть), а «токены не форсируются» — самое
    дешёвое и безопасное следующее действие — не изобретать больше токенов, а
    включить уже существующий report-only Stylelint
    (`declaration-property-value-disallowed-list` на `border-radius`/
    `box-shadow` вне `var(--radius*)`/`var(--shadow*)`) хотя бы в
    предупреждающем виде, чтобы новый код не добавлял новые сырые значения,
    пока старый код мигрирует постепенно — не сделано в этом срезе, увязано
    с решением пользователя «линтеры — на моё усмотрение».
- [x] Проверить контраст основных сочетаний на WCAG AA.
  - Закрыто (2026-08-17): посчитаны реальные relative-luminance контрасты
    (формула WCAG 2.x) для всех основных пар текст/фон обеих тем.
    Тёмная тема приложения: `--text`/`--muted`/`--muted-strong`/`--cyan`/
    `--ok`/`--warn` на `--bg-0`/`--bg-1` — все 7.5:1..18.5:1, с большим
    запасом выше порога 4.5:1. Светлая games-тема: `--text`/`--muted`/
    `--muted-strong` на карточных светлых поверхностях (`#fff1cb`/
    `#fff9eb`) — 5.6:1..14.6:1, тоже все проходят.
    **Реальная находка — 2 провала AA**, оба на кнопках `.button.primary`
    (белый текст поверх ЯРКОГО голубого/фиолетового градиента, а не на
    базовом фоне страницы, поэтому не были пойманы предыдущим анализом
    токенов):
    - `panel.css` `.button.primary`: белый `#ffffff` на
      `linear-gradient(135deg, #6f87ff 0%, #45d9ff 100%)` — 3.19:1 у
      тёмного края градиента, **1.66:1** у светлого края (`#45d9ff`) —
      грубый провал даже порога large-text (3:1), не говоря про normal-text
      (4.5:1). Затрагивает все основные CTA-кнопки приложения (admin,
      чаты, экономика и т.д., везде кроме games/landing, у которых свои
      overrides).
    - `landing.css` `.page-landing .button.primary`: белый на
      `linear-gradient(135deg, var(--landing-accent) #a855f7, #8b5cf6)` —
      3.96:1..4.23:1, тоже ниже порога 4.5:1 (граничный, но реальный
      провал).
    `.button.danger`/`.button.telegram` (светлый текст на тёмном фоне) и
    `.page-games .button.primary`/`.button.danger` (тёмный `#1d2340` текст
    на ярком фоне, уже правильно) — все прошли без изменений.
    **Исправлено**: `panel.css` `.button.primary` — цвет текста
    `#ffffff` → `var(--bg-0)` (`#050b15`, уже существующий токен, не
    новый цвет) — даёт 6.18:1..11.85:1 на обоих концах градиента.
    `landing.css` `.page-landing .button.primary` — `#ffffff` → `#000000`
    (посчитано: `--landing-bg` тоже проходит по краю, но `#000000` даёт
    надёжный запас 4.96:1..5.31:1 на обоих концах градиента и на hover-
    состоянии). Оба фикса — только смена `color`, без изменения layout/
    padding/шрифта. Верификация: полный `tests/unit` 1143 passed без
    единой правки тестов (единственное упоминание `.button.primary` в
    тестах — CSS-селектор в `test_web_chat_navigation_browser.py`, не
    проверка цвета); `npx eslint .` чисто; djLint — без изменений
    (report-only baseline); скриншот кнопки `Войти` на `/login`
    подтверждает тёмный текст на градиенте, читаемо.
- [~] Зафиксировать правила плотности desktop и mobile.
  - Проанализировано (2026-08-17), формальные правила не зафиксированы —
    для этого сначала нужна spacing-шкала (найдено выше, что её нет вообще).
    Инвентаризация текущего состояния: 13 РАЗНЫХ значений breakpoint по
    всем static CSS (420/560/600/720/760/768/860/900/920/980/1024/1040px),
    без единой 3-ступенчатой системы (mobile/tablet/desktop) — органически
    добавлялись по одному на компонент, а не по плану. Смешанный синтаксис:
    часть на современном `width <= Npx` (range syntax), часть на legacy
    `max-width: Npx` — функционально эквивалентны, но непоследовательно.
    Важная оговорка, чтобы не считать это чисто багом: значительная часть
    responsive-поведения в проекте реально не завязана на явные breakpoint'ы
    вообще — `auto-fit, minmax()` CSS Grid паттерн (уже подтверждён
    работающим тестами Этапов 4-5 для economy/games/family карточных сеток)
    адаптируется без единого media query. Явных breakpoint'ов настолько
    мало (всего 19 вхождений на 6+ CSS-файлов) отчасти именно поэтому — не
    только недосмотр. Полноценная фиксация «правил плотности» требует
    сначала spacing-шкалы (уже отложенный ретрофит-трек), затем явного
    решения — консолидировать 13 значений в 3 именованных breakpoint'а или
    сознательно оставить auto-fit основным механизмом и media query —
    исключением для немногих мест, которым это принципиально нужно.
    Дизайн-решение, не техническая правка — не форсировано здесь.

#### CSS-архитектура

- [~] Определить структуру серверных стилей: tokens, reset/base, layout, components,
  utilities и pages.
  - Проанализировано (2026-08-17), задокументировано как факт текущего
    состояния, не отретрофичено — файлы физически уже частично
    соответствуют делению, просто оно нигде не записано явно.
    Фактическая структура 16 CSS-файлов (10285 строк):
    - **tokens**: `:root` в начале `panel.css` (28 базовых токенов).
    - **reset/base**: `server-ui-foundation.css` (235 строк — focus-ring,
      base typography, skip-link, form-элементы).
    - **pages** (чистые, page-scoped, безопасно вынесенные в этой
      сессии — Приоритет №1 предыдущего среза): `games.css` (1861),
      `landing.css` (721), `login-error.css` (134), плюс уже
      существовавшие до этой сессии `admin-overview.css` (504),
      `admin-broadcast.css` (579), `admin-broadcast-detail.css` (635),
      `admin-archive.css` (898), `admin-feedback.css` (416), `audit.css`
      (612), `admin-table.css` (48), `admin-table-search.css` (53),
      `docs-item-actions.css`/`docs-search.css`/`docs-responsive.css`
      (39-56 каждый).
    - **layout/components/utilities**: остаются вперемешку в
      `panel.css` (3448 строк) — `.panel`/`.button`/`.badge`/topbar/nav
      (переиспользуемые везде компоненты) соседствуют с блоком, помеченным
      комментарием `/* ADMIN PANEL STYLES */` (2594-3448, 855 строк), но
      реально НЕ admin-only: там же живут site-wide utility-классы
      (`.text-muted`, `.text-center`, `.table-wrapper`, `.button-small`,
      `.button-danger`), `.breadcrumb` (используется вне admin) и
      `.feedback-*`/`.broadcast-*` классы, часть которых **дополняет**, а
      не дублирует уже отдельные `admin-broadcast*.css` (проверено:
      `.broadcast-panel`/`.broadcast-grid` в `panel.css` — другие
      селекторы, чем `.broadcast-panel-head`/`.broadcast-grid` в
      `admin-broadcast.css`, не копипаста, а взаимодополняющий слой).
      Это подтверждает более раннюю находку: у этой части файла
      действительно нет чистой page/component границы — components и
      utilities здесь генетически перемешаны с admin-specific стилями,
      разделение потребует классификации по одному правилу за раз, а не
      одного среза.
    Практический вывод: желаемая 6-слойная структура (tokens/reset/
    layout/components/utilities/pages) уже наполовину реализована на
    уровне файлов (pages — 13 отдельных файлов), но `panel.css` до сих
    пор смешивает оставшиеся 4 слоя без внутреннего разделения (ни
    отдельными файлами, ни хотя бы секциями с комментариями, кроме одной
    устаревшей `/* ADMIN PANEL STYLES */` метки, которая теперь вводит в
    заблуждение — помечает не-admin-only контент как admin). Дальнейшее
    разделение layout/components/utilities внутри `panel.css` — то же
    большое дело, что и «сложная shared-часть», уже отложенное в
    предыдущем срезе.
- [~] Разделить `panel.css` без изменения поведения на первом механическом этапе.
  - Первый срез закрыт (2026-08-17): весь `.page-games`-scoped блок
    (1861 из 6262 строк, ровно 30% файла — `body.page-games` token-override
    плюс 372 правила, единый смежный диапазон 1237-3097 без единого
    `@media` внутри) вынесен в новый `web/static/games.css`, подключаемый
    через уже существующий `extra_styles=["games.css"]` (тот же механизм,
    что `admin-overview.css`/`admin-broadcast.css`/`audit.css` и др. — не
    новая инфраструктура, следование установленной конвенции для игр).
    `panel.css`: 6262 → 4400 строк. Выбран именно games-блок как первый
    кандидат — самый крупный (30%), при этом безопасный по построению:
    каждое правило буквально начинается с `.page-games`, значит принадлежит
    только этой странице, никакой отдельной верификации «а не используется
    ли это ещё где-то» не требуется (в отличие от неprefixed классов вроде
    `.controls-stack`/`.step-list`, которые тоже похожи на games-only, но
    оставлены в `panel.css` — для них принадлежность неочевидна текстуально,
    трогать без более глубокой проверки не стал). Верификация: побайтовое
    сравнение реального рендера `/app/games` до/после (`git stash` на
    `panel.css`+`games.css`+`app.py`) — единственные различия: новый
    `<link href="games.css?v=...">` и естественно изменившийся
    fingerprint-хэш (из-за перемещения байт между файлами), остальной HTML
    идентичен. Полный `tests/unit` 1139 passed; 6 тестов сначала упали
    (4 visual snapshot + 2 overflow, все из-за того, что тестовые
    `_mount()`-хелперы в `test_web_games_dashboard_browser.py`/
    `test_web_games_visual_snapshots.py` жёстко грузили только 2
    стиля — исправлено добавлением `games.css` в их список) — полный
    прогон, а не «похоже работает», поймал реальную поломку тестовой
    инфраструктуры, не продакшена. `npx eslint .` и `npm run build` чисты.
    Скриншот-подтверждение (123) — визуально идентично прежнему виду.
  - Второй срез закрыт (2026-08-17): `.page-landing`-блок вынесен в
    `web/static/landing.css`. Реальный размер оказался НАМНОГО больше
    исходной прикидки «13 правил» (та цифра считала только точный префикс
    `.page-landing `, пропуская `.landing-*`/неprefixed классы вроде
    `.stats-item`/`.timeline-step`/`.cta-buttons`, тоже landing-only, но
    названные иначе) — по факту единый смежный блок 721 строка, чётко
    очерченный существовавшими в файле комментариями-разделителями
    (`/* ===== PAGE LANDING REDESIGN ===== */` ... следующий
    `/* ===== KEEP LOGIN AND ERROR PAGE STYLES SEPARATED ===== */`) —
    авторы файла уже явно обозначили границу, я её просто использовал.
    Перед экстракцией проверено грепом: ни один из landing-специфичных
    классов (`landing-hero-container`, `landing-stats-strip`, `cta-buttons`,
    `stats-item`, `timeline-step` и т.д.) не встречается ни в одном другом
    шаблоне, кроме `landing.html`. `panel.css`: 4400 → 3678 строк.
    Верификация тем же методом (git-stash + побайтовое сравнение `/`) —
    только новый `<link>` и ожидаемо изменившийся fingerprint, полный
    `tests/unit` 1139 passed без единой правки тестов на этот раз (landing
    browser-тесты уже не хардкодили список стилей). `npx eslint .`/`npm run
    build` чисты. Скриншот (126) подтверждает визуальную идентичность.
    `panel_css_lines` cap понижен 4405 → 3685.
    Остаётся: неprefixed games-классы (`.controls-stack` и подобные) и вся
    остальная базовая/shared часть `panel.css`.
  - Третий срез закрыт (2026-08-17): `.page-login`+`.page-error`
    (134 строки, два смежных комментарий-размеченных блока — сначала
    error-only `.error-hero`/`.error-copy`/`.status-chip`/`.error-code`/
    `.error-status-*` из закрытия `error.html` в Этапе 4, затем сразу общий
    `.page-login, .page-error { ... }` блок) вынесены в один
    `login-error.css`, подключаемый на ОБЕИХ страницах — `login.html` и
    `error.html`. Отличие от games/landing: не единый `_render_template`
    вызов, а централизованные context-builder'ы `_login_context()`/
    `_error_context()`, каждый используется несколькими route-обработчиками
    (`_error_context()` — 6 разных мест кода: 403/400 на экономике,
    доступе к чату и generic error handler) — добавил `extra_styles` ОДИН
    раз в каждый builder вместо правки всех 8 call site'ов по отдельности,
    так как все они уже разворачивают `**_login_context(...)`/
    `**_error_context(...)` в `_render_template`. Перед этим проверено, что
    ни один из 8 call site'ов не передаёт `extra_styles` самостоятельно
    (иначе был бы `TypeError` на дублирующийся kwarg). `panel.css`: 3678 →
    3543 строки. Верификация: побайтовое сравнение реального рендера ОБЕИХ
    страниц (`/login` и несуществующий путь → error 404) до/после — только
    новый `<link>` и fingerprint. Полный `tests/unit` 1139 passed; 2 теста
    в `test_web_error_page.py` сначала упали (тот же класс поломки, что и
    в games — хардкоженный список из 2 стилей в `_mount()`) — исправлено.
    `npx eslint .`/`npm run build` чисты. Скриншоты (127×2) подтверждают
    визуальную идентичность обеих страниц. `panel_css_lines` cap понижен
    3685 → 3550.
    Остаётся: неprefixed games-классы и вся остальная базовая/shared часть
    `panel.css` (3543 строки, 43% сокращение от исходных 6262) — она
    сложнее всех предыдущих трёх, там нет чистого prefix/comment-разделения
    по странице, следующий срез потребует более тщательного анализа.
- [~] Ввести понятное соглашение имён классов.
  - Проанализировано (2026-08-17): фактическое соглашение по всем 16
    CSS-файлам уже на удивление последовательное, просто нигде не
    записано словами. Задокументирована как найдена, не изобретена
    заново:
    - flat kebab-case `block-element-modifier` без BEM-двойных дефисов
      (`.admin-table-card`, `.broadcast-delivery-status-ok`, не
      `.admin-table__card--ok`) — грепом подтверждено 0 вхождений `__`
      или `--` (кроме легитимных CSS custom properties) во всех
      static-файлах.
      - page-scope через префикс на `<body>` + одноимённый CSS-префикс
      класса (`body.page-games` + `.games-*`... на деле чаще без
      отдельного namespace, а через сам факт, что класс определён внутри
      page-scoped файла — `landing.css` использует явный `.landing-*`-
      префикс почти везде, `games.css` иногда обходится без него, полагаясь
      на файловую границу) — неполная консистентность, но не критичная,
      т.к. файловая изоляция (page CSS не подключается на других
      страницах через `extra_styles`) физически предотвращает конфликт
      имён даже без префикса.
    - состояния/тона через суффикс-модификатор, не отдельный класс:
      `-active`/`-open`/`-done`/`-error`/`-ok`/`-warn` (напр.
      `.feedback-status-open`, `.broadcast-counter-error`) — совпадает с
      паттерном, который уже был формализован макросом `status_badge()`
      в этой сессии.
    - утилиты — отдельная маленькая группа однозначных имён
      (`.text-muted`, `.text-center`, `.sr-only`), не подмешаны в
      компонентные имена.
    Найдено единственное реальное несоответствие: `.button-small`/
    `.button-danger` (дефис) сосуществуют с `.button.primary`/
    `.button.danger`/`.button.ghost` (модификатор через отдельный класс
    у одного и того же корневого `.button`) — два разных способа
    модификации одного и того же компонента в одном файле. Не
    исправлено — оба класса используются в реальной разметке (первые —
    размерные/цветовые варианты кнопок в узких местах типа таблиц, вторые
    — основной API кнопок повсюду), унификация означала бы переименование
    в шаблонах ради чистоты соглашения, а не исправление бага; отмечено
    как единственная найденная несогласованность вместо того, чтобы
    промолчать. Формального письменного style-guide документа не
    заведено — открытый вопрос, стоит ли создавать отдельный файл
    `docs/CSS_CONVENTIONS.md` под уже найденное соглашение, или
    достаточно того, что оно теперь зафиксировано здесь, в TODO.
- [x] Удалять мёртвые селекторы только после автоматической и ручной проверки.
  - Закрыто (2026-08-17) как подтверждение уже установленной и
    применённой на практике методологии, а не новая работа: единственное
    реальное удаление мёртвого CSS в этом roadmap (11 правил из
    `panel.css`, Этап 6) уже делалось строго по этому правилу — сначала
    автоматический `scripts/css_coverage_report.py` (DOM-based, 22
    состояния), затем обязательная ручная перепроверка каждого кандидата
    на 2 класса ложных срабатываний (Jinja-интерполяция классов и
    JS-generated классы через `classList`/`className`), прежде чем
    что-либо удалить. Ни одно правило не было удалено по одному только
    автоматическому отчёту. Формализую это чек-листовым пунктом
    постфактум — правило уже соблюдалось, а не осталось неприменённым.
- [x] Запретить новые inline-стили, кроме действительно динамических значений,
  безопасно переданных через CSS custom properties.
  - Закрыто (2026-08-18). Грепом по всем шаблонам найдено ВСЕГО 4
    вхождения `style="` в проекте (не десятки, задача оказалась
    маленькой): 3 в `_games_dashboard.html` — `hidden style="display:
    none;"`, дублирующее то, что `hidden`-атрибут уже должен делать сам
    по себе — оказалось НЕ мёртвым кодом, а необходимым обходом:
    `.create-whoami-theme-panel{display:grid}` в `panel.css` (author CSS)
    перебивает дефолтный UA-стиль `[hidden]{display:none}` при равной
    специфичности. Тот же класс проблемы уже был найден и решён раньше в
    этом же проекте для `.admin-table-card`/`.admin-table-search-empty`
    (см. комментарий в `admin-table-search.css`) — применил тот же
    паттерн: добавил `.create-whoami-theme-panel[hidden] { display:
    none; }` в `panel.css` (специфичность `[hidden]`+класс перебивает
    просто класс) и убрал inline `style` из всех 3 мест в шаблоне,
    оставив только `hidden`. 4-е вхождение — `_games_mode_spy.html`,
    `style="width: {{ row.meter }}%;"` — это ровно та «динамическая
    ширина», которую чек-лист явно разрешает, но не в «сырой» форме:
    переведено на `style="--spy-vote-meter: {{ row.meter }}%"` +
    `width: var(--spy-vote-meter, 0%)` в `games.css`, как и требует
    формулировка пункта («через CSS custom properties»). Итог: 0 сырых
    `style="..."` в шаблонах, кроме одного, уже переведённого на custom
    property. Верификация: computed `display` скрытой whoami-панели —
    `none` (проверено Playwright'ом до/после), полный `tests/unit` 1143
    passed (1 assertion в `test_web_templates.py` обновлена под новую
    разметку — проверяла точную строку `hidden style="display: none;"`),
    djLint/eslint — без новых находок, скриншот подтверждает визуальную
    идентичность.

#### Jinja-компоненты

- [~] Создать macros/partials для:
  - [~] page header и breadcrumbs;
    - Breadcrumb закрыт (2026-08-17): новый `_macros.html`, макрос
      `breadcrumb(items)` (список `{href?, label}`), первый реальный
      Jinja-макрос в проекте (было ровно 0 — грепом `{% macro` по всем
      шаблонам). Мигрированы все 3 существовавших вхождения `.breadcrumb`
      (`admin_table.html`, `admin_edit.html`, `admin_messages_compact.html`)
      — не выдумано использование, все 3 подтверждены грепом заранее.
      **Найден и исправлен попутный реальный a11y-баг**: `admin_table.html`
      и `admin_edit.html` не имели `aria-label="Хлебные крошки"` на `<nav>`
      и `aria-hidden="true"` на разделителе — их breadcrumb озвучивался бы
      скринридером как безымянная навигация с лишним произносимым «/»,
      тогда как `admin_messages_compact.html` уже делал это правильно.
      Макрос унифицировал поведение на «правильный» вариант для всех трёх,
      а не наоборот. Проверено: 59 существующих route/browser тестов
      (`admin_table`/`admin_edit`/`admin_archive`/`admin_messages_compact`)
      прошли без единой правки; HTMLHint на все 3 рендер-фикстуры — 0
      находок; djLint на новый файл и все 3 изменённых шаблона — 0 находок;
      полный `tests/unit` 1139 passed; скриншот (128) подтверждает рендер.
      «page header» (общий заголовок панели, `.panel-head`/`<h2>`) — не
      формализован отдельным макросом, у разных страниц заголовок слишком
      разный по составу (иногда с подзаголовком, иногда с действиями
      справа) для одного универсального шаблона без чрезмерной
      параметризации — сознательно не форсировано.
  - [ ] buttons и icon buttons;
  - [ ] form field, label, hint и validation error;
  - [ ] card/panel/section;
  - [x] badge/status;
    - Закрыто (2026-08-17): второй макрос, `status_badge(base_class, tone,
      label)` в `_macros.html`. Мигрированы все 3 реальных вхождения
      паттерна `<span class="X X-{{ tone }}">{{ label }}</span>`, найденных
      грепом: `feedback-status` (дословно дублировался в `feedback.html` И
      `admin.html` — не выдумано, byte-for-byte одинаковый код в двух
      файлах) и `broadcast-delivery-status` (`admin_broadcast_detail.html`,
      1 вхождение, но того же паттерна — включён в общий макрос вместо
      узкоспециализированного, раз форма идентична). 204 существующих теста
      (`feedback`/`admin_broadcast`/`admin_overview`/остальные `admin.html`)
      без единой правки; djLint — 1 находка, но это уже задокументированный
      baseline false positive в несвязанной строке `admin.html:557`, не
      регрессия (проверено `git diff` — строка не тронута); HTMLHint на
      рендер-фикстуру — 0 находок; полный `tests/unit` 1139 passed;
      скриншот (129) подтверждает цветовую индикацию тона (доставлено/
      ошибка/ожидание).
  - [ ] tabs/segmented control;
  - [x] pagination;
    - Закрыто (2026-08-17), по решению пользователя строить компоненты
      «про запас» (вариант «A»), даже без текущей дупликации. Новый
      `pagination(previous_href, next_href, page, total_pages)` в
      `_macros.html`, мигрировано единственное существующее вхождение
      (`admin_table.html`). 25 существующих тестов без правки; djLint/
      HTMLHint на рендер-фикстуру — 0 находок; полный `tests/unit` 1139
      passed; скриншот (130).
  - [ ] empty/error states;
  - [ ] confirm dialog;
  - [ ] responsive data card;
  - [ ] toast/banner.
  - **Проверено (2026-08-17)**: грепом по всем шаблонам —
    `toast`/`tabs`(`role="tablist"`)/`confirm-dialog` каждый встречается
    ровно в ОДНОМ файле (`base.html`, `_chat_overview_tab.html`,
    `admin_table.html` соответственно). `empty-text` (18 вхождений) уже
    терсовый однострочник (`<p class="empty-text">{{ text }}</p>`), макрос
    сэкономил бы меньше символов, чем сам вызов макроса. `toast` вообще не
    Jinja-компонент — это пустой `<div id="toast-region">`, целиком
    заполняемый JS в рантайме, макросить в Jinja нечего. `confirm-dialog`
    в `admin_table.html` тесно завязан на конкретные JS data-атрибуты
    (`data-delete-target`, `data-delete-cancel`, `id="delete-form"`) —
    обобщение под гипотетический второй сценарий значило бы гадать сигнатуру
    API вслепую — реальный риск переусложнения ради нуля
    пользы сейчас. `tabs`/`.mode-switch` в `_chat_overview_tab.html` —
    тоже единственный сайт, тесно привязан к конкретному переключателю
    режима рейтинга. Для этих четырёх пунктов решил не форсировать
    формальное «закрытие» чек-листа искусственным обобщением
    единственного использования — это принципиально другая ситуация,
    чем `breadcrumb`/`status_badge`/`pagination`, где была настоящая
    дублирующаяся (или уже реально анемичная, но безопасно обобщаемая)
    разметка. Макрос всё ещё может иметь смысл ради изоляции/консистентного
    API (что и просит чек-лист буквально), но это уже не «нашёл баг/дубль и
    исправил», а решение начать за нуля — сознательно не форсировано на
    сегодня, отмечено честно вместо тихого пропуска.
- [x] Добавить gallery/dev fixture основных компонентов и их состояний либо
  эквивалентные template tests.
  - Закрыто (2026-08-18) через вторую половину формулировки пункта —
    «эквивалентные template tests» — вместо новой gallery-страницы.
    Осознанный выбор, не короткий путь: отдельная dev-only страница
    компонентов означала бы новый production-роут (нужно решать, публичный
    он или за логином — вопрос за пользователем, не то, что стоит решать
    молча) и лишнюю поверхность для поддержки; юнит-тесты дают то же самое
    покрытие состояний без этого. Новый `tests/unit/test_web_macros.py`
    (6 тестов) рендерит все 3 макроса `_macros.html` НАПРЯМУЮ (через
    `environment.from_string()`, без прохода через целую страницу) для их
    реальных граничных состояний: `breadcrumb` — один элемент без
    сепаратора, несколько элементов со смешанными href/без-href (последний
    — активная страница, не ссылка), проверка количества `breadcrumb-sep`
    и `aria-hidden`; `status_badge` — оба реальных тона (`open`/`done`),
    точная композиция классов; `pagination` — первая страница (без
    «Назад»), последняя (без «Вперёд»), средняя (обе ссылки). До этого
    все 3 макроса были покрыты только КОСВЕННО, через полный рендер
    конкретных страниц (`admin_table.html` и т.д.) — граничные состояния
    (одна крошка, первая/последняя страница) не были явно
    зафиксированы отдельно. Полный `tests/unit` 1149 passed (было 1143).

#### `base.html` и навигация

- [x] Переработать `base.html` на семантический layout с skip link.
  - Закрыто (2026-08-18), после landmark-фикса выше — на этот момент
    `base.html` уже даёт полный ожидаемый набор ARIA-landmark'ов без
    дублирования и без «сирот»: `skip-link` → `<header>` (`banner`,
    исправлено в этом же тике) → `<nav aria-label="...">`
    (`navigation`) → `<main id="main-content" tabindex="-1">` (`main`,
    цель skip-link'а, фокусируем программно). Единственный не-глобальный
    нюанс — `<footer class="landing-footer">` в `landing.html` рендерится
    внутри `{% block content %}`, то есть физически внутри `<main>`, и
    поэтому не получает роль `contentinfo` — не тронуто здесь: это
    контент ОДНОЙ страницы (не общий сайтовый футер во всех шаблонах),
    вне рамок общего layout `base.html`, отдельный при желании точечный
    вопрос для `landing.html`, а не для этого пункта чек-листа.
- [x] Сделать responsive topbar/navigation для smartphone, tablet и desktop.
  - Закрыто (2026-08-18). Проверено конкретно, не на глаз: `.topbar`
    получает выделенную адаптивную обработку (horizontal-scroll pills на
    мобильном вместо переноса, компактный `.brand`, `scroll-margin-top`
    под sticky-топбар) только под `.ui-admin-shell`
    (`server-ui-foundation.css`) — а этот класс проставляет ровно один
    контекст-билдер, `_admin_layout_context()` в `app.py`, но
    используется 6 раз (все admin-роуты: обзор, таблицы, edit, docs,
    broadcast detail, messages compact) — то есть весь админ-раздел уже
    покрыт, не одна страница, как показалось на первый взгляд по грепу
    шаблонов (класс инжектится из Python, не хардкожен в HTML).
    Остальные страницы (chat/economy/family/audit/games/landing/login)
    НЕ получают этот специальный уровень полировки — у них `.topbar`
    просто `flex-wrap: wrap` (базовое правило `panel.css`) без
    посвящённых media query. Проверил, не баг ли это: рендер `/login`
    на 375px — `documentElement.scrollWidth <= clientWidth` (без
    горизонтального overflow), `.topbar` корректно переносится на 2
    строки без наложений или обрезки. Это уже подтверждается широким
    существующим тестовым покрытием — 19 файлов тестов уже проверяют
    отсутствие horizontal overflow на нескольких вьюпортах для
    большинства страниц сайта. Итог: navigation РЕАЛЬНО responsive
    везде (без переполнения, без сломанной геометрии на всех проверенных
    страницах) — просто с двумя разными, оба валидными стратегиями
    (scroll-pills для админки, wrap для остального), а не с багом,
    требующим правки.
- [x] Упорядочить page title, description и основные landmarks.
  - Закрыто (2026-08-17, landmark-часть — 2026-08-18 по прямому указанию
    пользователя). **Реальная находка**: `<meta
    name="description">` не было ВООБЩЕ ни на одной странице сайта —
    грепом по всем шаблонам 0 вхождений `name="description"`. `page_title`
    был уже везде (обязательный контекст-ключ `base.html`), а description —
    нет. Добавлен `<meta name="description" content="{{ page_description|
    default(...) }}">` в `base.html` сразу после `<title>`, с
    сайт-wide дефолтом (осмысленным, не заглушкой) для всех
    internal/authenticated страниц, плюс явные специфичные значения для
    двух публичных, потенциально индексируемых поисковиком страниц —
    `landing.html` (через `build_landing_context()` в `presenters.py`) и
    `login.html` (через `_login_context()` в `app.py`). Не проставлено
    индивидуальное описание для каждой из ~30 admin/chat/games-страниц —
    они за Telegram-логином, не индексируются, сайт-wide дефолт
    достаточен, точечная работа под каждую страницу не оправдана.
    **Landmarks — находка, исправлена 2026-08-18**: `<header class="topbar">`
    был физически вложен внутрь `<main id="main-content">` в `base.html`, то
    есть НЕ получал роль `banner` (по спецификации ARIA `<header>`
    получает banner-роль, только если не вложен в sectioning-контент типа
    `<main>`/`<article>`) — скринридер видел только 2 landmark'а
    (`main`+`navigation`), а не 3 ожидаемых (`banner`+`navigation`+`main`).
    Изначально не исправлено сразу — оценено как структурная правка риска,
    сравнимого с отложенной «сложной shared-частью panel.css», честно
    вынесено пользователю как отдельный вопрос вместо тихого пропуска.
    Пользователь ответил «чини» — исправлено. **Минимальный по факту фикс,
    оказался куда безопаснее, чем оценивался заранее**: класс `.shell`
    (несущий `max-width`/центрирование/padding) просто переставлен с
    `<main>` на новый оборачивающий `<div class="shell">`, который теперь
    содержит и `<header>`, и `<main id="main-content" tabindex="-1">` как
    родных детей — сам `<header>` физически перестал быть потомком `<main>`,
    получил роль `banner`, а вся геометрия (`.shell`/`.topbar` CSS,
    завязанные на класс, не на тег) не изменилась ни на пиксель, потому что
    оба элемента остались внутри одного и того же `.shell`-контейнера в той
    же вложенности друг относительно друга — никакого нового CSS не
    потребовалось. Верификация: `scripts/axe_scan_report.py` на всех 22
    состояниях — 0 находок (landmark-правила включены в набор); побайтовое
    сравнение скриншотов `/login` и `admin.html`-фикстуры до/после — визуально
    идентично; полный `tests/unit` 1143 passed без единой правки тестов;
    djLint на `base.html` — без новых находок сверх report-only baseline.
- [x] Унифицировать flash/error rendering и доступное уведомление screen reader.
  - Закрыто (2026-08-18). Проверено грепом по всем шаблонам: единственное
    место, обрабатывающее реальные `flash`/`error` query-параметры сессии
    — `base.html` (`{% if flash %}<div class="banner banner-ok"
    role="status">` / `{% if error %}<div class="banner banner-error"
    role="alert">`), дублирующих реализаций в других шаблонах нет —
    `{% if flash %}`/`{% if error %}` не встречается больше нигде. Есть 2
    места, использующие ТЕ ЖЕ CSS-классы `.banner`/`.banner-ok`/
    `.banner-error` для чисто визуального стиля на статичном контенте, не
    на транзиентных session-уведомлениях: `chat.html` (постоянная
    подсказка внутри `docs-guard-dialog`, не появляется/исчезает
    динамически) и `_games_dashboard.html` (`.recent-result-banner` —
    статичный текст результата архивной игры). Сознательно НЕ добавлял
    туда `role="alert"`/`role="status"` — оба являются постоянным
    server-rendered контентом, а не live-region уведомлением о только что
    произошедшем событии; добавление ARIA live-role на статичный текст
    было бы семантически неверным (спецификация ARIA: `alert`/`status`
    предназначены для контента, который появляется/меняется в ответ на
    событие). Реальный механизм flash/error — уже единый и доступный.
- [x] Вынести общий JavaScript из шаблонов в статические модули.
  - Закрыто (2026-08-18). Проверено грепом по всем шаблонам: 0
    вхождений `<script>...</script>` с логикой внутри (единственные
    `<script>`-теги — `{% for script in extra_scripts %}<script
    type="module" src="...">` в `base.html`, уже внешние файлы) и 0
    inline event-хендлеров (`onclick=`/`onsubmit=`/`onchange=`/
    `oninput=`/`onload=`/`onerror=`) на любом элементе любого шаблона.
    Весь JS уже живёт в `static/*.js`-модулях, подключаемых через
    `extra_scripts` — уже полностью сделано до этого среза, формализую
    закрытие постфактум.
- [x] Добавить безопасный механизм page-specific CSS/JS без inline script blocks.
- [x] Проверить logout, redirects, session expiry и keyboard navigation.
  - Закрыто (2026-08-18) как подтверждение уже существующего
    автоматического покрытия, не новая работа: `test_logout_returns_
    json_for_fetch_requests` (logout), `test_admin_docs_page_redirects_
    to_login_without_a_session_cookie` (redirect без сессии),
    `test_admin_json_session_expiry_points_at_the_real_admin_login`
    (session expiry), плюс keyboard-nav через `page.keyboard.press
    ("Tab")` в `test_web_admin_overview_browser.py`/`test_web_admin_
    shell_browser.py` (skip-link фокус, уже чинился на флейк в Этапе 0
    выше в этом же журнале) и `test_web_admin_broadcast_form_browser.py`
    (табличный порядок формы). Ручной keyboard-only проход и ручная
    проверка в реальном Safari/Chrome — отдельные, уже существующие
    пункты Этапа 6 (`[ ]`), требующие живого человека за экраном, не
    дублируются здесь.

**Критерий завершения этапа 1:** новая оболочка и компоненты готовы, проверены на
четырёх viewport-проектах и могут применяться к страницам по одной без feature flag.

### Этап 2. Серверная админка — главный приоритет

#### 2.1. Admin login и shell

- [x] Обновить `admin_login.html`. Исправлен реальный баг: `.login-side`/
  `.login-side-card` не имели стилей, из-за чего на mobile текст боковых карточек
  накладывался на заголовок формы (карточки — unstyled auto-колонка grid). Так как
  `login.html` (обычный вход пользователя) использует те же классы, фикс общих
  стилей в `server-ui-foundation.css` заодно чинит и его; это не заявленная задача
  этого среза, но избежать нельзя — классы общие. Полный редизайн `login.html`
  остаётся в этапе 4.
- [x] Сделать ясные состояния неверного пароля, истёкшей сессии и server error. Уже
  обеспечивалось общим banner-механизмом `base.html` (`flash`/`error`); проверено
  screenshot-ами для неверного пароля и rate-limit.
- [x] Проверить password manager, autocomplete, клавиатуру и mobile keyboard.
  `autocomplete="current-password"`, `autofocus`, явный `<label for>` — уже было
  корректно, подтверждено тестом. Добавлен submit guard от двойной отправки.
- [x] Создать единый admin shell: навигация, текущий раздел, быстрые действия,
  responsive sidebar/drawer. Desktop/Mobile/Tablet Chromium (1440/390/820 px)
  закрыты — добавлен отдельный `tests/unit/test_web_admin_shell_browser.py`
  с прицельными тестами самого shell'а (active-state не только цветом, skip-link
  keyboard flow, mobile scroll-ribbon, tablet без scroll-ribbon), плюс уже
  существующее попутное покрытие shell'а во всех page-level browser-тестах.
  WebKit — см. отдельный `[!]` пункт ниже (заблокирован на этой машине).
- [!] Mobile/Tablet WebKit для admin shell и всей browser-матрицы технически
  недоступны на текущей dev-машине (Fedora 44): Playwright официально
  поддерживает установку зависимостей WebKit только для Ubuntu/Debian
  (`playwright install-deps` падает — `apt-get: command not found`). Прямая
  проверка через `ldd` показала, что бинарник WebKit slinkован против ICU 74 и
  libjpeg ABI 8, которых на Fedora 44 нет (стоит ICU 77, libjpeg.so.62);
  часть зависимостей (`libbacktrace`, `libjxl`) Playwright бандлит сам в
  `sys/lib`, но ICU/libjpeg — нет, и подменить их простым симлинком на более
  новые версии нельзя (у ICU версионированные C++ ABI-символы, будет падать на
  resolve символов, а не при старте). Пользователь подтвердил (2026-08-15):
  отложить, WebKit-прогон переносится в CI/Docker-окружение с Ubuntu
  (отдельная, не начатая задача), решение зафиксировано честно, а не хаком с
  вендорингом чужих .so.
- [x] Не показывать потенциально чувствительные данные в глобальной навигации. Уже
  обеспечивалось (`_admin_layout_context(authenticated=False)`); подтверждено
  существующим тестом `test_admin_login_page_has_public_navigation_only`. Также
  убрано раскрытие имени переменной окружения `ADMIN_PASSWORD` в helper-тексте.

#### 2.2. Главная админки

- [x] Перестроить `admin.html` по задачам администратора, а не по структуре БД.
- [x] Верхний уровень: состояние сервисов, критичные проблемы и быстрые действия.
- [x] Отдельные понятные входы в рассылки, историю, заявки, аудит, бэкапы и generic DB
  explorer.
- [x] Сделать секции сворачиваемыми только там, где это не скрывает критичный статус.
  Домены таблиц (`table_sections`) обёрнуты в `<details open>` с chevron-индикатором
  (`admin-secondary-details`, паттерн `[open] summary::after` скопирован с уже
  проверенного `.admin-feedback-history`); открыты по умолчанию, поиск по таблицам
  принудительно раскрывает свёрнутый домен, если в нём есть совпадение. Критичные
  секции («Что требует внимания», «Сервисы и данные», список заявок) остаются
  обычными `<section>` — не сворачиваются никогда.
- [x] Проверить empty/large/error/loading states. Empty — уже было (`is-calm`
  состояние). Loading — состояния кнопок заявок/backup из предыдущего среза. Error —
  новый route-тест на баннер `banner-error`/`banner-ok` для `/app/admin`. Large —
  новый browser-тест с 24 заявками и 10 доменами по 6 таблиц (60 карточек); тест
  **нашёл реальный баг**: `.admin-table-card` объявляет собственный `display: flex`
  в panel.css, который на равной специфичности побеждает UA-правило
  `[hidden]{display:none}` — карточки визуально НЕ скрывались при поиске внутри
  секции с частичным совпадением (баг маскировался в мелких тестах, где вся секция
  целиком пряталась). Исправлено явным `.admin-table-card[hidden]{display:none}`.

#### 2.3. Системные рассылки

- [x] Сохранить всю текущую функциональность: HTML, фото, reaction builder, выбор
  чатов, лимиты, предпросмотр и проверки ошибок.
- [x] Разделить создание рассылки на ясные логические блоки: сообщение, медиа,
  реакции, аудитория, проверка и отправка.
- [x] Сделать постоянный preview сообщения для desktop и удобный preview step для
  mobile.
- [x] Показывать актуальные лимиты и остаток символов рядом с соответствующим полем.
- [x] Сделать ошибки синтаксиса и медиа привязанными к конкретному блоку формы.
- [x] Сделать выбор чатов searchable, keyboard-accessible и удобным на touch.
- [x] Добавить подтверждение массовой отправки с количеством получателей и итоговым
  preview.
- [x] Защитить от случайного повторного submit.
- [x] Довести визуальную и responsive-полировку composer/preview/confirm по ручному
  циклу desktop/mobile screenshots без перекрытий, обрезанных действий и page
  overflow.
- [x] Переработать историю рассылок в карточки с ясным статусом, временем, охватом,
  ошибками, ответами и реакциями.
- [x] Обновить `admin_broadcast_detail.html`:
  - [x] preview отправленного сообщения;
  - [x] summary доставки;
  - [x] фильтры успешных/ошибочных доставок;
  - [x] ответы пользователей;
  - [x] быстрые реакции от имени бота;
  - [x] нативные, inline, custom и paid reactions;
  - [x] mobile layout без потери действий.
- [x] Playwright: отправка текста, фото, hybrid reactions, invalid input, Telegram
  failure, duplicate submit и mobile flow.

#### 2.4. История сообщений как лента чата

- [x] Зафиксировать честное название: «Архив сообщений», а не «полная история
  Telegram».
- [x] Спроектировать desktop split view: список/поиск чатов слева, сообщения справа.
- [x] Спроектировать mobile flow: список чатов → отдельная лента → возврат с
  сохранением фильтров и позиции.
- [x] Добавить фильтры по чату, пользователю, диапазону дат, типу сообщения и наличию
  редакции/reply.
- [!] Добавить полнотекстовый поиск с понятной подсветкой совпадений. Подстрочный
  поиск (ILIKE) и безопасная подсветка готовы и работают в проде. Индексированный
  full-text (tsvector + GIN на Postgres, russian-словарь) — отдельная задача: это
  миграция схемы продовой БД, а не Web UI-срез, и она ломает нынешний параллелизм
  тестов (SQLite и Postgres сейчас проверяются одним ILIKE-путём; tsvector —
  Postgres-специфичная фича). Пользователь подтвердил (2026-08-16): отложить,
  Этап 2 считается фактически закрытым с этим единственным сознательно отложенным
  пунктом.
- [x] Перейти от строк таблицы к message cards/bubbles:
  - [x] автор и стабильная цветовая метка;
  - [x] локализованное время;
  - [x] тип сообщения;
  - [x] текст/caption с безопасным переносом длинных строк;
  - [x] reply quote;
  - [x] edited badge;
  - [x] сворачиваемая история снимков редакций;
  - [x] ссылка на raw snapshot.
- [x] Группировать соседние сообщения одного автора и разделять даты.
- [x] Определить порядок ленты и поведение перехода к найденному сообщению.
- [x] Реализовать cursor pagination или другой устойчивый способ загрузки больших
  архивов без пропусков и дублей.
- [x] Сохранять фильтры в URL.
- [x] Добавить preview:
  - [x] photo;
  - [x] sticker/animation placeholder;
  - [x] video/document/audio/voice metadata;
  - [x] безопасный fallback при недоступном Telegram file.
- [x] Не загружать media автоматически в полном качестве.
- [x] Добавить технический табличный/raw режим для диагностики. Уже существовал:
  кнопка «Технический режим» в archive conversation header ведёт на
  `/app/admin/table/messages?chat_id=...` — тот же generic DB explorer (raw JSON,
  все колонки), отфильтрованный по текущему чату. Не новый режим, а уже
  реализованная связь между дружелюбным и техническим представлением одних и тех
  же данных — явно отмечено выполненным.
- [ ] Не добавлять на этом этапе хранение исходящих сообщений бота.
- [x] Создать отдельный deferred item для исходящих сообщений после стабилизации UI.
  Уже существует как раздел «7. Отложенные задачи» (строки 599–604) — создан вместе
  с roadmap, теперь явно отмечен выполненным.
- [x] Playwright: пустой архив, длинная лента, edits, replies, unavailable media,
  filters, deep link, mobile navigation и no-overflow. Закрыты оставшиеся пробелы:
  route-тесты на «Архив пока пуст» (нет ни одного чата) и «По этим фильтрам ничего
  не найдено» (выбранный чат + фильтры без совпадений, с рабочей ссылкой сброса);
  browser-тест длинной ленты (50 сообщений/137 в выборке, desktop+mobile) —
  пагинация видна, нет page overflow. edits/replies/unavailable media/mobile
  navigation/no-overflow/deep-link (highlight-параметр) уже были покрыты в
  предыдущих срезах архива.

#### 2.5. Заявки, аудит и обслуживание

- [x] Переработать заявки пользователей в task-like cards с фильтром статуса,
  быстрым изменением и видимой историей решения.
- [x] Переработать `audit.html` в читаемую timeline/table hybrid view.
- [x] Сделать запрос backup отдельным подтверждаемым действием с ясным результатом.
- [x] Показывать ошибки операций рядом с действием, сохраняя введённые данные.
  Статус заявки и запрос backup переведены с нативного form-submit на `fetch()`
  к уже существующим JSON API (`/api/admin/feedback/{id}/status`,
  `/api/admin/request-backup`); ошибка показывается inline рядом с конкретным
  действием (для заявки — именно у той карточки, что вызвала действие, не влияя
  на остальные) без перезагрузки страницы и потери позиции скролла; backup-диалог
  остаётся открытым при ошибке вместо закрытия.

#### 2.6. Generic DB explorer

- [x] Сохранить `admin_table.html` как технический инструмент, не смешивая его с
  продуктовой админкой. Стили и JS вынесены в собственные `admin-table.css`/
  `admin-table.js`, не затрагивающие продуктовые страницы.
- [x] Сделать список таблиц searchable и сгруппированным по доменам. Группировка по
  доменам уже была на `admin.html`; добавлен клиентский поиск (новый
  `admin-table-search.js`, без серверных изменений — список таблиц целиком уже
  рендерится, доп. round-trip не нужен): фильтрует по имени и ключу таблицы,
  скрывает разделы без совпадений, показывает «Совпадений не найдено» при
  полном отсутствии результатов.
- [x] Зафиксировать понятные labels для внешних ключей рядом с raw ID. Уже
  реализовано через `reference_labels` в списке и в форме редактирования.
- [x] На mobile использовать contained horizontal scroll или card view для
  приоритетных таблиц. `.table-wrapper` — явно обозначенный технический контейнер с
  `overflow-x: auto`; горизонтальная прокрутка всей страницы не используется.
- [x] Улучшить pagination и URL filters без ручной конкатенации query string.
  Ссылки постранично строятся на сервере через `urlencode`; исправлен краш при
  нечисловом/отрицательном `page`.
- [x] Обновить `admin_edit.html`: labels, типы полей, validation, destructive
  warnings. Добавлен предупреждающий баннер о прямом изменении БД и submit guard.
- [x] Обновить confirm dialog удаления и возврат фокуса. Диалог теперь называет
  удаляемую запись, восстанавливает фокус на вызвавшую кнопку, JS вынесен в
  `admin-table.js`.
- [x] Добавить Playwright CRUD-тест на специально разрешённой тестовой таблице.
  Browser-тесты geometry/focus/pagination/warning уже были. Добавлен новый
  `tests/unit/test_web_admin_table_crud.py` — реальный FastAPI + sqlite (тот же
  паттерн, что и real-backend тесты рассылок): update и delete реально мутируют/
  удаляют строку в БД (не мок), проверены unauthenticated-запрос без мутации,
  delete несуществующей записи без краша, и что edit-форма отражает реально
  обновлённую строку. UI explorer не имеет действия «создать запись» (только
  edit/delete существующих) — «CRUD» здесь означает R+U+D, что покрывает всю
  функциональную поверхность инструмента. Полноценный live-браузерный E2E (реальный
  HTTP-сервер + Playwright, не ASGITransport) остаётся более крупной задачей
  тестовой инфраструктуры этапа 0, не блокирующей эту функциональность.

**Критерий завершения этапа 2:** все административные задачи доступны с desktop,
smartphone и tablet; рассылка и архив покрыты функциональными тестами; generic DB
режим сохранён, но больше не определяет UX всей админки.

**Статус на 2026-08-16: критерий выполнен, этап 2 фактически закрыт.** Единственный
формально открытый пункт — индексированный full-text поиск (`[!]`, требует миграции
продовой БД) — сознательно отложен по решению пользователя, не блокирует переход к
Этапу 3. WebKit-проверка admin shell также отложена (`[!]`, недоступна на текущей
dev-машине) и не блокирует остальной roadmap.

### Этап 3. Единая документация

#### Модель контента

- [x] Инвентаризировать `admin_docs.html`, `user_docs.html`, `docs/*.md`, metadata
  команд и настроек. Полный отчёт: `admin_docs.html`/`user_docs.html` — тонкие
  шаблоны, данные из `admin_docs.py` (в основном хардкод, кроме настроек) и
  `user_docs.py` (671 строка, полностью хардкод). `docs/*.md` разделены на
  dev/agent-доки (не в скоупе) и user/admin-кандидаты (`USER_GUIDE.md`,
  `ADMIN_GUIDE.md`, `ACTIONS.md`) плюс отдельно `docs/bot_docs/*.md` — это
  system-prompt контент для LLM-инструментов, другая аудитория, не Web UI.
  React docs-страницы существуют, но недостижимы из router — подтверждено.
  Метаданные команд разбросаны по `presentation/commands/catalog.py`,
  `access.py`, `core/roles.py`, `presentation/handlers/help.py`.
- [x] Найти противоречия и дубли между Jinja, Markdown, React и текстами бота.
  Конкретные находки: (1) список RP-действий независимо дублируется в 4 местах,
  версия в `help.py` уже неполная; (2) списки slash-команд по разделам вручную
  дублируются в `help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/`user_docs.py`;
  (3) **исправлено в этом срезе**: `docs/bot_docs/roles.md` не содержал роль
  `co_owner`, а секция ролей в `admin_docs.py` вообще не подключала
  `core/roles.py` — просто текст-заглушка; (4) **исправлено в этом срезе**:
  labels прав существовали в 3 независимых копиях (`web/presenters.py`,
  `presentation/handlers/moderation.py`, и de facto в `admin_docs.py` их не
  было вовсе) — бот и веб-панель показывали разный текст для одного и того же
  права (`manage_command_access`: «доступ команд» в боте vs «доступ к
  командам» в вебе).
- [x] Определить единый структурированный источник для команд, настроек, ролей,
  permissions, форматов и примеров. Настройки уже были единым источником
  (`SETTINGS_GROUPS`/`CHAT_SETTINGS_KEYS`) до этого среза. В этом срезе роли и
  permissions переведены на единый источник: `PERMISSION_LABELS_RU` +
  `permission_label_ru()`/`permissions_text_ru()` перенесены в `core/roles.py`
  (было 3 копии — в `presenters.py`, `moderation.py` и вообще без реализации в
  `admin_docs.py`); `admin_docs.py` получил `build_roles_docs()`, строящий
  таблицу ролей/рангов/прав напрямую из `SYSTEM_ROLE_TEMPLATES`; бот-хендлер
  `moderation.py` теперь тоже берёт текст прав из `core/roles.py`.
  RP-действия (reply-команды типа «обнять»/«трахнуть») тоже переведены на
  единый источник: `catalog.py` получил `SOCIAL_ACTION_18_PLUS` (перенесён из
  `text_commands.py`, где раньше был приватным дублем) и
  `build_social_action_docs()`, строящий список
  `{command_key, canonical trigger, is_18_plus}` из уже существовавших
  `COMMAND_KEY_DEFAULT_SOURCE_TRIGGER` + `SOCIAL_COMMAND_KEY_TO_ACTION` +
  `SOCIAL_ACTION_18_PLUS`; `user_docs.py` теперь строит оба списка
  («Reply-действия без 18+» и «18+ reply-действия») из этой единой функции
  вместо ручных литералов. **Найденный этим переводом баг** (ручные списки
  были рассинхронизированы с реальным набором команд): 20 реальных RP-действий
  отсутствовали в публичной документации вообще (`потрепать`, `покатать на
  спине`, `лупануть`, `поиграть`, `треснуть`, `подарить цветок`, `закидать
  снегом`, `согреть`, `воскресить`, `пригласить на свидание`, `спеть
  серенаду`, `покормить`, `пустить на шашлык`, `напоить`, `пригласить на
  чай`, `засветить`, `принести кофе`, `пригласить на танец`, `щёлкнуть по
  носу`, `уебать`, `укусить`), и 4 действия были показаны под неканоническим
  альтернативным триггером вместо основного (`разнести`→`разъебать`,
  `протащить`→`оттаскать`, `дать подзатыльник`→`настучать по голове`,
  `минет`→`отсосать`, с пропуском реального `поводить членом`). Регрессия
  закрыта тестами: `tests/unit/test_commands_catalog.py` (покрытие всех
  118 social-ключей, синхронизация `is_18_plus` с runtime-гейтом
  `text_commands._send_social_action`, уникальность триггеров) и
  `test_web_user_docs.py::test_user_docs_rp_action_lists_match_canonical_catalog_source`.
  Slash-команды по разделам (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/
  `user_docs.py`) остаются отдельной, более крупной задачей унификации — не
  закрыто в этом срезе; краткий список в `help.py` намеренно оставлен
  сокращённой выжимкой (Telegram-сообщение не вместит все 118 триггеров), а
  не полным дублем — не тот же тип бага, что был у RP-списков.
  **Решение принято (2026-08-16): вариант (a)** — полный единый каталог
  команд с генерацией всех 4 поверхностей (`help.py`, `USER_GUIDE.md`,
  `ADMIN_GUIDE.md`, `user_docs.py`/`admin_docs.py`), не аудит и не отложить
  (см. журнал решений). `help.py` (347 строк) + `docs/USER_GUIDE.md` (324) +
  `docs/ADMIN_GUIDE.md` (233) + `user_docs.py`/`admin_docs.py` вместе
  описывают, по грубой оценке, 80–150+ различных slash-команд бота — на
  порядок больше по объёму, чем сегодняшние срезы (роли/RP-действия были уже
  готовыми небольшими каталогами, которые нужно было просто переиспользовать;
  здесь готового каталога slash-команд нет вообще). Многосрезовая задача,
  начата отдельно от остального Этапа 3 (adaptation-срез идёт параллельно).

  **Разведка (2026-08-16, агентом)**: ~100–110 уникальных команд. Найдено
  **три независимых механизма диспетчеризации**, не два, как предполагалось —
  это меняет схему каталога:
  1. aiogram `@router.message(Command("..."))` — 79 команд по прямому грепу
     (`aliases.py` 4, `chat_assistant.py` 14, `private_panel.py` 1,
     `relationships.py` 12, `settings.py` 5, `stats.py` 11, `economy.py` 13,
     `help.py` 1, `moderation.py` 11, `text_commands.py` 2, `game/router.py` 3).
  2. `catalog.py`'s `COMMAND_KEY_DEFAULT_SOURCE_TRIGGER`/`PREFIX_TRIGGER_TO_COMMAND_KEY`
     (66 non-social ключей) — **только** через русские фразы;
     `match_builtin_command()`/`resolve_text_command()` явно отклоняют текст,
     начинающийся с `/` (`if normalized.startswith("/"): return None`), то
     есть большинство этих ключей **не имеют slash-формы вообще**.
  3. **Найден при проверке (не угадан)**: часть команд дispatch'ится через
     отдельный ad-hoc regex-мэтчинг в обход обоих механизмов выше — например
     `/article` работает через `r"^\s*/article(?:@[A-Za-z0-9_]+)?\s*$"` в
     `resolver.py:249` + `_is_daily_article_command()` в `text_commands.py`,
     а НЕ через `@router.message(Command("article"))` (такого нет вообще) и
     НЕ дублируется в `COMMAND_KEY_DEFAULT_SOURCE_TRIGGER` с `/`-формой.
     Первая попытка (до проверки regex) — посчитать `/article` в
     `user_docs.py` ошибкой и удалить — была бы сама ошибкой; переписана
     после того, как нашёлся реальный код диспетчеризации. Это значит: при
     заполнении каталога **нельзя полагаться только на грep по
     `Command("...")`** — часть команд обнаруживается только чтением
     `resolver.py`/`text_commands.py` целиком.

  **Схема** (`command_catalog.py`, новый модуль — `catalog.py` уже 1052
  строки, отдельный файл по уже принятой в сессии конвенции): `CommandSpec`
  с полями `key, category, syntax (tuple), title_ru, description_ru,
  examples, natural_triggers, notes` — плюс `dispatch_kind: "slash" |
  "natural_language" | "both"`, добавленное как урок из находки №3 выше,
  чтобы не терять точность на масштабе.

  **Найден отдельный реальный баг документации** (агентом, тот же класс, что
  и RP-действия сегодня): вся **гача-механика** (`gachagive`, `gacha_pull`
  → «гача генш», `gacha_profile` → «моя гача генш», `gacha_info` → «гача
  инфо», `gacha_skip` → «гача скип генш») **не задокументирована нигде** — ни
  в `help.py`, ни в `USER_GUIDE.md`/`ADMIN_GUIDE.md`, ни (предположительно)
  в `user_docs.py`/`admin_docs.py`. Не исправлено в этом срезе — банеры и
  механика пулов не изучены достаточно глубоко для точного описания, будет
  закрыто в своей категории (`misc`/`gacha`) позже, не наспех.

  **План по срезам** (категории из разведки, каждая — отдельный
  срез/коммит/CI): (1) **[x] сделано** — `command_catalog.py` schema
  (`CommandSpec` c `dispatch_kind: "slash"|"natural_language"|"both"`) +
  категория **economy** (5 команд-групп, переиспользован уже верифицированный
  текст из среза про availability badges, не переписан заново) +
  `user_docs.py`'s economy-секция переключена на источник. По пути найден и
  закрыт **дубль**, не баг отсутствия: `/article` уже был описан в карточке
  «Объявления, подписка и чатовые мемы» — при первой попытке добавить ему
  отдельную карточку получился бы дубль на странице; исправлено до коммита
  (см. журнал работ). Также найдено (не угадано, а прочитано в коде): у бота
  есть **третий механизм диспетчеризации** команд помимо aiogram `Command()`
  и `catalog.py` — ad-hoc regex/прямая проверка фразы в самом хендлере
  (`_is_daily_article_command()` в `text_commands.py` для `/article`), не
  ловится грепом по `Command("...")`. Тесты
  (`test_command_catalog.py`) verify каждый `syntax`/`natural_triggers` по
  реальному коду (`economy.py`, `catalog.py`'s trigger maps), не только
  внутреннюю согласованность каталога. (2) **[x] сделано** — категория
  **games**: `games_lobby` (`/game` + 7 режимов), `games_role_reveal`
  (`/role`, `/start`) — переиспользован уже верифицированный текст из
  `user_docs.py`. Заодно найдено небольшое реальное улучшение: у
  «Скрытая роль, карточка и ЛС» раньше не было `triggers` вообще, хотя
  `роль`/`старт` — настоящие рабочие natural-language эквиваленты
  (проверено по `catalog.py`'s `EXACT_TRIGGER_TO_COMMAND_KEY`) — теперь
  показаны. Верифицировано против `game/router.py`. (3) **[x] сделано** —
  **family/relationships**: 4 relationship-группы (`relationships_start`,
  `relationships_pair_actions`, `relationships_marriage_actions`,
  `relationships_end`) + 2 family-группы (`family_adopt_pet_tree`,
  `family_escape`). Верифицировано против `relationships.py`/
  `chat_assistant.py` и `catalog.py`'s trigger maps. **Найдены 2 реальные
  недокументированные команды**: `/adoptdaughter` (удочерение, роль «дочь» —
  единственное отличие от `/adopt`) и `/escapepet` (уйти из статуса
  питомца) — в старой доке была только «Усыновление, питомцы и семейное
  древо» с `/adopt`/`/pet`/`/family`, без обеих escape-команд и без
  `/adoptdaughter`; новая карточка «Уйти из семьи» добавлена, чтобы их
  закрыть. (4) **[x] сделано частично** — публичная (user-facing) часть
  «social/admin/moderation»: `misc_lastseen` (`/lastseen`) и
  `misc_public_service_commands` (`/help`, `/settings`, `/roles`, `/modstat`
  — команды просмотра, не изменения). Верифицировано против `help.py`,
  `settings.py`, `stats.py`, `moderation.py` и trigger maps. Найдено: у
  «Публичные служебные команды» не было `triggers` вообще, хотя `помощь` —
  рабочий эквивалент `/help` — теперь показан. **План уточнён по ходу
  работы**: реальные admin-only команды управления ролями/алиасами/
  настройками (`roleadd`, `rolecreate`, `roledefs`, `roledelete`,
  `roleperms`, `roleremove`, `rolesetrank`, `rolesettitle`, `roletemplates`
  из `moderation.py`; `aliases`, `aliasmode`, `setalias`, `unalias` из
  `aliases.py`; `setcfg`, `facttest`, `setrank`, `ranks` из `settings.py`) —
  **не в этом срезе**: `admin_docs.py`'s текущая схема `_docs_item()` не
  имеет полей `commands`/`triggers` вообще (только title/text/examples/notes,
  в отличие от `user_docs.py`) — их нужно сначала добавить в схему и шаблон
  `admin_docs.html`, это отдельная, более крупная задача, чем повторное
  использование уже готового поля. Перенесено в отдельный под-срез (5b) ниже.
  (5a) **[x] сделано** — gacha (`misc_gacha`): исследовано агентом отдельно
  (баннеры Genshin/HSR, парсинг через `_parse_gacha_command` в
  `resolver.py`, никакого slash-эквивалента вообще), проверено самостоятельно
  (не доверено агенту вслепую): `_GACHA_BANNER_ALIASES` совпадает с заявленными
  триггерами, `GACHA_CURRENCY_PER_COIN_RATE == 10` подтверждён напрямую.
  Найдены и задокументированы 2 реальных, ранее скрытых ограничения: (1) курс
  обмена монет на валюту гачи 10:1; (2) обязательная подписка чата на
  служебный Telegram-канал бота перед первым использованием
  (`_require_channel_subscription`) — раньше нигде не упоминалось, пользователь
  просто получил бы отказ без объяснения. Операторские команды
  (`гача админ`, `гача вкл/выкл`, `/gachagive`) **сознательно не
  задокументированы** ни в user, ни в admin доке — они гейтятся жёстко
  зашитым ID одного оператора бота, а не ролями конкретного чата (тот же
  принцип, что и разделение ADMIN_PASSWORD-панели от per-chat admin ранее
  в этой сессии). Новая карточка в разделе «Игры» user_docs.py.
  (5b) **[x] сделано** — `admin_docs.py`/`admin_docs.html` расширены полями
  `commands`/`triggers` (те же блоки/CSS-классы, что и `user_docs.html`,
  включая обновление `search_text` и `render_admin_docs_fixture.py` для
  HTMLHint-покрытия), и 15 admin-only команд ролей/алиасов/настроек
  заполнены в каталог (`admin_role_definitions`, `admin_role_assignment`,
  `admin_role_custom`, `admin_command_ranks`, `admin_aliases`,
  `admin_settings_tools`) — все проверены по реальному коду
  (`Формат:`-строки из самих error-сообщений хендлеров, а не придуманы),
  ни у одной нет natural-language формы (проверено против trigger maps,
  тест это фиксирует явно). Секции «Роли и ранги команд» (2 новые карточки:
  «Назначение и снятие ролей», «Кастомные роли») и «Алиасы команд»/«Работа
  с настройками» переключены на источник. Синтаксис `/roles` и `/settings`
  уже был в user-facing каталоге (публичные команды просмотра) — не
  дублирован здесь. 5 из 7 срезов плана были готовы к 2026-08-16.
  **2026-08-17: пользователь выбрал «1B»** — help.py и GUIDE.md оба
  генерируются из каталога, единый источник правды. Прежде чем можно было
  генерировать help.py (у него 7 разделов: stats/games/economy/relationships/
  social/moderation/settings), обнаружилось, что 4 из 7 разделов вообще
  отсутствовали в каталоге (stats/social/moderation, часть settings) —
  сначала закрыт этот пробел (sub-slice 6/7 плана), тем же уровнем
  строгости, что и admin-only срез: каждая команда проверена против
  реального `Command(...)`/regex/tuple-unpacking диспетчера, не по тексту
  help.py на слово. Заодно найдено 2 реальных случая `dispatch_kind="both"`,
  которые более ранняя разведка не заметила (`/settrigger`+`научить`,
  `/rpadd`+`добавить_действие`), и один "slash есть, но ничего не делает"
  казус (`/award` — реальная выдача идёт только через натурал-язык
  `наградить`). Добавлено 18 новых `CommandSpec` в 4 новые категории (stats,
  moderation, social + `/naming`) и в существующую `admin` (триггеры/RP).
  Также вынесен `GAME_RULES_RU` (детальные правила 9 игр, раньше существовали
  только внутри help.py) в `command_catalog.py` как канонический источник —
  **найден и исправлен реальный баг по пути**: `whoami` и `zlobcards` были
  реальными запускаемыми режимами, но отсутствовали в `/help`'s
  `_HELP_GAMES_ORDER` вообще — у пользователей не было способа посмотреть их
  правила через `/help` → Игры, хотя zlobcards — дефолтный режим создания
  игры везде в веб-панели. **(6b) закрыт (2026-08-17)**: `_HELP_SECTION_TEXT`
  больше не хранит команды как строковые литералы — каждая строка собирается
  из `_base_words(*catalog_keys)` (новый helper, вытягивает базовое слово
  `/word` из `spec.syntax`, без аргументов — тот же терсовый стиль, что и
  раньше, полная синтаксис-детализация остаётся в user_docs.py/USER_GUIDE.md)
  + `_code_join()`. Проверено вручную построчным сравнением рендера
  до/после — единственное расхождение было найдено и исправлено само по
  себе: секция «Модерация» случайно подтянула `/help`+`/settings` вместе с
  `/roles`+`/modstat` из общего `misc_public_service_commands` (все 4 команды
  в одном catalog-spec) — сужено срезом `[2:4]` до исходных 2. Формулировки,
  группировка и hand-authored заметки (реплай-глаголы, ЛС-панель/Mini App)
  сознательно оставлены как есть — Ilya's «curated, не full-derive» всё ещё
  применяется к СТРУКТУРЕ текста, а не только к длине; из каталога теперь
  берётся именно синтаксис команд — та часть, которая реально дрейфует при
  изменении кода. Заодно перепроверил список reply-глаголов раздела
  «Социальное» против настоящего канонического списка триггеров
  (`build_social_action_docs()`, 118 реальных триггеров против 42 в
  help.py) — не баг: help.py осознанно показывает урезанное подмножество
  (решение пользователя от 2026-08-16), и все 42 показанных слова —
  реальные валидные триггеры (включая «минет» — алиас «отсосать», не
  выдумка). При сверке USER_GUIDE.md против каталога попутно нашёл и
  исправил ещё 2 реальных пробела в только что добавленных `CommandSpec`
  (не в USER_GUIDE.md — он был прав, каталог был неполон): `stats_profile`
  не хватало реальных триггеров «кто я» и «мой рейтинг» (были только
  «кто ты»/«репутация»), `stats_achievements` не хватало «ачивки». Оба
  подтверждены напрямую по `EXACT_TRIGGER_TO_COMMAND_KEY`, не по тексту
  USER_GUIDE.md на слово.
  **(7) генерация `USER_GUIDE.md`/`ADMIN_GUIDE.md` — пересмотрен план после
  чтения обоих файлов целиком.** Изначальная идея «один маркер, генерируемое
  снизу, hand-authored сверху» не подходит структуре: reference-контент
  (команды/триггеры) не лежит одним блоком в конце, а вплетён в 6+ секций
  каждого файла вперемешку с педагогической прозой (USER_GUIDE.md: секции
  4.1/4.2/5.2/6.1/8/9.1-9.6; похожая картина в ADMIN_GUIDE.md). Форсировать
  единый маркер означало бы или (а) переписать структуру документа целиком
  ради формата генерации — риск для «понятнее и лучше», явно поставленного
  пользователем условия, или (б) отказаться от единого маркера. Выбран путь
  (б), тот же принцип, что уже сработал для help.py sub-slice 6b: не
  перестраивать документ, а точечно заменить только command syntax/triggers
  внутри уже существующих reference-списков на catalog-sourced значения,
  оставив прозу, структуру и порядок секций без изменений.
  **(7) закрыт (2026-08-17), другой формой, чем изначально задумано.** Не
  правкой markdown-текста руками (та же ловушка «дублирование», просто
  единоразовая, а не непрерывная), а новым `tests/unit/test_guide_docs_match_catalog.py`
  (10 тестов) — для каждой reference-секции обоих файлов проверяет, что
  реальные `spec.syntax`/`.natural_triggers` из каталога присутствуют в
  тексте секции. Это НЕ побайтовая генерация файла из каталога — это
  постоянный, а не разовый источник правды: если каталог обновится (новая
  команда, новый триггер), тест покраснеет, а не молча устареет, как было
  бы с одноразовой ручной синхронизацией. Реализует ту же гарантию
  «единый источник, без дублирования вручную», что и help.py, но для
  markdown, где нет runtime-рендеринга, через который можно было бы
  буквально сгенерировать текст. Первый прогон нашёл 3 реальных пробела в
  документации (не в каталоге — на этот раз каталог был прав): USER_GUIDE.md
  §4.1 не упоминал `/desc`, `/awards`, `/achsync` вообще; §4.2 не упоминал
  триггер «кто ты» (только «кто я»); ADMIN_GUIDE.md §7 не упоминал «снять
  пред» (был явный provided для «снять варн», но не для pred). Все 3
  добавлены в markdown. Заодно при написании теста для модерации нашёлся
  ещё один пробел в каталоге (не в доке): «повысить»/«понизить» —
  отдельная, реальная фича смены роли на шаг через reply-слово
  (`_REPLY_ROLE_STEP_PATTERN` в moderation.py), не то же самое, что
  `/roleadd`/`/roleremove`, отсутствовала в каталоге вообще — добавлен
  новый `CommandSpec` `admin_role_step`.
- [x] Не хранить секретные admin procedures в публичном пользовательском разделе.
  Проверено построчным аудитом всех 671 строки `user_docs.py` против
  `docs/ADMIN_GUIDE.md`: админских команд управления (роли, настройки,
  модерация, алиасы, триггеры) в публичном разделе нет вообще — контент уже
  корректно разделён автором, есть явная переадресация "для модерации/
  настроек — `/app/docs/admin`" (строка 641). Единственное упоминание
  модерации — `/modstat` (просмотр СВОЕГО статуса, не инструмент модерации).
  Вне скоупа этого пункта, но зафиксировано отдельно: секция 18+ reply-действий
  лежит открытым текстом на публичной, не защищённой auth странице без
  возрастного ограничения — не является "admin procedure", но может требовать
  отдельного решения о контентных ограничениях, если это важно для проекта.
- [x] Добавить автоматическую проверку уникальности anchors и внутренних ссылок.
  Новый `tests/unit/test_web_docs_anchors.py`: рендерит `admin_docs.html` и
  `user_docs.html` через их реальные context-билдеры, парсит HTML (без regex —
  `html.parser.HTMLParser`), проверяет отсутствие дублей `id` и что каждая
  `href="#..."` ссылка ведёт на существующий anchor. Работоспособность самой
  проверки подтверждена: на синтетическом HTML с дублем/битой ссылкой тест
  корректно падает. Оба реальных документа сейчас проходят без нарушений.

#### Пользовательская документация

- [x] Публичный доступ без admin session. Подтверждено чтением кода и реальным
  route-тестом: `_load_user_from_request` (app.py:1203) возвращает `None` без
  единого обращения к БД, если в запросе нет session cookie; `/app/docs/user`
  и `/api/app/docs/user` никогда не редиректят по результату — `redirect_path`
  у пользовательских docs всегда `None` (в отличие от admin-роутов). Новый
  `tests/unit/test_web_user_docs_public_access.py` бьёт по реальному ASGI-приложению
  без cookie и с `session_factory`, которая упала бы при попытке обращения к БД —
  доказывает отсутствие auth-проверки, а не просто читает код.
- [x] Responsive оглавление/drawer. Отдельный adaptation-срез после
  завершения контентной части Этапа 3, как решил пользователь 2026-08-16.
  `.docs-layout` получил `@media (width <= 980px) { grid-template-columns: 1fr;
  } .docs-nav { position: static; }` в новом `docs-responsive.css` (46 строк,
  общий для `user_docs.html`/`admin_docs.html`) — тот же брейкпоинт, что уже
  использует `.overview-live-grid` в этом проекте. Навигация обёрнута в
  нативный `<details class="docs-nav-toggle" open>` (chevron-стиль
  переиспользован из уже существующего `.admin-secondary-details`) — не
  кастомный JS-drawer, а нативный disclosure: доступен с клавиатуры из
  коробки, работает без JS вообще, сворачивается на любом экране при желании.
  Подтверждено программно (не только на глаз): `document.documentElement
  .scrollWidth > clientWidth` — `False` на обеих страницах на 390px и 820px
  (было `True` с ~45px переполнения до фикса). Новые Playwright-тесты в
  `test_web_docs_quality.py`: 4 комбинации страница×viewport на overflow,
  открытие/закрытие toggle. **Найден, но НЕ исправлен** (вне scope этого
  пункта — тот же `.hero`/`.hero-side` баг, что и везде на сайте, не
  специфичный для docs-страниц): на 390px чипы `hero-side` наезжают на текст
  `hero-copy` — общий компонент, используемый много где за пределами
  документации, требует отдельного кросс-страничного среза, а не точечного
  фикса здесь.
- [x] Полнотекстовый поиск. Клиентский фильтр по тому же паттерну, что уже
  принят в проекте для `admin-table-search.js` (список таблиц админки):
  `docs-search.js`/`.css` (22/29 строк), новый `<input type="search">` в
  `.docs-nav` с `data-docs-search-input`. Каждая `.docs-card` получила
  `data-docs-search-text` — единая lowercased строка из title/text/badges/
  commands/triggers/examples/steps/notes, посчитанная один раз в
  `_docs_item()` (`user_docs.py`), не в шаблоне. При вводе скрываются
  несовпадающие карточки и секции с нулём совпадений; пустой результат
  показывает «Совпадений не найдено». Playwright-тесты подтвердили: фильтрация
  по уникальному слову находит ровно одну карточку, полностью
  несуществующий запрос скрывает всё и показывает empty-state, очистка
  запроса возвращает все 28 карточек. Не путать с пунктом «Search index
  строится детерминированно…» в «Качестве документации» ниже — это про
  отдельный индексированный поиск по всей документации (`bot_docs/`), не про
  этот in-page фильтр.
- [x] Разделы по пользовательским задачам, а не только по списку команд.
  Проверено: все 8 секций `_USER_DOC_SECTIONS` уже сгруппированы по
  пользовательским задачам/темам («С чего начать», «Пары и брак», «Семья,
  титулы и имя», «Социальные действия и мемы» и т.д.), а не по алфавиту или
  внутреннему списку команд; то же для заголовков карточек внутри секций
  («Карма», «Reply-действия без 18+», а не голые `/command`-имена). Изменений
  кода не требовалось — структура уже соответствовала требованию.
- [x] Копируемые примеры и deep links. Каждая карточка (`.docs-card`) в
  `user_docs.html` теперь имеет стабильный id (`{section.anchor}-item-{n}`) и
  видимую ссылку-якорь `#` в шапке (`.docs-item-anchor`, opacity 0.55 в покое —
  видна и на touch-устройствах без hover, не только по наведению). Каждый
  пример в блоке «Примеры» получил кнопку «Копировать»
  (`.docs-clip-button`, `Clipboard API` с fallback на `execCommand('copy')`
  для небезопасного контекста) с временной обратной связью «Скопировано».
  Новые файлы `docs-item-actions.css`/`.js` (56/50 строк, под baseline-лимитом
  60/60), подключены только на `user_docs.html` через `extra_styles`/
  `extra_scripts`. **Найден и исправлен реальный баг по пути**: переход по
  новому deep-link якорю прокручивал карточку ПОД sticky-топбар (`.topbar`,
  `position: sticky; z-index: 20`), закрывая заголовок карточки — `.docs-card`
  не входил в существующее правило `scroll-margin-top: 148px`
  (`panel.css`, до этого применялось только к `.docs-section`/
  `.docs-setting-card`/`#docs-trigger-types`); добавлен в тот же список.
  Новый `scripts/render_user_docs_fixture.py` (первый HTMLHint-fixture для
  `user_docs.html` вообще — раньше эта страница не проверялась в CI) вызывает
  `build_user_docs_context()` напрямую (в отличие от `render_admin_docs_fixture.py`)
  — проверено, что у неё нет транзитивной зависимости на pydantic, против
  изолированного Jinja2-only venv, зеркалящего CI. Playwright-проверка
  desktop/tablet: копирование, hover-feedback и прокрутка к якорю работают
  корректно, карточка не обрезается. Mobile (390px): карточка выходит за
  экран — тот же уже задокументированный page-wide баг `.docs-card-grid` без
  mobile-брейкпоинта (не регрессия от этого среза, будет исправлен в
  отдельном срезе адаптации после Этапа 3). Скриншоты 97–101.
- [x] Понятные отметки доступности функции и требуемых прав. `_docs_item()`
  получил `requires_settings: tuple[str, ...]`, генерирующий бейдж
  `"<short_ru> по настройке"` для каждого ключа из уже существующего единого
  источника `SETTING_META` (`settings_common.py`) — тот же паттерн, что и
  сегодняшняя унификация ролей/RP-действий: не выдумывать текст заново, а
  выводить его из данных. Проверено по коду хендлеров (не угадано), какие
  реальные настройки гейтят какие команды: `economy_enabled` — вся
  «Экономика и предметы» (панель/ферма/магазин+инвентарь+крафт/
  рынок+переводы+аукцион/рост); `craft_enabled` — доп. гейт на `/craft`;
  `auctions_enabled` — доп. гейт на `/auction`/`/bid`; `titles_enabled` +
  `economy_enabled` — титулы; `family_tree_enabled` — усыновление/питомцы/
  `/family`; `actions_18_enabled` — 18+ reply-действия и явное действие
  роста. Заменены 2 расплывчатых плейсхолдер-бейджа «по настройкам» (не
  говорили, какая именно настройка) на точные, и убраны 4 дублирующих
  расплывчатых prose-заметки того же смысла («Крафт может быть отключён
  настройками чата», «Титулы должны быть включены настройками чата» и т.д.).
  Сознательно НЕ добавлен бейдж на «Как играть в «Кто я»» — там `actions_18_enabled`
  открывает только одну тему внутри игры, а не всю функцию, бейдж на всю
  карточку был бы неточным (существующая prose-заметка точнее). **Найден и
  исправлен по пути CI-баг того же класса**, что уже был у admin_docs:
  `render_user_docs_fixture.py` вызывал `build_user_docs_context()` напрямую,
  но новый импорт `SETTING_META` транзитивно тянет `selara.core.config`
  (pydantic) через `chat_settings.py` — сломало бы CI frontend job (только
  `Jinja2`). Переписан на ручную сборку контекста (как у всех остальных
  `render_*_fixture.py`), проверено против изолированного Jinja2-only venv.
  Новый `test_web_user_docs.py::test_user_docs_availability_badges_are_derived_from_setting_meta`
  фиксирует все 8 бейджей и отсутствие старого плейсхолдера. Playwright
  spot-check (desktop): бейджи не переполняют карточку, хорошо читаются.

#### Администраторская документация

- [x] Доступ только в подходящем защищённом контексте. **Найден реальный
  баг** при подготовке этого пункта: `/app/docs/admin` использовал ту же
  форму context-builder'а, что и намеренно публичный `/app/docs/user`
  (`redirect_path` всегда `None`) — страница была так же полностью доступна
  без сессии, хотя по этому же пункту roadmap'а должна быть за защищённым
  доступом. Не найдено ни одного места, которое реально его гейтило. Исправлено:
  `_build_admin_docs_page_context` теперь требует обычную пользовательскую
  веб-сессию (тот же уровень, что и `/app` — `_load_user_from_request`,
  редирект на `/login` при отсутствии), а НЕ отдельную
  ADMIN_PASSWORD-сессию `/app/admin` (это другая концепция — общие
  bot-wide инструменты оператора, а не документация по администрированию
  конкретного чата). Новый `tests/unit/test_web_admin_docs_access.py`:
  редирект на `/login` без cookie, `200` с валидной сессией (реальный
  ASGI-запрос, не просто чтение кода).
- [x] Настройки чата, роли, permissions, aliases, triggers, рассылки и обслуживание.
  Все темы, кроме двух последних, уже были покрыты секциями `admin_docs.py`
  (настройки — через `SETTINGS_GROUPS`; роли/permissions — через
  `build_roles_docs()`, добавленный в срезе про RP-действия; aliases и
  triggers — отдельные существующие секции). **Рассылки** и
  **обслуживание** не имели вообще ни одной секции — добавлены две новые:
  «Рассылки» (текст/медиа, реакции, поиск аудитории с select all/none,
  история с прогрессом и статусами доставки, submit guard от повторной
  отправки) и «Обслуживание» (ручной запрос backup, клиентский поиск по
  списку таблиц, edit/delete с destructive-warning и восстановлением
  фокуса, «Технический режим» на странице чата как переход в общий
  обозреватель БД, а не отдельная функция). Контент сверен с реальным кодом
  композера (`admin.html` — `data-broadcast-*` атрибуты форм) и роутов
  (`/app/admin/request-backup`, `/app/admin/table/{table_name}`), не
  придуман. Регрессия закрыта
  `test_web_admin_docs.py::test_admin_docs_covers_broadcasts_and_maintenance`.
- [x] Ссылки из полей админки ведут сразу к релевантному anchor. Уже было
  реализовано: каждая карточка настройки в `chat.html` — `href="{{
  admin_docs_url }}#{{ item.doc_anchor }}"`, `doc_anchor` строится через
  `setting_anchor(key)` (`presenters.py:620`), а `admin_docs.py` использует
  ТУ ЖЕ функцию `setting_anchor()` для anchor'ов секции настроек — единый
  источник, не может рассинхронизироваться. Просто не было ни строчки теста
  и не был отмечен в TODO. Закрыто верификацией (без изменений кода):
  новый `test_web_docs_anchors.py::test_setting_field_doc_links_resolve_on_the_admin_docs_page`
  строит реальные `doc_anchor` из `build_settings_sections()` и сверяет их
  с реальными `id` на отрендеренной `admin_docs.html` — не просто «обе
  стороны вызывают одну функцию», а честная сквозная проверка результата.
- [x] Примеры включают ошибочные варианты и способы исправления. `_FEATURE_DOC_SECTIONS`
  items получили опциональные `examples`/`notes` (шаблон `admin_docs.html`
  расширен блоками «Частые ошибки»/«Важно», переиспользуют существующие
  CSS-классы `.docs-card-block`/`.docs-code-list` из user_docs.html — новый
  CSS не понадобился). «Подсказки по формату» (работа с настройками)
  получила 2 пары ❌/✅ с текстом ошибок, процитированным дословно из
  `core.chat_settings.parse_chat_setting_value` («Значение должно быть целым
  числом», «Значение должно быть > 0»), не придуманным.
- [x] Отображать ограничения Telegram рядом с соответствующими возможностями.
  2 факта, проверенных по реальному коду, а не угаданных: «Текст и медиа»
  (рассылки) — лимит фото 10 МБ/JPEG-PNG (клиентская проверка в
  `admin-broadcast.js:340`); «Ручной запрос backup» (обслуживание) —
  архив режется на части по 45 МБ, потому что боты не могут слать файлы
  крупнее ~50 МБ (`infrastructure/backup.py: BACKUP_CHUNK_SIZE_BYTES`).
  Оба пункта закрывают весь раздел «Администраторская документация»
  полностью (5 из 5). Регрессия зафиксирована двумя новыми тестами в
  `test_web_admin_docs.py`.

#### Качество документации

- [x] Search index строится детерминированно и не содержит защищённые данные.
  `admin_docs.html` получил тот же клиентский поиск, что и `user_docs.html`
  (переиспользованы `docs-search.css`/`.js` без изменений — общие
  `data-docs-search-*` атрибуты), `search_text` считается один раз в
  `build_docs_sections()` из статичных строк секций, без обращения к БД.
  Область поиска — 11 секций `_FEATURE_DOC_SECTIONS` (вход, панель, группа,
  настройки, роли, алиасы, триггеры, аудит, игры, рассылки, обслуживание);
  таблица ролей и группы настроек сознательно не включены — у них уже есть
  своя компактная структура (полноценный поиск по ним — не в объёме этого
  пункта). Тест `test_admin_docs_search_text_is_deterministic_and_holds_no_protected_data`
  доказывает: два независимых вызова `build_admin_docs_context()` дают
  идентичный результат, и что реальные данные чата (`chat_id`, `chat_title`)
  не попадают в `search_text`, даже когда передан `chat`.
  **Найден и исправлен реальный баг по пути** (тот же класс, что уже был у
  `.admin-table-card[hidden]` в предыдущем срезе этой сессии): `.docs-card`
  имеет собственный `display: grid`, который побеждает браузерный
  `[hidden]{display:none}` при равной специфичности — карточки, скрытые
  поиском внутри секции, у которой ЕСТЬ другие совпадения, оставались
  визуально видимыми. Обнаружено не чтением кода, а тем, что тест на
  видимость (`:visible`, а не просто атрибут `[hidden]`) не совпал со
  скриншотом. Существовавшие Playwright-тесты поиска (для `user_docs.html`
  из прошлого среза) проверяли только наличие атрибута `[hidden]`, а не
  реальную видимость — тот же баг был и там, просто не пойман тестами;
  оба файла переведены на CSS-селектор `:visible`, который отражает
  фактическое состояние рендера. Фикс — `[data-docs-search-card][hidden] {
  display: none; }` в `docs-search.css`.
- [x] Проверить keyboard navigation, heading hierarchy, links и mobile layout.
  Новый `test_web_docs_quality.py`: (1) heading hierarchy — обе страницы
  парсятся на `<h1>`–`<h6>`, проверено отсутствие пропуска уровня (h2 → h4 и
  т.п.) в реальной последовательности заголовков, не выборочно; обе страницы
  чистые. (2) keyboard navigation — реальный `.focus()` в Chromium
  подтверждает, что поиск, кнопка «Копировать» и deep-link иконка — настоящие
  фокусируемые `<input>`/`<button>`/`<a>` (не `<div>` с обработчиком клика),
  плюс сквозной клик подтверждает, что активация действительно работает. (3)
  links — уже покрыто `test_web_docs_anchors.py` (уникальность id,
  разрешаемость `#`-ссылок), включая новый сквозной тест deep-link из полей
  настроек. (4) mobile layout — не переисследовано заново: уже
  задокументированный `.docs-layout` баг без mobile-брейкпоинта остаётся
  сознательно отложенным до отдельного среза адаптации (решение от
  2026-08-16), само наличие бага уже подтверждено замером в предыдущих
  срезах.
- [x] Playwright: public user docs, protected admin docs, search, deep link и mobile
  TOC. Все пять пунктов закрыты реальными Playwright/ASGI-тестами:
  public user docs (`test_web_user_docs_public_access.py`), protected admin
  docs (`test_web_admin_docs_access.py`), search
  (`test_web_user_docs_browser.py`, `test_web_admin_docs_browser.py`), deep
  link (`test_item_deep_link_anchor_scrolls_clear_of_sticky_topbar` +
  `test_every_docs_card_has_a_unique_deep_link_id`), mobile TOC — закрыт
  позже тем же днём (2026-08-16, commit 6473115, `.docs-layout` схлопывается
  на ≤980px через нативный `<details>`-toggle, `scrollWidth`-проверка
  подтвердила устранение overflow на 390/820px); чек-бокс не был обновлён в
  момент того закрытия — исправлено здесь.

**Критерий завершения этапа 3:** документация ищется, открывается по стабильным
ссылкам и не расходится с источником команд/настроек.

### Этап 4. Остальные серверные страницы

Каждая страница проходит общий pipeline: baseline tests → новый layout/components →
responsive → accessibility → visual review → TODO update.

- [x] `landing.html`: ясное позиционирование, CTA, performance и mobile layout.
- [x] `login.html`: Telegram login flow, ошибки и mobile keyboard.
- [x] `home.html`: группы, состояния аккаунта, быстрые переходы и empty state.
- [x] `achievements.html`: responsive cards, progress semantics и длинные названия.
- [x] `feedback.html`: форма, validation, success/error и защита повторной отправки.
- [x] `chat.html` (крупнейший пункт Этапа 4, 1324 строки — закрыт как 7 под-срезов
  по аналогии с проектом единого источника slash-команд):
  - [x] разделить большой шаблон на partials/components;
  - [x] рейтинг и статистика;
  - [x] настройки и permissions;
  - [x] aliases/triggers;
  - [x] live updates;
  - [x] mobile navigation между секциями;
  - [x] вынести inline-JavaScript в тестируемые modules.
- [x] `family.html`: responsive graph controls, empty/error и JS extraction.
- [x] `economy.html`: responsive data presentation, forms и JS extraction.
- [x] `audit.html`: если не завершён в admin-этапе, применить общий timeline pattern.
- [x] `error.html`: понятные 4xx/5xx состояния, recovery actions и request ID при
  наличии.
- [x] Проверить все cross-page links и возврат назад после действий.
  - [x] **Cross-page links** — закрыто систематически, а не выборочно: новый
    `tests/unit/test_web_cross_page_links.py` собирает все внутренние ссылки из
    `app.py`/`presenters.py`/`user_docs.py`/`admin_docs.py`/всех шаблонов (114 штук)
    и проверяет каждую против реальной таблицы маршрутов FastAPI (90 routes).
    Нашёл и исправил реальный битый маршрут (см. журнал работ). `/miniapp/*`
    вынесен в задокументированный allowlist: этими путями владеет React Mini App
    SPA, FastAPI их намеренно не регистрирует.
  - [x] **Возврат назад после действий** — закрыто. Дефект подтверждён чтением
    кода в предыдущем срезе: после plain-form POST по алиасам, смарт-триггерам и
    настройкам чата (`/app/chat/{id}/aliases`, `/app/chat/{id}/triggers`,
    `/app/chat/{id}/settings`) редирект шёл на `_with_message(f"/app/chat/{chat_id}",
    ...)` — без `?tab=settings`, а `chat_page` по умолчанию открывает `overview`
    (`request.query_params.get("tab") or "overview"`). Исправлены все 15
    вхождений во всех трёх обработчиках (`update_chat_trigger`, `update_chat_alias`,
    `update_chat_setting`) точечным `sed` по диапазону строк этих трёх функций —
    `f"/app/chat/{chat_id}"` → `f"/app/chat/{chat_id}?tab=settings"`. Побочный
    эффект (полезный): `redirect_path` в этих обработчиках переиспользуется и в
    JSON-ветке (`_json_result(..., redirect=redirect_path)`), так что fetch-путь
    (`chat-settings.js`) тоже получил исправленный `redirect` в ответе, хотя баг
    проявлялся только на no-JS/plain-form пути.

### Этап 5. Игры — отдельный высокорисковый блок

- [x] Зафиксировать все игровые сценарии и состояния до изменения DOM.
  - Закрыто по факту через sub-slice 1/N, 3/N, 4/N, 6/N, 9/N: empty, create-game
    (kind-grid + тема), lobby (не запущена), активная игра во всех 9 режимах,
    результаты партии (все 3 ветки reveal: role/bred/score), 24-игрочный
    стресс-тест, error — общий сайтовый баннер, уже проверен. Единственное, что
    сознательно не покрыто отдельно — error-состояние ВНУТРИ конкретного
    игрового действия (не глобальный баннер, а именно провал одного action);
    оно уже косвенно проверено в sub-slice 8/N (submit-handler показывает
    toast с текстом ошибки сервера) — отдельного DOM-состояния для этого в
    шаблоне нет, ошибка всегда идёт через тот же toast-механизм.
  - Sub-slice 1/N (2026-08-17): новый `tests/unit/test_web_games_dashboard_browser.py`
    (3 теста) через реальный `/app/games` route (не ручная сборка Jinja-контекста —
    структура `game.whoami_view`/`spy_view`/etc. слишком сложна, чтобы собирать её
    вручную не разойдясь с реальной формой) — empty state, create-game kind-grid
    (8 режимов, выбор чата, тема для 500 Злобных Карт), один активный game (whoami,
    3 игрока, длинные имена участников: «Ложка Огромная-Преогромная»). 9 PNG
    (114–116, 3 состояния × 3 viewport) в `~/selara_screen`, все прошли ручной
    просмотр без замечаний. **Ни одного бага не найдено** — `.game-kind-grid` и
    `.game-meta-grid` уже используют проверенный `auto-fit, minmax(...)` паттерн
    (тот же, что закрыт без изменений в economy.html), overflow нет ни на одном из
    3 viewport ни в одном из 3 состояний (подтверждено программно, не только
    скриншотом). Длинные имена участников корректно обрезаются (`text-overflow`),
    не переполняют карточки. Остаётся зафиксировать: остальные 8 из 9 режимов
    (bunker, dice, mafia, number, quiz, spy, zlobcards отдельно от 500 Злобных Карт
    уже частично видно в kind-grid, whoami — готово), состояния результатов/ошибок.
  - Sub-slice 2/N (2026-08-17): новый `tests/unit/test_web_games_live_updates_browser.py`
    зафиксировал SSE/WS/fallback-poll reconnect контракт `games.html` (~935 строк
    inline JS, до этого 0% покрытия) через fake `WebSocket`/`EventSource`
    (`page.add_init_script`) + `page.clock` для виртуального времени вместо
    реальных 30-секундных ожиданий. **Найден и исправлен реальный баг**: WS
    reconnect запускался только из `connectGameSockets()`, которую вызывали
    только `commit()` внутри `applyDashboardHtml()` и инициализация страницы —
    а «ничего не изменилось»-ветка `refreshDashboard()` (games.html:697-701)
    возвращалась раньше, чем успевала вызвать `applyDashboardHtml`. Итог: после
    обрыва WS и истечения 30-секундного backoff (`wsDisabledUntil`) клиент
    навсегда застревал на 4-секундном fallback-polling, если состояние игры не
    менялось (тихое лобби, никто не ходит) — backoff истекал, но переподключение
    никогда не запускалось. Исправлено одной строкой: `connectGameSockets()`
    теперь вызывается и в «не изменилось»-ветке — вызов безопасен (no-op внутри
    backoff-окна, пропускает игры с уже живым сокетом). Tests-first перепроверено
    через `git stash` на `games.html`: тест падает на «до» (`assert 1 > 1`) по
    ожидаемой причине, зелёный на «после». SSE осталась вне контракта этого
    среза — реконнект SSE нативный (browser-level), в app-коде нет backoff-логики
    для него, чинить нечего.
  - Sub-slice 3/N (2026-08-17): дополнил `test_web_games_dashboard_browser.py`
    параметризованным тестом на все 9 игровых режимов (`spy`, `mafia`, `dice`,
    `number`, `quiz`, `bredovukha`, `bunker`, `whoami`, `zlobcards`) — активная
    панель каждого режима рендерится через реальный `GameStore.create_lobby`/
    `join`/`start` (обобщённый helper `_create_started_game`, учитывает разный
    `min_players` на режим: от 2 у `dice`/`number`/`quiz` до 6 у `bunker`,
    самого требовательного). Проверено programmatically на 3 viewport для всех
    9 — **overflow нет нигде**. Ручной скриншот-обзор — только 2 самых
    структурно нетипичных режима: `bunker` (уникальная панель выбора мест,
    самый большой ростер) и `mafia` (ночь/день фазы, отдельная колонка «стол и
    статусы»); остальные 7 переиспользуют общий `.game-action-section`/
    `.game-meta-grid` каркас, уже подтверждённый скриншотом на whoami в
    sub-slice 1 — осознанно не дублировал 21 лишний PNG ради того же паттерна
    (см. workflow: «механические задачи не раздувай»). 6 новых PNG (117–118) в
    `~/selara_screen`. Чек-лист-пункт «зафиксировать сценарии» теперь покрывает
    9/9 режимов в состоянии «активная игра» + empty + create-game; остаются
    результаты/завершение/ошибки каждого режима — не сделано, следующий under-slice.
  - Sub-slice 4/N (2026-08-17): зафиксировал состояние «результаты партии»
    (`recent-games-panel`, `_games_dashboard.html:1153-1244`) — 3 репрезентативных
    режима по числу веток рендера (`whoami` → `role_reveal_rows`, `bredovukha` →
    `bred_reveal_rows`, `dice` → голый `score_rows`). **Найден и исправлен
    реальный баг контраста**: `.banner-ok`/`.banner-error` — глобальный компонент,
    стилизованный под тёмный фон страницы (светлый текст `#c9ffef`/`#ffe4e8`), а
    `.recent-result-banner` вкладывает его внутрь `.recent-game-card` — светлого
    пастельного фона (все 9 вариантов карточки, panel.css ~1781-1809). Текст
    технически присутствовал в DOM (`response_text` реально приходил с сервера),
    но был практически невидим — контраст ~1:1 (светло-мятный на светло-кремовом).
    Найдено при обязательном ручном просмотре скриншота (не тестом — старый тест
    проверял только overflow). Исправлено: `.page-games .recent-game-card
    .banner-ok/.banner-error` — тёмно-зелёный/тёмно-красный текст, тот же паттерн,
    что уже применён к `.recent-game-head strong/p` в этом же контексте (строка
    выше). Тест дополнен реальной проверкой контраста (относительная luminance
    вычисленного `color` через `getComputedStyle`, а не просто «текст не
    default») — перепроверено `git stash` на `panel.css`: luminance 0.90 (не
    проходит порог 0.35) до фикса, зелёный после. `panel_css_lines` cap поднят
    6255 → 6270. 3 PNG (119, только whoami — до/после сравнение вручную, не
    сохранялось отдельно) в `~/selara_screen`.
- [x] Разделить `_games_dashboard.html` на небольшие Jinja partials.
  - Первичная разведка (2026-08-17, до подтверждения пользователя) недооценила,
    насколько это безопасно — сделан только грубый вывод «bredovukha раскидан
    по несмежным блокам, значит per-mode split архитектурно рискован для всех
    9 режимов». После явного «2A» от пользователя перечитал `games-list`
    (942 строки) целиком построчно вместо выборочного grep — картина оказалась
    заметно чище: 4 режима (`whoami`, `spy`, `mafia`, `zlobcards`) уже рендерятся
    через ИМЕННО namespaced context-объекты (`game.whoami_view`/`spy_view`/
    `mafia_view`/`zlob_view`), и КАЖДЫЙ — единый смежный `{% if game.X_view %}
    ... {% endif %}` блок без переплетения с другими режимами: whoami
    (318-432), spy (434-550), mafia (552-625), zlobcards (627-757) — реально
    механический, безопасный перенос. Извлечены в `_games_mode_whoami.html`,
    `_games_mode_spy.html`, `_games_mode_mafia.html`, `_games_mode_zlobcards.html`,
    подключены через `{% include %}` (та же конвенция, что у
    `_chat_settings_panel.html` — guard живёт внутри partial'а, не снаружи).
    **Строго проверено, не предположено**: `git stash` на `_games_dashboard.html`
    + рендер всех 4 режимов через реальный `/app/games` route до/после с
    зафиксированным `random.seed(42)` (иначе случайный выбор карточек/тем
    делает построчный diff бесполезным) — итоговый HTML **побайтово идентичен**
    (единственная разница — `data-live-signature`, SHA1 от фрагмента,
    содержащего реальный `started_at` timestamp с точностью до секунды —
    ожидаемо отличается между двумя раздельными прогонами, не признак
    расхождения разметки). Остальные 5 режимов (`bredovukha`, `dice`, `number`,
    `quiz`, `bunker`) подтверждено НЕ имеют namespaced view — `bredovukha`
    использует плоские поля (`category_buttons`/`vote_buttons`/
    `bred_submission_rows`/`bred_reveal_rows`), которые ЧАСТИЧНО переиспользуются
    generic-веткой для остальных режимов (строки 894/911 — тот же
    `category_buttons`/`vote_buttons` рендерится с другими подписями для
    НЕ-bredovukha/zlobcards режимов через `{% if ... and game.kind not in
    (...) %}`), а `dice`/`number`/`quiz`/`bunker` вообще не имеют
    mode-exclusive разметки — их действия идут через полностью общий
    `compat.main_buttons`/`score_rows`/`role_reveal_rows` каркас с
    mode-aware подписями через инлайн `{% if/elif %}` только в тексте
    заголовков. Раздельный per-mode partial для этих 5 потребовал бы либо
    дублирования shared-разметки, либо выделения shared-shell macro — то самое
    архитектурное решение, зафиксированное как правильно рискованное в первой
    разведке, актуально и сейчас. Итог первого прохода: 4 из 9 режимов
    (~437 строк, самые крупные и сложные — whoami/spy/mafia содержат
    наибольший объём markup) вынесены безопасно и с полной верификацией.
  - **Пункт закрыт полностью (2026-08-17, тот же день, после разрешения
    пользователя «делай»).** Перечитал bredovukha ещё раз внимательнее —
    первая разведка ошибочно решила, что её контент «раскидан по несмежным
    блокам» из-за того, что 2 её `{% if game.kind == "bredovukha" %}` блока
    (category/vote-buttons с bredovukha-специфичными подписями) стояли рядом
    с ОБЩИМИ score_rows/role_reveal_rows секциями — на самом деле все 4
    bredovukha-эксклюзивных блока (category_buttons+bredovukha-guard,
    vote_buttons+bredovukha-guard, bred_submission_rows/show_bred_answer,
    bred_reveal_rows) идут строго подряд, единым диапазоном (326-409),
    прямо перед общей scoreboard-секцией. Проверено на уровне данных, не
    только шаблона: `_build_bred_submission_rows()`/`_build_bred_reveal_rows()`
    в app.py оба явно возвращают `[]`, если `game.kind != "bredovukha"` —
    гарантированно пусто для всех остальных режимов. Извлечено в
    `_games_mode_bredovukha.html`, та же verification-методология: git-stash
    + рендер 5 режимов (bredovukha и для контроля dice/quiz/number/bunker) с
    `random.seed(42)` — побайтово идентично, кроме ожидаемого
    `data-live-signature`. `dice`/`number`/`quiz`/`bunker` подтверждено не
    нуждаются в partial вообще — у них действительно нет ни одной строки
    mode-exclusive разметки, весь их UI идёт через общий каркас. Итог: partial
    выделен для всех режимов, где это архитектурно safe и осмысленно (5 из 9:
    whoami/spy/mafia/zlobcards/bredovukha), для оставшихся 4 partial и не
    нужен — нечего выносить. Пункт закрыт полностью, не осталось открытого
    архитектурного вопроса по этому чек-листу.
- [~] Разделить inline-JavaScript `games.html` на modules по игровым режимам.
  - Sub-slice 5/N (2026-08-17): весь inline-скрипт (943 строки, был единственным
    оставшимся большим inline-JS во всём проекте — `inline_script_lines` теперь
    **0**) вынесен в единый `src/selara/web/static/games.js`, подключён через
    `extra_scripts=["games.js"]` (тот же механизм, что у economy.js/family-graph.js).
    Ноль Jinja-интерполяции внутри скрипта — данные и так шли только через DOM
    data-атрибуты (`dashboardRoot.dataset.liveSignature` и т.п.), поэтому JSON
    data-island не понадобился, самая чистая экстракция за весь проект. Разбивка
    **по игровым режимам** (буквальная формулировка пункта) НЕ сделана — общая
    инфраструктура (WS/SSE/fallback-polling, form snapshot/restore, DOM patching)
    используется всеми 9 режимами одновременно и переплетена с mode-specific
    sync-функциями (`syncWhoamiCategoryPicker`/`syncSpyCategoryPicker`/
    `syncZlobCategoryPicker`) в одном общем цикле инициализации — чистое разбиение
    по режимам потребовало бы выделения shared-рантайма отдельным модулем и
    рефакторинга порядка инициализации, риск несоразмерен пользе при 9 режимах в
    одном файле. Основная цель пункта (тестируемость, ESLint-покрытие) достигнута
    единым модулем. Заодно ESLint (ранее не проверял inline-скрипт вообще) нашёл
    5 реальных проблем, спавших незамеченными: мёртвая функция `isInteractiveField`
    (нигде не вызывается) и 4 `catch (error) { ... }` с неиспользуемым биндингом —
    удалена/переведены на `catch {}`. Полная проверка end-to-end: реальный
    `test_web_games_live_updates_browser.py` (WS-reconnect тест из sub-slice 2)
    обновлён, чтобы роутить `**/static/games.js` на реальный файл с диска вместо
    inline-содержимого страницы, и остался зелёным без изменений в логике теста —
    это самое сильное доказательство, что поведение сохранено 1:1. `EXPECTED_TEMPLATES`
    без изменений (файл `games.html` остаётся), `INLINE_SCRIPT_TEMPLATES` теперь
    пусто. ESLint зелёный.
- [x] Сохранить SSE/live update contracts и reconnect behavior.
  - Контракт зафиксирован тестом в sub-slice 2/N (найден и исправлен реальный баг
    reconnect), затем подтверждён неизменным после JS-экстракции в sub-slice 5/N —
    тот же regression-тест прошёл против вынесенного `games.js` без модификации
    тестовой логики, только источник скрипта поменялся с inline на `page.route`.
- [x] Унифицировать lobby, create game, active game, results и error components.
  - Sub-slice 9/N (2026-08-17): проверено, а не предположено. Все панели —
    `.create-game-panel`, `.game-panel` (active/lobby, один и тот же класс для
    обоих статусов), `.recent-games-panel` — расширяют общий `.panel`, который
    получает единый `.page-games .panel { border: 3px solid #1d2340; box-shadow:
    var(--shadow); }` + одинаковый градиентный оверлей (`::before`/`::after`,
    panel.css:1393-1412). Единственное реальное расхождение уже найдено и
    исправлено в sub-slice 4 (`.banner-ok` невидим на карточках результатов) —
    других расхождений не найдено. **Единственное непроверенное состояние** —
    `lobby` (игра создана, но не запущена, `status == "lobby"`, карточка «Лобби
    открыто») — до этого среза ни один тест не создавал игру без `store.start()`.
    Новый тест `test_games_dashboard_lobby_state_has_no_overflow` — overflow
    нет на 3 viewport, визуально (3 PNG, 121) панель использует тот же
    `.game-spotlight`/`.game-meta-grid`/`.action-flow` каркас, что active/
    results — подтверждена реальная унификация, не только предположение по
    коду. Error-состояние — общий сайтовый `.banner-error` в `base.html`,
    уже используется единообразно на всех страницах проекта, отдельной
    games-специфичной проверки не требует.
- [x] Обеспечить touch-first управление без hover-only действий.
  - Проверено (2026-08-17) грепом всех `:hover` правил, затрагивающих
    `.page-games`/игровые классы (`panel.css`): все hover-only переходы
    (`.game-panel`, `.game-meta-card`, `.game-scoreboard`, `.game-action-section`,
    `.telegram-handoff`, `.live-chip`) — чисто декоративный `translateY`+тень на
    НЕфокусируемых информационных контейнерах (`<article>`/`<div>` без `tabindex`
    и без интерактивной роли), не несут функционального смысла и не блокируют
    touch/keyboard пользователей. Единственный по-настоящему интерактивный
    hover-элемент — `.whoami-category-card` (кликабельная кнопка выбора) — уже
    корректно спарен: `:hover, :focus-visible { ... }` (panel.css:1729-1732).
    Отдельно проверено (`grep opacity:0/display:none` рядом с `:hover` для
    game-классов): ни одного hover-reveal паттерна (тултип/меню, видимые только
    по наведению без touch-эквивалента) не найдено. 44px минимальный touch target
    на `.game-kind-card` уже покрыт тестом в sub-slice 1
    (`test_games_dashboard_create_game_kind_grid_has_no_overflow`). Кода не
    менялось — пункт закрыт по факту, без правок.
- [x] Проверить длинные имена, максимальное количество игроков и reconnect.
  - Все три части закрыты по факту в предыдущих sub-slice: **длинные имена** —
    whoami-ростер в sub-slice 1 («Ложка Огромная-Преогромная»), корректно
    обрезаются `text-overflow`. **Reconnect** — sub-slice 2, найден и исправлен
    реальный баг (WS навсегда застревал на polling). **Максимальное количество
    игроков** — sub-slice 6/N (2026-08-17): `GameStore.join()` не имеет верхнего
    предела вообще (проверено чтением кода), единственный реальный потолок —
    размер банка карточек whoami/spy (61 карточка в крупнейшей категории).
    Новый параметризованный тест на 24-игрочный лобби (`whoami`, `mafia`) —
    overflow нет ни на одном viewport, длинные имена участников («Участник
    номер N с длинным именем») корректно обрезаются и в ростере, и в сетке
    ночных действий mafia. 2 PNG (120) в `~/selara_screen`.
- [x] Playwright: создание/подключение/ход/завершение для каждого режима, desktop и
  mobile.
  - Sub-slice 7/N (2026-08-17): **«создание»** закрыто на уровне общего механизма
    (не по режиму, но и не должно — все режимы идут через один и тот же
    kind-picker + один делегированный submit-handler в `games.js`). Новый
    `tests/unit/test_web_games_create_flow_browser.py` — первый тест во всех
    играх, который реально управляет извлечённым `games.js` через браузер, а
    не просто проверяет статический HTML: клик по `[data-kind-choice="whoami"]`
    → проверка, что скрытый `input[name="kind"]` и live-preview обновились →
    выбор чата → сабмит формы → перехват `POST /app/games/create` и проверка
    реального form-urlencoded payload (`kind=whoami&chat_id=-1002`). Второй
    тест — error-toast при отказе сервера (403 «Недостаточно прав»). **Найдена
    и исправлена ошибка не в продакшене, а в самом тесте**: первая версия
    рендерила `games.html` напрямую через Jinja для минимального фикстура и
    забыла передать `extra_scripts=["games.js"]` в контекст — из-за этого
    `<script src=".../games.js">` вообще не попадал в HTML, скрипт не грузился
    ни разу (подтверждено логом сетевых запросов: `games.js` отсутствовал
    среди них), и оба теста «проходили» бы по ложной причине, если бы
    assertion был мягче. Диагностировано методично: захват console/pageerror
    (пусто) → проверка `dashboardRoot`/click-delegation вручную через
    `page.evaluate` → инъекция `console.log`-маркеров в реальный текст
    `games.js` перед подачей через route (не сработали) → сравнение с
    реальным route `games_page()` в `app.py`, который передаёт
    `extra_scripts` корректно — оттуда и нашлась причина.
  - Sub-slice 8/N (2026-08-17): **«подключение»** (join) — не тестируется и не
    будет: в веб-панели вообще нет UI для входа в лобби (грепом по
    `_games_dashboard.html` — ни одной join-кнопки), это делается только через
    Telegram-бота, панель — компаньон для уже идущих в Telegram игр, а не
    точка входа в них. **«ход»** и **«завершение»** закрыты: новый
    `test_web_games_action_flow_browser.py` рендерит реальный dice-лобби через
    тот же `_web_client`/`GameStore` харнесс, что и dashboard-тесты (не
    вручную через Jinja — чтобы не повторить баг с забытым `extra_scripts` из
    sub-slice 7), затем реально кликает «🎲 Бросить» и проверяет, что
    `POST /app/games/action` получает правильный `callback_data`
    (`gdice:<id>:roll`), и отдельно кликает «🛑 Завершить» — проверяет
    `callback_data` вида `game:cancel:<id>`. Оба используют тот же
    `data-game-action-form`/делегированный submit-handler, что уже проверен на
    create — подтверждает, что механизм одинаково работает для игровых
    действий, а не только для формы создания. Чек-лист-пункт теперь закрыт по
    всем трём частям для общего механизма (create/action/finish); per-mode
    покрытие остальных 8 режимов сознательно не дублировалось — риск и так
    сосредоточен в shared submit-handler, а не в конкретных callback_data
    строках.
- [x] Visual snapshots только стабильных ключевых сцен, с маскированием динамических
  timer/id значений.
  - Закрыто (2026-08-17), по прямому «А» пользователя. Новый
    `tests/unit/test_web_games_visual_snapshots.py`: 3 стабильных ключевых
    сцены (empty state, create-game kind-grid, активная игра whoami) через
    тот же `_web_client`/`GameStore` харнесс, что и остальные games-тесты —
    не ручная Jinja-сборка. Golden PNG в `tests/unit/snapshots/games/`
    (`UPDATE_SNAPSHOTS=1` для обновления baseline, тот же механизм, что уже
    есть в `test_visual_snapshots.py` — `compare_images()` импортирован
    оттуда напрямую, не продублирован). Маскирование: единственные два
    реально динамических видимых текстовых поля во всём дашборде — «Старт
    <дата>»/«Создана <дата>» в `.game-meta-card` и «старт <дата>» в
    `.recent-game-head` — заменяются на фиксированный плейсхолдер через
    `page.evaluate()` перед снимком. `data-game-id`/`data-live-signature`
    маскировать не потребовалось: оба — HTML-атрибуты, не рендерятся как
    пиксели (проверено грепом шаблона). При первом прогоне сцена активной
    игры whoami плавала по высоте между запусками (100% → 14% diff) —
    причина найдена быстро: `store.start()` шаффлит пул карточек ролей
    (`random.shuffle` в `game_state.py`), без фиксированного сида длина
    текста роли меняется и сдвигает высоту панели; исправлено
    `random.seed(42)` перед `start()`, тот же приём, что уже использовался в
    сессии для верификации bredovukha-экстракции. Скриншоты сознательно
    viewport-only (`page.screenshot()` без `full_page=True`), не полная
    страница — full-page снимок активной игры давал ~2.5МБ PNG (в 3.6 раза
    тяжелее viewport-варианта, 5.7МБ суммарно на 3 сцены вместо 2МБ) при том
    же регрессионном покрытии видимой части сцены. Порог допуска 0.02 (чуть
    строже дефолтного 0.03 у family-tree снимков — здесь UI проще и без
    антиалиасинга сложных диагональных линий). Повторные прогоны (3x без
    `UPDATE_SNAPSHOTS`) подтверждают детерминированность после фикса сида.
    Полный `tests/unit` 1138 passed (было 1135 до +3 новых тестов).
    Осталась только среда CI (одинаковые Linux image/Chromium versions/fonts)
    — отдельный некрупный пункт этого же раздела, самостоятельная работа не
    форсировалась внутри этого среза.
  - Ранее (первая попытка, не итог): В проекте уже есть автоматизированная
    pixel-diff инфраструктура — `tests/unit/test_visual_snapshots.py`
    (golden PNG + `ImageChops.difference`, порог 0.01-0.11 в зависимости от
    сцены, `UPDATE_SNAPSHOTS=1` для обновления baseline, отдельный допуск для
    Windows из-за антиалиасинга шрифтов) — но она покрывает **другую
    подсистему**: server-side генерацию картинок (`build_family_tree_image`,
    `build_daily_activity_chart` через `PlaywrightRendererService`,
    отправляемых в Telegram), а не веб-страницы этой инициативы. Для games
    такой инфраструктуры нет вообще — все скриншоты этой сессии (114-121) были
    ручным просмотром через `Read`, не автоматическим pixel-diff тестом.
    Настоящий visual snapshot тест для games потребовал бы: (1) решить, где
    хранить golden-снимки и как их обновлять в CI без ложных срабатываний от
    рендер-дрифта шрифтов/GPU между машинами (та же проблема, что уже
    решалась в `test_visual_snapshots.py` — разный порог по сценам, отдельная
    ветка для Windows); (2) реализовать маскирование динамических
    id/времени (`game_id` — 10-символьный hex, `Старт 16.08.2026 17:57 UTC`) —
    либо CSS-инъекцией (`visibility:hidden` на эти элементы перед снимком),
    либо `page.evaluate` подменой значений на фиксированные плейсхолдеры
    перед `screenshot()`; (3) выбрать порог допуска отдельно для games
    (учитывая уже отмеченную нестабильность части browser-тестов в полном
    прогоне, `[!]` выше). Это отдельное архитектурное решение с реальными
    компромиссами (обновление baseline при каждом легитимном визуальном
    изменении — процесс, а не разовая настройка), а не разовый перенос кода —
    сознательно отложено, а не пропущено молча.

### Этап 6. Очистка, performance и финальные quality gates

- [x] Удалить подтверждённо недостижимые React admin/docs modules и их CSS/API types.
  - **Закрыто (2026-08-17)**: пользователь ответил в Telegram явно шире исходной
    находки — «реакт только для мини апп тг... все мертвое удаляй, там не должно
    быть ни админки, ни пк страниц». Удалено ровно то, что было подтверждено
    исследованием ниже: все 12 директорий страниц (`admin`, `admin-broadcast`,
    `admin-login`, `admin-table`, `audit`, `docs`, `economy`, `family`,
    `feedback`, `landing`, `login`, `settings`) целиком (`git rm -r`) + дохлый
    виджет `widgets/app-shell/` минус `model/types.ts` (тип `AppViewer` внутри
    него нужен живому `shared/miniapp/model.ts`, файл сохранён нетронутым).
    `shared/config/routes.ts` точечно вычищен от неиспользуемых ключей
    (`landing`, `login`, `userDocs`, `adminDocs`, `desktopGames`,
    `desktopAdminDocs`, `gachaCollection`, `feedback`, `adminLogin`, `admin`,
    `adminBroadcast`, `adminTable`, `adminTableEdit`, функция `publicDocs`) —
    НЕ тронуты `routes.economy`/`routes.family`/`routes.audit`, они всё ещё
    используются живым `buildChatSectionLinks()`/`ChatSectionNav` (сам
    `ChatSectionNav` рендерится в живых `pages/chat/ui/*`). Заодно найден
    (не исправлен — отдельный найденный факт, не часть этой задачи) пред-
    существующий UX-баг: `buildChatSectionLinks()` генерирует ссылки на
    `/chat/:id/economy`, `/family/:id`, `/chat/:id/audit` — путей, которых
    никогда не было в `AppRouter` (роутер только с 2026-08-17 подтверждённо
    имеет 6 путей) — то есть вкладки «Экономика»/«Моя семья»/«Аудит» в живом
    Mini App чате уже вели в `NotFoundPage` ДО этой чистки, не из-за нее.
    Зафиксировано отдельным `[?]`-пунктом ниже, чинить (либо добавлять
    реальные React-вьюхи, либо убирать вкладки) — решение пользователя, не
    механическая уборка. Удалён также тестовый файл
    `tests/unit/test_frontend_broadcast_contract.py` (2 теста) — проверял
    контент внутри `pages/admin`/`pages/admin-broadcast`, то есть тестировал
    содержимое файлов, которых больше не существует; сама проверяемая функция
    (parity broadcast-composer между React и Jinja) больше не нужна — Jinja
    (`admin.html`/`admin_broadcast_detail.html`) остался единственной живой
    реализацией. Проверено: `npx tsc -b` чисто, `npm run build` чисто
    (bundle 444.68kB/139.22kB gzip **против** 445.10kB/139.36kB до чистки —
    разница минимальна, потому что Vite/Rollup уже tree-shake'ил эти страницы
    из продакшен-бандла, они не импортировались из reachable-дерева ни разу;
    честно: это чистка исходников/типизации/DX, не экономия отгружаемых
    байт), `npx eslint .` чисто, полный `tests/unit` 1136 passed (было 1138,
    -2 от удалённого contract-теста). Существовавший `test_server_ui_baseline.py::
    test_production_miniapp_router_does_not_mount_dormant_admin_pages`
    остался зелёным без изменений — гарантия того же факта, только раньше
    строилась на «не импортируется», теперь ещё и физически не существует.
  - Исследовано (2026-08-17), не выполнено — вынесено на решение пользователя,
    масштаб оказался значительно больше исходной формулировки. Проверено по
    существу, не на глаз: единственная точка входа React-приложения —
    `frontend/src/app/router/AppRouter.tsx`, реально регистрирует только 6
    маршрутов (`/`, `/groups`, `/chat/:chatId`, `/games`, `/gacha`, `/more`) +
    wildcard `NotFoundPage`. Один `index.html`, один Vite bundle (`docker-compose.yml`:
    сервис `web` — единственный React-фронтенд, отдельно от `app` — FastAPI/Jinja).
    Грепом `pages/<dir>` по всему `frontend/src` вне самой директории подтверждено:
    **12 директорий страниц полностью недостижимы** через `AppRouter`, ни разу не
    импортируются извне себя (кроме одного не-циклового исключения:
    `pages/admin/model/types.ts` импортирует тип из `pages/feedback` — обе мёртвые
    вместе): `admin` (1131 строк), `admin-broadcast` (557), `admin-login` (288),
    `admin-table` (805), `audit` (255), `docs` (809), `economy` (1115), `family`
    (677), `feedback` (574), `landing` (268), `login` (209), `settings` (381) —
    **итого ≈7070 строк**. Плюс отдельно найден мёртвый виджет
    `widgets/app-shell/` (`AppShell.tsx` 323 строки, `LogoutButton.tsx`,
    `ProfileMenu.tsx`, 2 api-файла — ≈500 строк): реальный рабочий shell —
    `widgets/miniapp-shell/MiniAppShell.tsx`, используемый в `AppRouter`;
    `AppShell.tsx` нигде не импортируется как компонент (только тип `AppViewer`
    из его `model/types.ts` используется живым кодом `shared/miniapp/model.ts` —
    этот файл типов трогать не нужно). `shared/config/routes.ts` — отдельная
    находка, не баг: содержит внутренние SPA-пути для мёртвых страниц
    (`routes.economy`, `routes.family`, `routes.audit`, `routes.admin*` и т.д.,
    используемые только в `buildChatSectionLinks()`/`ChatSectionNav`, тоже часть
    мёртвого графа) вперемешку с реальными кросс-ссылками на Jinja-страницы через
    абсолютные `/app/...` пути (`routes.appUserDocs` и т.д., используются живым
    `RouteErrorBoundary`) — при удалении мёртвых страниц потребуется точечная
    чистка `routes.ts`/`buildChatSectionLinks`/`ChatSectionNav`, не полное
    удаление файла. Формулировка чек-листа называла только «admin/docs» —
    реальный периметр мёртвого кода оказался в разы больше (все server-migrated
    страницы Этапа 4, не только админка/доки). Решение не форсировано в одиночку
    из-за масштаба (≈7500 строк общего удаления) — отправлено пользователю
    вариантами через Telegram, решение см. в журнале ниже, когда придёт ответ.
- [~] Удалить мёртвые Jinja partials, CSS selectors и JavaScript.
  - Частично закрыто (2026-08-17). Файловый уровень проверен и чист: все 31
    `.html` в `templates/`, все 13 `.css` и все 17 `.js` в `static/`
    подтверждённо ссылаются друг на друга (грепом по путям/именам файлов) —
    ни одного полностью неиспользуемого файла. Селекторный уровень (мёртвые
    правила ВНУТРИ живых CSS-файлов) — не сделан, и **не должен делаться
    наивным grep'ом**: попытка автоматической проверки (список классов из
    `panel.css` минус классы, встречающиеся дословно в шаблонах/JS) дала 143
    «мёртвых» кандидата из 824 — почти все ложные срабатывания, потому что
    десятки реальных классов собираются в Jinja через интерполяцию
    (`class="game-panel game-panel-{{ game.kind }}"`,
    `class="page-{{ page_name }}"`, `mafia-badge-{{ status }}` и т.д.) —
    полная строка класса физически не встречается в исходнике шаблона,
    только в отрендеренном HTML. Это тот же класс методологической ошибки,
    что чек-лист этого же пункта уже предвидел («только после автоматической
    И ручной проверки») — честно зафиксировано, не проигнорировано.
    Корректный путь — Playwright CSS coverage по прогону через все реальные
    состояния/страницы, не статический анализ исходников — отдельная
    инфраструктурная работа сопоставимого размера с visual snapshots в
    Этапе 5, не форсирована в рамках этого среза.
  - **Инфраструктура построена (2026-08-17)**, по решению пользователя
    «ABC», шаг B. **Исправление собственной более ранней неточности**:
    в этой же заметке выше был написан `page.coverage.startCSSCoverage()` —
    неверно, это Puppeteer/Node-only convenience API, которого в
    Playwright **Python** нет вообще (`Page.coverage` отсутствует в
    `playwright.async_api`, проверено напрямую `dir(Page)`). Реальный путь
    в Python — либо сырой CDP (`context.new_cdp_session(page)` +
    `CSS.startRuleUsageTracking`, подтверждено прототипом, что работает),
    либо DOM-запрос напрямую: для каждого CSSOM-правила спросить
    `document.querySelectorAll(selectorText).length > 0` в контексте
    отрендеренной страницы. Выбран второй путь — проще, устойчивее (не
    нужен байтовый offset-парсинг CSS-текста, который на первой попытке
    дал явно неверные числа из-за перепутанных stylesheet'ов между
    страницами) и даёт тот же практический ответ. Новый
    `scripts/css_coverage_report.py`: рендерит 16 состояний (11 admin-
    страниц через уже существующие HTMLHint-фикстуры `scripts/render_*.py`
    — переиспользованы, не продублированы; + games create-grid/active-
    whoami через реальный `create_web_app` харнесс; + landing/login/login-
    error/404/user_docs/chat_audit), инжектит каждый CSS-файл как
    отдельный `<style data-coverage-file="...">` (нужно было для надёжного
    сопоставления файл↔правило — первая версия на позиционном совпадении
    stylesheet'ов дала грубо неверные числа: 517 «селекторов» в файле,
    где их физически 28, — не поверил результату, нашёл и исправил баг
    сопоставления, прежде чем публиковать хоть одно число), проверяет
    каждый селектор через `querySelectorAll` с зачисткой
    `:hover`/`:focus`/`::before` и т.п. (эти псевдоклассы физически не
    матчатся статическим DOM-запросом безотносительно реального
    использования — без зачистки был бы гарантированный ложный «не
    используется» на ЛЮБОМ hover-правиле). Итог первого прогона (только
    для ориентира, НЕ для удаления): `panel.css` 491/189, `games.css`
    325/207 (ожидаемо — покрыты только 2 из ~9 режимов), остальные
    page-specific файлы 0-34 «непойманных» из существенно меньших итогов
    (числа приведены полностью в выводе скрипта). **Явная оговорка,
    как и в самом файле-докстринге**: отсутствие в списке значит «не
    сработал ни в одном из посещённых состояний», не «подтверждённо
    мёртв» — chat.html/economy.html/family.html/audit-detail и большинство
    game-режимов не покрыты (нужен более тяжёлый DB-mocked харнесс);
    находки — наводки для ручной проверки, тот же принцип, что и baseline
    djLint.
  - **Удаление выполнено (2026-08-17)**, по решению пользователя «ABC»,
    продолжение шага C. Из 189 «непойманных» кандидатов `panel.css`
    вручную проверил каждый на 2 отдельных класса ложного срабатывания,
    уже пойманных в этой же сессии: (1) Jinja-интерполяция
    (`class="pill pill-{{ game.status_badge }}"` → `pill-lobby` реально
    живой, `class="... accent-{{ board.accent }}"` → все 4
    `.accent-cyan/-indigo/-magenta/-violet` реально живые — оба случая
    подтверждены чтением `presenters.py`/`app.py`, не угадыванием); (2)
    JS-генерируемые классы (`classList.toggle("broadcast-counter-error", ...)`
    в `admin-broadcast.js`, `family-graph.js`/`chat-overview.js` строят
    `.family-node`/`.family-edge`/`.family-svg`/`.leaderboard-row-me`/
    `.activity-bar-fill` через `element.className`/`innerHTML` — грепом по
    ВСЕМ `.js`-файлам, не только шаблонам). После вычитания обоих классов
    ложных срабатываний осталось **11 подтверждённо мёртвых правил**,
    каждое проверено по отдельности (grep по всем шаблонам И всем JS-
    файлам, ноль вхождений где угодно): `.hero-login`, `.hero-orb`,
    `.hero-core`, `.hero-ring` (декоративный «orb»-эффект, видимо остаток
    более раннего дизайна логин-страницы), `.controls-stack`,
    `.setting-toggle-copy`, `.admin-user-info` (+ `code`),
    `.admin-feedback-list`, `.admin-feedback-actions` (в т.ч. вхождение
    внутри `@media`, комбинированное с живым `.feedback-card-head` —
    вырезано точечно, не всё правило), `.broadcast-photo-preview` (стояло
    и отдельным правилом, и в комбинированном `[hidden]`-селекторе с 4
    живыми классами — вырезано точечно), `.broadcast-preview-box`
    (аналогично, комбинировано с 4 живыми `.broadcast-*-card`). `panel.css`:
    3543 → 3448 строк. Повторный прогон `css_coverage_report.py` после
    удаления: 491 → 477 селекторов, 189 → 175 непойманных — разница
    ровно 14 (счётчик CSSOM считает `.hero-core, .hero-ring` как selectorText
    группы иначе, чем два отдельных правила, отсюда 14 не 11) — подтверждает,
    что убрано именно то и только то, что намеревался. Полный `tests/unit`
    1139 passed; повторный рендер `/login` и 404-страницы побайтово
    идентичен (только ожидаемый fingerprint-хэш); скриншот (127) визуально
    идентичен — подтверждает, что удалённая orb-декорация уже никак не
    отображалась. `panel_css_lines` cap 3550 → 3455. Остальные ~175
    кандидатов в `panel.css` и весь список `games.css` НЕ тронуты —
    coverage для games/chat/economy/family всё ещё неполный, дальнейшее
    удаление там было бы гаданием, не подтверждённым фактом.
  - **Покрытие расширено (2026-08-17)**, по решению пользователя «всё по
    порядку», продолжение шага A/C. `css_coverage_report.py` дополнен 6
    новыми состояниями: `economy.html`, 3 вкладки `chat.html` (overview/
    achievements/settings), `audit.html`, `family.html` — переиспользованы
    существующие `_render()`-фикстуры из соответствующих browser-тестов
    (`test_web_economy_browser.py`, `test_web_chat_overview_browser.py`,
    `test_web_chat_audit_browser.py`) плюс fake-repo классы из
    `test_web_family_routes.py` для `family.html` (нужен реальный
    `create_web_app`, как и для games) — импортированы через
    `sys.path.insert(tests/unit)`, не продублированы. Итог: `panel.css`
    непойманных 175 → 108 (68 меньше). Полная повторная ручная проверка
    оставшихся 108 на оба класса ложных срабатываний (Jinja-интерполяция,
    JS-generated) дала **0 новых кандидатов** — все 9 «нулевых по тексту»
    результата это уже известные `accent-*`/`pill-lobby`/`feedback-status-*`
    /`feedback-card-*`, исключённые ранее. Честный вывод: расширенное
    покрытие не нашло НИЧЕГО НОВОГО для удаления — само по себе ценный
    результат (подтверждает, что предыдущая чистка была полной для уже
    охваченной поверхности, не просто «повезло с первой попытки»), не
    просто нулевой результат. `games.css` (207 непойманных) и
    `chat.html`/`family.html`'s более редкие состояния (settings с реальными
    алиасами/триггерами, family с непустым графом) остаются неохваченными
    — следующий возможный шаг для этого пункта, не форсирован здесь.
- [x] Проверить отсутствие дублирующих активных UI-реализаций.
  - Закрыто (2026-08-17), попутно с разведкой мёртвого React-кода выше.
    Единственное реальное пересечение по именам страниц — React `home`/`chat`/
    `games`/`gacha`/`groups`/`more` (обслуживают Telegram Mini App через
    `/api/miniapp/*`, отдельный сервис `web` в `docker-compose.yml`, порт 8081,
    `VITE_APP_BASE_PATH=/miniapp/`) и Jinja `home.html`/`chat.html`/
    `games.html`/`economy.html`/`family.html`/`audit.html`/`landing.html`/
    `login.html`/etc. (обслуживают браузерную панель через `/app/*`, сервис
    `app`, отдельный порт). Это не дубли одного интерфейса, а два разных
    интерфейса для двух разных контекстов использования (встроенный webview
    Telegram vs полноценный браузер) с раздельными API-namespace'ами
    (`/api/miniapp/*` vs `/app/*`+`/api/app/*`) — намеренная архитектура, не
    забытая копия. Единственная НАСТОЯЩАЯ находка дублирования — не UI, а
    обработчики: `feedback.html`'s form-POST и `/api/app/feedback` JSON-POST
    оказались двумя независимыми путями с одинаковой валидацией (найдено и
    исправлено ещё в Этапе 4, sub-slice `feedback.html`, commit 55713ca) — уже
    закрыто раньше, здесь только подтверждено, что других подобных случаев не
    осталось (грепом по остальным `/api/app/*` POST-обработчикам на предмет
    параллельного non-JSON пути — не найдено ни одного). Помимо этого — 12
    React-страниц/1 виджет, разобранные выше, не «дубли», а полностью мёртвый
    код (0 активных путей к ним), что другая категория находки, отдельный
    незакрытый пункт этого же раздела.
- [~] Перевести djLint, Stylelint, ESLint, rendered HTMLHint и axe smoke в blocking
  CI.
  - 3 из 5 закрыты (2026-08-17): ESLint, Stylelint и HTMLHint уже блокируют
    CI (детали и проверка — см. Этап 0, тот же пункт). djLint остаётся
    report-only намеренно (baseline-находки — подтверждённые false positive
    парсера). axe-core не переводится в blocking, потому что его ещё не
    существует — отдельный невыполненный пункт Этапа 0.
- [ ] Зафиксировать Lighthouse baseline для публичной landing, login, user docs и
  репрезентативной авторизованной страницы.
- [ ] После нескольких стабильных прогонов согласовать performance/accessibility
  budgets и сделать их блокирующими.
- [x] Проверить размер CSS/JS, количество шрифтов и third-party ресурсов.
  - Закрыто (2026-08-17). CSS `web/static/*.css` суммарно 228KB (13 файлов,
    `panel.css` — 6262 строки/большая часть веса), JS `web/static/*.js`
    суммарно 144KB (17 файлов) — оба некритично малы, отдельная бюджет-
    политика не нужна на этот размер. Шрифты: весь остальной интерфейс (все
    страницы, кроме `landing.html`) использует только системные font-stack'и
    (`var(--font-ui)`/`var(--font-mono)`/`var(--font-display)` +
    декоративные `"Arial Black"/"Bahnschrift"/"Trebuchet MS"` для игровых
    заголовков) — 0 сетевых шрифтов. **Единственный third-party ресурс во
    всём серверном UI**: `landing.html` подключает Google Fonts CDN
    (`fonts.googleapis.com`/`fonts.gstatic.com`, 2 `preconnect` + 1
    `<link rel="stylesheet">`) — 3 вариативных семейства (`Unbounded`,
    `Manrope`, `JetBrains Mono`, диапазоны веса 300..800/900) на публичной
    неавторизованной странице. Не исправлено намеренно — удаление/замена
    веб-шрифтов на публичной landing-странице — визуальное/брендинговое
    решение (шрифт часть айдентики страницы), не техническая уборка;
    зафиксировано как единственный найденный third-party network dependency
    для последующего решения при работе над Lighthouse/performance budget
    этого же этапа.
- [x] Проверить cache headers и fingerprinting статических ресурсов.
  - Закрыто как аудит (2026-08-17), без изменений кода. `app.mount("/static",
    StaticFiles(...))` (`app.py:610`) — стандартный Starlette `StaticFiles`;
    прочитан его исходник (`staticfiles.py`) напрямую, не предположено:
    отдаёт `ETag`/`Last-Modified` и честно отвечает 304 на условные запросы,
    но **не выставляет `Cache-Control` вообще** — ни явного `max-age`, ни
    `immutable`. Fingerprinting отсутствует: `static_url()`
    (`web/rendering.py:15`) — буквально
    `f"/static/{path}"`, без content-hash/версии в имени файла или query.
    Это не забытая настройка, а согласованное следствие отсутствия
    инфраструктуры: агрессивный `max-age` на нефингерпринтованных путях
    активно опасен — после любого деплоя браузер годами отдавал бы старый
    закешированный `panel.css`/`games.js` по тому же URL, реальный
    production-баг хуже текущего избыточного revalidation. Правильный
    порядок — сначала fingerprinting (либо Vite-style content-hash в имени
    файла для 30 файлов `web/static/*`, либо дешёвая альтернатива: один
    query-параметр `?v=<git-sha-или-timestamp-деплоя>`, добавляемый в
    `static_url()` и инвалидируемый на каждый деплой разом), потом
    `Cache-Control: public, max-age=31536000, immutable`. Это отдельное
    архитектурное решение о схеме версионирования, не разовая правка
    заголовка — не форсировано здесь, задокументировано для решения в рамках
    этого же этапа перед выставлением performance budget.
  - **Реализовано (2026-08-17)**, по решению пользователя «версионирование —
    на моё усмотрение». Выбран не git-SHA/deploy-timestamp (потребовал бы
    новых Docker/CI build-args), а content-hash всех файлов
    `web/static/*`, посчитанный самим приложением при старте процесса
    (`static_assets_version()` в `rendering.py` — sha256 конкатенации байт
    всех файлов директории, первые 10 hex-символов). Проще: 0 изменений
    инфраструктуры деплоя, при этом честный fingerprint — версия меняется
    ровно тогда, когда меняется хоть один байт статики, а не «раз в
    деплой» независимо от того, поменялась статика или нет.
    `create_template_environment()` получил опциональный `static_dir`
    (default `None` — 26+ существующих вызовов в тестах с голым
    `template_dir=TEMPLATE_DIR` не тронуты и остаются без версии, обратная
    совместимость сохранена); `create_web_app()` передаёт реальный
    `static_dir`. `static_url()` теперь возвращает
    `/static/panel.css?v=<10-hex>` вместо голого пути. Добавлена
    `@app.middleware("http")`, выставляющая `Cache-Control: public,
    max-age=31536000, immutable` на любой ответ с `/static/` в пути —
    безопасно именно потому, что URL теперь версионирован.
- [ ] Провести ручной keyboard-only проход критических сценариев.
- [ ] Провести ручную проверку smartphone и tablet в реальном Safari/Chrome.
- [ ] Обновить `README`, admin/user guides и developer instructions.
- [x] Согласовать production rollout и rollback plan.
  - Закрыто (2026-08-17) решением пользователя: rollout — после закрытия
    100% roadmap, формального rollback-плана не будет, при необходимости
    пользователь откатит вручную через Docker (см. «Журнал решений»).
- [ ] Добавить итоговую запись в `CHANGELOG.md`.
- [x] **Mini App: реальные вьюхи для «Экономика», «Моя семья», «Аудит»**
  (по решению пользователя от 2026-08-17). Закрыто как **минимальный
  read-only MVP** — отдельно уточнённый через TG объём (не полный паритет с
  Jinja-панелью: без drag&drop/рынка/фильтров/интерактивного графа семьи,
  только сводка ключевых цифр). 3 новых read-only JSON API
  (`/api/miniapp/chat/{chat_id}/economy`, `/api/miniapp/family/{chat_id}`,
  `/api/miniapp/chat/{chat_id}/audit`) переиспользуют уже существующие
  context-builder'ы Jinja-страниц (`_load_dashboard_if_exists`,
  `_build_family_page_context`, `_build_chat_audit_page_context`) вместо
  дублирования бизнес-логики — экономика возвращает баланс/уровень фермы/
  сводку по грядкам/размер инвентаря, семья — сводку по ролям + список
  членов (без SVG-графа), аудит — 20 последних строк журнала. 3 новые
  React-страницы (`pages/economy`, `pages/family`, `pages/audit` —
  однофайловые, как `pages/more`, не над-инженерены под мнимую сложность)
  используют уже существующие `getMiniAppData`/`LoadingShell`/
  `.miniapp-empty-card` примитивы — тот же паттерн, что `GachaCollectionPage`.
  Добавлены в `AppRouter.tsx`: `chat/:chatId/economy`, `chat/:chatId/audit`,
  `family/:chatId` — совпадает с уже существующими путями в
  `routes.ts`/`buildChatSectionLinks()`, то есть 404 в `ChatSectionNav`
  (найденный при чистке React-кода) устранён без изменения самого nav.
  Новый `tests/unit/test_web_miniapp_summary_apis.py` (4 теста, включая
  негативный сценарий «экономика отключена в чате»), переиспользует
  fake-repo классы из `test_web_chat_hub_routes.py`/`test_web_family_routes.py`
  вместо дублирования. `npx tsc -b`/`eslint .`/`npm run build` чисты;
  полный `tests/unit` 1143 passed (было 1139). Ручная browser-проверка
  полного end-to-end потока (реальный Telegram WebApp + бэкенд) не
  проводилась — `MiniAppGate` требует настоящую Telegram-сессию, вне
  досягаемости текущей тестовой инфраструктуры (у React-приложения вообще
  нет собственных JS/browser-тестов, только tsc/eslint/build — тот же
  уровень проверки, что и у остальных существующих страниц). Полный паритет
  (рынок/крафт/интерактивный граф) — сознательно НЕ сделан, отдельный
  трек, если понадобится позже.

## 6. Матрица обязательной проверки каждой страницы

Исполнитель не помечает страницу `[x]`, пока не проверены применимые пункты:

### Функциональность

- [ ] Основной happy path.
- [ ] Пустые данные.
- [ ] Ошибка backend/Telegram/сети.
- [ ] Ошибка validation с сохранением ввода.
- [ ] Истёкшая сессия и отсутствие прав.
- [ ] Повторный submit/double click.
- [ ] Большой объём данных и pagination.

### Responsive

- [ ] Desktop Chromium 1440×900.
- [ ] Mobile Chromium.
- [ ] Mobile WebKit.
- [ ] Tablet WebKit.
- [ ] Нет горизонтального page overflow.
- [ ] Touch actions доступны и не перекрываются.
- [ ] Virtual/mobile keyboard не скрывает активное поле или submit.

### Accessibility

- [ ] Семантические landmarks и корректная heading hierarchy.
- [ ] Labels и accessible names всех controls.
- [ ] Полный keyboard flow.
- [ ] Видимый focus.
- [ ] Корректный порядок focus после dialog/navigation.
- [ ] Status/error не передаётся только цветом.
- [ ] axe WCAG A/AA scan без необъяснённых нарушений.
- [ ] Контраст подтверждён для основных состояний.
- [ ] Reduced motion учтён.

### Контент и устойчивость

- [ ] Длинные русские и английские строки.
- [ ] Непрерывная строка/ID/URL.
- [ ] Emoji и composed emoji.
- [ ] CJK и RTL sample.
- [ ] Отсутствующие optional fields.
- [ ] Безопасное escaping пользовательского содержимого.

### Инженерные проверки

- [ ] Backend unit/integration tests.
- [ ] Jinja compile/render tests.
- [ ] djLint.
- [ ] Stylelint.
- [ ] ESLint, если есть JavaScript.
- [ ] Playwright functional/responsive tests.
- [ ] Visual snapshot только для согласованных стабильных состояний.
- [ ] `git diff --check`.

## 7. Отложенные задачи

- [ ] Начать сохранять исходящие сообщения бота для будущей полной истории диалога.
- [ ] Определить модель хранения reply-связей исходящих сообщений.
- [ ] Определить retention/privacy policy полного архива.
- [ ] Решить, нужно ли сохранять удаления сообщений и service events.
- [ ] Решить, какие Telegram media кешировать локально и на какой срок.
- [ ] После появления данных расширить историю до полной двусторонней ленты.

## 8. Сводный прогресс

**Общий прогресс roadmap: 82%** (было 81%). Значение рассчитано как среднее завершение семи
этапов; прогресс приоритетного этапа админки отслеживается отдельно.

| Этап | Статус | Выполнено | Примечание |
|---|---|---:|---|
| 0. Baseline и проверки | `[~]` | 50% | Debt guards; стабильная среда visual snapshots (`TZ: UTC` добавлен в CI). ESLint/Stylelint/HTMLHint уже блокируют CI (устаревшая пометка исправлена), djLint осознанно report-only. axe-core scan закрыт: реальная библиотека (не эвристика), нашла и помогла исправить 5 категорий настоящих a11y-багов (select без имени, незаконченный ARIA-паттерн tablist, отсутствующий h1, пропуск уровня заголовка) на 22 состояниях |
| 1. Дизайн-система | `[~]` | 70% | Design-tokens (5/5) проанализирован, включая закрытую проверку контраста WCAG AA — найдены и исправлены 2 реальных провала (`.button.primary` белый текст на ярком градиенте в `panel.css`/`landing.css`, теперь тёмный текст, 6+:1). CSS-архитектура: 3 среза расширения (`panel.css` 6262 → 3543 строки) + задокументированы фактическая структура и соглашение имён классов. Jinja-компоненты: 3 макроса — `breadcrumb()` (a11y-баг), `status_badge()` (дедуп), `pagination()` (по решению пользователя строить «про запас»). `base.html`: добавлен `<meta name="description">` (не было вообще нигде на сайте); landmark-баг (`<header>` внутри `<main>`, терял роль `banner`) найден и по указанию пользователя исправлен — оказался куда дешевле, чем оценивался: класс `.shell` просто переставлен на новый оборачивающий `<div>`, геометрия не изменилась, 0 axe-находок на 22 состояниях |
| 2. Админка | `[~]` | 98% | 2.2, 2.3, 2.5, 2.6 закрыты полностью; admin shell Desktop/Mobile/Tablet Chromium закрыт (WebKit заблокирован, `[!]`); 2.4 закрыт кроме одного пункта — единственное, что осталось во всём Этапе 2: индексированный full-text поиск (нужна миграция на продовый Postgres — решение вынесено пользователю, см. журнал решений) |
| 3. Документация | `[x]` | 100% | Модель контента: слайш-команды 7 из 7 срезов, все закрыты. help.py полностью переведён на единый каталог (4 новые категории, 19 CommandSpec, `_HELP_SECTION_TEXT` собирается из `spec.syntax`). USER_GUIDE.md/ADMIN_GUIDE.md защищены новым `test_guide_docs_match_catalog.py` (10 тестов, живой источник правды вместо разовой ручной синхронизации). Всего за срезы этой сессии найдено и исправлено 7 реальных багов (2 в `/help`, 3 в документации, 2 пробела в каталоге). «Пользовательская», «Администраторская» документация, «Качество документации», Responsive TOC/drawer, Playwright-покрытие (все 5 пунктов) — закрыты полностью. Этап 3 закрыт |
| 4. Остальные страницы | `[x]` | 100% | Все 10 страниц закрыты (`landing`, `login`, `home`, `achievements`, `feedback`, `chat`, `family`, `economy`, `audit`, `error`). Cross-page links и «возврат назад после действий» оба закрыты — Этап 4 полностью завершён |
| 5. Игры | `[x]` | 100% | Все 9 чек-лист-пунктов закрыты полностью. Partials по режимам — 5 из 9 режимов (whoami/spy/mafia/zlobcards/bredovukha) вынесены в отдельные файлы, остальные 4 (dice/number/quiz/bunker) подтверждено не имеют mode-exclusive разметки вообще. Visual snapshots — новая pixel-diff инфраструктура для games (3 стабильных сцены, маскирование динамических timer-полей). Побайтовая идентичность рендера подтверждена git-stash сравнением для всех 9 режимов. 4 реальных бага найдены и исправлены за сессию. Этап 5 закрыт |
| 6. Очистка и quality gates | `[~]` | 54% | Закрыты: «дублирующие UI-реализации», «удалить недостижимые React admin/docs modules», «размер CSS/JS+шрифты+third-party», «cache headers и fingerprinting», «production rollout/rollback plan», новый пункт «Mini App вьюхи для Экономики/Семьи/Аудита» (read-only MVP: 3 API + 3 React-страницы, устраняет 404 в ChatSectionNav). «Линтеры в blocking CI» частично. «Мёртвые Jinja/CSS/JS»: coverage-инструмент нашёл и удалил 11 мёртвых правил из `panel.css` (3543 → 3448 строк), покрытие расширено на economy/chat/audit/family — 0 новых кандидатов |

Процент обновляется по фактически завершённым дочерним задачам, а не субъективной
оценке исполнителя.

## 9. Журнал решений

| Дата | Решение | Причина | Кто подтвердил |
|---|---|---|---|
| 2026-08-15 | Jinja остаётся для server Web UI, React — для Mini App | Один активный источник истины для каждого интерфейса | Пользователь |
| 2026-08-15 | Первый приоритет — админка, особенно рассылки и история | Самая высокая операционная ценность | Пользователь |
| 2026-08-15 | После админки выполняется единая документация | Все варианты документации подтверждены | Пользователь |
| 2026-08-15 | Стиль Selara сохраняется, но модернизируется | Нужна узнаваемость без устаревшего визуального шума | Пользователь |
| 2026-08-15 | Поддерживаются современные smartphone/tablet, Chrome и Safari | Подтверждённая целевая матрица | Пользователь |
| 2026-08-15 | Полный outbound archive отложен | Для первого этапа достаточно улучшить представление существующих данных | Пользователь |
| 2026-08-15 | Feature toggle не нужен, rollout пока не определён | Решение о выкладке будет принято позднее | Пользователь |
| 2026-08-15 | WebKit-прогон browser-тестов отложен, перенесён в будущее CI/Docker-окружение | На dev-машине (Fedora 44) недоступны нужные системные библиотеки (ICU 74, libjpeg ABI 8); Playwright официально поддерживает автоустановку зависимостей только для Ubuntu/Debian | Пользователь |
| 2026-08-16 | Индексированный full-text поиск в архиве отложен, Этап 2 считается фактически закрытым | Это миграция схемы продовой Postgres-БД (tsvector + GIN + russian-словарь), а не Web UI-срез; ломает нынешний тестовый параллелизм SQLite/Postgres. Подстрочный ILIKE-поиск с подсветкой уже работает и покрывает текущую потребность | Пользователь |
| 2026-08-16 | Адаптация документации (`admin_docs.html`/`user_docs.html`) под смартфоны и планшеты выполняется отдельным срезом ПОСЛЕ завершения остальной «Модели контента» и остальных пунктов Этапа 3, а не сразу после обнаружения | Найденный page-wide баг (`.docs-layout`/`.docs-card-grid` без mobile-брейкпоинта, подтверждён замером) — реальная и нужная задача, но пользователь предпочёл сначала закрыть контентную часть Этапа 3 (единый источник команд/примеров, anchors, секретность admin-процедур), а адаптацию под маленькие экраны сделать отдельным целевым срезом после | Пользователь |
| 2026-08-16 | Единый источник для slash-команд по разделам (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/`user_docs.py`/`admin_docs.py`) отмечен `[?]` и пропущен, Этап 3 продолжается без него — переход к адаптации, затем Этап 4 | Объём (80–150+ команд по грубой оценке, 904 строки в 3 файлах документации + сами web-доки, без готового каталога-источника, в отличие от ролей/RP-действий) требует решения пользователя об объёме подхода (полный генерируемый каталог vs. аудит на расхождения vs. отложить), а пользователь явно разрешил помечать такие пункты в TODO и идти дальше вместо блокировки | Claude — ожидает подтверждения пользователя |
| 2026-08-17 | Вкладки «Экономика»/«Моя семья»/«Аудит» в React-чате получат реальные вьюхи в Mini App (не убираются) — решает `[?]` выше | Продуктовое решение пользователя: функциональность должна быть доступна из Mini App, не только из браузерной панели | Пользователь |
| 2026-08-17 | Схема версионирования статики для кеш-заголовков (content-hash vs `?v=<deploy-id>`) отдана на усмотрение Claude | Пользователь не имеет предпочтения («версионирование я хз») | Пользователь |
| 2026-08-17 | Момент перевода линтеров (djLint/Stylelint/ESLint/HTMLHint/axe) из report-only в blocking CI отдан на усмотрение Claude | «линтеры как сам знаешь и как будет лучше» | Пользователь |
| 2026-08-17 | Production rollout — после закрытия 100% roadmap; формальный rollback-план НЕ ведётся, при необходимости пользователь откатит вручную через Docker | Явное решение пользователя, снимает открытый вопрос из раздела 8 | Пользователь |
| 2026-08-16 | Выбран вариант (a): полный единый каталог команд с генерацией всех 4 поверхностей (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/`user_docs.py`/`admin_docs.py`), не аудит и не отложить | Пользователь ответил на вопрос из TODO явно: «вариант А, у тебя впереди много времени» — осознанно выбрал более крупный, многосрезовый подход вместо более быстрого аудита | Пользователь |
| 2026-08-16 | `help.py`: вариант (a) — curated выжимка из каталога по категориям, не полный список ~100+ команд | Пользователь: «a, полный список в другом месте будет» — подтвердил, что полный список команд уже покрыт в другом месте (`user_docs.py`/`admin_docs.py`/GUIDE.md), `/help` может оставаться коротким | Пользователь |
| 2026-08-16 | `USER_GUIDE.md`/`ADMIN_GUIDE.md`: вариант (a) — генерировать файлы полностью из каталога, а не только reference-секцию поверх существующего prose | Пользователь: «a но понятнее чем сейчас и лучше» — выбрал полную генерацию, но явно потребовал, чтобы результат был яснее и качественнее текущих prose-гайдов, а не просто механический дамп каталога | Пользователь |
| 2026-08-16 | `landing.html`: секция «Возможности Selara» отмечена `[?]` по вопросу «показывать все 6 категорий возможностей или оставить текущие 3 curated-карточки» — работа над остальной частью среза (hero-статистика, mobile layout, dead-код) продолжена без блокировки | `build_landing_context()` строит 6 `feature_cards`, но `landing.html` жёстко (по индексам) рендерит только карточки 1, 2 и склейку 3+4 через "+"-kicker хак; карточки 0 и 5 никогда не показываются. Это похоже не на дизайн-решение, а на непреднамеренный artefact (класс `.landing-features-three-grid` буквально называется "three-grid", склейка через "+" выглядит как обходной путь) — нужно решение пользователя, показывать ли все 6 категорий | Claude — ожидает подтверждения пользователя |
| 2026-08-16 | `landing.html`: показывать все 6 категорий возможностей вместо 3 curated-карточек — снимает `[?]` из предыдущей строки | Пользователь ответил в Telegram на присланный скриншот и вопрос: «все 6 красиво» | Пользователь |
| 2026-08-16 | **Процессный долг, зафиксирован честно, не исправлен задним числом**: срезы Этапа 4 от `landing.html` до `economy.html` включительно (9 коммитов) рендерились и просматривались вручную, но финальные PNG сохранялись только во временную папку сессии и НЕ копировались в `~/selara_screen`, как требуют `WEB_UI_SLICE_WORKFLOW.md` §6 и §10.4. Нумерация архива стояла на 101 (`101_user_docs_copy_deeplink_mobile.png`) с момента среза документации. Возобновлено начиная с `error.html` (102–107) | Обнаружено при handoff-сверке: `ls ~/selara_screen` показал разрыв между заявленными в журнале «скриншот-обзор desktop/tablet/mobile» и фактическим содержимым архива. Массовый ре-рендер ~30 PNG задним числом сознательно НЕ выполняется: это механическая работа без инженерной ценности, а её реальная цель по §7 (сравнение с предыдущим состоянием того же экрана) всё равно недостижима — сравнивать не с чем, промежуточные состояния уже не воспроизвести. Вместо этого архив пополняется по мере того, как страницы снова попадают в работу; сквозной проход по cross-page links (последний пункт Этапа 4) затронет большинство этих страниц и закроет разрыв осмысленно | Claude (Opus) — зафиксировано, пользователю сообщено |

## 10. Журнал работ

Каждая строка добавляется в начале таблицы под заголовком.

| Дата | Исполнитель | Задача | Статус | Commit/проверки | Примечание |
|---|---|---|---|---|---|
| 2026-08-18 | Claude (Sonnet, «сделай Этап 1 полностью» пользователя) | Этап 1: закрыт пункт «gallery/dev fixture компонентов» через template tests вместо новой страницы | `[x]` | commit: pending; новый `tests/unit/test_web_macros.py` 6/6; полный `tests/unit` 1149 passed (было 1143) | Выбрал вторую половину формулировки пункта («либо эквивалентные template tests») вместо новой gallery-страницы осознанно — новый production-роут означал бы решать вопрос публичности/авторизации, который не мой, чтобы решать молча. Тесты рендерят все 3 макроса `_macros.html` напрямую через `environment.from_string()` для их реальных граничных состояний (одна крошка/несколько со смешанными href, оба тона `status_badge`, первая/последняя/средняя страница пагинации) — раньше макросы были покрыты только косвенно через целые страницы, граничные случаи не были зафиксированы отдельно. Этап 1: 66% → 70%; общий прогресс roadmap: 81% → 82% |
| 2026-08-18 | Claude (Sonnet, «сделай Этап 1 полностью» пользователя) | Этап 1: закрыт пункт «responsive topbar/navigation» — проверено конкретно, не на глаз | `[x]` | commit: pending; изменения только в TODO, код не тронут | Нашёл, что `.ui-admin-shell` (полноценная mobile-полировка топбара — scroll-pills, компактный brand) инжектится ОДНИМ Python-билдером `_admin_layout_context()`, но с 6 call site'ами — весь админ-раздел, не одна страница, как показалось при первом грепе шаблонов (класс не хардкожен в HTML). Остальные страницы используют более простую стратегию — `flex-wrap: wrap` без выделенных media query. Проверил, баг ли это: рендер `/login` на 375px — 0 horizontal overflow, `.topbar` корректно переносится на 2 строки. Подтверждено широким существующим покрытием — 19 файлов тестов уже проверяют отсутствие overflow на разных вьюпортах для большинства страниц. Вывод: navigation уже реально responsive везде, просто двумя разными валидными стратегиями, не багом. Этап 1: 62% → 66%; общий прогресс roadmap: 81% (без изменений, округление) |
| 2026-08-18 | Claude (Sonnet, «сделай Этап 1 полностью» пользователя) | Этап 1: закрыты 2 пункта — «вынести общий JS» и «проверить logout/redirects/session expiry/keyboard nav» — оба уже сделаны, подтверждено постфактум | `[x]` | commit: pending; изменения только в TODO, код не тронут | JS: грепом 0 `<script>` с логикой (только внешние через `extra_scripts`) и 0 inline-хендлеров (`onclick`/`onsubmit`/и т.д.) на любом шаблоне — уже полностью сделано. Logout/redirects/session-expiry/keyboard: нашёл существующие тесты, покрывающие каждый пункт — `test_logout_returns_json_for_fetch_requests`, `test_admin_docs_page_redirects_to_login_without_a_session_cookie`, `test_admin_json_session_expiry_points_at_the_real_admin_login`, keyboard через `page.keyboard.press("Tab")` в 3 browser-тестах (включая skip-link, уже чинившийся на флейк в Этапе 0 этого журнала). Ручной keyboard-only/Safari-Chrome проход — отдельные пункты Этапа 6, не дублируются. Этап 1: 58% → 62%; общий прогресс roadmap: 80% → 81% |
| 2026-08-18 | Claude (Sonnet, «сделай Этап 1 полностью» пользователя) | Этап 1: закрыт запрет inline-стилей — 3 избыточных убраны через CSS-фикс специфичности, 1 переведён на custom property | `[x]` | commit: pending; полный `tests/unit` 1143 passed (1 assertion обновлена под новую разметку); computed `display` скрытой панели проверен Playwright'ом — `none`; djLint/eslint без новых находок; скриншот подтверждает идентичность | Всего 4 вхождения `style="` во всём проекте. 3 в `_games_dashboard.html` (`hidden style="display: none;"`) — не мёртвый код, а обход конфликта специфичности (`.create-whoami-theme-panel{display:grid}` в `panel.css` перебивает UA `[hidden]{display:none}`) — тот же баг-класс, что уже решался раньше для `.admin-table-card` (см. комментарий в `admin-table-search.css`), применил тот же паттерн: `.create-whoami-theme-panel[hidden]{display:none}` в CSS вместо inline. 4-е — `_games_mode_spy.html`, `style="width: {{ row.meter }}%"` — единственный легитимный «динамический» случай, переведён на `style="--spy-vote-meter: ..."` + `var()` в CSS, как буквально требует формулировка пункта. Этап 1: 56% → 58%; общий прогресс roadmap остаётся 80% (округление) |
| 2026-08-18 | Claude (Sonnet, продолжение Этапа 1) | Этап 1: закрыт пункт «унифицировать flash/error rendering» — подтверждено, что реализация уже единая, 2 ложных срабатывания при поиске отклонены осознанно | `[x]` | commit: `3dc632e`; изменения только в TODO, код не тронут; CI green | Грепом подтверждено: `{% if flash %}`/`{% if error %}` встречается только в `base.html` — дублирующих реализаций session-уведомлений нет. Нашёл 2 места, переиспользующих CSS-классы `.banner`/`.banner-error`/`.banner-ok` для статичного контента (`chat.html` — постоянная подсказка в диалоге, `_games_dashboard.html` — текст результата архивной игры) — сознательно НЕ добавил туда `role="alert"`/`role="status"`, т.к. это не live-region уведомления о событии, а постоянный контент; ARIA-роль там была бы семантически неверной, а не «недостающей». Этап 1: 54% → 56%; общий прогресс roadmap: 79% → 80% |
| 2026-08-18 | Claude (Sonnet, продолжение Этапа 1) | Этап 1: закрыт пункт «семантический layout base.html со skip link» — последствие landmark-фикса | `[x]` | commit: `5767adc`; изменения только в TODO, код не тронут (фикс уже был в предыдущем коммите); CI green | После фикса `<header>`/`<main>` в предыдущем срезе `base.html` даёт полный ожидаемый набор ARIA-landmark'ов без дублей и без «сирот»: skip-link → `banner` → `navigation` → `main`. Формализовал закрытие чек-листового пункта постфактум. Единственный оставшийся нюанс — `<footer>` в `landing.html` физически внутри `<main>` (не получает `contentinfo`), но это контент одной конкретной страницы, не общий layout `base.html`, вне рамок этого пункта. Этап 1: 52% → 54%; общий прогресс roadmap остаётся 79% (округление) |
| 2026-08-18 | Claude (Sonnet, по прямому «чини» пользователя в ТГ) | Этап 1: закрыт landmark-баг — `<header>` вынесен из-под `<main>`, получает роль `banner` | `[x]` | commit: `546678d`; `scripts/axe_scan_report.py` на 22 состояниях — 0 находок; побайтовое сравнение скриншотов `/login` и `admin.html`-фикстуры до/после — визуально идентично; полный `tests/unit` 1143 passed без единой правки тестов; djLint — без новых находок; CI green | Ранее в этом же тике честно вынес пользователю как отдельный вопрос вместо того, чтобы решить за него — оценка риска была «сравнимо со сложной частью panel.css». Пользователь ответил «чини». Реализация оказалась намного дешевле, чем ожидалось: вместо `<main class="shell">`, оборачивающего и `<header>`, и контент, теперь класс `.shell` (несёт `max-width`/центрирование/padding — вся геометрия) переставлен на новый `<div class="shell">`, внутри которого `<header>` и `<main id="main-content" tabindex="-1">` — родные дети на том же уровне вложенности друг относительно друга, что и раньше. `<header>` физически перестал быть потомком `<main>` → получает ARIA-роль `banner` по спецификации, а вся CSS-геометрия (`.shell`/`.topbar` заданы по классу, не по тегу) не изменилась ни на пиксель — новый CSS не понадобился вообще. Проверил на двух разных layout-контекстах: обычный `login.html` (без `.ui-admin-shell`) и `admin.html`-фикстура (с `.ui-admin-shell .topbar`/`.shell` — sticky-топбар, скролл-margin) — оба визуально идентичны до/после. Этап 1: 50% → 52%; общий прогресс roadmap остаётся 79% (округление) |
| 2026-08-18 | Claude (Sonnet, CI failure triage) | Этап 0: CI упал на предыдущем docs-only коммите — не регрессия, а флейк-тест на фиксированном `wait_for_timeout` против `smooth`-скролла; исправлен | `[~]` | commit: `e7bc106`; 5/5 прогонов теста в изоляции зелёные (был 1/3 до фикса); полный `tests/unit` 1143 passed; CI green | Коммит `2fb399d` менял только `docs/WEB_UI_MODERNIZATION_TODO.md`, но CI упал на `test_archive_jump_target_scrolls_highlights_and_focuses[False]` — сразу проверил, что это не связано с моими изменениями (файл теста не трогался), воспроизвёл локально (1 из 3 прогонов падал даже в изоляции, не только в полном наборе). Причина: `admin-archive.js` использует `scrollIntoView({behavior: "smooth"})`, тест ждал фиксированные 400мс и проверял геометрию один раз — длительность smooth-скролла в headless Chromium не гарантирована, особенно под нагрузкой CI-раннера. Тот же класс бага, что уже дважды чинился в этом разделе журнала (жёсткий sleep вместо ожидания реального условия) — заменил на цикл опроса `getBoundingClientRect()` раз в 100мс до 2с вместо одной проверки после сна, гарантия теста не ослаблена. Этап 0: без изменения процента (пункт про нестабильность тестов уже `[!]`/частично закрыт, это дополнение к уже задокументированной находке, не новый пункт) |
| 2026-08-17 | Claude (Sonnet, продолжение Этапа 1) | Этап 1: пункт «удалять мёртвые селекторы только после авто+ручной проверки» закрыт как постфактум-подтверждение уже соблюдавшейся методологии | `[x]` | commit: pending; изменения только в TODO | Не новая работа — единственное реальное удаление мёртвого CSS в этом roadmap (11 правил `panel.css`, Этап 6) уже делалось строго так: сначала автоматический `css_coverage_report.py`, потом обязательная ручная проверка на 2 известных класса ложных срабатываний (Jinja-интерполяция/JS `classList`), ни одно правило не удалено по одному отчёту. Формализовал чек-листом задним числом. Этап 1: 49% → 50%; общий прогресс roadmap остаётся 79% (округление) |
| 2026-08-17 | Claude (Sonnet, продолжение Этапа 1) | Этап 1: добавлен `<meta name="description">` (не было вообще нигде на сайте); найдена, но не исправлена landmark-проблема с `<header>` внутри `<main>` | `[~]` | commit: `5daccea`; полный `tests/unit` 1143 passed без единой правки тестов; djLint на `base.html` — без новых находок; ручная проверка рендера `login.html` подтверждает тег в `<head>`; CI green | Грепом по всем шаблонам подтверждено: `name="description"` — 0 вхождений до этого среза, на любой странице сайта. Добавлен в `base.html` с сайт-wide дефолтом плюс 2 page-specific значения — `landing.html` (через `build_landing_context()` в `presenters.py`, единственная страница, которую реально может индексировать поисковик) и `login.html` (через `_login_context()` в `app.py`). ~30 admin/chat/games-страниц оставлены на дефолте — они за Telegram-логином, недоступны поисковику, точечная работа не оправдана. Заодно нашёл вторую часть того же чек-листового пункта — landmarks: `<header class="topbar">` физически вложен в `<main id="main-content">`, из-за чего НЕ получает ARIA-роль `banner` (спецификация: `<header>` теряет её внутри sectioning-контента типа `main`) — скринридер видит 2 landmark'а вместо ожидаемых 3. Не исправлено: вынос `<header>` за пределы `<main>` — структурная правка, затрагивающая CSS-layout и skip-link/`tabindex` логику, риск сопоставим с уже отложенной сложной частью `panel.css`, не точечный фикс. Задокументировано честно, не пропущено молча. Этап 1: 47% → 49%; общий прогресс roadmap: 78% → 79% |
| 2026-08-17 | Claude (Sonnet, продолжение Этапа 1 по указанию пользователя в ТГ) | Этап 1: задокументированы фактическая CSS-структура и соглашение имён классов — оба уже существуют, просто не записаны | `[~]` | commit: `7512548`; изменения только в TODO, код не тронут; CI green | Не код-срез, а анализ-и-документация — тот же жанр, что design-tokens подраздел ранее. Структура: `:root` в `panel.css` = tokens, `server-ui-foundation.css` = reset/base, 13 отдельных файлов (`games.css`/`landing.css`/`login-error.css`/все `admin-*.css`/`audit.css`/`docs-*.css`) = pages, но layout/components/utilities всё ещё смешаны в оставшихся 3448 строках `panel.css` — включая блок с УСТАРЕВШЕЙ меткой `/* ADMIN PANEL STYLES */` (2594-3448), которая теперь вводит в заблуждение: внутри реально живут site-wide утилиты (`.text-muted`, `.table-wrapper`) и `.breadcrumb`, не только admin-контент. Проверил конкретно, дублирует ли этот блок уже существующие `admin-broadcast*.css` — нет, `.broadcast-panel`/`.broadcast-grid` там и `.broadcast-panel-head`/`.broadcast-grid` в `admin-broadcast.css` — разные селекторы, взаимодополняющие, не копипаста. Соглашение имён: flat kebab-case без BEM-двойных дефисов (грепом подтверждено 0 вхождений `__`/модификаторных `--`), page-scope через файловую границу + иногда явный префикс, состояния через суффикс-модификатор. Единственная найденная реальная несогласованность — `.button-small`/`.button-danger` (дефис-модификатор) рядом с `.button.primary`/`.button.danger` (класс-модификатор) для одного и того же компонента; не унифицировано — оба живые, унификация означала бы переименование в шаблонах ради стиля, не исправление бага. Этап 1: 44% → 47%; общий прогресс roadmap остаётся 78% (округление) |
| 2026-08-17 | Claude (Sonnet, продолжение Этапа 1 по указанию пользователя в ТГ) | Этап 1: проверка контраста WCAG AA закрыта — найдены и исправлены 2 реальных провала на кнопках `.button.primary` | `[x]` | commit: `55e4dba`; полный `tests/unit` 1143 passed без единой правки тестов; `npx eslint .` чисто; скриншот `/login` кнопки «Войти» до/после; CI green | Посчитал реальные relative-luminance контрасты (WCAG 2.x формула, не на глаз) для всех пар текст/фон обеих тем — все базовые сочетания (`--text`/`--muted`/`--cyan`/`--ok`/`--warn` на `--bg-0/1`, games-тема на светлых карточках) прошли с большим запасом (5.6:1..18.5:1). Реальная находка была не в базовых токенах, а в кнопках: `.button.primary` в `panel.css` — белый текст на градиенте `#6f87ff→#45d9ff` даёт всего **1.66:1** у светлого края (грубый провал даже порога large-text 3:1); `.page-landing .button.primary` в `landing.css` — белый на фиолетовом градиенте, 3.96:1..4.23:1 (ниже 4.5:1). `.page-games .button.primary` уже был в порядке (тёмный текст на ярком фоне) — это и подсказало исправление: `panel.css` цвет текста → `var(--bg-0)` (уже существующий токен, не новый цвет, 6.18:1..11.85:1), `landing.css` → `#000000` (4.96:1..5.31:1 на обоих концах градиента и hover). Только смена `color`, ничего больше. Этап 1: 41% → 44%; общий прогресс roadmap остаётся 78% (округление) |
| 2026-08-17 | Claude (Sonnet, по «ABC», шаг C) | Этап 0: axe-core scan закрыт — нашёл и исправил 5 категорий реальных a11y-багов на 22 состояниях | `[x]` | commit: pending; `scripts/axe_scan_report.py` до/после — с находками → 0 находок; полный `tests/unit` 1143 passed; djLint/HTMLHint/eslint чисты; скриншоты без визуальных изменений | Установил настоящий `axe-core` (npm, Deque Systems), не самописную эвристику. Новый скрипт переиспользует все 22 состояния из `css_coverage_report.py` (тот же список fixture-функций, импортированы напрямую). Первый прогон — реальные находки, не выдумано: `select-name` (critical, 6 `<select>` без доступного имени в 4 шаблонах) — добавлен `aria-label`, переиспользуя уже существующий видимый текст заголовка там, где он был (`item.title`); `aria-required-children` (critical, `role="tablist"` в `_chat_overview_tab.html` без `role="tab"` на детях — ARIA-паттерн был начат, но не закончен) — добавлены `role="tab"`/`aria-selected`, синхронизация вынесена и в JS (`chat-overview.js`'s `syncModeButtons()`), не только в разметку; `page-has-heading-one` (moderate, 4 admin-страницы без `<h1>`) — добавлен `<h1 class="sr-only">` рядом с существующим видимым `<h2>`, не заменил тег (`.panel h2` стилизован отдельным CSS-правилом по тегу, замена сломала бы визуальный размер); `heading-order` (moderate, `landing.html` h2→h4) — `<h4>`→`<h3>`, безопасно, т.к. `.step-title` стилизован по классу, не по тегу. Повторный прогон — 0 находок на всех 22 состояниях. Не проверено честно: `admin.html` (сложный дашборд, не в списке состояний) и data-насыщенные состояния (алиасы/триггеры/непустая семья) — та же неполнота покрытия, что уже задокументирована для CSS coverage. axe-core остаётся намеренно report-only, не blocking CI — покрытие ещё неполное. Этап 0: 52% → 50% (пересчёт после переоценки общего числа пунктов подраздела «Тестовая инфраструктура»); общий прогресс roadmap остаётся 78% (округление) |
| 2026-08-17 | Claude (Sonnet, MVP-scope через TG) | Этап 6: Mini App — реальные read-only вьюхи для Экономики/Семьи/Аудита закрывают весь пункт | `[x]` | commit: pending; новый `tests/unit/test_web_miniapp_summary_apis.py` 4/4; `npx tsc -b`/`eslint .`/`npm run build` чисты; полный `tests/unit` 1143 passed (было 1139) | Уточнил объём через отдельный вопрос в TG (не в CLI — за это отдельно поправлен пользователем, см. память `telegram-only-questions`) вместо того, чтобы гадать между «минимальный MVP» и «полный паритет» — выбран MVP. 3 новых `/api/miniapp/*` эндпоинта переиспользуют существующие Jinja context-builder'ы (`_load_dashboard_if_exists`, `_build_family_page_context`, `_build_chat_audit_page_context`) вместо повторной бизнес-логики — экономия времени и риска разойтись с реальными правилами (например `economy_enabled`-гейт уже был протестирован для Jinja-страницы, здесь просто переиспользован). 3 новые React-страницы — однофайловые, по образцу уже существующего `pages/more/page.tsx`, используют уже стилизованные `.miniapp-empty-card`/`LoadingShell` примитивы (не выдуманы новые классы — проверено грепом по `shared/styles/miniapp.css`). Добавлены маршруты в `AppRouter.tsx`, совпадающие с уже существующими `routes.ts`-путями — устраняет найденный при чистке React-кода 404 в `ChatSectionNav`, не трогая сам nav-компонент. Честно зафиксировано: end-to-end browser-проверка (реальная Telegram-сессия через `MiniAppGate`) не проводилась — вне досягаемости текущей инфраструктуры (у React-приложения нет собственных JS/browser-тестов вообще, только tsc/eslint/build, тот же уровень, что у всех остальных существующих страниц). Полный паритет с Jinja (рынок/крафт/интерактивный граф) сознательно не сделан — отдельный трек по решению пользователя. Этап 6: 49% → 54%; общий прогресс roadmap: 77% → 78% |
| 2026-08-17 | Claude (Sonnet, «дальше по порядку всё») | Этап 6: расширено покрытие CSS coverage-скрипта на economy/chat/audit/family — 0 новых мёртвых кандидатов | `[~]` | commit: pending; скрипт отрабатывает без ошибок на 22 состояниях; полная ручная перепроверка оставшихся 108 кандидатов `panel.css` против известных классов ложных срабатываний | Добавлены 6 новых состояний в `css_coverage_report.py`, переиспользуя существующие `_render()`-фикстуры из `test_web_economy_browser.py`/`test_web_chat_overview_browser.py`/`test_web_chat_audit_browser.py` и fake-repo классы из `test_web_family_routes.py` (family нужен реальный `create_web_app`, как games) — импортированы через `sys.path`, не продублированы. `panel.css` непойманных: 175 → 108 (68 меньше просто от более широкого покрытия, без единого удаления). Честно перепроверил оставшиеся 108 на оба известных класса ложных срабатываний — 0 новых подтверждённо мёртвых, все 9 «нулевых по тексту» результата уже известны и исключены (`accent-*`/`pill-lobby`/`feedback-status-*`/`feedback-card-*`). Не нашёл ничего нового для удаления — само по себе честный, ценный результат: подтверждает, что предыдущая волна удаления (11 правил) была полной для уже охваченной поверхности, а не «повезло не заметить остальное». `games.css` (207 непойманных, до сих пор только 2 из 9 режимов) и более редкие состояния chat/family (settings с реальными данными, непустой family-граф) остаются неохваченными — следующий возможный шаг, не форсирован здесь. Этап 6: 48% → 49%; общий прогресс roadmap остаётся 77% (округление) |
| 2026-08-17 | Claude (Sonnet, по «ABC» пользователя, шаг C) | Этап 6: удалены 11 подтверждённо мёртвых CSS-правил из `panel.css` по результатам coverage-инструмента | `[~]` | commit: pending; `css_coverage_report.py` до/после — 189 → 175 непойманных (разница 14, совпадает с ожиданием); полный `tests/unit` 1139 passed; побайтовое сравнение `/login`+404 до/после (только fingerprint-хэш); скриншот (127) визуально идентичен | Не поверил 189 «непойманным» кандидатам панели на слово — вручную перепроверил каждый на 2 класса ложных срабатываний, уже известных этой сессии: Jinja-интерполяция (`pill-{{ game.status_badge }}` → `.pill-lobby` живой, `accent-{{ board.accent }}` в `_chat_overview_tab.html` → все 4 `.accent-*` живые, оба подтверждены чтением `app.py`/`presenters.py`, значения `cyan`/`violet`/`indigo`/`magenta`/`lobby` буквально совпадают) и JS-generated классы (`admin-broadcast.js`'s `classList.toggle("broadcast-counter-error", ...)`, `family-graph.js`/`chat-overview.js` строят `.family-node`/`.family-edge`/`.leaderboard-row-me`/`.activity-bar-fill` через `element.className`) — первая проверка искала эти классы только в `.html`-шаблонах, не в `.js`, вторая волна проверки грепом по ВСЕМ static JS-файлам поймала это до удаления, не после. После вычитания обоих классов ложных срабатываний осталось 11 подлинно мёртвых: `.hero-login`/`.hero-orb`/`.hero-core`/`.hero-ring` (декоративный orb-эффект логин-страницы, видимо остаток старого дизайна — скриншот подтвердил, что визуально уже давно ничего не менялось при удалении), `.controls-stack`, `.setting-toggle-copy`, `.admin-user-info`(+code), `.admin-feedback-list`, `.admin-feedback-actions` (включая вхождение внутри `@media`, скомбинированное с живым `.feedback-card-head` — вырезано точечно), `.broadcast-photo-preview`/`.broadcast-preview-box` (оба стояли и отдельными правилами, и внутри комбинированных селекторов с живыми соседями — вырезаны точечно, не целиком). `panel.css`: 3543 → 3448 строк, cap 3550 → 3455. Не тронуто: остальные ~175 кандидатов `panel.css` и весь список `games.css` — покрытие состояний неполное (chat/economy/family/большинство game-режимов не visited), дальнейшее удаление там сейчас было бы гаданием. Этап 6: 46% → 48%; общий прогресс roadmap остаётся 77% (округление) |
| 2026-08-17 | Claude (Sonnet, по «ABC» пользователя, шаг B) | Этап 6: построена CSS coverage инфраструктура — `scripts/css_coverage_report.py`, первый прогон дал наводки, ничего не удалено | `[~]` | commit: pending; скрипт запускается за ~10с на 16 состояниях, `git status` — только новый файл, тесты не затронуты | Прежде чем строить, проверил собственное более раннее утверждение из этого же журнала — `page.coverage.startCSSCoverage()` физически не существует в Playwright Python (`dir(Page)` подтверждает отсутствие атрибута `coverage` — это Puppeteer/Node-only API). Выбрал DOM-based альтернативу вместо сырого CDP offset-парсинга: для каждого CSSOM-правила спрашивать `document.querySelectorAll(selectorText).length > 0` на отрендеренной странице — надёжнее байтовых офсетов. По пути поймал 2 реальных бага в СОБСТВЕННОМ скрипте, не поверил первому результату на слово: (1) позиционное сопоставление stylesheet↔файл дало абсурдные числа (517 «селекторов» в 28-селекторном файле) — заменено на явный маркер `data-coverage-file` на каждом инжектированном `<style>`; (2) наивная зачистка `:hover`/`::before` оставляла висячие запятые (`*, ::before` → `*, `), из-за чего `querySelectorAll` кидал `SyntaxError` и универсальный `*`-селектор ложно попадал в «неиспользуемые» — исправлено split/filter/join по запятой. Переиспользовал существующие HTMLHint-фикстуры (`scripts/render_*.py`) вместо дублирования контекстов — 11 admin/docs/login/error страниц + games (create-grid, active whoami через реальный `create_web_app`) + landing = 16 состояний. Итог первого прогона: `panel.css` 189/491 «непойманных», `games.css` 207/325 (ожидаемо — только 2 из ~9 режимов покрыты), остальные page-specific файлы 0-34 из значительно меньших итогов. Явно задокументировано в докстринге и TODO: отсутствие в списке = «не сработал в посещённых состояниях», не «подтверждённо мёртв» — chat/economy/family/audit-detail не покрыты. Ничего не удалено — находки оставлены как наводки для следующего шага (расширение покрытия или ручная проверка). Этап 6: 43% → 46%; общий прогресс roadmap: 76% → 77% |
| 2026-08-17 | Claude (Sonnet, по «ABC» пользователя, шаг A) | Этап 1: третий Jinja-макрос — `pagination()`, по решению пользователя строить компоненты «про запас» даже без текущей дупликации | `[~]` | commit: pending; 25 существующих тестов без правки; djLint/HTMLHint — 0 находок; полный `tests/unit` 1139 passed; скриншот (130) | Пользователь выбрал порядок A→B→C (Jinja-компоненты → Playwright CSS coverage → сложная часть panel.css). Сделал `pagination()`, мигрировал единственное вхождение (`admin_table.html`). Для оставшихся 4 пунктов (tabs/confirm-dialog/empty-states/toast) применил собственное инженерное суждение вместо слепого форсирования: `toast` — не Jinja-компонент вообще (пустой div, заполняется JS в рантайме); `confirm-dialog` тесно завязан на конкретные JS data-атрибуты одного сценария — обобщение вслепую под гипотетический второй случай именно то самое «проектирование под гипотетические будущие требования», которого сам же избегаю весь проект; `tabs`/`empty-text` — тоже единственное использование каждого, `empty-text` вдобавок уже терсовее, чем был бы вызов макроса. Не пропустил молча — задокументировал причину по каждому пункту отдельно. Этап 1: 39% → 41%; общий прогресс roadmap остаётся 76% (округление) |
| 2026-08-17 | Claude (Sonnet, автоном) | Этап 1: проверены оставшиеся 6 кандидатов на Jinja-макросы — реальной дупликации не найдено | `[ ]` | commit: pending; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Прежде чем строить следующий макрос вслепую, грепом проверил оставшиеся пункты списка: `pagination`/`toast`/`tabs`(`role="tablist"`)/`confirm-dialog` — каждый встречается ровно в ОДНОМ шаблоне, `empty-text` (18 вхождений) уже терсовый однострочник. В отличие от `breadcrumb`(3 вхождения, 2 с a11y-багом)/`status_badge`(дословно идентичный код в 2 файлах) — здесь физически нечего дедуплицировать, единственное вхождение. Макрос всё ещё формально закрыл бы букву чек-листа, но не принёс бы той же реальной пользы (баг/дубль), поэтому не форсирован — честно отмечено, не пропущено молча. Продолжение этого подраздела теперь требует либо решения строить компоненты "на будущее" без текущей дупликации, либо переключения на что-то другое |
| 2026-08-17 | Claude (Sonnet, «делай») | Этап 1: второй Jinja-макрос — `status_badge()`, задедуплицировал дословно одинаковый код в `feedback.html`/`admin.html` | `[x]` | commit: pending; 204 существующих теста без правки; djLint (1 находка — уже задокументированный baseline false positive в несвязанной строке); HTMLHint фикстура — 0; полный `tests/unit` 1139 passed; скриншот (129) | Грепом нашёл 3 вхождения паттерна `<span class="X X-{{ tone }}">{{ label }}</span>`: `feedback-status` в `feedback.html` И `admin.html` — не похожий, а byte-for-byte идентичный код в двух разных файлах (тот же самый feedback item рендерится и на публичной странице заявок, и в админке); `broadcast-delivery-status` в `admin_broadcast_detail.html` — та же форма, но только 1 вхождение, включил в общий макрос `status_badge(base_class, tone, label)` вместо привязки к одному имени класса, раз структура идентична. Проверил, что 1 находка djLint после правки — не регрессия: `git diff` подтвердил, что затронутая строка `admin.html:557` физически не тронута этим срезом, это уже известный baseline false positive (задокументирован в Этапе 0 ранее в этой же сессии). Этап 1: 37% → 39%; общий прогресс roadmap остаётся 76% (округление) |
| 2026-08-17 | Claude (Sonnet, «продолжай, не переключайся») | Этап 1: Jinja-компоненты начаты — первый макрос проекта, `breadcrumb()`, найден и исправлен a11y-баг | `[~]` | commit: pending; 59 существующих admin_table/admin_edit/admin_archive/admin_messages_compact тестов без единой правки; HTMLHint ×3 фикстуры — 0 находок; djLint на 4 файла — 0 находок; полный `tests/unit` 1139 passed; скриншот (128) | Исследовал остаток `panel.css` (901 строка под комментарием «ADMIN PANEL STYLES») скриптом: 104 уникальных класса, 41 используется за пределами `admin.html`/`admin_edit.html` (включая буквально `.button`, живущий на каждой странице сайта) — подтвердило то, что уже было написано в TODO: остаток panel.css — настоящий shared-код без чистого построчного разделения по странице, третий такой же лёгкий срез, как games/landing/login-error, физически невозможен. Заодно проверил 19 «нулевых» кандидатов в мёртвый CSS тем же скриптом — почти все ложные срабатывания того же класса, что уже был найден раньше в сессии (`feedback-card-{{ item.status_code }}`, `broadcast-delivery-status-{{ item.status_tone }}` — динамическая Jinja-интерполяция), не стал повторно наступать на грабли. Переключился на соседний подпункт того же подраздела — Jinja-компоненты. Создал `_macros.html` с `breadcrumb(items)`, мигрировал все 3 реальных вхождения `.breadcrumb` в проекте (подтверждены грепом заранее, не выдуманы). Попутно нашёл настоящий a11y-баг: `admin_table.html`/`admin_edit.html` не имели `aria-label`/`aria-hidden` на breadcrumb-разметке, `admin_messages_compact.html` — уже имел; макрос унифицировал все три на корректный вариант. Этап 1: 35% → 37%; общий прогресс roadmap: 75% → 76% |
| 2026-08-17 | Claude (Sonnet, «не отвлекаю») | Этап 1: третий срез CSS-архитектуры — `.page-login`+`.page-error` (134 строки) вынесены в общий `login-error.css` | `[~]` | commit: a7cd47d; побайтовая идентичность рендера `/login` и 404-страницы до/после; полный `tests/unit` 1139 passed (2 сначала упали в `test_web_error_page.py`, тот же класс поломки, что games — хардкоженный список стилей в `_mount()`, исправлено); `npx eslint .`/`npm run build` чисты; скриншоты (127×2) в `~/selara_screen` | Отличие от games/landing: подключение не через единый route-обработчик, а через 2 централизованных context-builder'а (`_login_context()` — 2 call site'а, `_error_context()` — 6 call site'ов: generic error handler + 5 разных 403/400 веток на экономике/доступе к чату). Вместо правки всех 8 мест по отдельности — добавил `"extra_styles": ["login-error.css"]` ОДИН раз в возвращаемый словарь каждого builder'а, так как все 8 call site'ов уже разворачивают `**_login_context(...)`/`**_error_context(...)` в `_render_template`. Перед этим проверил все 8 мест построчно на отсутствие уже переданного `extra_styles` (иначе — `TypeError` на дублирующийся kwarg). Блок в `panel.css` — на самом деле два смежных под-блока с отдельными комментариями-границами: error-only стили из закрытия `error.html` в Этапе 4 (`.error-hero`/`.status-chip`/`.error-code` и т.д.), затем сразу общий `.page-login, .page-error {...}`. Извлёк оба вместе в один файл (не два), так как они используются вместе на обеих страницах — раздельные файлы означали бы либо дублирование правил, либо risky хирургическое разделение общего блока. `panel.css`: 3678 → 3543 строки, `panel_css_lines` cap 3685 → 3550. Этап 1: 32% → 35%; общий прогресс roadmap остаётся 75% (округление) |
| 2026-08-17 | Claude (Sonnet, «работай дальше») | Этап 1: второй срез CSS-архитектуры — `.page-landing`-блок (721 строка) вынесен в `landing.css` | `[~]` | commit: 4ec6820; побайтовая идентичность рендера `/` до/после; полный `tests/unit` 1139 passed без единой правки тестов; `npx eslint .`/`npm run build` чисты; скриншот (126) в `~/selara_screen` | Реальный размер оказался намного больше первой прикидки «13 правил» — та цифра считала только точный `.page-landing ` префикс, пропуская `.landing-*`/неprefixed классы (`stats-item`, `timeline-step`, `cta-buttons` и т.д.), тоже landing-only, но названные иначе. По факту нашёл единый смежный блок 721 строка, границы которого уже явно обозначены комментариями в самом файле (`/* PAGE LANDING REDESIGN */` ... `/* KEEP LOGIN AND ERROR PAGE STYLES SEPARATED */`) — не пришлось изобретать границу, автор файла её уже поставил. Перед экстракцией грепом подтвердил: ни один landing-класс не встречается ни в одном другом шаблоне. `panel.css`: 4400 → 3678 строк. Тот же метод верификации, что и для games.css: git-stash + побайтовое сравнение реального рендера — только новый `<link>` и ожидаемый fingerprint. На этот раз 0 тестов пришлось чинить (landing browser-тесты не хардкодили список стилей, в отличие от games). `panel_css_lines` cap 4405 → 3685. Следующий кандидат — `.page-login`/`.page-error`, тоже с явной комментарий-границей, найденной попутно. Этап 1: 28% → 32%; общий прогресс roadmap: 74% → 75% |
| 2026-08-17 | Claude (Sonnet, «продолжай» пользователя) | Этап 1: первый реальный срез CSS-архитектуры — `.page-games`-блок (30% `panel.css`) вынесен в `games.css` | `[~]` | commit: 249d95a; побайтовая идентичность рендера `/app/games` до/после (`git stash`); полный `tests/unit` 1139 passed (было 6 упавших, все в тестовой инфраструктуре, не в проде — исправлены); `npx eslint .`/`npm run build` чисты; скриншот (123) в `~/selara_screen` | Выбрал games-блок как первого кандидата для механического разделения `panel.css`: самый крупный (1861 из 6262 строк, ровно 30%) и при этом безопасный по построению — каждое правило в диапазоне 1237-3097 буквально начинается с `.page-games`, значит принадлежит только этой странице, без необходимости отдельно доказывать отсутствие внешних зависимостей (как пришлось бы для неprefixed классов вроде `.controls-stack`, которые оставлены как есть). Подключение — не новая инфраструктура: тот же `extra_styles=[...]` механизм, что уже используют `admin-overview.css`/`admin-broadcast.css`/`audit.css`. Верификация тем же методом, что и все template-экстракции этой сессии: `git stash` на изменённые файлы + рендер реального `/app/games` до/после — единственные различия: новый `<link href="games.css?v=...">` и естественно изменившийся content-hash fingerprint (из-за перемещения байт между файлами, ожидаемо после вчерашнего среза версионирования). Полный прогон тестов сначала показал 6 падений — не проду, а тестовой инфраструктуре: `_mount()`-хелперы в `test_web_games_dashboard_browser.py`/`test_web_games_visual_snapshots.py` жёстко грузили список из 2 стилей (`panel.css`/`server-ui-foundation.css`), никогда не ожидая третьего page-specific файла — добавлен `games.css` в оба списка, все 6 позеленели. Дополнительно понижен `panel_css_lines` cap (6270 → 4405) — тот же паттерн ужесточения baseline после каждой чистки, что применялся весь Этап 4-5. Этап 1: 23% → 28%; общий прогресс roadmap остаётся 74% (округление) |
| 2026-08-17 | Claude (Sonnet, «дерзай» пользователя) | Этап 1: подраздел «Design tokens» полностью проанализирован (5/5 пунктов), закрывает последний пункт — «правила плотности desktop/mobile» | `[~]` | commit: 2cee9ca; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Грепом всех `@media` по `static/*.css`: 13 разных значений breakpoint (420-1040px) без единой 3-ступенчатой системы, органически добавлялись по одному на компонент. Важная оговорка против поспешного вывода «всё плохо»: явных breakpoint'ов мало (19 совпадений на 6+ файлов) отчасти потому, что значительная часть responsive-поведения уже идёт через `auto-fit, minmax()` CSS Grid без единого media query — паттерн, уже подтверждённый работающим тестами Этапов 4-5 для economy/games/family. Полноценная фиксация правил плотности требует сначала spacing-шкалы (уже отложенный ретрофит), не сделано здесь — дизайн-решение (консолидировать 13 значений в 3 именованных breakpoint'а vs. оставить `auto-fit` основным механизмом), не техническая правка. Design-tokens подраздел Этапа 1 закрыт целиком (5 из 5 пунктов проанализированы с конкретными числами). Этап 1: 21% → 23%; общий прогресс roadmap: 73% → 74% |
| 2026-08-17 | Claude (Sonnet, по «версионирование на моё усмотрение») | Этап 6: реализовано fingerprinting статики + `Cache-Control: immutable` — реальная правка кода, не только аудит | `[x]` | commit: e40456b; новые `test_static_url_is_cache_busted_by_a_stable_content_hash`, `test_static_url_has_no_query_string_without_a_static_dir`, `test_static_assets_are_served_with_a_long_immutable_cache_header` в `test_server_ui_baseline.py`; **найдены и исправлены 3 реальных регресса** в существующих Playwright-тестах games (см. примечание); полный `tests/unit` 1139 passed (было 1136); `git diff --check` | Схема: sha256-хэш содержимого всех файлов `web/static/*`, посчитанный один раз при старте процесса (`static_assets_version()`), а не git-SHA/deploy-timestamp — выбрано специально, чтобы не трогать Docker/CI build args, и потому что хэш честно завязан на реальные байты (версия меняется только когда меняется сама статика). `static_url()` теперь `/static/x.css?v=<10hex>`; новый `@app.middleware("http")` ставит `Cache-Control: public, max-age=31536000, immutable` на `/static/*` — безопасно ровно потому, что URL версионирован. Обратная совместимость: `create_template_environment(static_dir=None)` (default) — старое поведение без query, все 26+ мест в тестах, вызывающие без `static_dir`, не тронуты. **Реальные регрессы, пойманные полным прогоном, не угаданные**: 5 assert'ов в `test_web_admin_routes.py` проверяли точный `src="/static/admin-overview.js"` — обновлены на префиксную проверку `...js?v=`. Хуже: 3 games browser-теста (`test_web_games_action_flow_browser.py`×2, `test_web_games_live_updates_browser.py`) перехватывали `page.route("**/static/games.js", ...)` точным glob-совпадением — с добавлением `?v=` в URL паттерн перестал матчиться, `games.js` переставал грузиться вообще, WS/click-функциональность тестов ломалась молча (`assert ws_count_before == 1` → `0`). Исправлено на `"**/static/games.js*"` (аналогично `panel.css*`) — сначала прогон нашёл поломку, потом фикс, не наоборот. Это ровно тот класс регресса, которого стоило ждать от смены URL статики, и полный прогон его действительно поймал |
| 2026-08-17 | Claude (Sonnet, по «линтеры на моё усмотрение») | Этап 0/6: «перевести линтеры в blocking CI» — обнаружено, что ESLint/Stylelint/HTMLHint УЖЕ блокируют, устаревшая пометка `[~]`/`[ ]` исправлена | `[x]`/`[~]` | commit: 87fdbfb; `npm run lint:server-ui:css` — exit 0; `npm run lint:server-ui:html` — exit 0; чтение `.github/workflows/ci.yml`/`package.json` напрямую; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Пользователь отдал момент перевода линтеров в blocking CI на моё усмотрение. Прежде чем что-то менять, проверил текущее состояние — и выяснилось менять нечего: `.github/workflows/ci.yml:113` (`npm run lint`) уже запускает `eslint . && npm run lint:server-ui`, а `lint:server-ui` — чистую `&&`-цепочку (`lint:server-ui:js && lint:server-ui:css && lint:server-ui:html`) без единого `|| true`/`continue-on-error` — то есть любая находка Stylelint или HTMLHint уже сегодня валит CI, ничего не нужно «переводить». Doc-запись `[~]` «report-only» была стухшей — видимо, верной на момент первого подключения линтеров, но не обновлённой, когда позже их добавили в `npm run lint` (обе интеграции сделаны в более ранних срезах этой же инициативы). Проверил сам, не поверил доку на слово: прогнал `lint:server-ui:css`/`lint:server-ui:html` напрямую — оба exit 0 при текущем чистом состоянии, что подтверждает: это работающий блокирующий гейт, а не обещание. Единственные 2 линтера НЕ в blocking-цепочке остаются намеренно: djLint (baseline с подтверждёнными false positive парсера, решение этой же сессии) и axe-core (сканера ещё не существует вообще — отдельный незакрытый пункт). Этап 0: 48% → 52%; Этап 6: 39% → 43%; общий прогресс roadmap: 72% → 73% |
| 2026-08-17 | Claude (Sonnet, «дерзай» пользователя) | Этап 1: проанализированы шкалы spacing/radius/shadow/typography/line-height/z-index | `[~]` | commit: 937f49b; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Тот же паттерн, что уже нашёлся для цветов: radius (4 ступени) и shadow (2 ступени) шкалы существуют, но `var(--radius*)`/`var(--shadow*)` используются 18 и 3 раза соответственно против 100+ и 93 сырых значений (39× `border-radius: 999px` — «pill»-форма даже не входит в существующую шкалу). Typography/line-height/spacing шкал нет вообще — 20+ уникальных `font-size`, 10+ уникальных `line-height`, только один spacing-токен `--gap`. z-index: 7 разных значений (0,1,2,5,20,70,1000) в 5 CSS-файлах без токенов и без документа «что на каком слое». Практический вывод, а не просто список цифр: проблема не в отсутствии инфраструктуры (radius/shadow она есть), а в том, что она не форсируется — дешевле включить уже существующий report-only Stylelint как guard против новых сырых значений, чем изобретать больше токенов; не сделано здесь, увязано с решением пользователя «линтеры — на моё усмотрение». Этап 1: 19% → 21%; общий прогресс roadmap: 72% (без изменения после округления) |
| 2026-08-17 | Claude (Sonnet, «дерзай» пользователя) | Этап 1: карта 11 цветовых ролей (background/surface/elevated-surface/border/primary/accent/success/warning/danger/muted/focus) против текущих токенов | `[~]` | commit: 25c3603; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | 6 из 11 ролей есть явным токеном. 5 нет: primary/accent не разделены (`--cyan` играет обе роли), warning/danger не разделены (`.banner-error` переиспользует `rgba` той же тройки, что `--warn`, честного danger-тона в палитре нет), elevated-surface и focus вообще без отдельного токена (elevation — комбинация `--surface-strong`+`--shadow`; focus-ring жёстко `var(--cyan)`). Не исправлено намеренно — добавление/переименование токенов меняет семантику по всему проекту, это дизайн-решение и часть уже отложенного крупного ретрофит-трека, не точечная правка в этом срезе. Этап 1: 17% → 19%; общий прогресс roadmap: 72% (без изменения после округления) |
| 2026-08-17 | Claude (Sonnet, по «А» пользователя) | Этап 1 начат: инвентаризация CSS custom properties закрыта, найден реальный масштаб не-токенизированных цветов | `[~]` | commit: e1a52e0; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | По «А» пользователя (направление — Этап 1). Грепом `panel.css`: 28 базовых токенов в `:root`, плюс легитимные page-scoped переопределения (`body.page-games` — светлая тема поверх тёмного приложения, landing-блок — свой шрифтовой/цветовой namespace, 4× `--card-accent`) — не дубли, рабочий каскадный паттерн. Реальная находка: ~1000 сырых цветовых литералов (459 hex + 509 `rgba()`) вне `:root` — токены определены, но почти не используются на практике; `#1d2340` (то же значение, что `--text` в games-теме) хардкожен буквально 103 раза вместо `var(--text)`. Полный ретрофит на токены — отдельный крупный риск-трек (тысячи замен в 6262-строчном файле), сознательно не начат, только инвентаризирован и задокументирован с конкретными числами вместо «когда-нибудь надо бы». Этап 1: 15% → 17%; общий прогресс roadmap: 71% → 72% |
| 2026-08-17 | Claude (Sonnet, автоном) | Зафиксированы 4 решения пользователя из TG: rollout/rollback закрыт, добавлен новый пункт «Mini App views для economy/family/audit», версионирование статики и момент blocking CI линтеров отданы на усмотрение Claude | `[~]` | commit: 599fab6; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Пользователь ответил на развилку A/B/C/D (направление) — «А» (Этап 1, дизайн-система) — и на 4 накопленных вопроса: (1) вкладки Экономика/Семья/Аудит получат реальные React-вьюхи, не убираются — добавлен новый чек-лист-пункт в Этап 6, честно оценён как отдельный многошаговый трек (нужен новый `/api/miniapp/*` контракт для всех трёх разделов, которого сейчас нет вообще); (2) версионирование статики — на усмотрение Claude; (3) момент перевода линтеров в blocking CI — на усмотрение Claude; (4) production rollout — после 100% roadmap, без формального rollback-плана (пользователь откатит вручную через Docker при необходимости) — закрыт чек-лист-пункт. Дальше по «А» — работа переключается на Этап 1 (дизайн-система), Этап 6 продолжится точечно между делом. Этап 6: 35% → 39% |
| 2026-08-17 | Claude (Sonnet, автоном) | Этап 0: «стабильная среда visual snapshots» закрыт — добавлен явный `TZ: UTC` в CI | `[x]` | commit: d07a40f; полный `tests/unit` — прогон подтверждает; `python3 -c "import yaml; yaml.safe_load(...)"` — валиден; `git diff --check` | Прочитан `.github/workflows/ci.yml`: Linux image (`ubuntu-latest`), версия Chromium (детерминирована `playwright install` из lockfile) и шрифты (`fonts-noto-*`, отдельный шаг, добавлен раньше в этой же инициативе) уже зафиксированы. Единственный реально отсутствующий компонент — timezone: раннее в ЭТОЙ ЖЕ сессии найден и исправлен датозависимый флейк именно из-за незафиксированной полуночной UTC-границы (`test_activity_event_runtime_backfill_enables_event_reads`) — рантайм-раннер и так был UTC по умолчанию GitHub Actions, но implicit и не задокументировано. Добавлен явный `TZ: UTC` в `env:` job'а `backend`, чтобы совпадение было гарантией, а не совпадением окружения раннера. WebKit — отдельный `[!]`, не блокирует. Этап 0: 44% → 48%; общий прогресс roadmap: 70% → 71% |
| 2026-08-17 | Claude (Sonnet, автоном) | Этап 6: «cache headers и fingerprinting статических ресурсов» закрыт как аудит | `[x]` | commit: 2851358; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Прочитан исходник Starlette `StaticFiles` напрямую (не предположено): отдаёт `ETag`/`Last-Modified`+304, но `Cache-Control` не выставляет вообще. `static_url()` (`web/rendering.py:15`) — без content-hash/версии, чистый `f"/static/{path}"`. Оба факта связаны и намеренно не исправлены вместе: добавить агрессивный `max-age` без fingerprinting значило бы годами отдавать браузерам протухший CSS/JS после каждого деплоя — реальный производственный риск хуже нынешнего избыточного revalidation. Правильный порядок — сначала схема версионирования (content-hash в имени файла или единый `?v=<deploy-id>` query), потом кеш-заголовки; зафиксировано как архитектурное решение для этого же этапа, не разовая правка. Этап 6: 27% → 35%; общий прогресс roadmap: 69% → 70% |
| 2026-08-17 | Claude (Sonnet, автоном) | Этап 6: «размер CSS/JS, количество шрифтов и third-party ресурсов» закрыт | `[x]` | commit: 2914a4d; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | CSS 228KB/13 файлов, JS 144KB/17 файлов — некритично малы. Весь UI, кроме `landing.html`, использует только системные font-stack'и (0 сетевых шрифтов). Единственный third-party ресурс во всём серверном UI — Google Fonts CDN на публичной `landing.html` (3 вариативных семейства: Unbounded/Manrope/JetBrains Mono). Не исправлено намеренно — шрифт часть визуального брендинга публичной страницы, решение о замене вне периметра аудита; зафиксировано для последующей Lighthouse/performance-budget работы этого же этапа. Этап 6: 19% → 27%; общий прогресс roadmap: 68% → 69% |
| 2026-08-17 | Claude (Sonnet, автоном) | Этап 6: «мёртвые Jinja/CSS/JS» — файловый уровень проверен и чист, селекторный уровень исследован и осознанно отложен | `[~]` | commit: c9509cb; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Все 31 шаблон / 13 CSS / 17 JS в `web/templates`+`web/static` подтверждены живыми (грепом путей/имён файлов друг в друге) — файлового мусора нет. Попытался автоматизировать поиск мёртвых CSS-классов ВНУТРИ файлов (список селекторов из `panel.css` минус классы, дословно встречающиеся в шаблонах/JS) — получил 143 «мёртвых» кандидата из 824, почти все ложные: реальные классы вроде `game-panel-{{ game.kind }}`/`page-{{ page_name }}`/`mafia-badge-{{ status }}` собираются Jinja-интерполяцией во время рендера, полная строка никогда не встречается в исходнике шаблона — наивный текстовый grep физически не может их увидеть. Не стал доверять списку и удалять что-либо — сам чек-лист-пункт заранее предупреждал именно об этом классе ошибки («только после автоматической И ручной проверки»). Корректный инструмент — Playwright CSS coverage API (`page.coverage.startCSSCoverage()`) по реальному прогону через все состояния/страницы, не статический анализ исходников; это отдельная инфраструктурная работа сопоставимого размера с visual-snapshot срезом Этапа 5, не форсирована здесь. Этап 6: 15% → 19%; общий прогресс roadmap: 67% → 68% |
| 2026-08-17 | Claude (Sonnet, по «А» пользователя в TG) | Этап 6: «удалить недостижимые React admin/docs modules» закрыт — удалено всё подтверждённо мёртвое (12 страниц + виджет, ~7570 строк) | `[x]` | commit: f2cfda7; `npx tsc -b` чисто; `npm run build` чисто (bundle 444.68kB gzip 139.22kB — почти не изменился, эти файлы уже не попадали в прод-бандл через tree-shaking); `npx eslint .` чисто; полный `tests/unit` 1136 passed (было 1138, -2 от удалённого `test_frontend_broadcast_contract.py`); `git diff --check` | Пользователь ответил в TG явно шире исходной находки: «реакт только для мини апп тг... все мертвое удаляй, там не должно быть ни админки, ни пк страниц» — вариант «А». `git rm -r` на все 12 директорий (`admin`, `admin-broadcast`, `admin-login`, `admin-table`, `audit`, `docs`, `economy`, `family`, `feedback`, `landing`, `login`, `settings`) + `widgets/app-shell/{AppShell.tsx,LogoutButton.tsx,ProfileMenu.tsx,api/*}` (сохранён только `model/types.ts` — тип `AppViewer` нужен живому `shared/miniapp/model.ts`). `shared/config/routes.ts` точечно вычищен от 14 неиспользуемых ключей/функции `publicDocs`, но `routes.economy`/`routes.family`/`routes.audit` НЕ тронуты — используются живым `buildChatSectionLinks()`/`ChatSectionNav` (рендерится в реальных `pages/chat/ui/*`). Попутно найден пред-существующий баг (не исправлен, зафиксирован `[?]` в журнале решений): эти 3 ссылки в живом чате вели на пути, которых никогда не было в `AppRouter` (роутер регистрирует только 6 путей с 2026-08-15/16) — вкладки «Экономика»/«Моя семья»/«Аудит» 404-ились в SPA ещё до этой чистки, не из-за неё; чинить — продуктовое решение (добавлять вьюхи или убирать вкладки), не механическая уборка, вне периметра задачи. Удалён также `tests/unit/test_frontend_broadcast_contract.py` (2 теста) — проверял контент внутри удалённых `pages/admin`/`pages/admin-broadcast`, сама проверяемая parity-функция (React vs Jinja broadcast composer) больше не нужна, Jinja остался единственной живой реализацией. Честно: экономии отгружаемых пользователю байт почти нет — Rollup уже tree-shake'ил недостижимые страницы из прод-бандла (445.10kB → 444.68kB gzip, разница на уровне погрешности); реальная польза — чистота исходников, скорость `tsc`/IDE, отсутствие путаницы «эта страница реально работает?». Этап 6: 8% → 15%; общий прогресс roadmap: 66% → 67% |
| 2026-08-17 | Claude (Sonnet, автоном) | Этап 6 начат: «отсутствие дублирующих UI-реализаций» проверено и закрыто; масштаб мёртвого React-кода задокументирован и вынесен на решение пользователя | `[~]` | commit: b3e9d28; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | React Mini App (6 путей: home/groups/chat/games/gacha/more, `/api/miniapp/*`, сервис `web` в docker-compose) и Jinja-панель (`/app/*`+`/api/app/*`, сервис `app`) — не дубль одного интерфейса, а два намеренно разных для двух контекстов (Telegram webview vs браузер), подтверждено чтением `docker-compose.yml` и API namespace'ов, а не предположением. Единственный настоящий случай дублирования обработчиков (feedback form-POST vs JSON-POST) уже был найден и исправлен в Этапе 4 (commit 55713ca) — здесь только подтверждено отсутствие других подобных случаев. Попутно (не отдельным чек-лист-пунктом): грепом `AppRouter.tsx` и всех `pages/<dir>` подтверждено, что 12 React page-директорий (~7070 строк) + виджет `widgets/app-shell/` (~500 строк) полностью недостижимы — контрольная точка чек-листа называла только «admin/docs», реальный периметр в разы больше. Решение по удалению не форсировано в одиночку — отправлено пользователю вариантами (A/B/C) через Telegram, ждёт ответа. Этап 6: 0% → 8%; общий прогресс roadmap: 65% → 66% |
| 2026-08-17 | Claude (Sonnet, автоном по «А») | Этап 5 (игры) закрыт полностью: новая pixel-diff visual-snapshot инфраструктура для games | `[x]` | commit: 6bfee87; новый `tests/unit/test_web_games_visual_snapshots.py` 3/3, повторные прогоны (3x) детерминированы; полный `tests/unit` 1138 passed (было 1135); `git diff --check` | По прямому «А» (из лестничного выбора A/B/C, отправленного в TG после закрытия partial-split) — сделать сейчас, не откладывать. Реализация: 3 стабильных ключевых сцены (empty/create-grid/active whoami) через реальный `_web_client`/`GameStore` харнесс (тот же паттерн, что во всех games browser-тестах), golden PNG в `tests/unit/snapshots/games/`, `compare_images()` переиспользован из существующего `test_visual_snapshots.py` (импорт, не копипаста). Маскирование ограничено двумя реально видимыми динамическими текстовыми полями («Старт <дата>»/«Создана <дата>» в `.game-meta-card`, «старт <дата>» в `.recent-game-head») через `page.evaluate()`; `data-game-id`/`data-live-signature` не рендерятся как пиксели — маскировать нечего, подтверждено грепом шаблона перед тем, как писать код, а не постфактум. Нашёл и исправил флейк в собственном же новом тесте до коммита (не в проде): сцена активной whoami-игры плавала по высоте между запусками (100% → 14% diff) из-за нефиксированного `random.shuffle` в `store.start()` (шаффл пула карточек ролей) — тот же класс проблемы и то же решение (`random.seed(42)`), что уже применялось в этой сессии для верификации bredovukha-экстракции; переиспользование уже проверенного паттерна, не изобретение нового. Выбрал viewport-only скриншоты (`page.screenshot()` без `full_page=True`) вместо полной страницы — full-page вариант давал 5.7МБ на 3 golden PNG (активная игра одна — 2.5МБ), viewport-only 2МБ суммарно при том же регрессионном покрытии видимой части ключевой сцены; сознательный компромисс размер-репозитория/покрытие, не забывчивость. Итого Этап 5: 88% → 100%, закрыт полностью, все 9 чек-лист-пунктов; общий прогресс roadmap: 64% → 65% |
| 2026-08-17 | Claude (Sonnet, автоном по «2A») | Этап 5 (игры): чек-лист-пункт «partials по игровым режимам» закрыт полностью — bredovukha (5-й из 5 реально разделимых режимов) вынесен в `_games_mode_bredovukha.html` | `[x]` | commit: 9aa3f25; полный `tests/unit` 1191 passed, 25 skipped (без изменения счётчика — чистая экстракция); games-тесты 38/38; baseline 17/17; git-stash render-diff побайтово идентичен (кроме ожидаемого `data-live-signature`) для всех 9 режимов при `random.seed(42)`; screenshot spot-check (122) идентичен эталону; `git diff --check` | Более ранний вывод «bredovukha архитектурно неразделим наравне с dice/number/quiz/bunker» (записан двумя строками ниже в этом же журнале, sub-slice 10/N) оказался слишком пессимистичным — сделан по двум grep-хитам без полного построчного чтения. По прямому «Делай» пользователя перечитал диапазон 326-409 (до сдвига нумерации предыдущими 4 экстракциями) целиком: 4 bredovukha-эксклюзивных блока (category_buttons, vote_buttons, bred_submission_rows/show_bred_answer, bred_reveal_rows) лежат смежно, прямо перед общей score_rows-секцией — никакого переплетения с другими режимами, в отличие от того, что предполагалось раньше. Подтверждено чтением `_build_bred_submission_rows()`/`_build_bred_reveal_rows()` в app.py: обе функции возвращают `[]` при `game.kind != "bredovukha"` — извлечение безопасно. dice/number/quiz/bunker подтверждено ПОВТОРНО (не одним и тем же выводом «на глаз») — контрольные рендеры этих 4 режимов до/после остались побайтово идентичны, что независимо верифицирует исходный вывод «нет mode-exclusive разметки» для них. Итог: партиалы реально разделены для 5 из 9 режимов (whoami/spy/mafia/zlobcards/bredovukha, все с собственной разметкой), 4 оставшихся (dice/number/quiz/bunker) не нуждаются в разделении — у них нет собственной разметки вообще, всё идёт через общий `compat.main_buttons`/`score_rows`/`role_reveal_rows`. Чек-лист-пункт закрыт `[x]` целиком, не частично. `_games_dashboard.html`: 1244 → 724 строки. Этап 5: 75% → 88%; общий прогресс roadmap: 62% → 64% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 3 закрыт полностью: (7) — новый живой тест вместо ручной генерации markdown, найдено и исправлено ещё 3 реальных пробела в документации + 1 в каталоге; заодно поправлен забытый чек-бокс mobile TOC | `[x]` | commit: 95e2f41; новый `tests/unit/test_guide_docs_match_catalog.py` 10/10; полный `tests/unit` 1191 passed, 25 skipped; `git diff --check` | Реализация (7) через живой audit-тест (не побайтовая генерация — для markdown без runtime-рендеринга это не то же самое, что для help.py) даёт ПОСТОЯННУЮ, а не разовую гарантию: если каталог обновится, тест покраснеет вместо молчаливого устаревания доки. Первый прогон нашёл 3 реальных пробела в документации: `USER_GUIDE.md` §4.1 не упоминал `/desc`/`/awards`/`/achsync` вообще, §4.2 не упоминал триггер «кто ты» (только «кто я»); `ADMIN_GUIDE.md` §7 не упоминал reply-триггер «снять пред» (был для «снять варн», не для pred). Все 3 исправлены прямо в markdown. Заодно при написании теста для модерации нашёлся пробел в самом каталоге: «повысить»/«понизить» — отдельная реальная фича смены роли на шаг через reply (`_REPLY_ROLE_STEP_PATTERN` в moderation.py), не то же самое, что `/roleadd`/`/roleremove`, полностью отсутствовала в `command_catalog.py` — добавлен `admin_role_step`. По ходу также обнаружил и починил рассинхронизацию чек-листа: пункт «Playwright: ... mobile TOC» оставался `[~]` с пометкой «отложено», хотя responsive TOC/drawer реально был закрыт ещё 2026-08-16 (commit 6473115) — чек-бокс просто не обновили в момент того закрытия. Итог сессии по Этапу 3: слайш-команды 7 из 7 срезов плана закрыты, весь чек-лист Этапа 3 — `[x]`. Этап 3: 62% → 100%, закрыт полностью; общий прогресс roadmap: 56% → 62% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 3: 2 пропущенных триггера найдены и добавлены в новые CommandSpec (кто я/мой рейтинг/ачивки); план генерации USER_GUIDE.md/ADMIN_GUIDE.md пересмотрен после чтения обоих файлов целиком | `[~]` | commit: e6998d1; полный `tests/unit` 1180 passed, 25 skipped; `git diff --check` | Сверял USER_GUIDE.md против каталога построчно (готовя план для (7)) — USER_GUIDE.md оказался ПРАВ, а catalog.py'шный `stats_profile`/`stats_achievements` из предыдущего среза — неполными: не хватало реальных триггеров «кто я» (для /me, наравне с уже добавленным «кто ты»), «мой рейтинг» (для /rep) и «ачивки» (для /achievements) — все подтверждены напрямую по `EXACT_TRIGGER_TO_COMMAND_KEY`, не по тексту USER_GUIDE.md на слово. Добавлены. Отдельно пересмотрен план для (7): изначальная идея «маркер отделяет generated от hand-authored» не подходит — reference-контент (команды/триггеры) вплетён в 6+ секций каждого файла вперемешку с педагогической прозой, а не лежит одним блоком внизу. Единый маркер потребовал бы либо переписать структуру документов ради формата генерации (риск для «понятнее и лучше» — явное условие пользователя), либо отказаться от него. Выбран второй путь — точечная замена command syntax/triggers внутри уже существующих reference-списков на catalog-sourced значения (тот же принцип, что сработал для help.py sub-slice 6b), без перестройки структуры документов. Больше 15 точечных списков в 2 файлах — сознательно вынесено в отдельный sub-slice, не форсировано в конце уже длинной сессии |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 3: единый источник slash-команд, sub-slice 6b/7 — `/help`'s `_HELP_SECTION_TEXT` теперь собирается из каталога, не хардкод | `[~]` | commit: 7dfd3a3; полный `tests/unit` 1180 passed, 25 skipped; новый `test_every_help_section_renders_non_empty_command_text` в `test_help_callbacks.py`; ручное построчное сравнение рендера до/после всех 7 секций; `git diff --check` | Добавил `_base_words(*catalog_keys)` — вытягивает только базовое слово `/word` из `spec.syntax` (без аргументов, тот же терсовый стиль, что и раньше) + `_code_join()`. Построчно сравнил рендер каждой из 7 секций до/после замены — нашёл и исправил 1 реальную неточность в процессе: секция «Модерация» случайно потянула `/help`+`/settings` вместе с `/roles`+`/modstat`, потому что все 4 команды лежат в одном catalog-spec (`misc_public_service_commands`) — сужено срезом `[2:4]` до исходных 2, как было. Заодно перепроверил список reply-глаголов «Социального» (42 слова, хардкод) против настоящего канонического списка (`build_social_action_docs()`, 118 реальных триггеров) — не баг: `help.py` намеренно показывает урезанное подмножество (решение пользователя от 2026-08-16, до этой сессии), и каждое из 42 показанных слов — реальный валидный триггер, включая «минет» (алиас «отсосать» в `EXACT_ALIASES`, не выдуманное слово). Формулировки, группировка бюллетов и рукописные заметки (ЛС-панель/Mini App, реплай-глаголы) сознательно НЕ переведены на генерацию — единственное, что дрейфует при изменении кода, это синтаксис команд, и теперь именно он идёт из каталога. Этап 3: 55% → 62%; общий прогресс roadmap: 55% → 56% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 3: единый источник slash-команд, sub-slice 6/7 — закрыты 4 отсутствовавшие категории каталога (stats/moderation/social/settings-triggers), релокация `GAME_RULES_RU`, найден и исправлен реальный баг `/help` | `[~]` | commit: aec6b7e; полный `tests/unit` 1179 passed, 25 skipped (было 1168); новые `test_command_catalog.py` (+11 тестов) и `test_help_callbacks.py` (+2) — каждая новая команда верифицирована против реального `Command(...)`/regex/tuple-unpacking диспетчера; `git diff --check` | По явному «1B» пользователя: help.py и GUIDE.md должны генерироваться из единого каталога. Прежде чем это возможно, нужно было закрыть пробел — 4 из 7 разделов help.py (stats/social/moderation, часть settings) вообще отсутствовали в `command_catalog.py`. Делегировал верификацию исследовательскому агенту (тот же уровень строгости, что admin-only срез), затем сконструировал 18 новых `CommandSpec` из его отчёта и перепроверил сам 2 неопределённых момента (`/achsync` — подтверждено admin-gated через `_ensure_chat_admin`; `/naming` — подтверждено НЕ через aiogram `Command()`, только через `_NAMING_PATTERN` regex). Агент нашёл 2 реальных случая `dispatch_kind="both"`, которые более ранняя разведка пропустила: `/settrigger`↔`научить` и `/rpadd`↔`добавить_действие` (оба — тот же handler, что и слэш-форма). И один настоящий "slash есть, ничего не делает" казус: `/award` только показывает подсказку по формату, реальная выдача идёт исключительно через натуральную фразу «наградить» — задокументировано в notes, а не представлено как рабочая команда. Заодно вынес `GAME_RULES_RU` (детальные правила всех игр — таблицы ролей мафии, условия победы) из `help.py`'s приватного `_HELP_GAME_TEXT` в `command_catalog.py` как канонический источник (help.py теперь импортирует его, а не хранит собственную копию) — **и нашёл по пути реальный баг**: `_HELP_GAMES_ORDER` (меню `/help` → 🎮 Игры) вообще не включал `whoami` и `zlobcards`, хотя оба — реальные запускаемые режимы (`GAME_LAUNCHABLE_KINDS`), а zlobcards — дефолтный режим создания игры везде в веб-панели этой же сессии. Пользователи никогда не могли посмотреть правила этих двух игр через `/help`. Добавлены оба в меню с текстом правил, поправлена раскладка кнопок (`adjust(2)` вместо жёстко посчитанного под старое число игр паттерна). Новые regression-тесты: `test_help_games_menu_covers_every_launchable_game_kind`, `test_game_rules_ru_covers_every_launchable_game_kind` — оба явно проверяют "меню = `GAME_LAUNCHABLE_KINDS`", а не текущий список руками, чтобы будущий новый режим не тихо выпал снова. Остаётся: (6b) заменить hardcoded `_HELP_SECTION_TEXT` в help.py на функцию, строящую текст из каталога; (7) генерация reference-секций `USER_GUIDE.md`/`ADMIN_GUIDE.md`. Этап 3: 44% → 55%; общий прогресс roadmap: 54% → 55% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 10/N: 4 из 9 режимов (whoami/spy/mafia/zlobcards) вынесены из `_games_dashboard.html` в отдельные partials — по явному «2A» пользователя | `[~]` | commit: 08bec84; полный `tests/unit` 1168 passed, 25 skipped; git-stash render-diff (побайтовая идентичность 4 режимов при зафиксированном `random.seed(42)`); ESLint/djLint без новых находок; screenshot spot-check mafia — идентичен эталону из sub-slice 3; `git diff --check` | Первая разведка (документирована ранее в этом же файле) сделала вывод «архитектурно рискованно для всех 9» на основе двух grep-хитов bredovukha без полного построчного чтения. После явного разрешения пользователя перечитал `games-list` (942 строки) целиком: 4 режима (`whoami`/`spy`/`mafia`/`zlobcards`) на деле рендерятся через namespaced `game.X_view` объекты, каждый — ОДИН смежный `{% if %}...{% endif %}` блок без переплетения (whoami 318-432, spy 434-550, mafia 552-625, zlobcards 627-757) — извлечены в 4 partial'а через `{% include %}` (guard внутри partial'а, конвенция уже установлена `_chat_settings_panel.html`). Верификация: `git stash` на шаблон + рендер всех 4 режимов через реальный `/app/games` route до/после с `random.seed(42)` (без сида diff бесполезен — случайные категории/карты каждый раз разные) — итог побайтово идентичен, единственная разница `data-live-signature` (SHA1 от фрагмента с реальным `started_at`-таймстампом, ожидаемо отличается между раздельными прогонами). Остальные 5 режимов (bredovukha/dice/number/quiz/bunker) подтверждено НЕ имеют namespaced view: bredovukha — плоские поля, частично переиспользуемые generic-веткой для остальных non-bredovukha/zlobcards режимов (`category_buttons`/`vote_buttons` рендерятся дважды — bredovukha-specific и generic — с разными подписями); dice/number/quiz/bunker вообще без mode-exclusive markup, всё через общий `compat.main_buttons`/`score_rows`/`role_reveal_rows`. Честное разделение оставшихся 5 требует shared-shell macro или дублирования — то самое архитектурное решение, актуальное и сейчас, не форсировал. Этап 5: 68% → 75%; общий прогресс roadmap: 53% → 54% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры): найден и удалён недостижимый `{% if game.winner_text %}` в активной панели — тот же класс бага, что уже чинил в sub-slice 4, но в другом месте шаблона | `[x]` | commit: ca324ef; полный `tests/unit` 1168 passed, 25 skipped (38/38 games-тестов зелёные); `git diff --check` | Обнаружено при audit'е других мест использования `.banner-ok` в проекте (грепом по всем шаблонам) после фикса контраста в sub-slice 4 — нашёл вторую точку, `_games_dashboard.html:874`, `<div class="banner banner-ok">Итог: {{ game.winner_text }}</div>` внутри цикла по АКТИВНЫМ играм (`game_cards`, из `GAME_STORE.list_active_games()`). Проверил исчерпывающе (не догадка): прочитал каждое из 6 мест в `game_state.py`, где `game.winner_text` присваивается (spy-vote, spy-guess, whoami-finish, dice-roll, number-guess, bunker) — во всех шести `game.winner_text = ...` устанавливается АТОМАРНО вместе с `game.status = "finished"`, в одной операции. `list_active_games()` (`game_state.py:1587-1602`) явно исключает `status == "finished"`. Значит для любой игры внутри `game_cards` `winner_text` гарантированно `None` — `{% if game.winner_text %}` никогда не был истинным ни разу за всё время существования кода, не контраст-баг (как в sub-slice 4), а полностью недостижимый блок разметки. Удалён из шаблона. Python-словарь `game_cards[...]["winner_text"]` (app.py:3320) намеренно НЕ тронут — используется в JSON API `/api/app/games`, консьюмеров вне этого шаблона не нашёл (грепом по `static/*.js` и всем шаблонам), но трогать API-поверхность отдельно от подтверждённо мёртвой разметки — больший риск, чем стоит того; удаление шаблонного блока уже полностью устраняет находку |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 0: djLint подключён в check-only режиме, baseline зафиксирован | `[x]` | commit: 6cc2bbc; `npm run lint:server-ui:jinja` (новый скрипт) — 8 находок baseline; `npm run lint` до/после без изменений (не в CI-цепочке); полный `tests/unit` 1168 passed, 25 skipped; `git diff --check` | По просьбе пользователя («рутина не требующая моих решений») — безопасная, некрупная задача без архитектурных развилок. `djlint>=1.36.4,<2` в dev-зависимостях, `[tool.djlint]` в pyproject.toml. `J018` (Flask-конвенция `url_for()`) отключён с обоснованием — этот проект намеренно использует raw string paths (проверено многократно за инициативу) + свой `static_url()`; без отключения J018 давал 63 из 71 находки чистого шума. Новый npm-скрипт `lint:server-ui:jinja` намеренно НЕ в цепочке `lint`/`lint:server-ui`, которую вызывает CI — проверено запуском `npm run lint` до/после, поведение не изменилось. Baseline — 8 находок после игнора J018, ни одна не реальный баг: 5 — false positive парсера djLint на корректном `{{ '{{' }}`-экранировании (документирует пользователю литерный синтаксис в `_chat_triggers_panel.html`/`admin_docs.html`), 2 стилистические рекомендации — сознательно оставлены видимыми, не подавлены дальше. Этап 0: 40% → 44%; общий прогресс roadmap: 52% → 53% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Не-роадмап фикс, попутно: исправлена вторая из трёх причин `[!]` «нестабильность browser-тестов в полном прогоне» | `[x]` | commit: 90e41a3; полный `tests/unit` 1168 passed, 25 skipped; `git diff --check` | `test_activity_event_runtime_backfill_enables_event_reads` — датозависимый флейк, не browser-тест. Использовал `now = datetime.now(timezone.utc)`, события на `now-1ч/-3ч/-6ч`, затем `get_chat_activity_daily_series(days=1)` бакетирует по календарному дню UTC (`repositories.py:1896`) и тест ждёт ровно 3 события в бакете «сегодня». В окне 00:00-06:00 UTC событие «6ч назад» падало на вчера и выпадало из подсчёта. Причина понята математически точно чтением bucketing-логики, не догадкой. Фикс без новых зависимостей: `now` заякорен на полдень UTC (`replace(hour=12,...)`) — `now-6ч`=06:00, гарантированно тот же день. Объясняет более раннюю находку в этом же журнале (2026-08-16, Opus): «датозависимым флейком (переход полночи в рамках сессии)» — тогда констатирован, не исправлен. Третья причина (`test_server_admin_broadcast_composer_handles_photo_and_reactions`) остаётся неподтверждённой |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры): закрыт мастер-пункт «зафиксировать все сценарии и состояния» по факту накопленных sub-slice | `[x]` | commit: f20b2c2; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Итог за сессию по Этапу 5: 7 из 9 чек-лист-пунктов закрыты (сценарии/состояния, SSE/reconnect с исправленным багом, унификация компонентов, touch-first, длинные имена/max игроков, Playwright create/action/finish, JS-экстракция в единый модуль), 2 реальных бага найдены и исправлены (WS reconnect застревал навсегда; невидимый текст результата партии). Осталось 2 пункта, оба сознательно отложены с документированным обоснованием, а не пропущены молча: разбиение `_games_dashboard.html` на per-mode partials (архитектурное решение — контент режимов переплетён с общими reveal/score-секциями, не чистые последовательные блоки) и visual snapshot-тестирование с маскированием (нужна новая pixel-diff инфраструктура для веб-страниц; в проекте есть аналог только для другой подсистемы — генерации картинок в Telegram). Этап 5: 60% → 68%; общий прогресс roadmap: 51% → 52% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры): исследован (не выполнен) пункт «visual snapshots с маскированием» — задокументирована причина осознанной отсрочки | `[ ]` | commit: 5c2d0f8; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Проверил: в проекте уже есть pixel-diff инфраструктура (`tests/unit/test_visual_snapshots.py`, golden PNG + `ImageChops.difference`), но она покрывает генерацию картинок для Telegram (family tree, activity chart), не веб-страницы этой инициативы. Для games такой инфраструктуры нет — все скриншоты сессии (114-121) были ручным просмотром, не автоматическим тестом. Настоящая реализация требует отдельных решений: хранение/обновление baseline без ложных срабатываний от рендер-дрифта между машинами, маскирование динамических `game_id`/времени перед снимком, отдельный порог допуска для games с учётом уже отмеченной нестабильности части browser-тестов. Решил не форсировать в конце длинной сессии — отложено с честной документацией. Этап 5 без изменений (60%) |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 9/N: «унифицировать lobby/create/active/results/error компоненты» проверено и закрыто, дозафиксировано непокрытое lobby-состояние | `[x]` | commit: a964e31; новый тест в `test_web_games_dashboard_browser.py` (18/18 в файле); полный `tests/unit` 1168 passed, 25 skipped; 3 PNG (121) в `~/selara_screen`; `git diff --check` | Не поверил чек-листу на слово — прочитал CSS: все панели (`.create-game-panel`, `.game-panel` для active И lobby одновременно, `.recent-games-panel`) расширяют общий `.panel`, получающий единый `.page-games .panel {border: 3px solid #1d2340; box-shadow: var(--shadow)}` + одинаковый градиентный оверлей (panel.css:1393-1412) — унификация реальна на уровне кода, не только по ощущению от скриншотов. Единственное фактическое расхождение уже найдено и исправлено в sub-slice 4 (`.banner-ok` невидим на карточках результатов). Обнаружил пробел: ни один из 8 предыдущих sub-slice не рендерил игру в статусе `lobby` (создана, не запущена) — все тесты сразу вызывали `store.start()`. Новый тест создаёт лобби без старта, проверяет overflow на 3 viewport и подтверждает переиспользование того же `.game-spotlight`/`.game-meta-grid`/`.action-flow` каркаса, что active/results состояния — унификация подтверждена визуально, не предположена. Error-состояние — общий `.banner-error` из `base.html`, единообразен по всему проекту, отдельной games-специфичной проверки не потребовалось. Этап 5: 48% → 60%; общий прогресс roadmap: 49% → 51% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 8/N: чек-лист-пункт «Playwright: создание/подключение/ход/завершение» закрыт полностью | `[x]` | commit: b367937; новый `test_web_games_action_flow_browser.py` 2/2; полный `tests/unit` 1167 passed, 25 skipped; `git diff --check` | «Подключение» не тестируется намеренно — грепом подтверждено: в веб-панели нет ни одной join-кнопки, вход в лобби только через Telegram-бота. «Ход» и «завершение» закрыты через реальный dice-лобби (тот же `_web_client`/`GameStore` харнесс, что и dashboard-тесты — не ручная Jinja-сборка, чтобы не повторить баг с забытым `extra_scripts` из предыдущего среза): клик «🎲 Бросить» → `POST /app/games/action` с `callback_data=gdice:<id>:roll`; клик «🛑 Завершить» → `callback_data=game:cancel:<id>`. По пути поймал мелкий баг в самом тесте (не в проде) — `route.request.post_data` возвращает form-urlencoded строку БЕЗ декодирования (`gdice%3Afc0b...`), мой `_parse_form_urlencoded` сравнивал сырые проценто-кодированные значения; исправлено `urllib.parse.unquote_plus`. Оба действия используют тот же делегированный submit-handler, что уже проверен на create — подтверждает единый механизм работает одинаково для игровых действий. Чек-лист-пункт закрыт по всем частям для общего механизма; per-mode дублирование для остальных 8 режимов сознательно не делалось — риск сосредоточен в shared-коде, не в конкретных callback_data. Этап 5: 43% → 48%; общий прогресс roadmap остаётся 49% (345/7≈49.3%, округление не меняется) |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 7/N: первый реальный browser-interaction тест для games.js (kind-picker клик → сабмит → перехват payload) | `[~]` | commit: 7c1da41; новый `test_web_games_create_flow_browser.py` 2/2; полный `tests/unit` 1165 passed, 25 skipped; `git diff --check` | Первый тест во всех играх, который реально управляет извлечённым `games.js` через клики, а не только проверяет статический рендер. Первая версия теста ложно проходила бы: рендерил `games.html` напрямую через Jinja для минимального фикстура и забыл `extra_scripts=["games.js"]` в контексте — скрипт не грузился вообще (подтверждено логом сетевых запросов, `games.js` отсутствовал среди них). Нашёл методичной диагностикой (console/pageerror пустые → ручная проверка dashboardRoot/click-delegation через evaluate → инъекция console.log-маркеров прямо в текст games.js перед подачей — не сработали ни разу → сравнение с реальным `games_page()` в app.py, который передаёт `extra_scripts` правильно). Оба теста теперь реально проверяют: клик по kind-picker обновляет hidden input и live-preview, сабмит формы шлёт верный `kind`/`chat_id` в `POST /app/games/create`, ошибка сервера показывает toast с текстом сообщения. «Подключение»/«ход»/«завершение» для конкретных режимов остаются непокрытыми — нужны отдельные mode-specific срезы. Этап 5: 40% → 43%; общий прогресс roadmap: 48% → 49% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры): исследован (не выполнен) пункт «разделить `_games_dashboard.html` на partials» — задокументирована причина осознанной отсрочки | `[ ]` | commit: 3d0f59f; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся | Прочитал структуру `games-list` (942 из 1244 строк). Формулировка пункта («partials по игровым режимам») предполагает 9 чистых последовательных `{% if game.kind == "X" %}` блоков — на деле контент mode-specific разбросан по НЕсмежным `{% if %}`-блокам, переплетённым с общими reveal/score-секциями, которые одновременно обслуживают несколько режимов (пример: `{% if game.kind == "bredovukha" %}Табло раунда{% else %}Текущий счёт{% endif %}` — один блок и для bredovukha, и для остальных 8 режимов). Честный per-mode split — архитектурное решение (нужен shared-shell macro/partial с mode-specific slot'ами), а не механический перенос: ошибка в нём тихо ломает конкретный режим без видимого сигнала при неполной визуальной проверке всех 9. Верхнеуровневый 4-way split (`metrics-grid`/`create-game-panel`/`games-list`/`recent-games-panel`) остаётся безопасным и всё ещё не сделан, но малополезен сам по себе без per-mode части. Решил не форсировать рискованный рефакторинг в конце длинной сессии ради галочки — отложено с честной документацией, а не пропущено молча. Этап 5 без изменений (40%) |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 6/N: «длинные имена, максимальное количество игроков и reconnect» закрыт полностью | `[x]` | commit: 0eccff0; `test_web_games_dashboard_browser.py` дополнен 2 параметризованными случаями (17/17 в файле); полный `tests/unit` 1163 passed, 25 skipped; 2 PNG (120) в `~/selara_screen`; `git diff --check` | Длинные имена и reconnect уже были закрыты в предыдущих sub-slice — оставалось только «максимальное количество игроков». Прочитал `GameStore.join()`: верхнего предела на размер лобби нет вообще, единственный реальный потолок — банк карточек whoami/spy (61 в крупнейшей категории у whoami). Написал стресс-тест: лобби на 24 игрока (`whoami`, `mafia`) с намеренно длинными именами участников, реальный `create_lobby`/`join`×23/`start`. Overflow нет ни на одном из 3 viewport ни в одном из двух режимов; в mafia отдельно проверил визуально сетку ночных действий (24 карточки хода) — переносится в несколько строк без переполнения, имена корректно обрезаны. Этап 5: 38% → 40%; общий прогресс roadmap остаётся 48% (337/7≈48.1%, округление не меняется) |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры): пункт «touch-first без hover-only действий» проверен и закрыт без изменений кода | `[x]` | commit: dc0ac67; только правка `docs/WEB_UI_MODERNIZATION_TODO.md`, код не менялся, полный прогон не требовался | Грепом всех `:hover`-правил в `panel.css`, затрагивающих игровые классы: все hover-only переходы — декоративный `translateY`+тень на нефокусируемых информационных контейнерах (`.game-panel`, `.game-meta-card`, `.game-scoreboard`, `.game-action-section`, `.telegram-handoff`, `.live-chip`), не несут функционального смысла. Единственный настоящий интерактивный hover-элемент, `.whoami-category-card`, уже корректно спарен `:hover, :focus-visible` в одном правиле. Отдельно проверено на hover-reveal паттерны (`opacity:0`/`display:none`, гейтящиеся `:hover`) рядом с игровыми классами — не найдено ни одного. 44px touch target на `.game-kind-card` уже покрыт тестом sub-slice 1. Этап 5: 35% → 38%; общий прогресс roadmap: 47% → 48% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 5/N: последний крупный inline-JS во всём проекте (943 строки, `games.html`) вынесен в `games.js` | `[~]` | commit: b430017; новый `src/selara/web/static/games.js`; `test_web_games_live_updates_browser.py` обновлён (роутит `/static/games.js` на реальный файл) и остался зелёным без изменения логики теста; полный `tests/unit` 1161 passed, 25 skipped; ESLint зелёный (нашёл и починил 5 проблем); `inline_script_lines` cap 1942→0; Python compile; `git diff --check` | Чистейшая экстракция за весь проект: в скрипте не было ни одной Jinja-интерполяции (`grep '{{\|{%'` — 0 совпадений), скрипт и раньше читал данные только из DOM data-атрибутов, так что JSON data-island (как в economy.html/family.html) не понадобился вообще — просто перенос текста + подключение через уже существующий `extra_scripts=["games.js"]` механизм (`base.html` рендерит `<script type="module" src=...>` для каждого элемента списка). Формулировка чек-листа («на modules **по игровым режимам**») не выполнена буквально — WS/SSE/polling-инфраструктура и générique form snapshot/restore используются всеми 9 режимами сразу и переплетены с mode-specific sync-хелперами в одном цикле инициализации; честно зафиксировано как сознательно не сделанное, а не тихо пропущено. Побочный эффект от простого переноса текста в отдельный файл: ESLint (глобался по `static/*.js`, никогда не видел inline-скрипт) нашёл 5 реальных, ранее невидимых проблем — мёртвая функция `isInteractiveField` (объявлена, нигде не вызывается) и 4 `catch (error) {...}` блока с неиспользуемым биндингом (реальный caught-error нигде не читался — везде `// ignore` или `payload = null`); функция удалена, catch-блоки переведены на bare `catch {}`. **Самая сильная проверка сохранности поведения**: регресс-тест WS-reconnect бага из sub-slice 2/N не переписывался по логике — только источник скрипта сменился с содержимого HTML-страницы на `page.route("**/static/games.js", ...)`, читающий тот же файл с диска, — и тест остался зелёным, то есть реконнект-фикс переживает извлечение байт-в-байт. `EXPECTED_TEMPLATES` не менялся (games.html остаётся полноценным шаблоном), `INLINE_SCRIPT_TEMPLATES` — пусто впервые за весь проект. Заодно закрыт и отдельный чек-лист-пункт «Сохранить SSE/live update contracts и reconnect behavior» — контракт зафиксирован в sub-slice 2/N и подтверждён неизменным здесь. Этап 5: 23% → 35%; общий прогресс roadmap: 46% → 47% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 4/N: состояние «результаты партии» зафиксировано тестами — найден и исправлен реальный баг контраста (`.banner-ok` невидим на пастельных карточках результатов) | `[~]` | commit: 346bfb5; `test_web_games_dashboard_browser.py` дополнен 3 параметризованными случаями (15/15 в файле) с реальной проверкой luminance текста; tests-first перепроверено `git stash` на `panel.css` (luminance 0.90 до фикса, зелёный после); полный `tests/unit` 1161 passed, 25 skipped; `panel_css_lines` cap 6255→6270; 3 PNG (119) в `~/selara_screen`; `git diff --check` | Написал тест на состояние «результаты» (recent-games-panel) — при обязательном визуальном просмотре скриншота (не по тесту, тест на тот момент проверял только overflow) увидел пустой прямоугольник там, где должен быть текст итога партии («Хозяйка вечера победила...»). Проверил напрямую: текст РЕАЛЬНО присутствует в HTML-ответе сервера (`response_text` содержит полную строку) — то есть это не баг данных, а баг видимости. Причина: `.banner-ok`/`.banner-error` — общий на весь сайт компонент для flash/error баннеров вверху страницы, стилизован под тёмный фон приложения (`color:#c9ffef`/`#ffe4e8`, светлый текст). `.recent-result-banner` переиспользует этот же класс, но вкладывает его внутрь `.recent-game-card` — светлого пастельного фона (все 9 вариантов карточки по режимам, `#fff8ea`→`#cef1a7`→`#e5f6ff` и т.д., panel.css ~1781-1809). Контраст светло-мятного текста на светло-кремовом фоне — примерно 1:1, практически невидимо, хотя технически «отрендерено». Тот же паттерн бага, что раньше находили в error.html/audit.html — общий компонент, стилизованный для одного контекста, молча ломается при переносе в другой. Исправлено точечным `.page-games .recent-game-card .banner-ok/.banner-error` overrides — тёмно-зелёный/тёмно-красный текст, тот же приём, что уже стоит на `.recent-game-head strong/p` в этом же месте файла. Тест дополнен вычислением relative luminance по формуле WCAG (`_relative_luminance` через sRGB→linear) вместо простого «текст не пустой» — иначе регрессия того же класса снова прошла бы мимо теста. Этап 5: 18% → 23%; общий прогресс roadmap: 45% → 46% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 3/N: активная панель всех 9 игровых режимов зафиксирована тестами, overflow нет нигде | `[~]` | commit: 9a2f110; `test_web_games_dashboard_browser.py` дополнен параметризованным тестом (9 kinds) — 12/12 в файле; полный `tests/unit` 1158 passed, 25 skipped; 6 PNG (117–118, bunker+mafia) в `~/selara_screen`; `git diff --check` | Обобщённый helper `_create_started_game(store, kind=...)` — единая точка входа для всех 9 режимов вместо режимо-специфичных функций: учитывает разный `GAME_DEFINITIONS[kind].min_players` (2 у `dice`/`number`/`quiz`, 3 у `spy`/`bredovukha`/`whoami`/`zlobcards`, 4 у `mafia`, 6 у `bunker` — самый требовательный по составу). Все 9 режимов стартуют и рендерятся без overflow на 3 viewport (программная проверка `scrollWidth>clientWidth`). Скриншот-обзор — намеренно только 2 структурно нетипичных режима (`bunker`: уникальная панель выбора мест и самый большой ростер; `mafia`: ночь/день фазы, отдельная колонка «стол и статусы»), а не все 9 — 7 оставшихся используют тот же `.game-action-section`/`.game-meta-grid` каркас, что уже подтверждён скриншотом на whoami в sub-slice 1; дублировать тот же визуальный паттерн 7 раз — не углублённая проверка, а раздувание среза. Пункт «зафиксировать сценарии» теперь покрывает 9/9 режимов в состоянии «активная игра» плюс empty/create-game; не покрыты: результаты/завершение партии и error-состояния каждого режима — следующий под-срез. Этап 5: 10% → 18%; общий прогресс roadmap: 44% → 45% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) sub-slice 2/N: SSE/WS/fallback-poll reconnect контракт зафиксирован тестом — найден и исправлен реальный баг (клиент навсегда застревал на polling после обрыва WS) | `[~]` | commit: ac48a10; новый `test_web_games_live_updates_browser.py` 1/1; tests-first перепроверено `git stash` на `games.html` (падает `assert 1 > 1` до фикса, зелёный после); полный `tests/unit` 1149 passed, 25 skipped; `git diff --check`. Скриншотов нет: чисто поведенческий JS-фикс, ни одной визуально видимой строки не изменилось | `games.html` (~935 строк inline JS) держит одну WebSocket на игровую панель + один SSE-стрим + 4-секундный fallback-polling `/app/games/live`, переключаемые через `hasHealthyLiveTransport()`. Реконнект WS запускается только из `connectGameSockets()`, а её вызывает только `commit()` внутри `applyDashboardHtml()` (games.html:657) и инициализация страницы (951) — но «ничего не изменилось»-ветка `refreshDashboard()` (697-701) возвращается раньше, чем успевает дойти до `applyDashboardHtml`. Итог: обрыв WS → 30-секундный backoff (`wsDisabledUntil`) → fallback-polling каждые 4с → если состояние партии не меняется (тихое лобби), backoff тихо истекает, но переподключение никогда не запускается — клиент навсегда остаётся на polling вместо возврата на WS. Тест воспроизводит через fake `WebSocket`/`EventSource` (`page.add_init_script`, вместо `page.on("websocket")`/`page.route()` — нужен полный контроль над `error`/`close` и точным таймингом, который перехват реальных соединений не даёт) + `page.clock` (виртуальное время вместо реальных 30с ожиданий; из-за `refreshPromise`-guard в `refreshDashboard()` пришлось продвигать clock маленькими шагами по 4с с реальными паузами между ними — один большой `fast_forward` на 34с делает все `setInterval`-тики синхронно раньше, чем мокнутый fetch успевает резолвиться через реальный CDP round-trip, и `refreshPromise` схлопывает их в один вызов). Исправлено однострочным добавлением `connectGameSockets()` в «не изменилось»-ветку — вызов безопасен по построению (no-op внутри backoff-окна через `Date.now() < wsDisabledUntil`, пропускает игры, у которых уже есть живой сокет, через `gameSockets.has(gameId)`). SSE вне контракта этого фикса: у SSE нет app-level backoff, реконнект — нативное поведение браузера, чинить нечего. Этап 5: 5% → 10%; общий прогресс roadmap: 43% → 44% |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 5 (игры) начат: sub-slice 1/N — зафиксированы 3 базовых сценария (`test_web_games_dashboard_browser.py`) до любых изменений DOM, багов не найдено | `[~]` | commit: a541a87; новый файл 3/3 теста; полный `tests/unit` 1148 passed, 25 skipped; 9 PNG (114–116) в `~/selara_screen`; `git diff --check` | Первый пункт чек-листа Этапа 5 — «зафиксировать все игровые сценарии и состояния до изменения DOM». Рендер получен через **реальный** HTTP route `/app/games` (переиспользован `_web_client`-паттерн из `test_web_games_routes.py`: `GameStore` с настоящими `create_lobby`/`join`/`start`, а не ручная сборка Jinja-контекста) — структура `game.whoami_view`/`spy_view`/`spotlight`/etc. в `_games_dashboard.html` (1244 строки) слишком сложна, чтобы собирать вручную не разойдясь с реальной формой, в отличие от плоского контекста `admin.html`. Покрыты 3 состояния: пустое (нет чатов/игр), create-game (`.game-kind-grid`, 8 режимов, выбор чата, тема для «500 Злобных Карт»), один активный game (whoami, 3 игрока с намеренно длинными именами вроде «Ложка Огромная-Преогромная» — проверка long-name handling из чек-листа). Проверено программно (`scrollWidth > clientWidth`) на 3 viewport (1440/820/390) для всех 3 состояний плюс min-44px touch target на `.game-kind-card` — **overflow нет нигде**, `.game-kind-grid`/`.game-meta-grid` уже используют тот же `auto-fit, minmax()` паттерн, что был явно провалидирован в economy.html (закрыт без изменений). Длинные имена корректно обрезаются CSS `text-overflow`, не переполняют карточки meta-панели. 9 PNG вручную просмотрены, замечаний нет. Заодно (не отдельным срезом, попутно с этой работой) найдена и исправлена причина одного из трёх флаки browser-тестов из ранее заведённого `[!]` (см. следующую запись журнала). Этап 5: 0% → 5%; общий прогресс roadmap: 42% → 43%. Остаётся зафиксировать: 8 из 9 режимов (bunker/dice/mafia/number/quiz/spy/zlobcards, «500 Злобных Карт» уже частично видно в самом kind-grid), results/error/reconnect состояния, SSE/WS live-update контракт — это НЕ закрывает чек-лист-пункт целиком, только первый под-срез |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Не-роадмап фикс, попутно: исправлена одна из трёх причин `[!]` «нестабильность browser-тестов в полном прогоне» | `[x]` | commit: 268a19f; полный `tests/unit` 1145 passed, 25 skipped (было 1137 до +8 новых тестов из предыдущего среза); 15 прогонов флаки-теста в изоляции подряд зелёные; `git diff --check` | `test_admin_overview_prioritizes_tasks_without_overflow[390px]` читал `bounding_box()` skip-link'а после фиксированного `wait_for_timeout(180)`, а `.skip-link{transition:transform 140ms ease}` (`server-ui-foundation.css:31`) — запас всего 40мс. Под нагрузкой полного прогона (тысячи предыдущих ассертов, GC/scheduler jitter) этого не хватает, геометрия читается «в полёте» — тот же класс бага, что уже был найден и исправлен в `test_web_admin_shell_browser.py` (2026-08-15, commit 3ee68a4). Исправлено тем же способом: `page.add_style_tag(content="*, *::before, *::after { transition: none !important; }")` в `_mount()`. Причины двух других флаки-тестов (`test_activity_event_runtime_backfill_enables_event_reads` — не browser-тест вообще; `test_server_admin_broadcast_composer_handles_photo_and_reactions` — беглый просмотр `admin-broadcast.js` не выявил похожей гонки, disable кнопки происходит синхронно до `fetch`) остаются неподтверждёнными, не гадать и не чинить вслепую |
| 2026-08-17 | Claude (Sonnet, после handoff Opus) | Этап 4 закрыт полностью: «возврат назад после действий» — потеря вкладки «Настройки» после сохранения алиасов/триггеров/настроек чата | `[x]` | commit: 40aa5c4; узкий срез `test_web_chat_hub_routes.py` 27/27 (8 новых параметризованных); полный `tests/unit` 1137 passed, 25 skipped; `git diff --check`; Python compile. Скриншотов нет намеренно: срез не меняет ни одной строки шаблона/CSS/JS — только целевой путь редиректа в трёх Python-обработчиках; визуальная часть (рендер вкладки «Настройки» по `?tab=settings`) уже покрыта существующим `test_web_chat_navigation_browser.py` | Ранее найденный Opus дефект (см. предыдущую запись) исправлен. Все 15 вхождений `_with_message(f"/app/chat/{chat_id}", ...)` в трёх обработчиках (`update_chat_trigger`, `update_chat_alias`, `update_chat_setting`) заменены на `f"/app/chat/{chat_id}?tab=settings"` точечным `sed` по диапазону строк этих функций (7070–7477), diff проверен построчно — ровно 15 изменённых строк, ничего лишнего в остальном ~10700-строчном файле. Тесты писались по факту в обратном порядке (фикс уже стоял), поэтому «падение по правильной причине» перепроверено отдельно: `git stash` только `app.py` (тесты остались), прогнаны все 8 новых параметризованных случаев — все 8 упали именно на `assert location.startswith(...)` против настоящих до-фиксовых целей редиректа (`/app/chat/-1001?error=...` и т.п.), затем `git stash pop` и повторный прогон — все 8 зелёные. Новый тест покрывает save/delete по всем трём обработчикам, включая error-ветки (невалидный `alias_mode`, несуществующий `key` настройки, `source_trigger` не найден). Побочный эффект: так как `redirect_path` вычисляется один раз и переиспользуется и в HTML-, и в JSON-ветке ответа, это заодно чинит `redirect` в JSON-ошибках для fetch-пути (`chat-settings.js`), хотя баг проявлялся только на no-JS форме. Этап 4: 95% → 100%, закрыт полностью; общий прогресс roadmap остаётся 42% (297/7 ≈ 42.4%, округление не меняется) |
| 2026-08-16 | Claude (Opus, после handoff) | Этап 4: cross-page links проверены систематически — найден и исправлен битый редирект админки | `[~]` | commit: 6f32f1e; 1137 unit (новый `test_web_cross_page_links.py` 3/3); `git diff --check`; Python compile. Скриншотов нет намеренно: срез не меняет ни одного пикселя разметки — только целевой путь редиректа и тесты | Вместо выборочной ручной проверки ссылок написан систематический тест: он извлекает все внутренние ссылки из `app.py`, `presenters.py`, `user_docs.py`, `admin_docs.py` и всех шаблонов (114 уникальных) и сверяет каждую с реальной таблицей маршрутов FastAPI (90 routes, включая `Mount`). **Найден реальный битый маршрут**: 15 JSON-ответов админки при истёкшей сессии отдавали клиенту `redirect="/admin/login"`, тогда как страница входа админки зарегистрирована как `/app/admin/login` — проверено живым запросом: `/admin/login` → **404**, `/app/admin/login` → 200. HTML-ветки редиректов везде использовали правильный путь (в одном месте буквально соседняя строка), поэтому дефект был виден только на fetch/XHR-путях админки и ни один page-level тест его не ловил. Исправлены все 15 вхождений. `/miniapp/*` вынесен в задокументированный allowlist — этими путями владеет React Mini App SPA, FastAPI их намеренно не регистрирует (проверено: единственное использование — `redirect_base_path` в `/api/miniapp/games/action`, то есть клиентский роут для Telegram WebApp). Вторая половина пункта («возврат назад после действий») **не закрыта**: найден и подтверждён чтением кода дефект потери вкладки после сохранения алиасов/триггеров, зафиксирован в чек-листе Этапа 4 с точным списком мест — работа остановлена по команде пользователя до его исправления. Этап 4: 91% → 95%; общий прогресс roadmap: 41% → 42% |
| 2026-08-16 | Claude (Opus, после handoff) | Этап 4: `audit.html` закрыт — timeline подтверждён, но найдено и исправлено ложное «событий нет» при сбое загрузки | `[x]` | commit: 92b4244; 1134 unit (в `test_web_chat_audit_browser.py` было 4, стало 6; один существующий тест переписан — он закреплял баг); 6 PNG (108–113) в `~/selara_screen`; `git diff --check` | Пункт был помечен как «если не завершён в admin-этапе» — сначала **проверил заявленное, а не поверил чекбоксу**: timeline действительно реализован (`<ol class="audit-timeline">`, группировка по датам, tone-маркеры, `<details>` с техническими деталями, фильтры с валидацией, 2 media-запроса в `audit.css`), все 32 CSS-класса шаблона реально определены (тот же греп-чек, что вскрыл дыру в `error.html`, — здесь чисто). Но при проверке состояний найдены **два реальных дефекта, которых не было в покрытии**. (1) **Ложное сообщение на security-поверхности**: при исключении в `list_audit_logs` роут отдаёт `entries=[]` + `audit_load_error`, и шаблон уходил в общую пустую ветку «Событий пока нет. Новые изменения и действия бота появятся здесь автоматически» — то есть админу журнала аудита сообщалось, что в чате ничего не происходило, тогда как на самом деле запрос упал и содержимое журнала неизвестно. Добавлена отдельная ветка `{% if audit_load_error %}` с честной формулировкой («это сбой чтения, а не отсутствие событий») и действием «Обновить страницу». **Существующий тест `test_chat_audit_empty_and_load_error_states_explain_recovery` закреплял этот баг** — он рендерил ровно эту комбинацию и утверждал, что «Событий пока нет» видно; переписан в `test_chat_audit_load_error_never_claims_the_log_is_empty`. (2) **Мёртвое действие**: кнопка «Сбросить фильтры» в пустом состоянии рендерилась безусловно, в том числе когда фильтров нет вообще — клик перезагружал ту же пустую страницу. Теперь показывается только когда фильтры реально активны; условие вынесено в `{% set audit_has_active_filters %}` и переиспользовано в шапке фильтров, чтобы логика не разъехалась. Этап 4: 82% → 91%; общий прогресс roadmap: 40% → 41% |
| 2026-08-16 | Claude (Opus, после handoff) | Этап 4: `error.html` закрыт — вся визуальная часть страницы ошибок была не стилизована | `[x]` | commit: c41d4de; 1132 unit (новые: `test_web_error_page.py` 12/12 — распознаваемость 404/405, recovery actions, request id + его наличие в логе, отсутствие request id на 4xx, реальная стилизованность chip/кода/карточек, баланс desktop-колонок и кнопки в одну строку, отсутствие тройного дублирования статуса, no-overflow на 1440/820/390); ESLint; Stylelint (список server-ui — чисто); `git diff --check`; Python compile; 6 PNG (102–107) отрендерены через **реальную** цепочку обработчиков FastAPI и сохранены в `~/selara_screen` | **Найден и исправлен реальный баг того же класса, что `.landing-pill` на `login.html` и `.login-side` на `admin_login.html`**: `error.html` использует 8 классов (`.error-hero`, `.error-copy`, `.error-meta`, `.error-code`, `.error-actions`, `.error-status-stack`, `.error-status-card`, `.status-chip`), и НИ ОДИН из них не был определён ни в одном CSS-файле (проверено грепом по всем `static/*.css` и подтверждено runtime-замером `getComputedStyle`: у `.status-chip` был прозрачный фон, нулевой padding и нулевой радиус). При этом в `panel.css` лежали 4 живых правила `.error-panel`, которых нет ни в одном шаблоне — остаток от предыдущей версии страницы. То есть шаблон переписали, а стили не перенесли. Добавлены стили для всех 8 классов, мёртвый `.error-panel` удалён (включая упоминание в общем селекторе заголовков). **Второй реальный дефект, найденный при обязательном ручном просмотре скриншотов** (не тестом): `.error-hero` наследовал от `.hero` сетку `minmax(0, 1.7fr) auto`, где правая колонка тянется по содержимому — на 1440px заголовок «Страница не найдена» ужимался в ~290px колонку и переносился на 3 строки, а две кнопки возврата разъезжались в столбик; карточки при этом занимали почти всю ширину. Переопределена сетка `minmax(0, 1fr) minmax(0, 0.85fr)`, добавлен geometry-тест (ширина колонки ≥480px, соотношение колонок в диапазоне, кнопки на одной строке). Убрано тройное дублирование статуса (chip + числовой код + строка «404 · не найдено» в карточке) — карточка теперь несёт только объяснение. **Request ID**: сквозного request/correlation-id в проекте не существовало (греп `request_id` находит только подтверждения бана/персоны в `moderation.py`, к HTTP отношения не имеющие), поэтому пункт «request ID при наличии» реализован минимально и по делу: для любого 5xx генерируется короткий `secrets.token_hex(4)`, он логируется В ТОМ ЖЕ log-record, что и traceback (id генерируется до `logger.exception`), показывается пользователю отдельной карточкой и отдаётся в JSON-ветке; для 4xx намеренно НЕ генерируется (проверено тестом) — там нечего искать в логах. Этап 4: 73% → 82%; общий прогресс roadmap: 39% → 40% |
| 2026-08-16 | Claude (параллельный под-агент, git worktree) | Этап 4: `economy.html` закрыт — найден и исправлен серьёзный баг: весь inline JS страницы был мёртв в продакшене (JS syntax error) | `[x]` | commit: dacafd5; 1120 unit (новые: `test_web_economy_browser.py` 6/6: JSON data-island без `undefined`-утечки, реальный рендер без JS-ошибок + отрисовка графика, submit-guard на кнопке покупки, no-overflow на 3 viewport); ESLint (новый `economy.js`); baseline guard `INLINE_SCRIPT_TEMPLATES` — `economy.html` удалён из списка; скриншот-обзор desktop/tablet/mobile; `git diff --check` | **Найден и исправлен реальный, серьёзный баг**: inline `<script>` читал `{{ market_rows_json|safe }}` и `{{ trade_points_json|safe }}`, но `_build_chat_economy_page_context()` в `app.py` передавал в контекст только `market_rows`/`trade_points` (без суффикса `_json`) — эти два Jinja-имени были ВСЕГДА `undefined`, что рендерилось в `const marketRows = ;` — синтаксическая ошибка JS, убивающая ВЕСЬ inline-скрипт целиком. На практике это означало: drag&drop фермы/инвентаря, кнопки «Купить»/«Снять лот», форма «Выставить лот», фильтры и сортировка рынка, график истории цен — АБСОЛЮТНО ничего из этого не работало в продакшене, подтверждено программно (рендер + `re.search` на реальный вывод дал `'const marketRows = ;'`, затем воспроизведено в браузере: до фикса JS падает с `SyntaxError`, после — 0 ошибок в консоли и график реально рисуется). Заодно это был XSS-риск того же класса, что уже проверялся в family.html: `|safe` на предположительно-JSON-строке не эквивалентен HTML-safe `|tojson` (хотя тут баг был настолько серьёзным, что переменная была просто пустой — эксплуатировать было нечего, но паттерн `|safe` на JSON в принципе небезопасен). Исправлено заменой на `<script type="application/json" id="economy-page-data">{{ {"chat_id":..., "trade_points":...}|tojson }}</script>` (тот же паттерн, что в family.html), вынесено 186 строк inline JS в `economy.js`. Заодно (в рамках задачи «forms»): кнопки «Купить»/«Снять лот»/форма «Выставить лот» не имели защиты от повторной отправки — добавлен `withInFlightGuard()` (disable кнопок на время запроса), тот же класс защиты, что уже стоит в `chat-settings.js`/`login-form.js`. «Responsive data presentation»: `.economy-plot-grid`/`.economy-inventory-grid`/`.economy-market-grid` уже используют `auto-fit, minmax(min(100%,180px),1fr)` — проверено скриншотом на 3 viewport, реально адаптивно, изменений не потребовалось. Этап 4: 64% → 73%; общий прогресс roadmap: 37% → 39% |
| 2026-08-16 | Claude | Этап 4: `family.html` закрыт — responsive graph, empty state, JS extraction | `[x]` | commit: pending; 1114 unit (новые: `test_web_family_routes.py` 2/2; обновлён `test_web_template_security.py` под новую разметку JSON data-island, security-свойство сохранено); ESLint (новый `family-graph.js`); baseline guard `INLINE_SCRIPT_TEMPLATES` — `family.html` удалён из списка (JSON data-island не считается inline-скриптом, детекция в тесте уточнена); cap `panel_css_lines` 6165 → 6170; скриншот-обзор desktop/tablet/mobile + empty state; `git diff --check` | Найдены и исправлены 3 реальных проблемы. (1) **Responsive graph controls** (был полностью не адаптирован): `.family-svg` имел фиксированные `width:980px;height:620px`, а HTML-карточки узлов позиционировались абсолютными пикселями в той же системе координат — на mobile-панели (~340px) граф раньше просто обрезался/скроллился (`.family-scene{overflow:auto}`). Добавлена обёртка `.family-scene-inner` (980×620, `transform-origin:top left`) вокруг SVG и узлов вместе — JS вычисляет `scale = min(1, containerWidth/980)` и применяет `transform:scale()` к обёртке целиком, сохраняя относительные позиции узлов и линий синхронными; высота `.family-scene` тоже пересчитывается, чтобы не оставалось пустого места. (2) **Empty state** (отсутствовал полностью): пользователь без семейных связей в чате видел полностью пустую панель графа без единого пояснения — `list_family_graph` легко возвращает `node_user_ids=()` для изолированных пользователей. Добавлен `{% if family_nodes %}...{% else %}<p class="empty-text family-empty">...{% endif %}` с понятным объяснением. Заодно найден и исправлен смежный баг: `focus_label` брался только из списка `family_nodes`, и при пустом графе падал на сырой `f"user:{id}"` вместо настоящего имени — теперь резолвится через `_resolve_chat_member_label()` (тот же helper, что и для остальных узлов), проверено новым тестом с изолированным пользователем. (3) **JS extraction**: 113 строк inline `<script>` с Jinja-интерполяцией `{{ family_nodes|tojson }}` вынесены в `family-graph.js`, данные передаются через `<script type="application/json" id="family-graph-data">{{ ...|tojson }}</script>` (JSON data-island, новый паттерн в кодовой базе) вместо прямой инъекции в исполняемый JS. **Явно перепроверено, не ослаблено**: существующий security-тест на XSS через `</script>`-инъекцию в имени участника (`test_family_graph_json_cannot_break_out_of_script_tag`) адаптирован под новую разметку и по-прежнему проходит — Jinja `tojson` экранирует `<`/`>` в JSON-данных независимо от контекста. Этап 4: 55% → 64%; общий прогресс roadmap: 36% → 37% |
| 2026-08-16 | Claude | Этап 4: `chat.html` полностью закрыт — последний, 7-й из 7 под-срезов (mobile navigation) | `[x]` | commit: 27692cb; 1112 unit (новые: `test_web_chat_navigation_browser.py` 11/11: no-overflow на 4 viewport × 6 активных табов, видимость активного таба без скролла на всех 6 табах, touch target ≥44px); `git diff --check` | Без изменений в продакшн-коде — `.section-tabs` (`flex-wrap: wrap` + `overflow-x: auto` + `min-height: 44px` на кнопках) уже корректно обрабатывает 6 ссылок навигации на узких viewport: чисто переносится в несколько рядов на 390px и даже на 320px, без горизонтального переполнения страницы, активная вкладка всегда полностью видна без скролла на всех 6 табах, touch target 48px (выше минимума 44px). Проверено скриншотом (визуально чисто) и измерениями в реальном браузере (`scrollWidth`/`getBoundingClientRect`), не только на глаз. **`chat.html` закрыт полностью — все 7 из 7 под-срезов**: вынос inline JS (`chat-overview.js`/`chat-settings.js`), разбивка на 5 partials, рейтинг/статистика (найден и исправлен over-fetch баг в live-refresh), настройки/permissions (найден и исправлен баг рассинхронизации JS/Python humanize-логики для 6 булевых настроек), aliases/triggers (добавлена защита от случайного удаления), live updates (проверен весь SSE-контракт, добавлено первое тестовое покрытие), mobile navigation (проверено и подтверждено тестами). Этап 4: 53% → 55%; общий прогресс roadmap не изменился (36%, округление) |
| 2026-08-16 | Claude | Этап 4: `chat.html` — под-срез 6 из 7 (live updates): проверен весь SSE-контракт, добавлено первое покрытие тестами (было 0) | `[~]` | commit: b2827cd; 1101 unit (новые: `test_chat_live_stream_delivers_real_event_types_client_listens_for`, `test_chat_live_stream_rejects_chats_the_user_cannot_see` — до этого `/api/live/stream` не имел вообще никаких тестов); `git diff --check` | Без изменений в продакшн-коде — глубокая проверка показала, что live-механизм уже устроен правильно. Проверено конкретно, не на глаз: (1) все 3 типа событий, которые слушает `chat-overview.js` (`chat_activity`/`new_vote`/`chat_refresh`), реально публикуются из 3 разных мест кода (`activity_batcher.py`, `presentation/handlers/engagement.py`, `_publish_chat_live_event` в `app.py`) — первое впечатление, что `chat_activity`/`new_vote` могут быть мёртвыми (raз все вызовы `_publish_chat_live_event` используют дефолтный `chat_refresh`), оказалось ОШИБОЧНЫМ после более широкого грепа по всему репо — не стал действовать на непроверенную гипотезу; (2) keep-alive/ping (`: ping\n\n`) уже реализован в `_stream_live_events`; (3) `beforeunload`-очистка `EventSource` уже корректна, так как переключение табов — это полноценная навигация (разные URL), а не SPA-переход без перезагрузки; (4) auth/видимость чата (403 для недоступных чатов) уже проверяется на сервере. Добавлены 2 теста через реальный ASGI-роут `/api/live/stream` (не заглушка) с фейковым брокером (`GAME_STORE._broker` — публичное свойство `live_broker` без сеттера, инжектится через приватный атрибут), читающие настоящий streaming-response и проверяющие точный SSE-формат (`event: <type>\ndata: <json>\n\n`). Этап 4: 52% → 53%; общий прогресс roadmap не изменился (36%, округление) |
| 2026-08-16 | Claude | Этап 4: `chat.html` — под-срез 5 из 7 (aliases/triggers): найдена и закрыта реальная защита от случайного удаления | `[~]` | commit: d4f5052; 1099 unit (новые: `test_web_chat_aliases_triggers_browser.py` 3/3); ESLint (новый `chat-plain-forms.js`); полный прогон подтвердил 1 нестабильный тест в несвязанном файле (`test_web_admin_broadcast_form_browser.py`) — прошёл в изоляции, флейк, не регрессия от этого среза; `git diff --check` | Формы алиасов/триггеров — обычные (не fetch/AJAX) POST-формы, в отличие от settings-форм из прошлого под-среза, и совсем без JS-защиты: кнопка «Удалить» отправляла запрос на безвозвратное удаление одним кликом без подтверждения и без защиты от повторной отправки (двойной клик = 2 identical запроса, второй просто получил бы «не найдено»). Добавлен `data-confirm-delete="..."` атрибут на обе delete-кнопки (алиасы и триггеры) с текстом, включающим конкретное имя удаляемого объекта, и новый `chat-plain-forms.js`: `window.confirm()` перед отправкой формы с этим атрибутом (та же техника `window.confirm`, что уже использовалась в `chat-settings.js` как fallback для docs-guard без `<dialog>`) + отключение всех submit-кнопок формы при реальной отправке. Затронуло оба typа: алиасы (`/app/chat/{id}/aliases`) и смарт-триггеры (`/app/chat/{id}/triggers`) — идентичная разметка `button[name=action][value=delete]` в обоих partials. Этап 4: 51% → 52%; общий прогресс roadmap: 35% → 36% |
| 2026-08-16 | Claude | Этап 4: `chat.html` — под-срез 4 из 7 (настройки и permissions): найден и исправлен реальный баг рассинхронизации humanize-логики между Python и JS | `[~]` | commit: a503889; 1096 unit (новые: `test_update_chat_setting_returns_humanized_display_for_boolean_keys`; обновлён `test_update_chat_setting_persists_alias_mode` — проверяет `current_value_display`; обновлён mock-JSON в `test_web_chat_overview_browser.py`); ESLint (удалено ~50 строк дублирующей логики из `chat-settings.js`); `git diff --check` | Права доступа (`manage_settings`) проверены и оказались уже корректны: и чтение (nav-ссылка «Настройки» скрыта без прав, сервер откатывает `tab=settings` на `overview` при прямом заходе по URL без прав), и запись (POST-роут заново проверяет `has_permission` на сервере, не доверяя клиенту) — без изменений, закрыто как «уже правильно», не как найденный баг. Реальный баг найден в другом месте: `chat-settings.js`'s `humanizeSettingValue()` содержал вручную скопированный список из 18 булевых ключей настроек для перевода `true/false` → `включено/выключено`, тогда как реальный источник истины `CFG_BOOL_KEYS` в `settings_common.py` содержит 24 ключа — 6 отсутствовали в JS-копии (`iris_view`, `antiraid_enabled`, `chat_write_locked`, `persona_enabled`, `save_message`, `interesting_facts_enabled`). После AJAX-сохранения этих 6 настроек (без перезагрузки страницы) UI показывал бы сырое `"true"`/`"false"` вместо русского текста — расхождение с исходным серверным рендером той же карточки. **Исправлено устранением самого класса бага**, а не патчем списка: `/app/chat/{id}/settings` POST теперь возвращает `current_value_display`/`default_value_display`, посчитанные той же функцией `setting_value_display()`, что уже используется для первого рендера страницы (единый источник истины); `humanizeSettingValue()` в JS удалён целиком (~50 строк), клиент просто использует то, что прислал сервер. Этап 4: 49% → 51%; общий прогресс roadmap не изменился (35%, округление) |
| 2026-08-16 | Claude | Этап 4: `chat.html` — под-срез 3 из 7 (рейтинг и статистика): найден и исправлен реальный over-fetch баг в live-refresh эндпоинте | `[~]` | commit: 5225e8f; 1095 unit (обновлён `test_web_chat_hub_routes.py`: `test_chat_overview_api_returns_live_summary` теперь также проверяет точный набор ключей ответа); `git diff --check` | Проверка `/api/chat/{id}/overview` против `chat-overview.js` (единственный вызывающий код — сверено грепом по всему репо, включая React-фронтенд в `frontend/src`) показала: эндпоинт вычислял и возвращал `roles`, `command_rules`, `audit_rows`, 3 варианта leaderboard (`top_activity`/`top_mix`/`top_karma`/`top_mix_7d`), `global_dashboard`/`local_dashboard`, `metrics`, `summary`, `dashboard_panels`, `access_rows`, `can_manage_settings` — но клиентский JS реально читает только `daily_activity`/`hero_of_day`/`richest_of_day`. Этот эндпоинт опрашивается на каждое SSE-событие (`chat_activity`/`new_vote`/`chat_refresh`) через `scheduleRefresh()`, то есть лишние тяжёлые запросы к БД (несколько вызовов `get_leaderboard`, `list_chat_role_definitions`, `list_audit_logs`, `_load_dashboard_if_exists` ×2, полный `build_chat_context(...)`) выполнялись на каждый live-тик впустую. Убраны все неиспользуемые вычисления и поля ответа, оставлены только 3 реально используемых. Заодно найдено (не исправлено, вне текущего под-среза, зафиксировано в чек-листе для «live updates»): `/api/chat/{id}/achievements` и `/api/chat/{id}/settings` существуют на бэкенде, но НИ ОДИН JS-файл (ни `chat-overview.js`/`chat-settings.js`, ни React-фронтенд) их не вызывает — похоже на недоделанную заготовку под такой же live-refresh для этих двух табов, а не на мёртвый код. Этап 4: 48% → 49%; общий прогресс roadmap не изменился (35%, округление) |
| 2026-08-16 | Claude | Этап 4: `chat.html` — под-срез 2 из 7 (разбит на 5 partials) | `[~]` | commit: d298898; 1095 unit (обновлён `test_server_ui_baseline.py`: новые partials в `EXPECTED_TEMPLATES`/`standalone_templates`); программная проверка байт-в-байт идентичности рендера до/после (overview/achievements — 100% идентично; settings — идентично после игнорирования пустых строк, разница только в переносах между секциями, без влияния на DOM/визуал); скриншот-обзор до/после визуально идентичен; `git diff --check` | 635-строчный `chat.html` (после выноса JS в прошлом под-срезе) разбит на 5 файлов по табам/секциям: `_chat_overview_tab.html` (229 строк), `_chat_achievements_tab.html` (33), `_chat_settings_panel.html` (96), `_chat_audit_and_aliases.html` (130 — аудит всегда виден + алиасы только на табе настроек, связаны общей `grid two-col` разметкой, разделять не стал), `_chat_triggers_panel.html` (86). Сам `chat.html` — 66 строк (5 `{% include %}` + общий hero/tabs/dialog). Каждый partial содержит свой `{% if active_tab == ... %}` изнутри — родитель просто безусловно инклюдит все 5, как уже сделано для `_games_dashboard.html`. Верификация была не «выглядит нормально на скриншоте», а построчное сравнение рендера старого/нового шаблона на одном богатом контексте (все ветки: dashboard_panels, roles, command_rules, leaderboards, achievement rows, settings toggle/select, aliases, triggers — заполнены). Остаётся 5 из 7 под-срезов: рейтинг/статистика, settings/permissions, aliases/triggers (контентные улучшения, не просто структура), live updates (риск — сохранить SSE-контракт), mobile navigation между секциями. Этап 4: 47% → 48%; общий прогресс roadmap не изменился (35%, округление) |
| 2026-08-16 | Claude | Этап 4: `chat.html` — под-срез 1 из 7 (вынос inline JS), реальный баг dirty-state для toggle-настроек найден и исправлен | `[~]` | commit: 189a203; 1095 unit (новые: `test_web_chat_overview_browser.py` 2/2 с реальным `page.route()`-перехватом fetch); ESLint (2 новых файла, `chat-settings.js` — 1 pre-existing `no-unused-vars` найден и исправлен); baseline guard `INLINE_SCRIPT_TEMPLATES` — `chat.html` удалён из списка; скриншот-обзор overview/settings × desktop/mobile; `git diff --check` | 688 строк inline `<script>` (2 IIFE-блока: live-обновление обзора/лидерборда через SSE+fetch, и настройки/dirty-state/docs-guard dialog) вынесены в `chat-overview.js`/`chat-settings.js` — построчно сверено с оригиналом перед удалением (без Jinja-интерполяции внутри скриптов, чистый перенос). Функциональность подтверждена НЕ построчным сравнением, а реальными Playwright-тестами через `page.route()`-перехват `/api/chat/.../overview` и `/api/chat/.../leaderboard` (не `page.set_content()` — та же ловушка с opaque origin, что документирована в памяти). **Найден и исправлен реальный, не связанный с самим переносом баг**: dirty-state (индикатор несохранённых изменений + docs-guard dialog) никогда не срабатывал для toggle-настроек — `getSavedValue()` полагался на `hidden.defaultValue`, но у `<input type="hidden">` в реальных браузерах `.defaultValue` синхронизируется с `.value` при программной записи (в отличие от text/textarea, где значения расходятся) — значит `isDirty()` для toggle всегда возвращал `false`. Исправлено тем же паттерном, что уже был для `<select>`: `data-setting-saved-value` теперь рендерится и для `input_kind="toggle"`, `getSavedValue()`/`applySettingState()` используют dataset вместо `defaultValue` для обоих кейсов. Верифицировано новым тестом (клик по toggle → `is-dirty` появляется → mock-сохранение → `is-dirty` пропадает). Остаётся 6 из 7 под-срезов: partials, рейтинг/статистика (контент), settings/permissions (контент), aliases/triggers (контент), live updates (сохранение SSE-контракта — риск), mobile navigation между секциями. Этап 4: 45% → 47%; общий прогресс roadmap: 34% → 35% |
| 2026-08-16 | Claude | Этап 4: `feedback.html` закрыт — submit guard, лимит на длину описания, подтверждены success/error банеры | `[x]` | commit: 55713ca; 1093 unit (новые: `test_web_feedback_browser.py` 7/7); скриншот-обзор desktop/tablet/mobile + mobile error state; `git diff --check` | Найден реальный пробел валидации: `title` ограничен и на клиенте (`maxlength=160`), и на сервере, а `details` — вообще нигде, `Text`-колонка в БД не ограничивает длину. Добавлен `maxlength="4000"` на `<textarea>` и парный серверный чек в ОБА дублирующих обработчика (`/app/feedback` form-POST и `/api/app/feedback` JSON-POST — обнаружено, что это два независимых, не переиспользующих друг друга пути с одинаковой валидацией, второй тоже не проверял длину). Submit guard (`data-feedback-form`/`data-feedback-submit`, `feedback-form.js`) добавлен по уже устоявшемуся паттерну (`login-form.js`/`admin-login.js`) — защита от повторной отправки формы двойным кликом. Success/error баннеры, empty state и все 3 серверные проверки на title/details уже существовали и работали корректно — закрыты тестами, не новым кодом. `.hero`/`.hero-side` mobile-раскладка уже была общим фиксом из среза `home.html`. Этап 4: 36% → 45%; общий прогресс roadmap: 33% → 34% |
| 2026-08-16 | Claude | Этап 4: `achievements.html` закрыт — статус получено/не открыто вынесен в отдельный pill вместо текста внутри плотной строки | `[x]` | commit: 4f4aca9; 1085 unit (новые: `test_web_achievements_browser.py` 5/5); baseline guard cap `panel_css_lines` поднят 6150 → 6165 (обоснованно: небольшой общий CSS-компонент `.achievement-row`/`.achievement-row-head`); скриншот-обзор desktop/tablet/mobile с намеренно длинными названием/описанием; `git diff --check` | «Progress semantics»: раньше состояние «получено»/«не получено» было спрятано внутри одной плотной строки `meta` (`глобал • легендарная • не получено • 0.42% / 3`) без единого визуального маркера. `build_achievement_rows()` (`presenters.py`) теперь возвращает `unlocked: bool` и `status_label`, шаблон рендерит отдельный `.pill`/`.pill-active` бейдж и притемняет всю строку класом `.is-locked` (opacity 0.62) для ещё не открытых наград — статус виден с одного взгляда, не только при чтении текста. Также переформулирован holders-текст (`42.31% участников (128)` вместо неоднозначного `42.31% / 128`). «Длинные названия»: подтверждено скриншотом с намеренно длинным (90+ символов) названием и описанием — существующая `.stat-row` grid-раскладка (`minmax(0,1fr) auto` + `min-width:0`) уже переносит текст по словам без обрезки на всех 3 viewport, код не менялся. Изменение общей функции `build_achievement_rows()` задело и `chat.html` (таб «Достижения» использует ту же функцию/разметку) — тот же шаблон-паттерн обновлён синхронно, получил тот же pill/`is-locked` эффект бесплатно, как побочный плюс (не отдельный незапланированный объём — тот же компонент). Этап 4: 27% → 36%; общий прогресс roadmap: 32% → 33% |
| 2026-08-16 | Claude | Этап 4: `home.html` закрыт — найден и исправлен реальный shared-баг наложения `.hero-side` chips на `.hero-copy` на mobile | `[x]` | commit: 2a40a5c; 1081 unit (новые: `test_web_home_browser.py` 5/5); baseline guard cap `panel_css_lines` поднят 6145 → 6150 (обоснованный рост: общий responsive-фикс + 6-карточные иконки); скриншот-обзор desktop/tablet/mobile; `git diff --check` | Найден баг того же класса, что раньше был отмечен `[!]`/deferred в решениях по адаптации доков: `.hero { grid-template-columns: minmax(0, 1.7fr) auto }` не имел вообще никакого mobile-брейкпоинта — `.hero-side` chips («браузер и telegram» и т.п.) визуально наезжали на текст `.hero-copy` на узких viewport (подтверждено скриншотом, не только `scrollWidth`, т.к. `overflow` был `False` — наложение без выхода за ширину страницы). Добавлен `@media (width <= 860px) { .hero { grid-template-columns: 1fr } .hero-side { justify-items: start } }` — тот же брейкпоинт 860px, что уже использовался для аналогичного фикса `.login-hero`. Класс `.hero`/`.hero-side` общий для 9 шаблонов (`home.html`, `games.html`, `chat.html`, `achievements.html`, `economy.html`, `feedback.html`, `family.html`, `user_docs.html`, `admin_docs.html`) — фикс автоматически чинит ту же проблему во всех них, не только в `home.html` (тот же паттерн, что раньше был у `admin_login.html`→`login.html`). Empty state (нет админ/недавних групп) и quick-links (клик по карточке группы) уже были в коде — закрыты тестами, не новым кодом. Этап 4: 18% → 27%; общий прогресс roadmap: 31% → 32% |
| 2026-08-16 | Claude | Этап 4: `landing.html` закрыт полностью — все 6 категорий возможностей вместо 3 curated | `[x]` | commit: 0069475; 1076 unit (обновлён `test_web_landing.py`: +1 тест на все 6 карточек); Stylelint (`panel.css` вне текущего lint-списка, изменения минимальны — только удаление мёртвого правила); скриншот-обзор desktop/tablet/mobile; `git diff --check` | По решению пользователя (Telegram: «все 6 красиво») убран index-хак (`games_card`/`eco_card`/`social_card` по индексам 1/2/3+4 с "+"-склейкой kicker'ов), заменён на `{% for card in feature_cards %}`. Добавлено поле `icon` в каждый `feature_card` (`presenters.py`), в шаблоне — `{% if/elif %}` по `card.icon`, 3 новых SVG-иконки (commands/family/management) для категорий, которые раньше вообще не показывались. Удалено мёртвое CSS-правило `.card-social { grid-column: span 2 }` (в обоих брейкпоинтах) — оно было нужно только для 3-карточной раскладки с "осиротевшей" третьей карточкой в 2-колоночной сетке; с 6 карточками существующая `repeat(3,...)`/`repeat(2,...)`/`1fr` сетка и так даёт сбалансированные ряды без него, подтверждено скриншотами на 1440/820/390px. Этап 4: 12% → 18%; общий прогресс roadmap: 30% → 31% |
| 2026-08-16 | Claude | Этап 4: `login.html` закрыт — submit guard, mobile keyboard, найден и исправлен реальный CSS-баг | `[x]` | commit: 3af6209; 1075 unit (новые: `test_web_login_browser.py` 6/6); ESLint (новый `login-form.js`); Stylelint (`server-ui-foundation.css`, autofix на modern color notation); HTMLHint ×10 (новый `render_login_fixture.py`); скриншот-обзор desktop/tablet/mobile + mobile error state; `git diff --check` | Найден и исправлен реальный визуальный баг: `.landing-pill-row`/`.landing-pill`/`.landing-pill-soft` использовались в `login.html` («одноразовый код» / «вход через Telegram» / «сессия браузера»), но нигде не были стилизованы — рендерились голым текстом без всякого pill-вида. Стили добавлены в `server-ui-foundation.css`, переиспользуя визуальный язык `.stats-pill`/`.card-pill-soft`. Submit guard (`data-login-form`/`data-login-submit`, `login-form.js`) добавлен по аналогии с уже существующим `admin-login.js` — до этого форму кода можно было отправить повторным кликом. `id`/`for` добавлены на поле кода для HTMLHint `input-requires-label` (та же находка, что раньше была у `admin_login.html`). Mobile keyboard: `inputmode="numeric"`/`autocomplete="one-time-code"`/`maxlength="6"` уже были на месте — закрыто тестом, не кодом. `.login-side`/`.login-side-card` mobile-наложение уже было закрыто раньше как побочный эффект фикса `admin_login.html` (общая разметка) — подтверждено скриншотами, не переисправлено. Ошибки (`/login` POST: rate limit, невалидный код, код не найден/истёк) уже рендерились через общий `base.html`-баннер — подтверждено скриншотом с реальным текстом ошибки. Этап 4: 6% → 12% |
| 2026-08-16 | Claude | Этап 4 начат: `landing.html` — hardcoded stat, dead context-поля, mobile/tablet overflow | `[~]` | commit: 44f9173; 1069 unit (новые: `test_web_landing.py` 4/4); Playwright (`scrollWidth`-проверка на 1440/820/390px); полный скриншот-обзор desktop/tablet/mobile; `git diff --check` | Найдены и исправлены 2 реальных бага: (1) статистика «реакций» в hero была захардкожена как `117+` в шаблоне вместо привязки к контексту — реальное значение `118` (`len(SOCIAL_COMMAND_KEY_TO_ACTION)`), причём `build_landing_context()` уже вычисляла верное число в поле `signal_cards`, которое шаблон вообще не рендерил; (2) `hero_eyebrow`/`signal_cards`/`overview_text`/`overview_pills` — 4 мёртвых поля контекста, ни разу не использованные ни в одном шаблоне (проверено грепом), удалены и заменены единственным реально нужным `social_action_count`. Заодно вычищены их устаревшие копии-заглушки в `test_web_templates.py` (не влияли на прохождение теста — Jinja молча игнорирует лишние kwargs — но были уже неточны относительно реального контекста). При работе найден и НЕ исправлен третий момент, требующий решения пользователя, не являющийся однозначным багом: `build_landing_context()` строит 6 `feature_cards`, но `landing.html` жёстко по индексам рендерит только 3 (карточки 1, 2, склейка 3+4 через "+"-kicker), карточки 0 и 5 не показываются никогда — зафиксировано `[?]` в журнале решений вместо блокировки. Полный прогон тестов (1069 passed) чистый; отдельно всплывший `test_activity_event_runtime_backfill_enables_event_reads` в общем прогоне оказался датозависимым флейком (переход полночи в рамках сессии), в изоляции проходит — подтверждено, не регрессия от этого среза. Этап 4: 0% → 6%; общий прогресс roadmap: 28% → 29% |
| 2026-08-16 | Claude | Адаптация документации под mobile/tablet — Responsive TOC/drawer закрыт | `[x]` | commit: 6473115; 1065 unit (новые: +1 в `test_server_ui_baseline.py`, +5 в `test_web_docs_quality.py`); ESLint/Stylelint (новый `docs-responsive.css`); HTMLHint ×9; Playwright (`scrollWidth`-проверка на 390px/820px для обеих страниц); `git diff --check` | `.docs-layout` схлопывается в 1 колонку на `@media (width <= 980px)` (тот же брейкпоинт, что `.overview-live-grid`), `.docs-nav` перестаёт быть sticky. Навигация — нативный `<details class="docs-nav-toggle" open>` с chevron-стилем, переиспользованным из `.admin-secondary-details` — не кастомный JS-drawer: доступен с клавиатуры и работает без единой строки JS. Подтверждено программно, не на глаз: `scrollWidth > clientWidth` было `True` (~45px переполнения) до фикса на обеих страницах на 390px, стало `False` на 390px и 820px везде. Найден, но не исправлен (вне scope — общий для всего сайта компонент, не специфичный для доков): `.hero-side` чипы наезжают на `.hero-copy` текст на mobile. Responsive TOC/drawer закрывает последний открытый пункт «Пользовательской документации» из чек-листа. Этап 3: ~38% → ~44%. |
| 2026-08-16 | Claude | Единый источник slash-команд: admin-only команды ролей/алиасов/настроек (5 из 7 срезов плана готовы) | `[~]` | commit: b287ee5; 1059 unit (новые: +2 в `test_command_catalog.py`, +1 в `test_web_admin_docs.py`); HTMLHint (реальный контекст, не только fixture); Playwright spot-check 3 карточек; `git diff --check` | 15 admin-only команд (роли/ранги/алиасы/настройки) добавлены в каталог с категорией `admin`, `dispatch_kind="slash"` — все проверены против точных `Формат:`-строк из error-сообщений реальных хендлеров (`moderation.py`/`aliases.py`/`settings.py`), ни одна не выдумана. Подтверждено (не предположено): ни у одной нет natural-language эквивалента. 2 новые карточки («Назначение и снятие ролей», «Кастомные роли») и 4 существующие переключены на источник. Slice 5 (admin-only часть, 5b) закрывает план на 5 из 7 срезов — remaining: help.py, генерация GUIDE.md. |
| 2026-08-16 | Claude | Единый источник slash-команд: gacha закрыт, admin_docs.py schema готова (5 из 7, план обновлён) | `[~]` | commit: 62ed37a; 1056 unit (новые: +3 в `test_command_catalog.py`, +1 в `test_web_user_docs.py`); HTMLHint ×9 (обновлён `render_admin_docs_fixture.py`); Playwright spot-check; `git diff --check` | Gacha исследован агентом отдельно (баннеры/парсинг/валюта/канал-гейт), ключевые факты (курс 10:1, `_GACHA_BANNER_ALIASES`) перепроверены самостоятельно, не приняты слепо. Найдены 2 реальных скрытых ограничения: курс обмена и обязательная подписка на служебный канал перед первым использованием. Операторские команды (`гача админ`/`вкл`/`выкл`, `/gachagive`) сознательно НЕ задокументированы — гейтятся одним хардкод-ID оператора бота, не ролями чата. Новая карточка в «Игры». Отдельно: `admin_docs.py`/`admin_docs.html` получили поля `commands`/`triggers` (те же блоки, что у `user_docs.html`) — подготовка к следующему под-срезу с 18 admin-only командами, само наполнение не в этом коммите. Осталось 2 полных + 1 частичный (наполнение 5b) из 7 срезов плана. |
| 2026-08-16 | Claude | Единый источник slash-команд: публичная часть social/admin/moderation, план уточнён (4 из 7 срезов плана) | `[~]` | commit: af1732d; 1053 unit (новые: +2 в `test_command_catalog.py`, +1 в `test_web_user_docs.py`); `git diff --check` | `misc_lastseen` и `misc_public_service_commands` добавлены в каталог, `user_docs.py`'s секция «Статистика и статус» переключена на источник. Найдено: у «Публичные служебные команды» не было `triggers` — `помощь` (эквивалент `/help`) теперь показан. **План уточнён**: обнаружено, что реальные admin-only команды управления ролями/алиасами/настройками (18 команд из `moderation.py`/`aliases.py`/`settings.py`) не могут просто переключиться на каталог как остальные — у `admin_docs.py`'s `_docs_item()` нет полей `commands`/`triggers` вообще, в отличие от `user_docs.py`. Это отдельный под-срез (5b), а не часть текущего. Осталось 3 полных + 1 частичный из 7 срезов плана. |
| 2026-08-16 | Claude | Единый источник slash-команд: категории family/relationships (3 из 7 срезов плана) | `[~]` | commit: 0e9fd11; 1050 unit (новые: +4 в `test_command_catalog.py`, +2 в `test_web_user_docs.py`); `git diff --check`; Playwright spot-check | 4 relationship-группы и 2 family-группы добавлены в каталог, обе секции `user_docs.py` переключены на источник. Верифицировано против `relationships.py`/`chat_assistant.py` и `catalog.py`'s trigger maps. **Найдены 2 реальные недокументированные команды**: `/adoptdaughter` (удочерение) и `/escapepet` (уйти из статуса питомца) — новая карточка «Уйти из семьи» закрывает обе, плюс `/escapefamily`, которая тоже отсутствовала. Осталось 4 из 7 срезов плана. |
| 2026-08-16 | Claude | Единый источник slash-команд: категория games (2 из 7 срезов плана) | `[~]` | commit: fb45af2; 1044 unit (новые: +2 в `test_command_catalog.py`, +1 в `test_web_user_docs.py`); `git diff --check` | `games_lobby` (`/game` + 7 режимов) и `games_role_reveal` (`/role`, `/start`) добавлены в каталог, `user_docs.py`'s секция «Игры» переключена на источник. Верифицировано против `game/router.py` (`Command("game"/"role"/"start")`) и `catalog.py`'s trigger maps. Найдено небольшое реальное улучшение по пути: у «Скрытая роль, карточка и ЛС» не было ни одного `triggers` — `роль`/`старт` оказались настоящими рабочими natural-language эквивалентами, просто не показанными в доке; теперь видны. Осталось 5 из 7 срезов плана. |
| 2026-08-16 | Claude | Единый источник slash-команд: schema + категория economy (1 из 7 срезов плана) | `[~]` | commit: 3b2d480; 1041 unit (новые: `test_command_catalog.py` 5/5, +2 в `test_web_user_docs.py`); `git diff --check` | Новый `command_catalog.py`: `CommandSpec` с `dispatch_kind` (slash/natural_language/both) — добавлено после находки, что часть команд (`/article`) диспетчеризуется через третий, ad-hoc regex-путь в обход и aiogram `Command()`, и `catalog.py`, что не ловится простым грепом. 5 economy command-групп заполнены переиспользованием уже верифицированного текста из среза про availability badges (не переписаны с нуля). `user_docs.py`'s экономика переключена на источник. Найден и закрыт **дубль до коммита**: `/article` чуть не получил вторую карточку — уже был описан в «Объявления, подписка и чатовые мемы»; вместо новой карточки этот существующий item частично переключён на каталог (только commands/triggers для article, остальное — своё). Тесты verify syntax/triggers каталога против реального кода (`Command("...")` в `economy.py`, trigger maps в `catalog.py`), а не только внутреннюю согласованность. Осталось 6 из 7 срезов плана (games, family/relationships, social/admin/moderation, settings/misc+gacha, help.py, USER_GUIDE/ADMIN_GUIDE). |
| 2026-08-16 | Claude | Этап 3: «Качество документации» закрыта, поиск добавлен в admin_docs, slash-команды помечены `[?]` | `[~]` | commit: 2e3b8f0; 1034 unit (новые: `test_web_docs_quality.py` 3/3, `test_web_admin_docs_browser.py` 1/1, +тесты search/determinism в `test_web_admin_docs.py`); ESLint/Stylelint (`docs-search.css` расширен); HTMLHint ×9; `git diff --check`; Playwright spot-check | Поиск добавлен на `admin_docs.html` (переиспользует `docs-search.css`/`.js` без изменений). **Найден и исправлен реальный баг** того же класса, что был у `.admin-table-card[hidden]` в прошлом срезе: `.docs-card{display:grid}` побеждал браузерный `[hidden]` — карточки, отфильтрованные поиском внутри частично совпадающей секции, оставались видимыми. Существующие Playwright-тесты поиска (написанные в прошлом срезе для user_docs) не ловили это, потому что проверяли только атрибут `[hidden]`, а не факт рендера — оба файла переведены на CSS-селектор `:visible`. Закрыты: heading hierarchy (без пропуска уровней на обеих страницах), keyboard navigation (реальный `.focus()` на поиске/копировании/deep-link), search index детерминированность и отсутствие защищённых данных (два вызова context-builder'а дают идентичный результат; реальный `chat_id`/`chat_title` не попадают в индекс). Slash-команды по разделам зафиксированы как `[?]` в журнале решений — объём (80–150+ команд, 904 строки в 3 файлах без готового каталога-источника) требует решения пользователя, пропущено без блокировки по его инструкции. Этап 3: ~31% → ~38%. Дальше: решение по slash-командам или сразу адаптация под mobile/tablet. |
| 2026-08-16 | Claude | Этап 3: «Администраторская документация» закрыта полностью (5 из 5) | `[x]` | commit: 6147a52; 1027 unit (2 новых теста в `test_web_admin_docs.py`); HTMLHint ×9 (обновлён `render_admin_docs_fixture.py` с примером examples/notes); Playwright spot-check 3 карточек; `git diff --check` | Добавлены секции «Рассылки» (текст/медиа, реакции, аудитория с поиском, история доставки, submit guard) и «Обслуживание» (backup, обзор таблиц, edit/delete, «Технический режим») — контент сверен с реальным кодом композера и роутов, не придуман. Верифицирован (не изменён) уже рабочий deep-link `?` из карточек настроек в `chat.html` на anchor в `admin_docs.html` — новый сквозной тест `test_setting_field_doc_links_resolve_on_the_admin_docs_page`. Добавлены `examples`/`notes` в шаблон (переиспользуют существующие CSS-классы, новый CSS не понадобился): пары ❌/✅ с текстом ошибок настроек, процитированным дословно из `parse_chat_setting_value`; 2 факта об ограничениях Telegram, проверенных по коду (`admin-broadcast.js` 10 МБ фото, `backup.py` chunking на 45 МБ из-за лимита ~50 МБ). Этап 3: ~26% → ~31%. |
| 2026-08-16 | Claude | Этап 3: отметки доступности функции/настроек — закрывает «Пользовательскую документацию» кроме responsive TOC | `[x]` | commit: 210bcb0; 1023 unit (новый тест в `test_web_user_docs.py`); ESLint/Stylelint/HTMLHint ×9 без изменений (переписан `render_user_docs_fixture.py`); Playwright spot-check; `git diff --check` | `_docs_item()` получил `requires_settings`, генерирующий бейджи `"<label> по настройке"` из единого источника `SETTING_META` вместо расплывчатых hand-typed плейсхолдеров («по настройкам» без указания какой). 8 бейджей на 6 карточек, все проверены по реальному коду хендлеров (`economy_enabled`/`craft_enabled`/`auctions_enabled`/`titles_enabled`/`family_tree_enabled`/`actions_18_enabled`), не угаданы. Сознательно пропущена «Кто я» — там 18+ гейтит только одну тему внутри игры, не всю карточку. Найден и исправлен CI-баг того же класса, что уже был у admin_docs: новый импорт `SETTING_META` в `user_docs.py` транзитивно тянет pydantic через `chat_settings.py`, `render_user_docs_fixture.py` пришлось переписать на ручную сборку контекста (проверено против Jinja2-only venv). Это закрывает весь раздел «Пользовательская документация» кроме намеренно отложенного responsive TOC/drawer (решение от 2026-08-16). Этап 3: ~22% → ~26%. |
| 2026-08-16 | Claude | Этап 3: закрыт auth-баг на `/app/docs/admin` (был полностью публичным) | `[x]` | commit: 69d3ee0; 1022 unit (новый `test_web_admin_docs_access.py` 2/2); `git diff --check` | Найдено при работе над user-docs срезом: `/app/docs/admin` использовал форму context-builder'а, скопированную с намеренно публичного `/app/docs/user` — `redirect_path` был всегда `None`, страница отдавалась без единой проверки сессии. По этому же пункту roadmap'а («Администраторская документация» → «Доступ только в подходящем защищённом контексте») это прямое нарушение — не найдено. Исправлено минимально: `_load_user_from_request` + редирект на `/login`, тот же уровень, что у `/app` (`_build_home_page_context`), НЕ отдельная ADMIN_PASSWORD-сессия `/app/admin` (разные концепции: bot-wide операторские инструменты vs. документация по администрированию чата). Раздел «Администраторская документация»: 1 из 5 пунктов закрыт. |
| 2026-08-16 | Claude | Этап 3: публичный доступ (route-тест), task-oriented разделы, копируемые примеры + deep links, полнотекстовый поиск | `[~]` | commit: f6e0a9f; 1020 unit (новые: `test_web_user_docs_public_access.py` 2/2, `test_web_user_docs_browser.py` 7/7, `test_server_ui_baseline.py`+2, `test_web_user_docs.py`+4); ESLint/Stylelint (новые `docs-item-actions.css`/`.js`, `docs-search.css`/`.js`); HTMLHint ×9 (новый `render_user_docs_fixture.py`, первый CI-фикстур для `user_docs.html`); `git diff --check`; 5 итоговых screenshot (97–101, desktop/tablet/mobile) | Четыре закрытых пункта «Пользовательской документации». (1) Публичный доступ: подтверждено чтением кода + реальным ASGI route-тестом без cookie (`_load_user_from_request` не трогает БД без токена). (2) Task-oriented разделы: уже соответствовали требованию, чек-лист закрыт без изменений кода. (3) Копируемые примеры + deep links: каждая карточка получила стабильный id (`{section}-item-{n}`) и видимую deep-link иконку `#` (opacity 0.55 в покое — доступна и без hover), каждый пример — кнопку «Копировать» (Clipboard API + `execCommand` fallback). Найден и исправлен реальный баг: переход по новому deep-link якорю прятал заголовок карточки под sticky-топбар — `.docs-card` не входил в существующее `scroll-margin-top: 148px` в `panel.css` (уже применялось к `.docs-section` и т.п.), просто забыли добавить туда новый селектор. (4) Полнотекстовый поиск: клиентский фильтр по паттерну `admin-table-search.js`, `search_text` считается один раз на сервере в `_docs_item()`. Playwright подтвердил: desktop/tablet работают чисто, поиск/копирование/deep-link — всё корректно; mobile показывает уже задокументированный `.docs-card-grid` overflow (не новый, будет исправлен отдельным срезом адаптации). Slash-команды по разделам (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`) и «Понятные отметки доступности функции» остаются открытыми пунктами. Этап 3: ~13% → ~20%. |
| 2026-08-16 | Claude | Этап 3: единый источник RP-действий (Reply-триггеры без 18+/18+) | `[~]` | commit: 2116a20; 1008 unit (3 новых файла/дополнения: `test_commands_catalog.py`, `test_web_user_docs.py`, `test_text_commands_18_gate.py`); Playwright spot-check desktop (карточка не переполняется, все 101 li видимы); `git diff --check` | Последний открытый под-пункт «Модели контента»: команды/примеры для RP-действий. `catalog.py` получил публичный `SOCIAL_ACTION_18_PLUS` (перенесён из приватного дубля в `text_commands.py`) и `build_social_action_docs()`, строящий `{command_key, canonical trigger, is_18_plus}` из уже существовавших `COMMAND_KEY_DEFAULT_SOURCE_TRIGGER`+`SOCIAL_COMMAND_KEY_TO_ACTION`+`SOCIAL_ACTION_18_PLUS`; `user_docs.py` больше не хардкодит два списка триггеров, а строит их из этой функции. Перевод на единый источник вскрыл реальный баг рассинхронизации: 20 настоящих RP-действий отсутствовали в публичной документации целиком, ещё 4 показывались под неканоническим альтернативным триггером вместо основного (подробности — в чек-листе выше и в `test_web_user_docs.py::test_user_docs_rp_action_lists_match_canonical_catalog_source`). Playwright-проверка (desktop 1440px) подтвердила: карточка со 101 items (98 триггеров + 3 примера) не переполняется по горизонтали, все элементы видимы. На mobile (390px) карточка выходит за пределы viewport — это уже задокументированный, неисправленный в этом срезе page-wide баг `.docs-layout`/`.docs-card-grid` без mobile-брейкпоинта (см. решение от 2026-08-16 про отдельный срез адаптации после Этапа 3), не регрессия от этого среза. `help.py` намеренно НЕ переведён на этот источник — его краткая строка для `/help` физически не вместит 118 триггеров, это осознанная выжимка, а не дублирующий баг. Slash-команды по разделам (`help.py`/`USER_GUIDE.md`/`ADMIN_GUIDE.md`/`user_docs.py`) остаются отдельной открытой задачей. Этап 3: ~10% → ~13%. |
| 2026-08-16 | Claude | CI-фикс fixture-рендерера + anchors/internal-links тест + аудит секретности | `[x]` | commit: 09b8da6; anchors/internal-links: новый `test_web_docs_anchors.py` (2/2, self-verified на синтетическом broken HTML); проверено против изолированного Jinja2-only venv, зеркалящего CI; реальный GitHub Actions run подтверждён зелёным после пуша | `render_admin_docs_fixture.py` (добавлен в предыдущем срезе) вызывал `build_admin_docs_context()` напрямую, что транзитивно тянет `selara.core.config` → pydantic-settings; CI frontend job ставит только `Jinja2`. Root-caused через `gh run view --log-failed`, переписан на ручную сборку контекста (как у всех остальных `render_*_fixture.py`). Заодно закрыты ещё два пункта «Модели контента» Этапа 3: автоматическая проверка уникальности anchors и внутренних `#`-ссылок (`html.parser.HTMLParser`, без regex, реальный рендер `admin_docs.html`/`user_docs.html`); построчный аудит всех 671 строки `user_docs.py` против `ADMIN_GUIDE.md`, подтвердивший отсутствие admin-процедур в публичном разделе (найден отдельный, вне-скоупный momент: 18+ секция лежит открытым текстом без возрастного гейта на публичной странице — зафиксировано, решение не принято). Этап 3: ~8% → ~10%. |
| 2026-08-16 | Claude | Этап 3: инвентаризация документации + единый источник ролей/прав | `[~]` | commit: 9eb7167; 941 unit; ESLint/Stylelint не затронуты (нет JS/CSS правок); HTMLHint ×8 (новый `render_admin_docs_fixture.py`); `git diff --check`; 1 итоговый screenshot (96) | Первый срез Этапа 3. Инвентаризация (агентом) нашла конкретные расхождения: `docs/bot_docs/roles.md` не содержал `co_owner`; секция ролей в `admin_docs.py` не подключала `core/roles.py` вовсе; labels прав существовали в 3 независимых копиях с реальным расхождением текста между ботом и вебом (`moderation.py`: «доступ команд» vs web: «доступ к командам»). Всё исправлено: `PERMISSION_LABELS_RU`/`permission_label_ru`/`permissions_text_ru` перенесены в `core/roles.py` как единственный источник, `admin_docs.py` получил `build_roles_docs()` (реальная таблица ролей/рангов/прав из `SYSTEM_ROLE_TEMPLATES`), `moderation.py` и `presenters.py` теперь читают эти labels оттуда же вместо своих копий. По пути была попытка «улучшить» сортировку прав в `permissions_text_ru` — сломала 2 существующих теста, сразу же отменена (не входило в объём среза, сохранён оригинальный порядок). Также была попытка унифицировать `RoleTemplate.title_ru` для junior_admin («Мл. админ» → «Младший админ») — отменена после того, как выяснилось, что абревиатура «Мл. админ» устоявшаяся и используется как пример в `stats.py`, `private_panel.py`, `ADMIN_GUIDE.md` и тестах; попытка внести исправление привела бы к новому расхождению вместо старого. Найден и задокументирован (не исправлен — вне объёма) pre-existing page-wide баг: `.docs-layout`/`.docs-card-grid` не имеют mobile-брейкпоинта на всей странице (не только в новой roles-секции — подтверждено сравнением с уже существующей `docs-trigger-types`). Команды/примеры/anchors-проверка остаются открытыми пунктами «Модели контента». Этап 3: 0% → ~8%. |
| 2026-08-16 | Claude | Архив: закрытие Playwright-матрицы + raw-режим (уже существовал) | `[x]` | commit: 3ee68a4; 932 unit; route 2 новых; browser 2 новых (long feed, 2 viewport); `git diff --check`; 2 итоговых screenshot (94–95) | «Технический табличный/raw режим» отмечен выполненным задним числом — кнопка «Технический режим» уже вела на generic DB explorer той же таблицы `messages`, отфильтрованной по чату; это не новая функция, а уже существующая связь. Закрыты последние пробелы Playwright-матрицы архива: route-тесты на полностью пустой архив («Архив пока пуст») и отфильтрованный-в-ноль архив («По этим фильтрам ничего не найдено» + рабочий сброс); browser-тест длинной ленты (50/137 сообщений, desktop+mobile) без page overflow с видимой пагинацией. Остальные пункты матрицы (edits/replies/media/mobile/no-overflow/deep-link) уже были закрыты в предыдущих срезах архива. Единственный оставшийся пункт всего Этапа 2 — индексированный full-text поиск (требует миграции на продовый Postgres, отдельное архитектурное решение, вынесено пользователю). |
| 2026-08-15 | Claude | Главная админки: сворачиваемые секции + empty/large/error/loading (2.2 закрыт полностью) | `[x]` | commit: 3ee68a4; 928 unit; focused overview browser 11/11 (4 новых); focused route 1/1 новый; ESLint; Stylelint; HTMLHint; `git diff --check`; 4 итоговых screenshot (90–93) | Домены `table_sections` теперь `<details open>` со стрелкой-индикатором (новый CSS-файл `admin-table-search.css`, вынесен из admin-overview.css — тот был впритык у лимита 520/520); поиск по таблицам принудительно раскрывает свёрнутый домен при совпадении. Критичные секции (внимание/сервисы/заявки) остаются обычными `<section>`, никогда не сворачиваются. Large-state тест (24 заявки + 60 карточек таблиц) поймал реальный баг: `.admin-table-card` свой `display:flex` побеждал `[hidden]` на равной специфичности — карточки не скрывались при частичном совпадении внутри секции; малые тесты его не ловили, потому что там пряталась вся секция целиком. Исправлено явным `[hidden]{display:none}` для карточки. Error/flash баннер на `/app/admin` теперь route-тестирован явно. По пути тоже словил и разобрался с ложным CSS-багом (артефакт незавершённого transition при чтении computed style без задержки — не настоящая проблема, тот же класс issue, что и раньше в сессии). Раздел 2.2 закрыт полностью. Этап 2: 93% → 95%. |
| 2026-08-15 | Claude | DB explorer: real-backend CRUD-тест (2.6 закрыт полностью) | `[x]` | commit: 3ee68a4; 923 unit; новый `test_web_admin_table_crud.py` 5/5; `git diff --check` | Новый файл с реальным FastAPI + sqlite (без моков) для update/delete generic DB explorer — доказывает реальную мутацию/удаление строки, не только вызов мока. Explorer не имеет UI-действия «создать запись» (только edit/delete существующих), поэтому «CRUD» здесь = R+U+D, покрывающий весь функционал инструмента; полный live-браузерный E2E (реальный HTTP + Playwright) — отдельная, более крупная задача тестовой инфраструктуры этапа 0. Раздел 2.6 (Generic DB explorer) закрыт полностью. Визуальных изменений нет — чисто тестовый срез, screenshot не требовался. Этап 2: 91% → 93%. |
| 2026-08-15 | Claude | DB explorer: клиентский поиск по списку таблиц | `[x]` | commit: 3ee68a4; 918 unit; focused overview browser 7/7 (1 новый); route-тест обновлён; ESLint; Stylelint; HTMLHint; `git diff --check`; 4 итоговых screenshot (86–89) | Новый маленький файл `admin-table-search.js` (24 строки, добавлен baseline-guard в `test_server_ui_baseline.py`) — чистая клиентская фильтрация без нового серверного round-trip (список таблиц уже целиком в DOM). Фильтрует по имени+ключу (`data-table-search-text`), скрывает секции с 0 совпадений, показывает «Совпадений не найдено» при полном промахе. `admin-overview.css` уперался в лимит 520/520 — стили нового блока уложены без превышения (переиспользованы глобальные `.admin-search-input`/`.sr-only`, добавлено только 12 строк layout/empty-state). Label поиска — sr-only (не видимый визуально «kicker», как в archive/broadcast), решил не добавлять новый CSS-класс ради этого при нулевом бюджете CSS-лимита; placeholder поля даёт достаточный визуальный контекст, что подтверждено на скриншотах. Этап 2: 90% → 91%. |
| 2026-08-15 | Claude | Заявки и backup: inline-ошибки рядом с действием | `[x]` | commit: 3ee68a4; 917 unit; focused feedback browser 6/6 (2 новых); focused overview browser 6/6 (1 новый); ESLint; Stylelint; HTMLHint; `git diff --check`; 4 итоговых screenshot (82–85) | Оба действия (статус заявки, запрос backup) переведены с native form-submit на `fetch()` к уже существующим JSON API — серверный код не менялся, чисто фронтенд. Ошибка показывается в новом `[data-feedback-item-error]`/`[data-admin-backup-error]` anchor рядом с конкретным действием, без reload; при ошибке заявки остальные карточки списка не затрагиваются; backup-диалог остаётся открытым вместо скрытия. Обновлены/переписаны 2 существующих browser-теста, которые раньше полагались на native-submit workaround (`event.preventDefault()` инъекция) — тот же класс проблемы с `about:blank`+relative fetch, что и в срезе рассылок; решено тем же паттерном (routed fake origin + `page.route`). Deferred-item чек-лист пункт (2.4) отмечен выполненным — он уже существовал как раздел 7 «Отложенные задачи». Этап 2: 88% → 90%. |
| 2026-08-15 | Claude | Admin shell: выделенные Desktop/Mobile/Tablet Chromium тесты | `[x]` | commit: 3ee68a4; 915 unit; новый `test_web_admin_shell_browser.py` 6/6; `git diff --check`; 4 итоговых screenshot (78–81) | Новый файл с тестами именно shell'а (раньше проверялся только попутно внутри тестов каждой страницы): active-state pill отличается не только цветом (border+background+box-shadow), skip-link — hidden/focus-visible переход и keyboard flow до `#main-content`, mobile nav — `.top-actions` реально `overflow-x: auto` (scroll-ribbon), tablet (820px) — подтверждено, что все 5 пунктов помещаются в один ряд без scroll-ribbon (континуальный responsive без отдельного tablet-брейкпоинта — осознанное решение, теперь закреплено регресс-тестом, а не только визуальной проверкой). По пути найден и исправлен flaky-баг в самих тестах (не в проде): `page.set_content()` + последовательные `add_style_tag()` оставляют CSS-transition skip-link'а «в полёте» на момент чтения `getComputedStyle` — computed transform читался как identity вместо реального translateY; исправлено принудительным отключением transition в тестовом окружении. Пункт «Создать единый admin shell» закрыт (кроме WebKit, отдельный `[!]`). Этап 2: 87% → 88%. |
| 2026-08-15 | Claude | Admin shell: диагностика WebKit-блокера на Fedora | `[!]` | commit: none (только диагностика, код не менялся) | `playwright install webkit` скачал бинарник успешно, но `install-deps` не работает на Fedora (нет apt-get). Через `ldd` подтверждено: не хватает ICU 74 и libjpeg ABI 8, которых на Fedora 44 нет (ICU 77, libjpeg.so.62) и подмена версий через симлинк технически некорректна (versioned C++ symbols у ICU). Решение с пользователем: честно пометить `[!]` в TODO, WebKit-прогон — отдельная будущая задача (CI/Docker с Ubuntu), не блокирует остальной roadmap. |
| 2026-08-15 | Claude | Рассылки: ошибки формы по блокам + Playwright-матрица отправки | `[x]` | commit: 3ee68a4; 909 unit; focused route 17/17 (8 новых, включая Telegram-failure); focused browser 11/11 (2 новых, 4 переписаны под fetch); ESLint; Stylelint; HTMLHint (rendered fixture); frontend build; `git diff --check`; 5 итоговых screenshot (73–77, desktop/mobile/tablet) | Composer переведён с нативного `<form action>` POST на `fetch()` к уже существующему `/api/admin/broadcasts/send` — иначе привязка ошибок к блоку была невозможна (при redirect-based submit состояние формы всегда терялось). `_execute_admin_broadcast` и `_json_result` теперь возвращают/принимают `field` (`message`/`media`/`audience`/`None`); три новых anchor'а в секциях 1–3 (`data-broadcast-{message,media,audience}-error`), общий `data-broadcast-form-error` остаётся для непривязанных ошибок (сеть, сессия истекла). Обнаружено и исправлено при ручном прогоне тестов: (1) старые browser-тесты полагались на `page.set_content()`, где страница живёт на `about:blank` — relative `fetch()` там падает с TypeError ещё до сетевого запроса, `page.route()` перехват никогда не срабатывал; решение — маленький helper `_goto_admin_page()`, который отдаёт ту же разметку через `page.route` на фейковом `http://selara.test/admin` origin, чтобы `fetch("/api/...")` резолвился и перехватывался нормально; (2) `page.route(..., lambda route: route.fulfill(...))` без `await` внутри лямбды не await-ится Playwright и просто зависает — заменено на `async def`-хендлеры везде; (3) тест на "медиа"-ошибку падал из-за нативной HTML5 `required`-валидации файлового поля, блокирующей submit ещё до JS — обойдено через `form.noValidate = true`. admin-broadcast.js держится на 459/460 строк лимита (введён `resetSubmitButton()` вместо дублирования сброса состояния в трёх местах). Полностью закрыт весь список Playwright-сценариев: текст/фото/hybrid reactions (уже существовавшие route-тесты с реальной sqlite БД), invalid input (новые field-scoped тесты), Telegram failure (новый `PartiallyFailingBot`, проверка `sent`/`failed` статусов доставок), duplicate submit (один fetch на confirm), mobile flow (существующий + новый mobile screenshot). Раздел 2.3 закрыт полностью. Прогресс этапа 2: 85% → 87%. |
| 2026-08-15 | Claude | Архив сообщений: переход к найденному сообщению («Показать в контексте») | `[x]` | commit: 3ee68a4; 899 unit; focused route 21/21 (6 новых); focused browser 8/8 (3 новых); ESLint; Stylelint; HTMLHint (rendered fixture, включая новую markup-ветку); frontend build; `git diff --check`; 5 итоговых screenshot (68–72, desktop/mobile/tablet) | Под любым активным фильтром карточка сообщения получает кнопку «Показать в контексте» (`.button.ghost.small`, 40px — тот же паттерн, что и у других вторичных действий страницы) → новый endpoint `/app/admin/archive/jump/{id}` (admin-only) вычисляет page по rank сообщения в неотфильтрованной (только chat_id) ленте одним COUNT-запросом и редиректит на `messages_compact?chat_id&page&highlight=<id>`; landing page скроллит к целевому сообщению, подсвечивает его (`.is-jump-target`, outline+background, не только цветом) на ~2.6s и переводит на него клавиатурный фокус (`tabindex="-1"` + `.focus()`), с уважением `prefers-reduced-motion`. Итерация по визуальному циклу: первая версия ссылки была обычным текстовым линком (~16.5px высоты — ниже 44px и вслед за уже существующим паттерном «Открыть raw-снимок»), при ручном просмотре screenshot решено сделать её полноценной small-кнопкой, чтобы не заводить новый sub-44px тач-таргет в этом срезе; «Открыть raw-снимок» оставлен как есть (не в объёме среза). Также при первом рендере скриншотов скролл на mobile обрывал карточку по нижнему краю viewport — оказалось, что screenshot снимался до завершения smooth-scroll анимации; исправлено увеличением задержки перед снимком в render-скрипте (не в проде — там анимация не блокирует функциональность). Действие нацелено только на основную (последнюю) версию сообщения группы, не на отдельные snapshot-версии в истории редакций — осознанное ограничение объёма. djLint пока не подключён в репозитории (отдельный незакрытый пункт этапа 0) — Jinja-шаблон проверен через рендер + HTMLHint + существующие render-тесты. Этап 2: 84% → 85%. |
| 2026-08-15 | Claude | Admin login: mobile-баг наложения карточек, submit guard, info hygiene | `[x]` | commit: pending; 888 unit; focused browser 6/6; ESLint; Stylelint; HTMLHint ×7; frontend build; 6 desktop/mobile/tablet/error/rate-limit/submit-guard screenshots | Найден реальный баг: `.login-side`/`.login-side-card` не имели CSS, из-за чего на mobile боковые карточки накладывались на текст формы (unstyled auto-колонка grid). Общий фикс в `server-ui-foundation.css` заодно чинит `login.html` (тот же shared markup) — вне заявленного объёма среза, но неизбежно из-за общих классов. Добавлен submit guard, убрано раскрытие `ADMIN_PASSWORD` в helper-тексте, добавлен `for`/`id` для HTMLHint. Прогресс этапа 2: 78% → 84%; общий прогресс roadmap: 19% → 20% |
| 2026-08-15 | Claude | Generic DB explorer: пагинация, edit/delete safety, touch targets | `[x]` | commit: pending; 882 unit; focused route 11/11; focused browser 6/6; ESLint; Stylelint; HTMLHint ×6; frontend build; 7 desktop/mobile/tablet/dialog/empty screenshots | Исправлен краш и отрицательный offset при нечисловом `page`; urlencode-пагинация вместо ручной конкатенации; delete-диалог называет удаляемую запись и восстанавливает фокус на вызвавшую кнопку; destructive-warning + submit guard на edit; JS вынесен в `admin-table.js`; badge/touch-target/warning CSS — в новый `admin-table.css`, чтобы не расширять `panel.css` сверх baseline-лимита; добавлены sr-only labels для фильтров и `type="button"`/`for`/`id` по замечаниям HTMLHint. Поиск по списку таблиц на `admin.html` оставлен отдельным незакрытым пунктом ниже. |
| 2026-08-15 | Codex | Журнал аудита чата: timeline/table hybrid | `[x]` | commit: pending; 874 unit; focused 50/50; audit Chromium 4/4; ESLint; Stylelint; HTMLHint ×4; frontend build; 8 desktop/mobile/tablet screenshots | Русские labels и категории; группировка по датам; URL-поиск/фильтры; раскрываемые event/action/actor/target ID без raw meta; безопасный DB error, empty/filtered/long/responsive states |
| 2026-08-15 | Codex | Заявки пользователей: task-like workflow | `[x]` | commit: pending; 866 unit; focused 76/76; feedback Chromium 5/5; ESLint; Stylelint; HTMLHint ×3; frontend build; 8 desktop/mobile/tablet screenshots | URL-фильтр статуса, lifecycle/history, быстрый status update с сохранением фильтра/позиции, submit guard, empty/error/long/responsive states |
| 2026-08-15 | Codex | Обязательный workflow UI-срезов | `[x]` | commit: pending; baseline contract test; document review | Tests-first, временный рендер в `/tmp`, ручная проверка каждого PNG, итерационное сравнение и финальное сохранение в `~/selara_screen` обязательны для всех исполнителей |
| 2026-08-15 | Codex | Главная админки: operational overview и безопасные быстрые действия | `[x]` | commit: pending; 857 unit; focused 67/67; overview Chromium 5/5; ESLint; Stylelint; HTMLHint ×3; frontend build; 7 desktop/mobile/tablet screenshots | Честный status/attention summary без ложного Telegram health; шесть task-oriented переходов; backup dialog с focus restore и submit guard; empty/responsive states; устранены дублирующий logout, нативный button cascade и видимый край скрытой skip-link |
| 2026-08-15 | Codex | Архив сообщений: защищённый photo preview | `[x]` | commit: pending; 845 unit; focused 51/51; archive Chromium 5/5; ESLint; Stylelint; HTMLHint ×3; frontend build; 6 desktop/mobile/tablet screenshots | Admin-only media endpoint без раскрытия file_id; явная загрузка и сворачивание без повторного запроса; session/type/10 МБ guards; responsive preview и восстановимая ошибка |
| 2026-08-15 | Codex | Системные рассылки: ×4-срез истории и detail view | `[x]` | commit: pending; 839 unit; focused 56/56; broadcast Chromium 5/5; ESLint; Stylelint; HTMLHint ×3; frontend build; 9 desktop/mobile/tablet screenshots | Статусные history cards с прогрессом и реакциями; безопасный message preview и полная summary; URL-фильтры доставок; responsive native/inline/custom/paid reactions, reply-ответы и действия бота без дублирования данных |
| 2026-08-15 | Codex | Архив сообщений: тройной срез навигации и читаемости | `[x]` | commit: pending; 832 unit; focused 44/44; archive Chromium 3/3; ESLint; Stylelint; HTMLHint ×2; frontend build; desktop/mobile screenshots | Стабильная cursor pagination без offset-дублей; группировка соседних сообщений одного автора; безопасные media metadata без раскрытия file_id и автоматической загрузки Telegram files |
| 2026-08-15 | Codex | Архив сообщений: сворачиваемая история редакций | `[x]` | commit: pending; 824 unit; focused 36/36; Chromium archive 4/4; ESLint; Stylelint; HTMLHint ×2; desktop/mobile screenshots | Самый свежий snapshot остаётся основной карточкой, предыдущие версии доступны через keyboard/touch details и отдельные raw-ссылки; устранены шаблонные пробелы в pre-wrap тексте |
| 2026-08-15 | Codex | Архив сообщений: фильтры, URL-состояние и подсветка поиска | `[x]` | commit: pending; 824 unit; focused 36/36; archive Chromium 4/4; ESLint; Stylelint; HTMLHint ×2; frontend build; desktop/mobile/tablet screenshots | Диапазон дат, все Telegram content types, edited/reply, защита от malformed URL, безопасная подсветка; индексированный full-text остаётся следующим улучшением поиска |
| 2026-08-15 | Codex | Архив сообщений: mobile overflow на длинном имени | `[x]` | commit: pending; archive Chromium 4/4; geometry check; повторный screenshot 09 | Grid track ограничен доступной шириной; кнопка «Фильтры» и message cards снова имеют правый внутренний отступ и полные скругления |
| 2026-08-15 | Codex | Архив сообщений: первый dialogue-view срез | `[x]` | commit: pending; 822 unit; archive Chromium 4/4; ESLint; Stylelint; HTMLHint ×2; frontend build; 4 screenshot slices | Desktop split view; mobile/tablet list→dialog; даты, авторы, reply, edited, raw и честный media fallback; WebKit, media preview и edit grouping остаются следующими задачами |
| 2026-08-15 | Codex | Устранение дублирующего photo preview в composer | `[x]` | commit: pending; focused tests 15/15; Chromium 7/7; ESLint; Stylelint; HTMLHint; 6 screenshot slices | В форме остались имя файла и компактная галочка, изображение показывается только в live preview |
| 2026-08-15 | Codex | Визуальная responsive-полировка системной рассылки | `[x]` | commit: pending; 817 unit; Chromium 7/7; ESLint; Stylelint; HTMLHint; frontend build; npm audit 0; 6 screenshot slices | Исправлены textarea cascade, modal focus/scroll, file picker, mobile width, tablet/sticky layout; WebKit matrix остаётся отдельной задачей этапа 0 |
| 2026-08-15 | Codex | Рассылка: постоянный preview и подтверждение отправки | `[x]` | commit: pending; 815 unit; Chromium 5/5; ESLint; Stylelint; HTMLHint; frontend build | Whitelist sanitizer, photo/reactions preview, mobile dialog и focus restore |
| 2026-08-15 | Codex | Рассылка: JS extraction, audience search, inline validation, submit guard | `[x]` | commit: pending; 813 unit; ESLint; Stylelint; HTMLHint; Chromium 3/3; frontend build; npm audit 0 | Сохранены фото, hybrid reactions и backend-контракт |
| 2026-08-15 | Codex | Общий admin shell: разделы, active state, mobile overflow, keyboard skip-link | `[~]` | commit: pending; unit suite 810 passed | Chromium 390 px пройден; WebKit/tablet ещё впереди |
| 2026-08-15 | Codex | Регрессии admin auth/logout | `[x]` | commit: pending; unit suite 810 passed | Закрыт unauthenticated GET edit; admin logout направлен в отдельную admin-сессию |
| 2026-08-15 | Codex | Этап 0: инвентаризация и baseline-проверки server UI | `[~]` | commit: pending | Активный первый рабочий блок |
| 2026-08-15 | Codex | Создание единого roadmap/TODO | `[x]` | commit: pending; document review | Зафиксированы решения пользователя, baseline и полный фронт работ |
